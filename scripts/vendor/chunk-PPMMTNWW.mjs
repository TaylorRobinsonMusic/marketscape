import { b as ji } from "chunk-XS3TYHUF.mjs";
import {
  $e as Bi,
  Ab as Id,
  Af as Ud,
  Ag as Rt,
  Ah as To,
  Bf as Wd,
  Bg as vo,
  Cf as go,
  Cg as Cn,
  Df as jd,
  Dg as nc,
  Eg as $t,
  Fg as Ui,
  If as Gd,
  Jf as Hd,
  Lf as yo,
  Mg as Wi,
  Nh as cc,
  Te as Ed,
  Ub as Nd,
  Ue as po,
  Wb as lo,
  df as Ai,
  ec as xd,
  ef as Md,
  ic as Ld,
  if as Fd,
  jf as Bd,
  lg as ve,
  mc as co,
  mg as Co,
  ng as Kd,
  of as $i,
  og as Qd,
  pa as Td,
  pg as Yd,
  qf as _d,
  qg as Xd,
  sg as Qn,
  tc as Dd,
  tf as mo,
  uf as ho,
  vg as Zd,
  wg as Yn,
  wh as oc,
  xh as ic,
  yc as Pd,
  yg as ec,
  zf as $d,
  zg as tc,
} from "chunk-RYAQJ2V7.mjs";
import { m as Vd, q as rc } from "chunk-AMYECL4X.mjs";
import { k as Jd } from "chunk-C4EY2TIT.mjs";
import { b as Kn } from "chunk-HEVPYMM6.mjs";
import { b as So } from "chunk-5D5WEAJK.mjs";
import { a as qd } from "chunk-BL7YLPCC.mjs";
import { Q as Qa } from "chunk-WGS5WK5D.mjs";
import { b as vn } from "chunk-LM76R6UX.mjs";
import { a as Xn, d as bo, f as ac, g as sc, h as lc, i as dc } from "chunk-Y3KD6FPJ.mjs";
import { d as Rd, e as wd, g as Oi, s as Ad } from "chunk-5C32A4K3.mjs";
import {
  $ as yl,
  $a as _l,
  $f as ft,
  Ah as Mi,
  Cc as dt,
  Fh as ud,
  Ga as Il,
  Gh as fd,
  Hh as pd,
  Ih as md,
  Jh as pt,
  Kh as Fi,
  Lh as Jn,
  Oh as zi,
  Pc as Hl,
  Ph as hd,
  Qc as Jl,
  Qh as mt,
  Rh as io,
  Sh as gd,
  T as Wr,
  Tg as ad,
  Th as yd,
  U as un,
  Uf as ze,
  Ug as ro,
  Vf as to,
  Wb as wi,
  Wc as ql,
  Wf as _t,
  Wh as Cd,
  Xa as Gn,
  Xf as nd,
  Xh as qn,
  Yf as rd,
  Yh as ki,
  Zf as ut,
  _ as fn,
  _f as no,
  aa as Re,
  ba as Cl,
  bg as od,
  ca as vl,
  cd as Kl,
  ce as Ql,
  cg as id,
  di as vd,
  ea as Sl,
  fi as Sd,
  oi as ao,
  ph as sd,
  qb as $l,
  qh as ld,
  rb as Ul,
  rh as yn,
  sb as Ri,
  sh as oo,
  si as bd,
  ub as Wl,
  uh as dd,
  vc as xi,
  vh as cd,
  wb as jl,
  wh as Vi,
  xa as Ii,
  xh as Ei,
  yb as Gl,
  yi as so,
  zh as Hn,
} from "chunk-H6ES2254.mjs";
import {
  a as Y,
  b as Yl,
  c as eo,
  f as Xl,
  g as Zl,
  h as ed,
  j as Di,
  k as gn,
  l as td,
  o as Pi,
} from "chunk-IZHBIYUK.mjs";
import { f as ct } from "chunk-4DDFUFU6.mjs";
import { c as qa, d as Ka } from "chunk-XE44TO42.mjs";
import { a as E, c as Li } from "chunk-2Q7D34YV.mjs";
import { ve as xr } from "chunk-PBNCD2KE.mjs";
import { c as zd, h as kd, k as Od, l as _i } from "chunk-43ZTSTYE.mjs";
import {
  $ as Xs,
  $l as Nt,
  Ac as Ge,
  Bg as Ci,
  Fi as Nl,
  Fl as T,
  G as Ks,
  Gl as Ni,
  H as Qs,
  Il as jr,
  Ll as Gr,
  Ml as Hr,
  Ng as lt,
  Ol as Fl,
  Q as Ys,
  Qg as ue,
  Qj as we,
  Tg as hl,
  Vg as gl,
  Vh as bl,
  Yb as nl,
  Yl as zl,
  _ as Un,
  aa as Wn,
  ad as cn,
  am as Kr,
  b as us,
  ba as Zs,
  bc as rl,
  bi as Ti,
  bm as mn,
  cm as hn,
  co as Al,
  da as pi,
  dm as Qr,
  dp as Zr,
  ea as el,
  ej as Rl,
  ek as Dl,
  fa as mi,
  fe as dl,
  fj as wl,
  ga as tl,
  gd as al,
  hd as gi,
  he as cl,
  id as sl,
  jd as ll,
  jg as fl,
  lh as Qe,
  li as Tl,
  pc as hi,
  pj as xl,
  sc as ol,
  sj as Ll,
  vc as il,
  vg as yi,
} from "chunk-ODCA7OFJ.mjs";
import { a as ul } from "chunk-RNHTTH2C.mjs";
import { c as uo, d as fo } from "chunk-HYVDAETN.mjs";
import {
  $r as bi,
  $u as Ol,
  Ad as D,
  Ae as Mr,
  Aj as Ke,
  Bd as We,
  Br as qs,
  Cd as Fe,
  Dd as Mt,
  En as Ot,
  Fd as os,
  Fl as zr,
  Fm as bt,
  Gd as Ne,
  Hd as Vr,
  Hf as fs,
  If as ps,
  Jd as is,
  Je as zt,
  K as Nr,
  Kd as Q,
  Ke as cs,
  Ld as Ft,
  Ll as kr,
  Mq as $r,
  Nm as Or,
  Od as as,
  Om as Vs,
  Pn as li,
  Rl as ws,
  Ro as Ar,
  Ru as El,
  Sn as sn,
  Tl as xs,
  Tn as _n,
  Tq as $s,
  Uk as Is,
  Vk as kt,
  Vo as Bs,
  Vq as Us,
  Wk as Ns,
  Wn as Br,
  Wo as As,
  Xg as si,
  Xl as Ls,
  Xm as an,
  Xn as di,
  Xr as pl,
  Xu as pn,
  Yk as Bn,
  Yn as Fs,
  Yr as vi,
  Yu as Ml,
  Zm as Es,
  Zr as Si,
  Zu as kl,
  _m as Ms,
  _r as ml,
  aa as ri,
  ac as Za,
  au as Pl,
  br as Ws,
  bu as Bt,
  cl as Fr,
  cu as Vl,
  cv as Bl,
  dd as nn,
  de as ss,
  dl as An,
  ek as vs,
  et as jn,
  fe as ls,
  fr as js,
  gr as ln,
  hd as St,
  he as on,
  hv as At,
  ie as ds,
  ik as Ss,
  ir as dn,
  jc as es,
  jd as Dr,
  jo as Tt,
  jr as ui,
  jv as Yr,
  kc as Lr,
  kd as Pr,
  ko as zs,
  kr as je,
  lo as $n,
  mc as ts,
  n as Ee,
  nl as Rs,
  nv as Xr,
  oi as ys,
  pd as Me,
  pe as ai,
  ph as ms,
  po as ks,
  pr as Ur,
  qd as rn,
  qk as bs,
  rb as Ya,
  rc as at,
  rj as _,
  ro as Os,
  se as st,
  sh as hs,
  sj as N,
  sr as Gs,
  tb as kn,
  te as Er,
  tj as Cs,
  ub as tn,
  uc as ii,
  uj as B,
  vb as Xa,
  vc as On,
  vh as gs,
  vo as ci,
  vp as It,
  vr as Hs,
  wc as ns,
  wd as rs,
  wj as A,
  wr as Js,
  xd as he,
  xj as p,
  yd as te,
  yj as qe,
  yp as _r,
  yr as fi,
  zd as Ce,
  zp as _s,
} from "chunk-AHICRW35.mjs";
import { a as qr } from "chunk-DXRKYNCR.mjs";
import { d as Ts } from "chunk-2NQVBTOC.mjs";
import { V as Ga, Z as Ds } from "chunk-SSHL25ZI.mjs";
import { a as Et } from "chunk-VKWF2PPO.mjs";
import { a as Jr } from "chunk-REJ2XCEK.mjs";
import { b as Ps } from "chunk-X3QW56PA.mjs";
import { a as Rr } from "chunk-T77QJFCJ.mjs";
import { o as Ue } from "chunk-CFUM4JW5.mjs";
import {
  a as Ha,
  b as L,
  e as U,
  f as $e,
  h as oi,
  j as Ja,
  l as q,
  o as it,
  r as wr,
} from "chunk-DYNCKUFC.mjs";
import { a as ja } from "chunk-4I6ENMO3.mjs";
import { i as Ir, j as Ve } from "chunk-GX2Q2FIZ.mjs";
import { c as Wa } from "chunk-NHVEFQOW.mjs";
import {
  $ as vt,
  Aa as Je,
  Ba as se,
  Qa as M,
  X as Oa,
  Ya as ni,
  da as Ba,
  ia as me,
  jb as ce,
  lb as Tr,
  qa as Aa,
  sa as _a,
  ta as zn,
  ua as ot,
  v as ka,
  va as $a,
  wa as Ua,
  ya as ye,
} from "chunk-V7MLU2WL.mjs";
import { a as y, b as ee } from "chunk-425IX65M.mjs";
import { a as en } from "chunk-HMF7T2NG.mjs";
import { e as Zt, j as C, k as I, l as de, m as _e, n as Ie, o as br } from "chunk-CT63CFX7.mjs";
var wt = me("tree:timeline"),
  Rf = 6e4,
  wf = 6e4,
  Sn = class {
    constructor(e) {
      C(this, "nodeChangesBuffers", e);
      C(this, "changes", new Map());
      this.nodeChangesBuffers.add(this);
    }
    trackChange(e, t) {
      let r = this.changes.get(e);
      if (r) {
        t && r.push(t);
        return;
      }
      this.changes.set(e, t ? [t] : []);
    }
    read() {
      let e = this.changes;
      return ((this.changes = new Map()), e);
    }
    clear() {
      this.changes = new Map();
    }
    dispose() {
      this.nodeChangesBuffers.delete(this);
    }
  },
  Io = class {
    constructor(e, t, r = [], o = !1) {
      C(this, "tree", e);
      C(this, "changes", t);
      C(this, "editReasons", r);
      C(this, "wasRebase", o);
      C(this, "wasScopeInsert", !1);
      C(this, "version", 0);
      C(this, "timestamp", performance.now());
    }
    toDebugData() {
      return {
        version: this.version,
        changes: this.changes,
        editReasons: this.editReasons,
        wasScopeInsert: this.wasScopeInsert,
        wasRebase: this.wasRebase,
      };
    }
  },
  uc = class {
    constructor() {
      C(this, "idToChanges", new Map());
    }
    getChanges() {
      return Array.from(this.idToChanges.values());
    }
    addChanges(e) {
      if (e)
        for (let t of e) {
          let r = this.idToChanges.get(t.id);
          (r || ((r = { id: t.id, to: {} }), this.idToChanges.set(t.id, r)), Jn(r, t));
        }
    }
  },
  fc = class {
    constructor(e, t, r) {
      C(this, "componentLoader", t);
      C(this, "tree");
      C(this, "entries");
      C(this, "latestReversibleNodeChanges", null);
      C(this, "enableAddRemoveOptimizations", !0);
      C(this, "recorder");
      C(this, "trimmed", 0);
      C(this, "isPartialLoading", !1);
      C(this, "internalRemoteTreeIndex", 0);
      C(this, "inErrorRecovery", !1);
      C(this, "nodeChangesBuffers", new Set());
      C(this, "legacyMode", !1);
      C(this, "remoteTreeVersion", 0);
      C(this, "recentEditReasons", []);
      C(this, "flagsForNextCommit");
      C(this, "eventLoopRanSinceWrite", !0);
      C(this, "onlineStartTime", 0);
      C(this, "validationEnabled", !0);
      C(this, "extraChangesForNextCommit");
      C(this, "resetTime", 0);
      C(this, "epoch", 0);
      this.reset(e, r);
    }
    get length() {
      return this.entries.length + this.trimmed;
    }
    get localTreeIndex() {
      return this.length - 1;
    }
    get remoteTreeIndex() {
      return this.internalRemoteTreeIndex;
    }
    incrementRemoteTreeIndex(e) {
      this.internalRemoteTreeIndex += e;
    }
    setOnline(e) {
      this.online !== e && (this.onlineStartTime = e ? performance.now() : 0);
    }
    get online() {
      return this.onlineStartTime !== 0;
    }
    setFlagsForNextCommit(e) {
      this.flagsForNextCommit = e;
    }
    validateUpdatesAreSentToServer() {
      if (!this.validationEnabled || !this.online || this.tree.isViewOnly) return;
      let e = performance.now();
      if (e - this.onlineStartTime < wf) return;
      let r = this.getOldestLocalEntry();
      if (!r || !this.eventLoopRanSinceWrite) return;
      let o = e - r.timestamp;
      y(o < Rf, "Local changes not been processed in a while");
    }
    setExtraChangesForNextCommit(e) {
      this.extraChangesForNextCommit = e;
    }
    recordEditReasons(e) {
      e && this.recentEditReasons.push(e);
    }
    trackChange(e, t = null) {
      for (let r of this.nodeChangesBuffers) r.trackChange(e, t);
    }
    getTreeForVersion(e) {
      if (!this.isPartialLoading) return this.entries.findLast((t) => t.version === e)?.tree;
    }
    addEntry(e, t, r = [], o = !1) {
      an(e, "json");
      let i = new Io(e, t, r, o);
      return (this.entries.push(i), (this.tree = e), i);
    }
    getLastEntry() {
      let e = this.entries[this.entries.length - 1];
      return (y(e, "Timeline has no entries"), e);
    }
    hasLocalEdits() {
      return this.remoteTreeIndex + 1 - this.trimmed < this.entries.length;
    }
    getOldestLocalEntry() {
      return this.getEntry(this.remoteTreeIndex + 1 - this.trimmed);
    }
    getEntry(e) {
      return this.entries[e];
    }
    reset(e, t) {
      if (((this.resetTime = performance.now()), this.inErrorRecovery && e === this.tree)) {
        (wt.debug("reset for error recovery..."),
          (this.inErrorRecovery = !1),
          this.invalidateAllCursors(),
          this.clearNodeChangesReader());
        return;
      }
      (wt.debug("reset with tree:", e.root.id, "size:", e.size()),
        (this.entries = []),
        this.addEntry(e, t?.initialChanges ?? [], ["load"]),
        (this.recentEditReasons = []),
        (this.flagsForNextCommit = void 0),
        (this.extraChangesForNextCommit = void 0),
        (this.latestReversibleNodeChanges = null),
        (this.trimmed = 0),
        (this.internalRemoteTreeIndex = 0),
        (this.eventLoopRanSinceWrite = !0),
        (this.isPartialLoading = !!t?.isLoading),
        this.invalidateAllCursors(),
        this.clearNodeChangesReader());
    }
    invalidateAllCursors() {
      this.epoch += 1;
    }
    openNodeChangesReader() {
      return new Sn(this.nodeChangesBuffers);
    }
    clearNodeChangesReader() {
      for (let e of this.nodeChangesBuffers) e.clear();
    }
    applyFlagsToChange(e) {
      e &&
        this.flagsForNextCommit &&
        (this.flagsForNextCommit.ignoreInUndo && (e.ignoreInUndo = !0),
        this.flagsForNextCommit.ignoreInCodeGeneration && (e.ignoreInCodeGeneration = !0));
    }
    commitLocalTree() {
      this.validateUpdatesAreSentToServer();
      let e = this.getLastEntry();
      y(this.tree === e.tree, "tree out of sync");
      let t = {};
      ((this.tree = this.tree.commit(this.componentLoader, (o, i) => {
        if (!o && !i) return;
        let a = mt(o, i);
        if ((this.applyFlagsToChange(a), this.trackChange(o?.id ?? i.id, a), a)) {
          if (a.to.parentid && a.to.parentid !== a.from?.parentid) {
            let l = this.tree.getScopeNodeAtStartFor(o),
              d = this.tree.getScopeNodeFor(i);
            l && l?.id !== d?.id && (a.previousScope = l.id);
          }
          t[a.id] = a;
        }
        let s = [];
        pt(s, o, i);
        for (let l of s) (this.applyFlagsToChange(l), this.trackChange(l.id, l), (t[l.id] = l));
      })),
        (this.latestReversibleNodeChanges = Object.values(t)));
      let r = this.latestReversibleNodeChanges;
      if (this.extraChangesForNextCommit) {
        r = [...r];
        for (let o of this.extraChangesForNextCommit)
          (this.applyFlagsToChange(o), this.trackChange(o.id, o), r.push(o));
      }
      return (
        (this.flagsForNextCommit = void 0),
        (this.extraChangesForNextCommit = void 0),
        wt.debug("commit local tree:", r.length, this.recentEditReasons),
        r.length > 0
          ? (y(e.tree !== this.tree, "must be a new tree"),
            e.tree.releaseMemory(),
            this.entries.push(new Io(this.tree, r, this.recentEditReasons)))
          : this.tree !== e.tree && (e.tree.releaseMemory(), (e.tree = this.tree)),
        (this.recentEditReasons = []),
        this.hasLocalEdits() &&
          this.online &&
          this.eventLoopRanSinceWrite &&
          ((this.eventLoopRanSinceWrite = !1),
          setTimeout(() => {
            this.eventLoopRanSinceWrite = !0;
          }, 0)),
        y(this.tree === this.getLastEntry().tree),
        { tree: this.tree }
      );
    }
    getLatestChangesForUndo() {
      return this.latestReversibleNodeChanges;
    }
    getChangeTrackingCursor() {
      let e = this.remoteTreeIndex,
        t = this.localTreeIndex;
      return { remoteTree: e, localTree: t, timeline: this, tree: this.tree, epoch: this.epoch };
    }
    invalidatedByLoadCompletedDocument(e) {
      return !(
        !e ||
        this.trimmed > 0 ||
        e.timeline !== this ||
        e.epoch + 1 !== this.epoch ||
        e.remoteTree + 1 !== this.remoteTreeIndex
      );
    }
    fetchForwardChanges(e) {
      if (
        !e ||
        e.epoch !== this.epoch ||
        e.tree.lineage !== this.tree.lineage ||
        e.tree.root.id !== this.tree.root.id ||
        e.timeline !== this ||
        (e.localTree > 0 && e.localTree <= this.trimmed) ||
        (e.remoteTree > 0 && e.remoteTree <= this.trimmed) ||
        (this.remoteTreeIndex > 0 && this.trimmed > 0 && e.remoteTree === 0)
      )
        return;
      let t = e.localTree;
      if (this.remoteTreeIndex > 0)
        for (
          t = e.remoteTree - 1;
          t < e.localTree && !this.entries[t + 1 - this.trimmed]?.wasRebase;
        )
          t += 1;
      (y(t - this.trimmed >= 0, "buffer cut too close to remoteTree"),
        y(t <= e.localTree, "startIndex incorrectly calculated"));
      let r = this.localTreeIndex;
      y(t <= r, "bad change tracking cursor");
      let o = e.tree;
      return (
        (e.remoteTree = this.remoteTreeIndex),
        (e.localTree = r),
        (e.tree = this.tree),
        this.computeForwardChanges(t, r, o)
      );
    }
    computeForwardChanges(e, t, r) {
      if (e >= t) return [];
      let o = {};
      for (let a = e + 1; a <= t; a++) {
        let s = this.entries[a - this.trimmed];
        for (let l of s.changes) {
          let d = l.id,
            f = o[d];
          f || (f = o[d] = { id: d, to: {} });
          let c = f.added;
          (Jn(f, l), !r && c && f.removed && this.enableAddRemoveOptimizations && delete o[d]);
        }
      }
      let i = r ?? this.entries[e - this.trimmed]?.tree;
      if (!i) throw new Error(`${e} - ${this.trimmed}`);
      return Object.values(o).filter((a) => {
        if (a.removed) return ((a.to = {}), !0);
        let s = i.getNodeAtStart(a.id);
        if (s && this.enableAddRemoveOptimizations)
          for (let [c, u] of Object.entries(a.to)) Et(s[c], u) && delete a.to[c];
        if (a.added || Object.keys(a.to).length > 0) return !0;
        let l = a.toChildren;
        if (!l) return !1;
        if (!s) return !0;
        i.beginAllowPartialScopeAccess();
        let d = s.children;
        if ((i.endAllowPartialScopeAccess(), !d || !this.enableAddRemoveOptimizations)) return !0;
        let f = d.length;
        if (f !== l.length) return !0;
        for (let c = 0; c < f; c++) if (d.at(c).id !== l[c]) return !0;
        return !1;
      });
    }
    getEditReasons(e, t) {
      let r = [],
        o = "";
      for (let i = e + 1; i <= t; i++) {
        let a = this.entries[i - this.trimmed].editReasons;
        for (let s of a) s && o !== s && ((o = s), r.push(s));
      }
      return r.join(" ");
    }
    getChangesBetweenEntries(e, t) {
      y(e < t, "inconsistency in getting local changes to send");
      let r = this.computeForwardChanges(e, t),
        o = this.getEditReasons(e, t);
      return { count: t - e, changes: r, reasons: o };
    }
    debugOverwriteCurrentTree(e) {
      (wt.debug("recover with tree:", e.root.id),
        an(e, "json"),
        (this.tree = e),
        (this.getLastEntry().tree = e));
    }
    resetTreesForRecovery(e, t) {
      wt.info(
        "reset trees for recovery, remote:",
        this.remoteTreeIndex,
        "local:",
        this.localTreeIndex,
        "changes already sent:",
        t
      );
      let r = this.entries[e]?.tree;
      y(r, "unable to get remote tree");
      let o = r.getService("loader"),
        i = r.lineage.editHooks,
        a = [];
      try {
        let l = JSON.parse(JSON.stringify(r.toJS()));
        r = Jd(l, this.componentLoader, a);
      } catch (l) {
        if (l instanceof RangeError && l.message.includes("Invalid string length"))
          (wt.warn("[recovery] Tree too large for JSON serialization, using original tree", {
            error: l.message,
            treeSize: r.size(),
          }),
            a.push("Recovery skipped: tree too large for serialization"));
        else throw l;
      }
      (o &&
        (o.resetTreeForRecovery(r),
        y(r.getService("loader") === o, "tree must have the same loader")),
        i && (r.lineage.editHooks = i),
        a.length > 0 && wt.warn("[recovery] encountered errors while reloading the tree:", a),
        (this.entries[e].tree = r),
        (this.entries.length = e + t + 1));
      let s = r;
      for (let l = e + 1; l < this.entries.length; l++) {
        let d = this.entries[l];
        (zi(r, d.changes),
          (r = r.commitDiffs(this.componentLoader)),
          (d.tree = r),
          r !== s && (s.releaseMemory(), (s = r)));
      }
      return ((this.inErrorRecovery = !0), an(r, "json"), (this.tree = r), r);
    }
    saveTimelineDataForRecovery() {
      if (window.localStorage)
        try {
          let e = `debugTimelineAtRecovery-${Math.floor(Math.random() * 100)}`,
            t = { date: new Date().toString(), entries: this.entries.map((r) => r.toDebugData()) };
          window.localStorage.setItem(e, JSON.stringify(t));
        } catch (e) {
          wt.warn("failed to store timeline in localStorage:", e);
        }
    }
    applyChanges(e) {
      zi(this.tree, e);
    }
    applyReverseChanges(e, t) {
      hd(this.tree, e);
    }
    discardChangeset() {}
    replayChangeset() {}
    getOrCreateDataTree() {}
    resetDataTree() {}
  };
function pc(n) {
  if (!n.children) return;
  let e = new Map();
  for (let t of n.children) nn(t) && e.set(t.id, { master: t, replicas: new Map() });
  for (let t of n.children) {
    if (!St(t)) continue;
    let r = e.get(t.replicaInfo.master);
    r && r.replicas.set(t.id, t);
  }
  for (let { master: t, replicas: r } of e.values()) xf(t, r);
}
function xf(n, e) {
  for (let t of e.values()) {
    let r = t.replicaInfo;
    y(r.master === n.id, "Replica must match master");
    let o = r.overrides;
    if (r.inheritsFrom) {
      let c = e.get(r.inheritsFrom);
      c && (o = al(o, c.replicaInfo.overrides));
    }
    let i = o[n.id],
      a = t.duplicatedFrom;
    (un.copyToNode(t, n, i), (t.duplicatedFrom = a));
    let s = !1;
    vs(t) && ((s = !0), sl(t, o, n), gi(n, t));
    let l = t.id,
      d = n.children;
    if (!d) return;
    let f = new Array(d.length);
    for (let c = 0, u = d.length; c < u; c++) f[c] = mc(l, o, d.at(c), l, s);
    t.children = new An(f);
  }
}
function mc(n, e, t, r, o) {
  let i = cd({ __class: t.__class, id: n + t.id, parentid: r });
  y(i, "Failed to create replica node");
  let a = e[t.id];
  if ((un.copyToNode(i, t, a), (i.duplicatedFrom = null), o && gi(t, i), t.children)) {
    let s = t.children,
      l = new Array(s.length);
    for (let d = 0, f = s.length; d < f; d++) l[d] = mc(n, e, s.at(d), i.id, o);
    i.children = new An(l);
  }
  return i;
}
var hc = [
    "amber",
    "azure",
    "bold",
    "bright",
    "chalky",
    "coral",
    "crisp",
    "dusky",
    "electric",
    "fluid",
    "frosted",
    "glassy",
    "glossy",
    "golden",
    "hazy",
    "indigo",
    "ivory",
    "linen",
    "lucid",
    "matte",
    "mellow",
    "misty",
    "muted",
    "neon",
    "opal",
    "pastel",
    "pearl",
    "plush",
    "polished",
    "satin",
    "sheer",
    "silken",
    "silver",
    "slate",
    "smoky",
    "smooth",
    "soft",
    "solid",
    "sunlit",
    "supple",
    "tactile",
    "velvet",
    "vivid",
    "warm",
    "waxy",
    "woven",
    "airy",
    "crystal",
    "dewy",
    "liquid",
  ],
  gc = [
    "anchor",
    "arc",
    "atlas",
    "beacon",
    "bloom",
    "canyon",
    "cove",
    "crest",
    "delta",
    "dome",
    "drift",
    "dune",
    "ember",
    "facet",
    "fjord",
    "flare",
    "foil",
    "glade",
    "glow",
    "grain",
    "grove",
    "harbor",
    "haven",
    "horizon",
    "isle",
    "lagoon",
    "lattice",
    "loop",
    "marble",
    "meadow",
    "mosaic",
    "orbit",
    "pebble",
    "plane",
    "prism",
    "quartz",
    "ridge",
    "ripple",
    "river",
    "shore",
    "slope",
    "spark",
    "spire",
    "stone",
    "tide",
    "vista",
    "wave",
    "willow",
    "halo",
    "thread",
  ];
function No(n) {
  let e = Math.floor(Math.random() * n.length);
  return n[e];
}
function yc(n) {
  for (let t = 0; t < 5; t++) {
    let r = `${No(hc)}-${No(gc)}`;
    if (!n.has(r)) return yi(r);
  }
  let e = `${No(hc)}-${No(gc)}`;
  return yi(Ql(e, n));
}
function py(n, e, t) {
  let r = new Set();
  for (let o of e) {
    let i = Ro(n, o.id, t);
    if ((i && r.add(i), o.key === "parentid" && Xl(o.value))) {
      let a = Zl(o.value);
      if (a !== Y) {
        let s = Ro(n, a, t);
        s && r.add(s);
      }
    }
  }
  return r;
}
function Ro(n, e, t) {
  let r = e;
  for (; r; ) {
    let o = n.getObjectKey(r, "__class");
    if (o && Lf(o, t)) return r;
    let i = n.getParentId(r);
    if (!i || i === Y) break;
    r = i;
  }
}
function Lf(n, e) {
  let t = e.get(n);
  if (t !== void 0) return t;
  let r = ld(n);
  if (!r) return !1;
  let o = Object.getPrototypeOf(r);
  for (; o; ) {
    if (o === hl) return (e.set(n, !0), !0);
    o = Object.getPrototypeOf(o);
  }
  return (e.set(n, !1), !1);
}
function Cc(n) {
  n.assertCanvasTree();
  let e = so.createEmpty({ mode: n.canvasTree.lineage.mode }),
    t = {
      get: (o) => n.get(o),
      getNode: (o) => n.getNode(o),
      getNodeAtStart: (o) => n.get(o),
      getNodeAsDraft: (o) => n.getNodeAsDraft(o),
      getCurrentOrDraft: (o) => n.get(o),
      has: (o) => n.has(o),
      hasNodeAnywhere: (o) => n.hasNodeAnywhere(o),
      latestTree: () => n.latestTree(),
      get editClosed() {
        return n.editClosed;
      },
      get isViewOnly() {
        return n.isViewOnly;
      },
      get inEditor() {
        return n.inEditor;
      },
      insertNode: (o, i, a) => n.insertNode(o, i, a),
      removeNode: (o) => n.removeNode(o),
      moveNode: (o, i, a) => n.moveNode(o, i, a),
      size: () => n.hierarchy.sizeWithoutReplicas(),
      sizeAtStart: () => n.hierarchy.sizeWithoutReplicas(),
      hasUncommittedChanges: () => n.hasUncommittedChanges(),
      unsafeGetDraftNodes: () => [],
      getNodesChangedByCommit: () => [],
      withPersistedCaches: (o) => n.withPersistedCaches(o),
      persistCaches: () => n.persistCaches(),
    },
    r = new Proxy(e, {
      get(o, i, a) {
        if (i === "isLoaded") return () => !0;
        if (i === "root" || i === "rootAtStart") return n.root;
        if (i === "commit") return () => a;
        if (i === "remove")
          return (l) => {
            let d = n.get(l);
            return (d && n.removeNode(d), a);
          };
        if (i === "move")
          return (l, d, f) => {
            let c = n.get(l);
            return (d || (d = n.root.id), c && n.moveNode(c, d, f), a);
          };
        if (i in t) return t[i];
        let s = Reflect.get(o, i, a);
        return typeof s == "function" ? s.bind(a) : s;
      },
    });
  return ((e.lineage.latestTree = r), r);
}
var wo = Symbol("isDataNode");
var Zn = new Map(),
  Df = (n) => n.startsWith("$");
function Pf(n, e) {
  if (n.canvasTree && e !== n.canvasTree.root.id) {
    let t = n.canvasTree.get(e),
      r = ue(t);
    if (t && (!r || t.isLoaded())) return t;
  }
}
function Ut(n, e) {
  let t = n.nodeProxyCache.get(e);
  if (t) {
    let f = t.deref();
    if (f) return f;
  }
  let r = Pf(n, e);
  if (r) return r;
  let o = n.store.getObjectKey(e, "__class");
  y(o, () => `${e} does not have class`);
  let i = Zn.get(o);
  y(i, () => `node ${e} has invalid class name ${o}`);
  let a = [],
    s,
    l = n.store.getObjectKey(`${e}.replicaInfo`, "master");
  if (l && typeof l == "string") {
    ((s = e), a.push(`${s}.replicaInfo.overrides.${l}`));
    let f = n.store.getObjectKey(`${s}.replicaInfo`, "inheritsFrom");
    (f && a.push(`${f}.replicaInfo.overrides.${l}`), a.push(l));
  } else a.push(e);
  let d = new i(n, e, a, s, l);
  return (n.nodeProxyCache.set(e, new WeakRef(d)), d);
}
function Ji(n, e, t) {
  let r = cn(e, t),
    o = n.nodeProxyCache.get(r);
  if (o) {
    let f = o.deref();
    if (f) return f;
  }
  let i = n.store.getObjectKey(t, "__class");
  if (!i) {
    let f = n.store.getObject(t);
    (y(f, () => `${r} does not exist`), y(i, () => `${r} does not have class`));
  }
  let a = Zn.get(i);
  y(a, () => `node ${r} has invalid class name ${i}`);
  let s = [];
  s.push(`${e}.replicaInfo.overrides.${t}`);
  let l = n.store.getObjectKey(`${e}.replicaInfo`, "inheritsFrom");
  (l && s.push(`${l}.replicaInfo.overrides.${t}`), s.push(t));
  let d = new a(n, r, s, e, t);
  return (n.nodeProxyCache.set(r, new WeakRef(d)), d);
}
function Gi(n, e, t) {
  Object.defineProperty(n, e, {
    enumerable: !0,
    get() {
      let r = this.dataTree.editedNodes.get(this.id)?.[e];
      if (r !== void 0) return r;
      let o = this.dataTree.canvasTree?.get(this.id);
      if (o) return o[e];
      for (let i of this.sourceIds)
        if (i.includes(".")) {
          if (((r = this.dataTree.store.getObjectKey(i, e)), r !== void 0)) return t(r);
          let a = this.dataTree.store.getObjectKey(i, "_deleted");
          if (Array.isArray(a) && a.includes(e)) return;
        } else if (((r = this.dataTree.store.getObjectKey(i, e)), r !== void 0)) return t(r);
      return this[qn][e];
    },
    set(r) {
      this.dataTree.getOrCreateEditedNodeFor(this.id)[e] = r;
    },
  });
}
function Vf(n, e, t) {
  for (let r of e)
    if (r.includes(".")) {
      if (n.getObjectKey(r, t) !== void 0) return !0;
      let o = n.getObjectKey(r, "_deleted");
      if (Array.isArray(o) && o.includes(t)) return !1;
    } else if (n.getObjectKey(r, t) !== void 0) return !0;
  return !1;
}
var Hi = class extends Fr {
  constructor(t) {
    super();
    C(this, "container", t);
  }
  getEditedChildren() {
    return this.container.dataTree.getEditedChildrenFor(this.container.id);
  }
  getChildIds() {
    let t = this.container.dataTree,
      r = t.store.getChildrenIds(this.container.originalId ?? this.container.id);
    if (!t.hasHiddenNodes) return r;
    let o = this.container.ownerId;
    return r.filter((i) => {
      let a = this.container.originalId && o ? cn(o, i) : i;
      return !t.isHiddenUntilCommit(a);
    });
  }
  get length() {
    let t = this.getEditedChildren();
    return t ? t.length : this.getChildIds().length;
  }
  at(t) {
    let r = this.getEditedChildren();
    if (r) return r.at(t);
    let o = this.getChildIds().at(t);
    if (o) return ke(this.container, o);
  }
  indexOf(t) {
    let r = this.getEditedChildren();
    if (r) return r.indexOf(t);
    let o = this.getChildIds(),
      i = this.container.ownerId;
    return this.container.originalId && i
      ? o.findIndex((a) => cn(i, a) === t.id)
      : o.findIndex((a) => a === t.id);
  }
  some(t) {
    let r = this.getEditedChildren();
    if (r) return r.some(t);
    let o = this.getChildIds();
    for (let i = 0; i < o.length; i++) {
      let a = o[i],
        s = ke(this.container, a);
      if (t(s, i)) return !0;
    }
    return !1;
  }
  every(t) {
    let r = this.getEditedChildren();
    if (r) return r.every(t);
    let o = this.getChildIds();
    for (let i = 0; i < o.length; i++) {
      let a = o[i],
        s = ke(this.container, a);
      if (!t(s, i)) return !1;
    }
    return !0;
  }
  forEach(t) {
    let r = this.getEditedChildren();
    if (r) return r.forEach(t);
    let o = this.getChildIds();
    for (let i = 0; i < o.length; i++) {
      let a = o[i],
        s = ke(this.container, a);
      t(s, i);
    }
  }
  map(t) {
    let r = this.getEditedChildren();
    if (r) return r.map(t);
    let o = this.getChildIds(),
      i = Array(o.length);
    for (let a = 0; a < o.length; a++) {
      let s = o[a],
        l = ke(this.container, s);
      i[a] = t(l, a);
    }
    return i;
  }
  reduce(t, r) {
    let o = this.getEditedChildren();
    if (o) return o.reduce(t, r);
    let i = this.getChildIds(),
      a = r;
    for (let s = 0; s < i.length; s++) {
      let l = i[s],
        d = ke(this.container, l);
      a = t(a, d, s);
    }
    return a;
  }
  filter(t) {
    let r = this.getEditedChildren();
    if (r) return r.filter(t);
    let o = this.getChildIds(),
      i = [];
    for (let a = 0; a < o.length; a++) {
      let s = o[a],
        l = ke(this.container, s);
      t(l, a) && i.push(l);
    }
    return i;
  }
  sort() {
    throw new Error("Unsupported");
  }
  find(t) {
    let r = this.findIndex(t);
    if (r !== -1) return this.at(r);
  }
  findIndex(t) {
    let r = this.getEditedChildren();
    if (r) return r.findIndex(t);
    let o = this.getChildIds();
    for (let i = 0; i < o.length; i++) {
      let a = o[i],
        s = ke(this.container, a);
      if (t(s, i)) return i;
    }
    return -1;
  }
  findLast(t) {
    let r = this.findLastIndex(t);
    if (r !== -1) return this.at(r);
  }
  findLastIndex(t) {
    let r = this.getEditedChildren();
    if (r) return r.findLastIndex(t);
    let o = this.getChildIds();
    for (let i = o.length - 1; i >= 0; i--) {
      let a = o[i],
        s = ke(this.container, a);
      if (t(s, i)) return i;
    }
    return -1;
  }
  slice(t, r) {
    let o = this.getEditedChildren();
    if (o) return o.slice(t, r);
    let i = this.getChildIds().slice(t, r),
      a = Array(i.length);
    for (let s = 0; s < i.length; s++) {
      let l = i[s],
        d = ke(this.container, l);
      a[s] = d;
    }
    return a;
  }
  splice() {
    throw new Error("Unsupported");
  }
  push() {
    throw new Error("Unsupported");
  }
  toArray() {
    let t = this.getEditedChildren();
    if (t) return t.slice();
    let r = this.getChildIds(),
      o = Array(r.length);
    for (let i = 0; i < r.length; i++) {
      let a = r[i],
        s = ke(this.container, a);
      o[i] = s;
    }
    return o;
  }
  *[Symbol.iterator]() {
    let t = this.getEditedChildren();
    if (t) return yield* t;
    let r = this.getChildIds();
    for (let o = 0; o < r.length; o++) {
      let i = r[o];
      yield ke(this.container, i);
    }
  }
};
function vc() {
  if (Zn.size > 0) return;
  let n = vd();
  y(
    n.type === "prototype.dynamicOverrides",
    () => `Need prototype.dynamicOverrides driver, got: ${n.type}`
  );
  let e = sd();
  for (let t of e) {
    class r {
      constructor(s, l, d, f, c) {
        C(this, "id", l);
        (this.setPrivateProperty("dataTree", s),
          this.setPrivateProperty("sourceIds", d),
          this.setPrivateProperty("ownerId", f),
          this.setPrivateProperty("originalId", c));
        for (let u of d) {
          let m = s.store.getObjectKeys(u, Df);
          for (let h of m) h in this || (Vf(s.store, d, h) && this.createControlPropForKey(h));
        }
      }
      createControlPropForKey(s) {
        s in this ||
          Object.defineProperty(this, s, {
            enumerable: !0,
            configurable: !0,
            get() {
              let l = this.dataTree.editedNodes.get(this.id)?.[s];
              if (l !== void 0) return l;
              for (let d of this.sourceIds)
                if (d.includes(".")) {
                  let f = this.dataTree.store.getObjectKey(d, s);
                  if (f !== void 0) return f;
                  let c = this.dataTree.store.getObjectKey(d, "_deleted");
                  if (Array.isArray(c) && c.includes(s)) return;
                } else {
                  let f = this.dataTree.store.getObjectKey(d, s);
                  if (f !== void 0) return f;
                }
            },
            set(l) {
              this.dataTree.getOrCreateEditedNodeFor(this.id)[s] = l;
            },
          });
      }
      get cache() {
        if (!this.$$cache$$) {
          let s =
            this.dataTree.getPersistedNodeCache(this.id) ??
            this.dataTree.setPersistedNodeCache(this.id, new this[ki].cacheClass());
          ((s.isShallowLoad = !1),
            this.ownerId && this.ownerId !== this.id && (s.replicaAncestorId = this.ownerId));
          let l = this.id,
            { store: d } = this.dataTree;
          return (
            Object.defineProperty(s, "localizationSourceGroup", {
              enumerable: !0,
              configurable: !0,
              get() {
                return d.getLatest(l)?.cache?.localizationSourceGroup;
              },
              set(f) {
                let c = d.getLatest(l);
                c && (c.cache || (c.cache = {}), (c.cache.localizationSourceGroup = f));
              },
            }),
            this.setPrivateProperty("$$cache$$", s),
            s
          );
        }
        return this.$$cache$$;
      }
      get loaded() {
        return this;
      }
      isLoaded() {
        return !0;
      }
      getDataOverrides() {
        let s = {};
        for (let f of ["id", "__class", "parentid", "originalid"]) f in this && (s[f] = this[f]);
        let l = this.dataTree.store.getObject(this.id) ? [this.id] : this.sourceIds;
        for (let f = l.length - 1; f >= 0; f--) {
          let c = l[f],
            u = this.dataTree.store.getObjectWithShallowChildren(c, 0);
          if (!u) continue;
          for (let h in u) {
            if (h === "_deleted" || !(h in this)) continue;
            let g = this[h];
            s[h] = g;
          }
          let m = u._deleted;
          if (Array.isArray(m)) for (let h of m) h in u || (s[h] = void 0);
        }
        let d = this.dataTree.getEditedNodeFor(this.id);
        if (d) for (let f in d) s[f] = d[f];
        return s;
      }
      tree() {
        return this.dataTree.getCanvasTreeProxy();
      }
      get update() {
        if (this.originalId) {
          let s = `${this.ownerId}.replicaInfo.overrides.${this.originalId}`,
            l = this.dataTree.store.getLatest(s)?.update ?? 0,
            d = this.dataTree.store.getLatest(this.originalId)?.update ?? 0;
          return l + d;
        }
        return this.dataTree.store.getLatest(this.id)?.update ?? 0;
      }
      cloneEmpty() {
        return new t();
      }
      *walk() {
        let s = [this];
        for (; s.length > 0; ) {
          let l = s.pop();
          if ((y(l), !this.dataTree.isDataNode(l))) {
            yield* l.walk();
            continue;
          }
          yield l;
          let d = l,
            f = this.dataTree.editedChildren.get(d.id);
          if (f) {
            for (let u = f.length - 1; u >= 0; --u) s.push(f[u]);
            continue;
          }
          let c = this.dataTree.store.getChildrenIds(d.originalId ?? d.id);
          if (this.dataTree.hasHiddenNodes) {
            let u = d.ownerId;
            c = c.filter((m) => {
              let h = d.originalId && u ? cn(u, m) : m;
              return !this.dataTree.isHiddenUntilCommit(h);
            });
          }
          for (let u = c.length - 1; u >= 0; --u) {
            let m = c[u];
            s.push(ke(d, m));
          }
        }
      }
      *fastWalk() {
        let l = [this.id],
          d = [];
        for (; l.length > 0; ) {
          let f = l.pop();
          if (f === -1) {
            d.pop();
            continue;
          }
          let c = f;
          y(c, "stackId must be set");
          let u = d.length > 0 ? d[d.length - 1] : void 0,
            m = this.dataTree.store.getObjectKey(`${c}.replicaInfo`, "master"),
            h,
            g,
            v;
          if (m) {
            ((h = u ? u.ownerSynthId + c : c), (g = m));
            let R = this.dataTree.store.getObjectKey(`${c}.replicaInfo`, "inheritsFrom");
            v = { ownerSynthId: h, ownerStoreId: c, masterId: m, inheritsFromOwnerStoreId: R };
          } else u ? ((h = u.ownerSynthId + c), (g = c)) : ((h = c), (g = c));
          let S = v ? [...d, v] : [...d],
            b = v !== void 0,
            w = c,
            P = this.dataTree.store.getObjectKey(c, "__class");
          y(P);
          let V = {
            id: h,
            __class: P,
            get: (R) => {
              if (R === "parentid") {
                if (b) {
                  let G = this.dataTree.store.getParentId(w);
                  if (!G || G === Y) return null;
                  let H = S[S.length - 2];
                  return H ? (G === H.masterId ? H.ownerSynthId : H.ownerSynthId + G) : G;
                }
                let $ = this.dataTree.store.getParentId(g);
                if (!$ || $ === Y) return null;
                let j = S[S.length - 1];
                return j ? ($ === j.masterId ? j.ownerSynthId : j.ownerSynthId + $) : $;
              }
              if (R === "originalid") return S.length === 0 ? null : g;
              let F = Zn.get(P)?.prototype?.[qn];
              if (!R.startsWith("$") && !(R in F)) return;
              if (R === "replicaInfo") return Hn(this.dataTree.store.getObjectKey(w, R));
              let J = this.dataTree.getEditedNodeFor(h);
              if (J && R in J) return J[R];
              let O = Vi;
              if (
                (R === "masks" ? (O = Mi) : (R === "guidesX" || R === "guidesY") && (O = Ei),
                S.length > 0)
              ) {
                for (let j = 0; j < S.length; j++) {
                  let G = S[j],
                    De = b && j === S.length - 1 ? G.masterId : h.slice(G.ownerSynthId.length),
                    be = [`${G.ownerStoreId}.replicaInfo.overrides.${De}`];
                  G.inheritsFromOwnerStoreId &&
                    be.push(`${G.inheritsFromOwnerStoreId}.replicaInfo.overrides.${De}`);
                  for (let re of be) {
                    let Te = this.dataTree.store.getObjectKey(re, R);
                    if (Te !== void 0) return O(Te);
                    let Be = this.dataTree.store.getObjectKey(re, "_deleted");
                    if (Array.isArray(Be) && Be.includes(R)) return;
                  }
                }
                let $ = this.dataTree.store.getObjectKey(g, R);
                if ($ !== void 0) return O($);
              } else {
                let $ = this.dataTree.store.getObjectKey(c, R);
                if ($ !== void 0) return O($);
              }
              return O(F[R]);
            },
            skipChildren: !1,
          };
          if ((v && (d.push(v), l.push(-1)), yield V, V.skipChildren)) {
            v && (y(l.pop() === -1), d.pop());
            continue;
          }
          let x = this.dataTree.store.getChildrenIds(m ?? c);
          for (let R = x.length - 1; R >= 0; R--) {
            let F = x[R];
            l.push(F);
          }
        }
      }
      setProp(s, l) {
        let d = this.dataTree.getOrCreateEditedNodeFor(this.id);
        ((d[s] = l), s.startsWith("$") && this.createControlPropForKey(s));
      }
      hasProp(s) {
        return s in this;
      }
      deleteProp(s) {
        (this.dataTree.assertCanvasTree(), delete this[s]);
        let l = this.dataTree.getOrCreateEditedNodeFor(this.id);
        l[s] = void 0;
      }
      assign(...s) {
        let l = this.dataTree.getOrCreateEditedNodeFor(this.id);
        Object.assign(l, ...s);
        for (let d of s) for (let f in d) f.startsWith("$") && this.createControlPropForKey(f);
      }
      get [wo]() {
        return !0;
      }
      setPrivateProperty(s, l) {
        Object.defineProperty(this, s, { value: l, enumerable: !1 });
      }
      asDraft() {
        return this.dataTree.getNodeAsDraft(this);
      }
    }
    let o = t.prototype;
    Object.setPrototypeOf(r.prototype, o);
    let i = Object.getOwnPropertyDescriptors(o);
    for (let a in i) {
      if (o[Cd].includes(a)) continue;
      let s = i[a];
      s.get &&
        s.set &&
        (a === "originalid"
          ? Object.defineProperty(r.prototype, a, {
              enumerable: !0,
              get() {
                return this.originalId;
              },
              set(l) {
                this.dataTree.getOrCreateEditedNodeFor(this.id)[a] = l;
              },
            })
          : ["isMaster", "isExternalMaster"].includes(a)
            ? Object.defineProperty(r.prototype, a, {
                enumerable: !0,
                get() {
                  let l = this.dataTree.editedNodes.get(this.id)?.[a];
                  return l !== void 0 ||
                    ((l = this.dataTree.store.getObjectKey(this.id, a)), l !== void 0)
                    ? l
                    : this[qn][a];
                },
                set(l) {
                  this.dataTree.getOrCreateEditedNodeFor(this.id)[a] = l;
                },
              })
            : a === "parentid"
              ? Object.defineProperty(r.prototype, a, {
                  enumerable: !0,
                  get() {
                    let l = this.originalId ?? this.id,
                      d = this.dataTree.store.getParentId(l);
                    if (!(d === void 0 || d === Y)) {
                      if (this.ownerId === this.id) return d;
                      if (this.originalId && this.ownerId) {
                        let f = Ut(this.dataTree, this.ownerId);
                        return d === f.originalId ? this.ownerId : cn(f.id, d);
                      }
                      return d;
                    }
                  },
                })
              : a === "replicaInfo"
                ? Object.defineProperty(r.prototype, a, {
                    enumerable: !0,
                    get() {
                      let l = this.dataTree.editedNodes.get(this.id)?.[a];
                      if (l !== void 0) return l;
                      let d = this.$$revivedReplicaInfo$$;
                      if (d) return d;
                      let f = this.dataTree.store.getObjectKey(this.id, a);
                      if (f !== void 0) {
                        let c = Hn(f);
                        return (c && this.setPrivateProperty("$$revivedReplicaInfo$$", c), c);
                      }
                      return this[qn][a];
                    },
                    set(l) {
                      this.dataTree.getOrCreateEditedNodeFor(this.id)[a] = l;
                    },
                  })
                : a === "masks"
                  ? Gi(r.prototype, a, Mi)
                  : a === "guidesX" || a === "guidesY"
                    ? Gi(r.prototype, a, Ei)
                    : Gi(r.prototype, a, Vi));
    }
    (o[ki].hasChildren &&
      Object.defineProperty(r.prototype, "children", {
        enumerable: !0,
        get() {
          if (this.$$children$$) return this.$$children$$;
          let a = new Hi(this);
          return (this.setPrivateProperty("$$children$$", a), a);
        },
      }),
      Zn.set(o.__class, r));
  }
}
function ke(n, e) {
  return n.ownerId ? Ji(n.dataTree, n.ownerId, e) : Ut(n.dataTree, e);
}
var xo = class n {
  constructor(e, t, r, o) {
    C(this, "store", e);
    C(this, "canvasTree", t);
    C(this, "treeQueryContext", r);
    C(this, "persistedCaches", o);
    C(this, "insertedNodes", new Map());
    C(this, "removedNodes", new Set());
    C(this, "editedNodes", new Map());
    C(this, "editedChildren", new Map());
    C(this, "excludedFromGet", new Set());
    C(this, "hierarchy");
    C(this, "seq");
    C(this, "treeProxy", null);
    C(this, "nodeProxyCache", new Map());
    ((this.seq = e.seq), (this.hierarchy = e.getHierarchy()), vc());
  }
  equals(e) {
    return e instanceof n
      ? !this.canvasTree || !e.canvasTree
        ? this === e
        : this.canvasTree !== e.canvasTree
          ? !1
          : this.seq === e.seq
      : !1;
  }
  assertCanvasTree() {
    y(this.canvasTree, "This DataTree is readonly because no canvasTree was provided.");
  }
  get lineage() {
    return (this.assertCanvasTree(), this.canvasTree.lineage);
  }
  get root() {
    let e = this.canvasTree?.root.id ?? this.hierarchy.getRootId();
    y(e, "DataTree root does not exist");
    let t = this.get(e);
    return (y(t, "Root node must exist"), t);
  }
  get editClosed() {
    return this.canvasTree?.latestTree().editClosed ?? !0;
  }
  get isViewOnly() {
    return this.canvasTree?.latestTree().isViewOnly ?? !0;
  }
  get inEditor() {
    return this.canvasTree?.latestTree().inEditor ?? !1;
  }
  latestTree() {
    return (
      this.assertCanvasTree(),
      new n(
        this.store,
        this.canvasTree.latestTree(),
        this.treeQueryContext,
        this.persistedCaches
      ).getCanvasTreeProxy()
    );
  }
  hasUncommittedChanges() {
    return this.removedNodes.size > 0 || this.insertedNodes.size > 0 || this.editedNodes.size > 0;
  }
  getOrCreateNodeById(e) {
    return Ut(this, e);
  }
  getAnyNode(e) {
    return e ? (this.get(e) ?? (this.store.has(e) ? Ut(this, e) : null)) : null;
  }
  pushChangesForNode(e, t, r) {
    let o = this.store.getObject(t);
    y(typeof o?.__class == "string", () => `editedNode id is missing a __class: ${t}`);
    let i = yn(o.__class);
    y(i, () => `constructor not found for __class: ${o.__class}`);
    let a = new i({ id: o.id, ...o }),
      s = r ? new i({ id: o.id, ...o, ...r }) : null,
      l = mt(a, s, !0);
    l && e.push(l);
    let d = [];
    pt(d, a, s);
    for (let f of d) e.push(f);
  }
  computeReversibleChanges() {
    this.assertCanvasTree();
    let e = [];
    for (let [u, m] of this.insertedNodes) {
      let h = null;
      if (this.hierarchy.inTree(m.id)) {
        let S = this.store.getObject(m.id);
        y(typeof S?.__class == "string", () => `inserted node id is missing a __class: ${m.id}`);
        let b = yn(S.__class);
        (y(b, () => `constructor not found for __class: ${S.__class}`),
          (h = new b({ id: S.id, ...S })));
      }
      let g = mt(h, m, !0);
      g && e.push(g);
      let v = [];
      pt(v, h, m);
      for (let S of v) e.push(S);
    }
    let t = [],
      r = new Set(this.removedNodes),
      o = this.hierarchy.getRootId(),
      i = (u) => {
        for (let m of this.hierarchy.getChildrenIds(u)) i(m);
        r.has(u) && (t.push(u), r.delete(u));
      };
    o && i(o);
    for (let u of r) t.push(u);
    let a = new Set(t),
      s = new Map();
    for (let u of t) {
      let h = this.store.getObject(u)?.replicaInfo,
        g = typeof h?.master == "string" ? h.master : void 0;
      if (!g || !a.has(g)) continue;
      let v = s.get(g) ?? [];
      (v.push(u), s.set(g, v));
    }
    let l = [],
      d = new Set(),
      f = new Set(),
      c = (u) => {
        if (!d.has(u) && !f.has(u)) {
          f.add(u);
          for (let m of s.get(u) ?? []) c(m);
          (f.delete(u), d.add(u), l.push(u));
        }
      };
    for (let u of t) c(u);
    for (let u of l) this.pushChangesForNode(e, u, null);
    for (let [u, m] of this.editedNodes) {
      let h = this.get(u);
      (y(h, () => `node not in data tree, id: ${u}`),
        !(h.originalid && !h.replicaInfo) && this.pushChangesForNode(e, u, m));
    }
    return e;
  }
  preCommit(e) {
    (this.assertCanvasTree(),
      this.moveEditsToRootOrShallowScopes(),
      this.markScopesAsDraft(),
      this.markReplicasAsDraft());
    for (let [r, o] of this.editedNodes) this.get(r)?.preCommit(this.getCanvasTreeProxy(), e);
    let t = this.computeReversibleChanges();
    return (
      this.editedNodes.clear(),
      this.removedNodes.clear(),
      this.editedChildren.clear(),
      this.insertedNodes.clear(),
      this.excludedFromGet.clear(),
      t
    );
  }
  moveEditsToRootOrShallowScopes() {
    this.assertCanvasTree();
    let e = new Set();
    for (let t of this.editedNodes.keys()) {
      if (t === this.canvasTree.root.id) {
        (this.canvasTree.root.set({ ...this.editedNodes.get(t) }), e.add(t));
        continue;
      }
      let r = this.canvasTree.get(t);
      r && ue(r) && (r.isLoaded() || (r.set({ ...this.editedNodes.get(t) }), e.add(t)));
    }
    for (let t of e) this.editedNodes.delete(t);
  }
  markScopesAsDraft() {
    this.assertCanvasTree();
    let e = new Map(),
      t = new Set(),
      r = (o) => {
        let i = this.get(o),
          a = Ro(this.store, i?.originalid ?? o, e);
        if (!a || t.has(a)) return;
        let s = this.canvasTree.get(a);
        !s || !Re(s, this.getCanvasTreeProxy()) || (t.add(a), s.asDraft(this.canvasTree));
      };
    for (let [o, i] of this.insertedNodes)
      (y(i.parentid, "node must have a parent id"), r(i.parentid));
    for (let o of this.removedNodes) r(o);
    for (let o of this.editedNodes.keys()) r(o);
  }
  markReplicasAsDraft() {
    this.assertCanvasTree();
    for (let e of this.removedNodes) {
      if (this.canvasTree.has(e)) continue;
      let r = this.hierarchy.getNodeData(e)?.inMaster;
      if (!r) continue;
      let o = this.get(r);
      if (!o) continue;
      o.asDraft();
      let i = this.hierarchy.masterIdToReplicaIds.get(r);
      if (i)
        for (let a of i) {
          let s = this.get(a);
          s && (this.editedNodes.has(a) || (this.editedNodes.set(a, {}), s.asDraft()));
        }
    }
    for (let e of this.editedNodes.keys()) {
      if (this.canvasTree.has(e)) continue;
      let t = this.get(e);
      if ((y(t, () => `node not in data tree, id: ${e}`), !(!!t.originalid && !t.replicaInfo)))
        continue;
      let o = this.get(t.parentid);
      for (; o && !o.replicaInfo; ) o = this.get(o.parentid);
      (y(o, "replica child must belong to a replica"),
        this.editedNodes.has(o.id) || (this.editedNodes.set(o.id, {}), o.asDraft()));
    }
  }
  getCanvasTreeProxy() {
    return (
      this.assertCanvasTree(),
      this.treeProxy ||
        ((this.treeProxy = Cc(this)),
        this.treeProxy.setService("treeQueryContext", this.treeQueryContext)),
      this.treeProxy
    );
  }
  getEditedNodeFor(e) {
    return this.editedNodes.get(e);
  }
  getOrCreateEditedNodeFor(e) {
    this.assertCanvasTree();
    let t = this.editedNodes.get(e);
    return (t || ((t = {}), this.editedNodes.set(e, t)), t);
  }
  getEditedChildrenFor(e) {
    return this.editedChildren.get(e);
  }
  isDataNode(e) {
    return e ? !!e[wo] : !1;
  }
  getScopeNodeFor(e) {
    if (!e) return null;
    for (; e; ) {
      if (ue(e)) return e;
      e = this.get(e.parentid);
    }
    return null;
  }
  getParent(e) {
    if (!e) return null;
    let t = this.hierarchy.getParentId(e);
    return t === void 0 || t === Y ? null : this.get(t);
  }
  isHiddenUntilCommit(e) {
    if (this.removedNodes.has(e)) return !0;
    let t = this.excludedFromGet.has(e);
    if (this.removedNodes.size === 0 && !t) return !1;
    let r = this.hierarchy.getLatestReplicaChild(e);
    if (r && this.removedNodes.has(r[2])) return !0;
    if (t) {
      let o = r?.[0] ?? e;
      if (!this.canvasTree?.has(o) && !this.insertedNodes.has(o)) return !0;
    }
    return !1;
  }
  get hasHiddenNodes() {
    return this.removedNodes.size > 0 || this.excludedFromGet.size > 0;
  }
  get(e) {
    if (!e || this.isHiddenUntilCommit(e)) return null;
    if (this.insertedNodes.has(e)) return this.insertedNodes.get(e) ?? null;
    let t = this.nodeProxyCache.get(e);
    if (t) {
      let r = t.deref();
      if (r) return r;
    }
    if (!this.hierarchy.inTree(e) && !this.canvasTree?.has(e)) {
      let r = this.hierarchy.getLatestReplicaChild(e);
      if (!r) return null;
      let [o, , i] = r;
      return Ji(this, o, i);
    }
    return Ut(this, e);
  }
  has(e) {
    return !e || this.isHiddenUntilCommit(e)
      ? !1
      : this.insertedNodes.has(e) || this.hierarchy.inTree(e) || this.canvasTree?.has(e)
        ? !0
        : this.hierarchy.getLatestReplicaChild(e) !== void 0;
  }
  hasNodeAnywhere(e) {
    return e
      ? this.insertedNodes.has(e) || this.canvasTree?.hasNodeAnywhere(e)
        ? !0
        : this.store.has(e)
      : !1;
  }
  getNodeWithTrait(e, t) {
    let r = this.get(e);
    return !r || !t(r) ? null : r;
  }
  getNodesWithTrait(e, t) {
    let r = [];
    for (let o of e) {
      let i = this.get(o);
      !i || !t(i) || r.push(i);
    }
    return r;
  }
  getNode(e) {
    return this.get(e);
  }
  getNodeAsDraft(e) {
    return (
      this.assertCanvasTree(),
      this.isDataNode(e)
        ? ((e.cache.draft = e), (e.mutable = !0), e)
        : this.canvasTree.getNodeAsDraft(e)
    );
  }
  isGroundNode(e) {
    let t = this.get(e.parentid);
    return !t || ue(e) ? !1 : ue(t);
  }
  insertChildrenRecursive(e) {
    if ((this.assertCanvasTree(), e.children))
      for (let t of e.children)
        (this.removedNodes.delete(t.id),
          this.insertedNodes.set(t.id, t),
          this.canvasTree.recordInsert({ node: t, toParent: e.id, toPosition: -1 }),
          this.insertChildrenRecursive(t));
  }
  isLoadedInCanvasTree(e) {
    if ((this.assertCanvasTree(), !this.canvasTree.has(e))) return !1;
    let t = this.canvasTree.get(e);
    return !ue(t) || t.isLoaded();
  }
  insertNode(e, t, r, o) {
    if ((this.assertCanvasTree(), (t = t ?? this.canvasTree.root.id), this.isLoadedInCanvasTree(t)))
      return this.canvasTree.insertNode(e, t, r, o);
    y(!this.has(e.id), "Node already exists in tree");
    let i = this.get(t);
    (y(i, () => `Parent: ${t} not found in tree`),
      Wr(this.getCanvasTreeProxy(), e, t, r, o),
      ai(e) && Ii(e, this.getCanvasTreeProxy()),
      (e.parentid = t));
    let a = this.getOrMaterializeEditedChildren(t);
    return (
      (r = r === void 0 ? a.length : r),
      r < 0 && (r += a.length + 1),
      a.splice(r, 0, e),
      this.canvasTree.recordInsert({ node: e, toParent: e.parentid, toPosition: r }),
      this.insertChildrenRecursive(e),
      this.removedNodes.delete(e.id),
      this.insertedNodes.set(e.id, e),
      e
    );
  }
  removeChildrenRecursive(e) {
    if ((this.assertCanvasTree(), e.children && !Pr(e)))
      for (let t of e.children)
        (this.removeChildrenRecursive(t),
          this.insertedNodes.delete(t.id),
          this.editedNodes.delete(t.id),
          this.editedChildren.delete(t.id),
          this.hierarchy.inTree(t.id) && this.removedNodes.add(t.id),
          this.canvasTree.recordRemove({ node: t, fromParent: e.id, fromPosition: 0 }));
  }
  excludeSubtreeFromGet(e) {
    if (e.children)
      for (let t of e.children)
        (this.removedNodes.has(t.id) || this.excludedFromGet.add(t.id),
          this.excludeSubtreeFromGet(t));
  }
  remove(e) {
    this.assertCanvasTree();
    let t = this.get(e);
    return t ? (this.removeNode(t), this) : this;
  }
  removeNode(e) {
    this.assertCanvasTree();
    let t = this.canvasTree.get(e.id);
    if (t) {
      (this.isDataNode(e) && ue(t) && !t.isLoaded() && this.removeChildrenRecursive(e),
        this.excludeSubtreeFromGet(e),
        this.canvasTree.removeNode(t),
        this.excludedFromGet.add(e.id));
      return;
    }
    (y(e.parentid, "Node must have a parent id"),
      y(!e.originalid || e.replicaInfo, "Cannot delete nodes of replica's"),
      Wr(this.getCanvasTreeProxy(), e),
      ai(e) && Ii(e, this.getCanvasTreeProxy()));
    let r = this.getOrMaterializeEditedChildren(e.parentid),
      o = r.findIndex((a) => a.id === e.id);
    y(o !== -1, () => `Node: ${e.id} not found in parent: ${e.parentid}`);
    let i = r.splice(o, 1)[0];
    (e.replicaInfo || this.removeChildrenRecursive(i),
      this.excludeSubtreeFromGet(i),
      this.insertedNodes.delete(e.id),
      this.editedNodes.delete(e.id),
      this.editedChildren.delete(e.id),
      this.hierarchy.inTree(e.id) && this.removedNodes.add(e.id),
      this.canvasTree.recordRemove({ node: e, fromParent: e.parentid, fromPosition: o }));
  }
  isAncestorOf(e, t) {
    let r = this.get(t);
    for (y(r, "Node to check must exist"); r.id !== this.root.id; ) {
      if (r.id === e) return !0;
      ((r = this.get(r.parentid)), y(r, "Ancestor node must exist"));
    }
    return r.id === e;
  }
  move(e, t, r) {
    return (this.assertCanvasTree(), this.moveNode(this.get(e), t || this.root.id, r), this);
  }
  moveNode(e, t, r = -1, o) {
    this.assertCanvasTree();
    let i = this.isLoadedInCanvasTree(t);
    if (this.canvasTree.has(e.id) && i) {
      let f = this.canvasTree.get(e.id);
      return this.canvasTree.moveNode(f, t, r, o);
    }
    if (
      (y(
        !this.isLoadedInCanvasTree(e.id) && !i,
        "cannot move a node from between loaded and unloaded scopes"
      ),
      Wr(this.getCanvasTreeProxy(), e, t, r, o),
      y(!e.originalid || e.replicaInfo, "Cannot move nodes of replica's"),
      e.parentid === t)
    )
      return this.moveNodeIndex(e, r);
    (y(this.has(e.id), "Node does not exist in the data tree"),
      y(this.has(t), "Parent does not exist in the data tree"),
      y(!this.isAncestorOf(e.id, t), "Node must not be an ancestor of its new parent"));
    let a = e.parentid;
    y(a, "Must be moving from a node");
    let s = this.getOrMaterializeEditedChildren(a),
      l = s.findIndex((f) => f.id === e.id);
    (y(l >= 0, "Node must be known as child"), s.splice(l, 1));
    let d = this.getOrMaterializeEditedChildren(t);
    return (
      r < 0 && (r += d.length + 1),
      d.splice(r, 0, e),
      e.set({ parentid: t }),
      this.insertedNodes.has(t) ||
        (this.getNodeAsDraft(this.get(t)), this.editedNodes.has(t) || this.editedNodes.set(t, {})),
      this.canvasTree.recordMove({
        node: e,
        fromParent: a,
        fromPosition: l,
        toPosition: r,
        toParent: t,
      }),
      e
    );
  }
  moveNodeIndex(e, t = -1) {
    (this.assertCanvasTree(),
      y(this.has(e.id), "Node does not exist in the data tree"),
      y(e.parentid, "Node must have a parent id"));
    let r = this.getOrMaterializeEditedChildren(e.parentid);
    t < 0 && (t += r.length + 1);
    let o = r.findIndex((i) => i.id === e.id);
    return (
      y(o >= 0, "Node to move not found in parent"),
      o === t ||
        (r.splice(o, 1),
        r.splice(t, 0, e),
        this.insertedNodes.has(e.id) ||
          (this.getNodeAsDraft(e), this.editedNodes.has(e.id) || this.editedNodes.set(e.id, {})),
        this.canvasTree.recordMove({
          node: e,
          fromParent: e.parentid,
          fromPosition: o,
          toPosition: t,
          toParent: e.parentid,
        })),
      e
    );
  }
  getOrMaterializeEditedChildren(e) {
    this.assertCanvasTree();
    let t = this.editedChildren.get(e);
    return (
      t ||
        ((t = this.store
          .getChildrenIds(e)
          .map((o) => {
            if (this.hierarchy.inTree(o)) return Ut(this, o);
          })
          .filter((o) => o !== void 0)),
        this.editedChildren.set(e, t)),
      t
    );
  }
  getValueForWithDefault(e, t) {
    let r = e.id;
    if (t === "parentid") return this.hierarchy.getParentId(r);
    let o = this.store.getRawObjectKey(r, t);
    if (o === Yl || o === Y) {
      let i = e?.__class;
      if (!i) return;
      let a = oo(i);
      return a ? a[t] : void 0;
    }
    return o;
  }
  *iterateAll() {
    for (let e of this.hierarchy.classToIds.values())
      for (let t of e) {
        if (!this.hierarchy.inTree(t)) continue;
        let r = this.hierarchy.getNodeData(t);
        r && (yield r);
      }
  }
  iterateByClass(e) {
    return this.iterateByClasses([e]);
  }
  *iterateByClasses(e) {
    for (let t of e) {
      let r = this.hierarchy.classToIds.get(t);
      if (r)
        for (let o of r) {
          if (!this.hierarchy.inTree(o)) continue;
          let i = this.hierarchy.getNodeData(o);
          i && (yield i);
        }
    }
  }
  *iterateWithDataIdentifier() {
    for (let e of this.hierarchy.withDataIdentifierIds) {
      if (!this.hierarchy.inTree(e)) continue;
      let t = this.hierarchy.getNodeData(e);
      t && (yield t);
    }
  }
  *iterateByDataIdentifier(e) {
    let t = this.hierarchy.dataIdentifierToIds.get(e);
    if (t)
      for (let r of t) {
        if (!this.hierarchy.inTree(r)) continue;
        let o = this.hierarchy.getNodeData(r);
        o && (yield o);
      }
  }
  loadScopes(e, t) {
    let r = e.map((o) => o && this.get(o.id));
    if (t) {
      t(r);
      return;
    }
    return Promise.resolve(r);
  }
  beginAllowPartialScopeAccess() {}
  endAllowPartialScopeAccess() {}
  getPersistedNodeCache(e) {
    return this.persistedCaches?.get(e);
  }
  setPersistedNodeCache(e, t) {
    return (this.persistedCaches?.set(e, t), t);
  }
  withPersistedCaches(e) {
    return this.persistedCaches ? this.persistedCaches.with(e) : e();
  }
  persistCaches() {
    return this.persistedCaches?.acquire() ?? ul;
  }
};
var Lo = class {
  constructor() {
    C(this, "activeScopes", 0);
    C(this, "caches");
  }
  get(e) {
    return this.caches?.get(e);
  }
  set(e, t) {
    if (!this.caches) {
      if (this.activeScopes <= 0) return;
      this.caches = new Map();
    }
    this.caches.set(e, t);
  }
  acquire() {
    this.activeScopes += 1;
    let e = !1;
    return () => {
      e ||
        ((e = !0),
        (this.activeScopes -= 1),
        this.activeScopes <= 0 && ((this.activeScopes = 0), (this.caches = void 0)));
    };
  }
  async with(e) {
    let t = this.acquire();
    try {
      return await e();
    } finally {
      t();
    }
  }
};
function Sc(n) {
  let {
      error: e,
      preReconcileDataLedger: t,
      hierarchyChanges: r,
      reconciledChanges: o,
      editReasons: i,
      probeStore: a,
    } = n,
    s = new Map();
  for (let c of o) s.set(c.id, c);
  let l = new Map();
  for (let c of r) l.set(c.node, c);
  let d = new Set();
  for (let c of o) d.add(c.id);
  for (let c of r) d.add(c.node);
  for (let c of Object.keys(t)) d.add(c);
  let f = [];
  for (let c of d) {
    if (f.length >= 50) break;
    let u = a(c);
    f.push({
      nodeId: c,
      reconciledChange: s.get(c),
      hierarchyChange: l.get(c),
      dataLedgerEntry: t[c],
      storeExists: u.exists,
      storeParentId: u.parentid,
    });
  }
  return {
    detail: e.message,
    editReasons: i,
    changedNodeCount: d.size,
    truncated: d.size > 50,
    records: f,
  };
}
var Do = class {
  constructor() {
    C(this, "offsetPerParent", new Map());
  }
  recordSkippedInsert(e, t, r) {
    let o = this.popOffset(e, r, "remove");
    (this.rebaseAfterInsert(e, t), o || this.pushOffset(e, -1, t, r));
  }
  recordSkippedRemove(e, t, r) {
    let o = this.popOffset(e, r, "insert");
    (this.rebaseAfterRemove(e, t), o || this.pushOffset(e, 1, t, r));
  }
  recordSkippedMove(e, t, r, o, i) {
    (this.popOffset(e, i, "insert"),
      this.rebaseAfterRemove(e, t),
      this.rebaseAfterInsert(r, o),
      this.pushOffset(r, -1, o, i));
  }
  commitInsert(e, t) {
    let r = this.offsetAt(e, t);
    return (this.rebaseAfterInsert(e, t), r);
  }
  commitRemove(e, t) {
    let r = this.offsetAt(e, t);
    return (this.rebaseAfterRemove(e, t), r);
  }
  commitMove(e, t, r, o) {
    let i = this.offsetAt(e, t);
    this.rebaseAfterRemove(e, t);
    let a = this.offsetAt(r, o);
    return (this.rebaseAfterInsert(r, o), { fromOffset: i, toOffset: a });
  }
  pushOffset(e, t, r, o) {
    let i = this.offsetPerParent.get(e);
    (i || ((i = []), this.offsetPerParent.set(e, i)), i.push({ offset: t, position: r, node: o }));
  }
  popOffset(e, t, r) {
    let o = this.offsetPerParent.get(e);
    if (!o) return !1;
    for (let i = o.length - 1; i >= 0; i--) {
      let a = o[i];
      if (a.node === t && !(r === "insert" && a.offset > 0) && !(r === "remove" && a.offset < 0))
        return (o.splice(i, 1), !0);
    }
    return !1;
  }
  rebaseAfterInsert(e, t) {
    let r = this.offsetPerParent.get(e);
    if (r) for (let o of r) o.position >= t && (o.position += 1);
  }
  rebaseAfterRemove(e, t) {
    let r = this.offsetPerParent.get(e);
    if (r) for (let o of r) o.position > t && (o.position -= 1);
  }
  offsetAt(e, t) {
    let r = this.offsetPerParent.get(e);
    if (!r) return 0;
    let o = 0;
    for (let i of r) {
      if (i.offset < 0) {
        t > i.position && (o += i.offset);
        continue;
      }
      t >= i.position && (o += i.offset);
    }
    return o;
  }
};
var Ki = Symbol("deletedPatchKeys");
function bc(n, e) {
  let t = n,
    r = t[Ki];
  r ? r.push(e) : (t[Ki] = [e]);
}
function Ef(n) {
  return n[Ki] ?? [];
}
function qi(n) {
  return typeof n == "object" && !Array.isArray(n) && n !== null;
}
function Tc(n, e, t) {
  if (t(e) || t(n)) return hi(e);
  if (qi(n) && qi(e)) {
    for (let r of Ef(e)) delete n[r];
    for (let r of Object.keys(e)) n[r] = Tc(n[r], e[r], t);
    return n;
  }
  return qi(e) && e.constructor !== Object ? Object.assign({}, e) : hi(e);
}
function Qi(n, e, t) {
  let r = n;
  for (let o of e) r = Tc(r, o, t);
  return r;
}
function Ic(n) {
  return n === void 0;
}
function Mf(n, e, t) {
  let r = t.replicaInfo;
  if (r?.master) {
    let a = n.get(r.master);
    if (!a || !nn(a)) return !0;
  }
  if (r?.inheritsFrom) {
    let a = n.get(r.inheritsFrom);
    if (!a || !(St(a) || nn(a))) return !0;
  }
  let o = t.parentid;
  if (!o) return !1;
  let i = n.get(o);
  return i ? n.isAncestorOfNode(i, e) : !0;
}
function Ff(n, e, { children: t, ...r }) {
  if ((y(!n.has(e), () => `Tree must not have node: ${e}`), !r.parentid)) return null;
  let o = n.get(r.parentid);
  if (!o || (ue(o) && !o.isLoaded())) return null;
  let i = r.__class;
  if (!i) throw new Error("Unknown node class for: " + e);
  let a = yn(i);
  if (!a) throw new Error("Unknown node class for: " + e);
  let s = r.replicaInfo;
  if (s) {
    let d = n.get(s.master);
    if (!d || !nn(d)) throw Error("broken diff, replica without master: " + e + " " + s.master);
    let f = Hn(s),
      c = ll.create(n, d, {
        overrides: f?.overrides,
        owner: e,
        inheritsFrom: f?.inheritsFrom,
        duplicatedFrom: r.duplicatedFrom,
        fromDiff: !0,
      });
    return ((c.parentid = r.parentid), n.insertNode(c, c.parentid), c);
  }
  if (dd(a)) {
    y(Il(e), "Invalid EntityReferenceNode ID: " + e);
    let d = new a({ ...Fi(r), id: e });
    return (n.insertNode(d, d.parentid), d);
  }
  let l = new a({ ...Fi(r), id: e });
  return (n.insertNode(l, l.parentid), l);
}
function zf(n, e) {
  n.has(e) && n.remove(e);
}
function Nc(n) {
  if (n._deleted === void 0) return n;
  let { _deleted: e, ...t } = n;
  return { ...t, __deleted: e };
}
function kf(n) {
  let e = n.overrides;
  if (!e) return n;
  let t = {};
  for (let r in e) {
    let o = e[r];
    o && (t[r] = Nc(o));
  }
  return { ...n, overrides: t };
}
function Po(n) {
  return at(n);
}
function Of(n, e, t) {
  return n === "replicaInfo" || Po(e) || Po(t) ? !1 : $e(e);
}
function Bf(n, e, { children: t, ...r }) {
  let o = n.get(e);
  if (!o) return null;
  if (r.parentid && o.parentid !== r.parentid) {
    if (!n.has(r.parentid)) return (n.remove(e), null);
    n.move(e, r.parentid);
  }
  let i = o.asDraft(n);
  for (let a in r) {
    let s = i[a],
      l = r[a];
    Of(a, s, l) && (y($e(s), "prevValue not an object"), (r[a] = Qi({}, [s, l], Po)));
  }
  if (r.replicaInfo && St(o)) {
    let { overrides: a, _deleted: s, ...l } = r.replicaInfo;
    for (let f in a) {
      let c = a[f],
        u = i.replicaInfo?.overrides[f];
      if (u !== null && typeof u == "object" && !Array.isArray(u)) {
        let m = Nc(u);
        c = Qi({}, [m, c], Po);
      }
      c && md(n, i, f, c);
    }
    let d = Object.assign({}, i.replicaInfo, l);
    return (un.updateNode(n, i, r), (i.replicaInfo = d), (i.cache.rebuildReplica = !0), i);
  } else return (un.updateNode(n, o, r), i);
}
function Af(n, e) {
  if (n.length !== e.length) return !1;
  for (let t = 0; t < n.length; t++) if (n.at(t).id !== e[t]) return !1;
  return !0;
}
function _f(n, e, t) {
  if (Af(e.children, t)) return;
  let r = [];
  for (let a = 0, s = t.length; a < s; a++) {
    let l = n.get(t[a]);
    l && l.parentid === e.id && r.push(l);
  }
  let o = new Set(t),
    i = e.children;
  for (let a of i) o.has(a.id) || r.push(a);
  $f(i, r) || (e.asDraft(n).children = new An(r));
}
function $f(n, e) {
  return n.length !== e.length ? !1 : n.every((t, r) => e[r] === t);
}
function Rc(n, e, t, r = !1) {
  r || (n.applyingDiffs = !0);
  let o = Array.from(t.keys()),
    i = [...o];
  for (; i.length > 0; ) {
    let a = [];
    for (let s of i) {
      let l = t.get(s);
      Ic(l) || (Mf(n, s, l) ? a.push(s) : n.has(s) ? Bf(n, s, l) : l.__class && Ff(n, s, l));
    }
    if (a.length === 0 || a.length === i.length) break;
    i = a;
  }
  for (let a of o) {
    let l = t.get(a)?.children;
    if (!l || l.length === 0) continue;
    let d = n.get(a);
    if (d) {
      if (!d.children) throw Error("assertion failure: node has no children");
      _f(n, d, l);
    }
  }
  for (let a of o) {
    let s = t.get(a);
    Ic(s) && zf(n, a);
  }
  return (r || (n.applyingDiffs = !1), r ? n.commit(e) : n.commitDiffs(e));
}
function er(n, e) {
  let t = n.getParentId(e);
  for (; t && t !== Y; ) t = n.getParentId(t);
  return t === Y;
}
function Uf(n) {
  return n.some((e) => e.length > 0 && (e[0] === "parentid" || e[0] === "children"));
}
function tr(n, e, t) {
  let r = new Map();
  for (let [o, i] of e) {
    if (er(n, o)) {
      r.set(o, void 0);
      continue;
    }
    let a = n.getObjectWithShallowChildren(o, 0);
    if (!a || !a.__class || !yn(a.__class)) continue;
    let s = n.isRoot(o),
      l = {
        id: o,
        __class: a.__class,
        parentid: s ? null : n.getParentId(o),
        children: n.getChildrenIds(o) ?? [],
      },
      d = t.has(o) || Uf(i),
      f;
    d ? (f = [...Object.keys(a).map((c) => [c]), ...i]) : (f = [...i]);
    for (let c of f) {
      if (d && c[0] === "replicaInfo") {
        a.replicaInfo && (l.replicaInfo = kf(a.replicaInfo));
        continue;
      }
      let u = l,
        m = a;
      for (let [h, g] of c.entries()) {
        if (
          g === "parentid" ||
          g === "children" ||
          g === eo ||
          Array.isArray(u) ||
          u === null ||
          typeof u != "object"
        )
          break;
        let v = m === a,
          S = c[0] === "replicaInfo" && c[1] === "overrides" && h === 3,
          b = S && g === "_deleted" ? "__deleted" : g;
        if (!(b in u))
          if (m[g] === void 0) {
            if (v || S) {
              (u?._deleted || (u._deleted = []), u._deleted.push(g));
              break;
            }
            g in m ? (u[b] = void 0) : bc(u, b);
            break;
          } else {
            let P = m[g],
              V = P === null || typeof P != "object";
            if (h === c.length - 1 || V) {
              u[b] = P;
              break;
            }
            if (Array.isArray(P)) {
              u[b] = [...P];
              break;
            }
            u[b] = {};
          }
        u = u[b];
        let w = m[g];
        if (!$e(w)) break;
        m = w;
      }
    }
    (r.set(o, l),
      l.parentid &&
        (r.has(l.parentid) || r.set(l.parentid, { children: n.getChildrenIds(l.parentid) })));
  }
  return r;
}
function Wf(n, e) {
  return n === e || (n.startsWith(e) && n.charCodeAt(e.length) === 46);
}
var Yi = class {
  constructor() {
    C(this, "paths", new Set());
  }
  add(e) {
    if (!this.hasParent(e)) {
      for (let t of this.paths) Wf(t, e) && this.paths.delete(t);
      this.paths.add(e);
    }
  }
  toFieldsList() {
    let e = [];
    for (let t of this.paths) e.push(t.split("."));
    return e;
  }
  hasParent(e) {
    if (this.paths.has(e)) return !0;
    let t = e.indexOf(".");
    for (; t !== -1; ) {
      if (this.paths.has(e.slice(0, t))) return !0;
      t = e.indexOf(".", t + 1);
    }
    return !1;
  }
};
function jf(n) {
  let e = 0;
  for (; e < n.length; ) {
    let t = n.indexOf(".", e),
      r = t === -1 ? n.slice(e) : n.slice(e, t);
    if (ed(r)) return e === 0 ? "" : n.slice(0, e - 1);
    if (t === -1) break;
    e = t + 1;
  }
}
function Vo(n, e = 0) {
  let t = new Map(),
    r = n.branchData.columns,
    o = n.getFirstRowForSeq(e);
  for (let i = o; i < r.client.length; ++i)
    r.seq.get(i) < e || wc(t, { id: r.id.get(i), key: r.key.get(i) });
  return xc(t);
}
function Gf(n) {
  let e = new Map();
  for (let t of n) wc(e, t);
  return xc(e);
}
function wc(n, e) {
  let t = e.id.indexOf("."),
    r = t === -1 ? e.id : e.id.slice(0, t),
    o = t === -1 ? "" : e.id.slice(t + 1),
    i = jf(o) ?? (o ? `${o}.${e.key}` : e.key),
    a = n.get(r);
  (a || ((a = new Yi()), n.set(r, a)), a.add(i));
}
function xc(n) {
  let e = new Map();
  for (let [t, r] of n) e.set(t, r.toFieldsList());
  return e;
}
function Lc(n, e, t) {
  let r = Vo(t),
    o = new Set();
  for (let [i, a] of r) {
    let s = t.getParentIdSeq(i);
    if (s === void 0 || t.getParentId(i) === Y) continue;
    let d = n.getParentIdSeq(i);
    d !== void 0 && ((n.hasOwnRow(i, "parentid") && d > s) || (er(e, i) && o.add(i)));
  }
  return o;
}
function Dc(n, e) {
  let t = [...e];
  for (let r of t) for (let o of n.getChildrenIds(r) ?? []) t.push(o);
  return new Set(t);
}
function yC(n, e, t = n.activeBranchId) {
  let r = td(e);
  if ((r.size === 0 || (r.size === 1 && r.has(E))) && t === E) {
    let h = n.getStore(E),
      g = r.get(E) ?? [],
      v = new gn({ client: 0, user: "temp-user" });
    return (v.addSerializableRows(g), Qf(h, v));
  }
  let i = n.getStore(t),
    a = qf(i),
    s = new Set(),
    l = new Set(),
    d = new Set(),
    f = [],
    c = new Set();
  for (let h of Hf(i)) {
    let g = n.getStore(h),
      v = g.base !== void 0 && c.has(g.base.branchId),
      S = r.get(h) ?? [];
    if (v) {
      let x = g.getHierarchy().resolveCycles(0);
      for (let R of x.nextOverrides)
        (s.add(R.nodeId), R.parentTo === Y && (l.add(R.nodeId), Wt(i, R.nodeId, d)));
      for (let R of x.prevOverrides)
        (s.add(R.nodeId), R.parentFrom === Y && (l.add(R.nodeId), Wt(i, R.nodeId, d)));
    }
    if (((v || S.length) && c.add(h), !S.length)) continue;
    let b = new gn({ client: 0, user: "temp-user" });
    b.addSerializableRows(S);
    let w = Lc(g, i, b),
      { minSeq: P, cycleResolutions: V } = g.append(b);
    if (V) {
      for (let x of V.nextOverrides) (w.add(x.nodeId), x.parentTo === Y && Wt(i, x.nodeId, d));
      for (let x of V.prevOverrides) (w.add(x.nodeId), x.parentFrom === Y && Wt(i, x.nodeId, d));
    }
    for (let x of w) s.add(x);
    f.push({ branchStore: g, minSeq: P, branchUpdatesStore: b });
  }
  for (let h of Jf(n, r, a)) {
    let g = n.getStore(h),
      v = g.base !== void 0 && c.has(g.base.branchId),
      S = r.get(h) ?? [];
    if ((v && g.getHierarchy().resolveCycles(0), (v || S.length) && c.add(h), !S.length)) continue;
    let b = new gn({ client: 0, user: "temp-user" });
    (b.addSerializableRows(S), g.append(b));
  }
  for (let { branchUpdatesStore: h } of f) for (let g of Kf(h, i)) d.add(g);
  for (let h of Array.from(s)) er(i, h) && s.delete(h);
  let u = Dc(i, s),
    m = new Map();
  for (let { branchStore: h, minSeq: g } of f) Ec(m, Vo(h, g));
  for (let h of u) m.has(h) || m.set(h, []);
  for (let h of l) m.has(h) || m.set(h, []);
  for (let h of d) m.has(h) || m.set(h, []);
  return tr(i, m, u);
}
function Pc(n, e, t) {
  let r = new Map();
  for (let o of t) {
    let i = n.getStore(o.branchId);
    Ec(r, Vo(i, o.fromSeq));
  }
  return r.size === 0 ? new Map() : tr(n.getStore(e), r, new Set());
}
function Xi(n, e, t) {
  let r = Gf(t);
  return r.size === 0 ? new Map() : tr(n.getStore(e), r, new Set());
}
function Vc(n, e, t) {
  let r = new Set();
  for (let [a, s] of t) {
    if (!s?.__class || !s.parentid || e.has(a)) continue;
    let l = e.get(s.parentid);
    !l || (ue(l) && !l.isLoaded()) || Wt(n, a, r);
  }
  let o = new Map();
  for (let a of r) t.has(a) || o.set(a, []);
  if (o.size === 0) return;
  let i = tr(n, o, new Set(o.keys()));
  for (let [a, s] of i) t.has(a) || t.set(a, s);
}
function Ec(n, e) {
  for (let [t, r] of e) {
    let o = n.get(t);
    o || ((o = []), n.set(t, o));
    for (let i of r) o.push(i);
  }
}
function Hf(n) {
  let e = [];
  for (let t = n; t !== void 0; t = t.base) e.push(t.branchId);
  return e.reverse();
}
function Jf(n, e, t) {
  let r = new Set();
  for (let s of e.keys()) t.has(s) || r.add(s);
  let o = new Set(),
    i = [];
  function a(s) {
    if (o.has(s)) return;
    o.add(s);
    let d = n.getStore(s).base;
    (d !== void 0 && r.has(d.branchId) && a(d.branchId), i.push(s));
  }
  for (let s of r) a(s);
  return i;
}
function qf(n) {
  let e = new Set();
  for (let t = n; t !== void 0; t = t.base) e.add(t.branchId);
  return e;
}
function Kf(n, e) {
  let t = new Set();
  for (let r of n.getRows()) {
    if (r.key !== "parentid") continue;
    let o = r.id;
    er(e, o) && Wt(e, o, t);
  }
  return t;
}
function Wt(n, e, t) {
  for (let r of n.getChildrenIds(e)) t.has(r) || (t.add(r), Wt(n, r, t));
}
function Qf(n, e) {
  if (e.length === 0) return new Map();
  let t = Lc(n, n, e),
    { minSeq: r, cycleResolutions: o } = n.append(e);
  if (o) {
    for (let s of o.nextOverrides) t.add(s.nodeId);
    for (let s of o.prevOverrides) t.add(s.nodeId);
  }
  for (let s of Array.from(t)) er(n, s) && t.delete(s);
  let i = Dc(n, t),
    a = Vo(n, r);
  for (let s of i) a.has(s) || a.set(s, []);
  return tr(n, a, i);
}
function Mc(n, e, t) {
  let r = [];
  for (let o of t.keys()) {
    let i = n.getNodeAtStart(o),
      a = e.get(o);
    if (i === a) continue;
    let s = mt(i, a);
    if (s) {
      if (!s.added && !s.removed && s.to.parentid && s.to.parentid !== s.from?.parentid) {
        let l = n.getScopeNodeAtStartFor(i),
          d = e.getScopeNodeFor(a);
        l && l.id !== d?.id && (s.previousScope = l.id);
      }
      r.push(s);
    }
    pt(r, i, a);
  }
  return r;
}
var bn = me("tree:crdt-committer"),
  Fc = 6e4,
  zc = 6e4,
  Yf = 5e3,
  kc = new Set([fn, Rl, wl]),
  Xf = new Set([fn, yl]),
  Zf = 120,
  Oc = 1e3,
  Zi = class {
    constructor(e, t, r, o) {
      C(this, "tree", e);
      C(this, "remoteVersion", t);
      C(this, "containsLocalEdits", r);
      C(this, "hasHierarchyChanges", o);
    }
  };
function ZC(n) {
  return n instanceof ea;
}
var ea = class {
  constructor(e, t, r) {
    C(this, "componentLoader", t);
    C(this, "tree");
    C(this, "remoteTrees", []);
    C(this, "branchStates", new Map());
    C(this, "branches");
    C(this, "oldestLocalCommitTime", 0);
    C(this, "eventLoopRanSinceWrite", !0);
    C(this, "afterLocalWrite");
    C(this, "onCommitVerifyError");
    C(this, "onLocalChangesProcessingTimeout");
    C(this, "verifyCrashed", !1);
    C(this, "recorder");
    C(this, "remoteTreeVersion", 0);
    C(this, "legacyMode", !1);
    C(this, "isPartialLoading", !1);
    C(this, "validationEnabled", !0);
    C(this, "onlineStartTime", 0);
    C(this, "latestReversibleNodeChanges", null);
    C(this, "pendingReplayedChangesetChanges");
    C(this, "autoBranchingSuppressed", !1);
    C(this, "flagsForNextCommit");
    C(this, "extraChangesForNextCommit");
    C(this, "recentEditReasons", []);
    C(this, "nodeChangesBuffers", new Set());
    C(this, "cachedTreeChanges", []);
    C(this, "cachedTreeChangesIndex", 0);
    C(this, "resetTime", 0);
    C(this, "persistedCaches", new Lo());
    C(this, "dataTree");
    C(this, "wasViewOnly", !1);
    C(this, "validateUpdatesAreSentToServerAgain", () => {
      if (!this.validationEnabled || !this.online) return;
      let e = performance.now();
      e - this.onlineStartTime < zc ||
        e - this.oldestLocalCommitTime < Fc ||
        ((this.onlineStartTime = e),
        this.onLocalChangesProcessingTimeout
          ? this.onLocalChangesProcessingTimeout(this.oldestLocalCommitTime)
          : y(
              !1,
              () =>
                `Local changes not been processed in a while: ${this.oldestLocalCommitTime} ${e}`
            ));
    });
    C(this, "epoch", 0);
    let o = Pi("", qd().client);
    ((this.branches = new Ui(o)),
      this.branchStates.set(o.branchId, this.createCleanBranchDrainState()),
      (this.afterLocalWrite = r?.afterLocalWrite),
      (this.tree = e),
      this.reset(e, r));
  }
  get store() {
    return this.branches.activeStore;
  }
  get effectiveViewBranchId() {
    return this.branches.effectiveViewBranchId;
  }
  get effectiveViewStore() {
    return this.branches.getStore(this.effectiveViewBranchId);
  }
  hasActiveLocalOnlyOverlays() {
    return this.branches.hasActiveLocalOnlyBranches;
  }
  activeBranchDrainState() {
    let e = this.branches.activeBranchId;
    return this.getOrCreateBranchState(e);
  }
  setOnline(e) {
    (this.onlineStartTime !== 0) !== e && (this.onlineStartTime = e ? performance.now() : 0);
  }
  get online() {
    return this.onlineStartTime !== 0;
  }
  reset(e, t) {
    ((this.resetTime = performance.now()),
      bn.debug("reset with tree:", e.root.id, "size:", e.size()),
      this.branches.clearLocalOnlyLayers(),
      this.replaceTree(e),
      (this.recentEditReasons = []),
      (this.remoteTreeVersion = 0),
      this.resetBranchDrainStatesToActiveStoreHead(),
      (this.oldestLocalCommitTime = 0),
      (this.eventLoopRanSinceWrite = !0),
      (this.isPartialLoading = !!t?.isLoading || !!e.getService("loader")));
  }
  switchActiveBranch(e) {
    this.branches.dropActiveLocalOnlyLayers();
    let t = this.branches.switchActiveBranch(e);
    return (this.getOrCreateBranchState(e), t);
  }
  createStagedDocumentLoaderStoreTarget() {
    let e = !1,
      t = this.branches.getStore(E),
      r = Pi(t.user, t.client),
      o = new Ui(r);
    return {
      get store() {
        return o.activeStore;
      },
      get branches() {
        return o;
      },
      loadSnapshot: (i) => {
        (y(!e, "Cannot update installed document load store target"), r.fromBuffer(i), o.reset());
      },
      reset() {
        e || (r.reset(), o.reset());
      },
      install: () => {
        e || ((this.branches = o), this.resetBranchDrainStatesToActiveStoreHead(), (e = !0));
      },
    };
  }
  createLiveDocumentLoaderStoreTarget() {
    let e = this.branches;
    return {
      get store() {
        return e.activeStore;
      },
      get branches() {
        return e;
      },
      loadSnapshot() {
        throw new Error("Cannot update live document load store target");
      },
      reset() {
        throw new Error("Cannot update live document load store target");
      },
      install() {
        throw new Error("Cannot update live document load store target");
      },
    };
  }
  replaceTree(e) {
    (this.branches.clearLocalOnlyLayers(),
      (this.tree = e),
      (this.latestReversibleNodeChanges = null),
      (this.remoteTrees = []),
      (this.flagsForNextCommit = void 0),
      (this.extraChangesForNextCommit = void 0),
      (this.verifyCrashed = !1),
      this.resetDataTree(),
      this.clearNodeChangesReader(),
      this.invalidateAllCursors(),
      (this.isPartialLoading = !!e.getService("loader")));
  }
  createBranch(e, t, r) {
    y(e !== E, "Can't create the main branch");
    let o = this.branches.getStore(E);
    if (o.getObjectKey(e, "baseId")) throw new Error(`Branch ${e} already exists`);
    let i = r ?? this.branches.activeBranchId;
    if (i !== E) {
      if (!o.getObjectKey(i, "baseId")) throw new Error(`Base branch ${i} does not exist`);
      if (o.getObjectKey(i, ze)) throw new Error(`Base branch ${i} is deleted`);
    }
    let a = yc(this.collectExistingBranchNames()),
      s = new nd({ id: e, baseId: i, name: a, createdBy: t, contributors: t ? [{ id: t }] : [] }),
      l = o.length;
    (o.batch(() => {
      (o.getObjectKey(ut, "__class") || o.insertNode(new ft().toJS(), this.tree.root.id),
        o.insertNode(s.toJS(), ut));
    }),
      this.afterStoreWrite(o, l),
      ft.get(this.tree) || this.tree.insertNode(new ft(), this.tree.root.id),
      this.tree.insertNode(s, ut),
      (this.tree = this.tree.commit(this.componentLoader)),
      this.invalidateAllCursors(),
      this.trackChange(ut),
      this.trackChange(e));
  }
  setAutoBranching(e) {
    let t = this.branches.getStore(E),
      r = t.length;
    (t.batch(() => {
      (t.getObjectKey(ut, "__class") || t.insertNode(new ft().toJS(), this.tree.root.id),
        t.updateNode(ut, { [no]: e }));
    }),
      this.afterStoreWrite(t, r));
    let o = ft.get(this.tree);
    (o ? o.set({ [no]: e }) : this.tree.insertNode(new ft({ [no]: e }), this.tree.root.id),
      (this.tree = this.tree.commit(this.componentLoader)),
      this.trackChange(ut),
      this.invalidateAllCursors());
  }
  addBranchContributor(e, t) {
    y(e !== E, "Can't add contributor to the main branch");
    let r = this.branches.getStore(E),
      o = r.getObjectKey(e, _t) ?? [];
    if (o.some((l) => l.id === t)) return;
    let i = [...o, { id: t }],
      a = r.length;
    (r.updateNode(e, { [_t]: i }), this.afterStoreWrite(r, a));
    let s = this.tree.get(e);
    (y(s, () => `Missing branch metadata node: ${e}`),
      s.set({ [_t]: i }),
      (this.tree = this.tree.commit(this.componentLoader)),
      this.invalidateAllCursors());
  }
  removeBranchContributor(e, t) {
    y(e !== E, "Can't remove contributor from the main branch");
    let r = this.branches.getStore(E),
      o = r.getObjectKey(e, _t) ?? [],
      i = o.filter((l) => l.id !== t);
    if (i.length === o.length) return;
    let a = r.length;
    (r.updateNode(e, { [_t]: i }), this.afterStoreWrite(r, a));
    let s = this.tree.get(e);
    (y(s, () => `Missing branch metadata node: ${e}`),
      s.set({ [_t]: i }),
      (this.tree = this.tree.commit(this.componentLoader)),
      this.invalidateAllCursors());
  }
  deleteBranch(e) {
    y(e !== E, "Can't delete the main branch");
    let t = this.branches.getStore(E);
    (this.assertBranchExists(t, e),
      y(!t.getObjectKey(e, ze), () => `Branch is already deleted: ${e}`),
      y(Bc(t, e), () => `Cannot delete branch with child branches: ${e}`));
    let r = Date.now(),
      o = t.length;
    (t.updateNode(e, { [ze]: r }), this.afterStoreWrite(t, o));
    let i = this.tree.get(e);
    (y(i, () => `Missing branch metadata node: ${e}`),
      i.set({ [ze]: r }),
      (this.tree = this.tree.commit(this.componentLoader)),
      this.invalidateAllCursors());
  }
  undoDeleteBranch(e) {
    let t = this.branches.getStore(E);
    y(t.getObjectKey(e, ze), () => `Branch is not deleted: ${e}`);
    let r = t.length;
    (t.updateNode(e, { [ze]: void 0 }), this.afterStoreWrite(t, r));
    let o = this.tree.get(e);
    (y(o, () => `Missing branch metadata node: ${e}`),
      o.set({ [ze]: void 0 }),
      (this.tree = this.tree.commit(this.componentLoader)),
      this.invalidateAllCursors());
  }
  redoDeleteBranch(e) {
    this.deleteBranch(e);
  }
  mergeBranchToBase(e = this.branches.activeBranchId) {
    y(e !== E, "Can't merge the main branch");
    let t = this.getBranchBaseId(e);
    return this.mergeBranch(e, t);
  }
  getBranchBaseId(e) {
    let t = this.branches.getStore(E);
    return this.assertBranchExists(t, e);
  }
  mergeBranch(e, t) {
    this.assertCanMergeBranch(e, t);
    let r = this.branches.getStore(E);
    return this.mergeBranchIntoTarget(e, t, r);
  }
  assertCanMergeBranch(e, t) {
    (y(e !== E, "Can't merge the main branch"),
      y(e !== t, () => `Cannot merge a branch into itself: ${e}`));
    let r = this.branches.getStore(E),
      o = this.assertBranchExists(r, e);
    if ((this.assertCanMergeBranchSource(r, e), t === E))
      return (y(o === E, "Can only merge a branch into its parent or same-parent sibling"), t);
    let i = this.assertBranchExists(r, t);
    return (
      this.assertCanMergeBranchTarget(r, t),
      t === o ||
        y(
          i === o,
          () => `Can only merge a branch into its parent or same-parent sibling: ${e} ${t}`
        ),
      t
    );
  }
  assertCanMergeBranchSource(e, t) {
    (y(!e.getObjectKey(t, ze), () => `Cannot merge deleted branch: ${t}`),
      y(Bc(e, t), () => `Cannot merge branch with child branches: ${t}`));
  }
  assertCanMergeBranchTarget(e, t) {
    y(!e.getObjectKey(t, ze), () => `Cannot merge into deleted branch: ${t}`);
  }
  assertBranchExists(e, t) {
    let r = e.getObjectKey(t, Li);
    return (y(typeof r == "string" && r.length > 0, `Branch ${t} does not exist`), r);
  }
  mergeBranchIntoTarget(e, t, r) {
    this.switchActiveBranch(t);
    let { replayResult: o, targetStore: i } = this.replayBranchRowsToTarget(e, t),
      a = Date.now(),
      s = r.length;
    (r.batch(() => {
      (r.updateNode(e, { [ze]: a }), t !== E && r.updateNode(t, { [to]: a }));
    }),
      this.afterStoreWrite(r, s));
    let l = this.tree.get(e);
    if ((y(l, () => `Missing branch metadata node: ${e}`), l.set({ [ze]: a }), t !== E)) {
      let d = this.tree.get(t);
      d && d.set({ [to]: a });
    }
    return (
      (this.tree = this.tree.commit(this.componentLoader)),
      this.invalidateAllCursors(),
      {
        baseId: t,
        supersededRows: o.supersededRows.filter((d) => d.id !== Di),
        branchOnlyNodeIds: o.addedNodeIds.filter((d) => d !== Di && i.hasOwnRow(d, "parentid")),
      }
    );
  }
  undoMergeBranch(e, t, r, o) {
    y(e !== E, "Can't undo merge of the main branch");
    let i = this.branches.getStore(t),
      a = [...r, ...o.map((s) => ({ id: s, key: "parentid", value: Y }))];
    if (a.length > 0) {
      let s = i.length;
      (i.replayRowList(a), this.afterStoreWrite(i, s));
    }
    this.undoDeleteBranch(e);
  }
  redoMergeBranch(e, t) {
    return this.mergeBranch(e, t);
  }
  renameBranch(e, t) {
    y(e !== E, "Can't rename the main branch");
    let r = this.branches.getStore(E);
    this.assertBranchExists(r, e);
    let o = r.length;
    (r.batch(() => {
      r.updateNode(e, { name: t });
    }),
      this.afterStoreWrite(r, o));
    let i = this.tree.get(e);
    return (
      y(i, () => `Branch metadata is missing from the canvas tree: ${e}`),
      i.set({ name: t }),
      (this.tree = this.tree.commit(this.componentLoader)),
      this.invalidateAllCursors(),
      this.tree
    );
  }
  collectExistingBranchNames() {
    let e = new Set(),
      t = ft.get(this.tree);
    if (!t) return e;
    for (let r of t.children) e.add(r.title);
    return e;
  }
  getOrCreateBranchState(e) {
    let t = this.branchStates.get(e);
    return (t || ((t = this.createCleanBranchDrainState()), this.branchStates.set(e, t)), t);
  }
  createCleanBranchDrainState() {
    return { localRanges: [] };
  }
  resetBranchDrainStatesToActiveStoreHead() {
    let e = this.branches.activeBranchId,
      t = this.createCleanBranchDrainState();
    this.branchStates = new Map([[e, t]]);
  }
  writeLocalOnlyOverlay(e, t) {
    let r = this.branches.getOrCreateLocalOnlyLayer(e),
      o = this.branches.getStore(r.branchId),
      i = o.length;
    o.batch(() => {
      t(tp(o));
    });
    let a = o.getSerializableRows(i),
      s = a[0];
    if (!s) return !1;
    let l = Pc(this.branches, this.effectiveViewBranchId, [
      { branchId: r.branchId, fromSeq: a.reduce((d, f) => Math.min(d, f.seq), s.seq) },
    ]);
    return (this.applyLocalOnlyOverlayPatches(l), !0);
  }
  promoteLocalOnlyOverlay(e) {
    let t = this.branches.getLocalOnlyLayer(e);
    if (!t) return !1;
    let o = this.branches.getStore(t.branchId).getRowsSorted(),
      i = this.branches.getStore(t.upstreamBranchId),
      a = i.length;
    (this.replayBranchRowsToTarget(t.branchId, t.upstreamBranchId),
      this.branches.removeLocalOnlyLayer(e));
    let s = i.getSerializableRows(a),
      l = Xi(this.branches, this.effectiveViewBranchId, s.length > 0 ? s : o);
    return (this.applyLocalOnlyOverlayPatches(l), !0);
  }
  discardLocalOnlyOverlay(e) {
    let t = this.branches.getLocalOnlyLayer(e);
    if (!t) return !1;
    let r = this.branches.getStore(t.branchId).getRowsSorted();
    this.branches.removeLocalOnlyLayer(e);
    let o = Xi(this.branches, this.effectiveViewBranchId, r);
    return (this.applyLocalOnlyOverlayPatches(o), !0);
  }
  getLocalOnlyOverlayRows() {
    return this.branches.getLocalOnlyRows();
  }
  applyLocalOnlyOverlayPatches(e) {
    (Vc(this.effectiveViewStore, this.tree, e), this.applyPatchesToCurrentTree(e));
  }
  getOrCreateDataTree(e) {
    if (Ue.isOn("dataOnlyTree") && !(this.store.branchId === E && this.store.length === 0))
      return (
        this.dataTree || (this.dataTree = new xo(this.store, this.tree, e, this.persistedCaches)),
        this.dataTree
      );
  }
  resetDataTree() {
    this.dataTree = void 0;
  }
  hasOnlyEmptyChangesForRemote() {
    for (let e of this.branchStates.values())
      if (e.localRanges.length !== 0) {
        for (let [t, r] of e.localRanges) if (t !== r) return !1;
      }
    return !0;
  }
  resetLastSeqTaken() {
    let e = this.activeBranchDrainState();
    ((e.localRanges = []),
      bn.debug("Resetting all locally collected row ranges for active branch"));
  }
  takePendingRows(e) {
    this.oldestLocalCommitTime = 0;
    let t = [],
      r = [];
    for (let [o, i] of this.branchStates) {
      if (i.localRanges.length === 0) continue;
      let a = this.branches.getStore(o);
      for (let s of this.collectLocalRows(a, i.localRanges, e)) t.push(s);
      r.push(o);
    }
    try {
      gn.verifyBatches(t);
    } catch (o) {
      bn.reportError(o);
    }
    for (let o of r) {
      let i = this.branchStates.get(o);
      i &&
        ((i.localRanges = []), o !== this.branches.activeBranchId && this.branchStates.delete(o));
    }
    return t;
  }
  collectLocalRows(e, t, r) {
    let o = [];
    for (let [i, a] of t)
      for (let s of e.getSerializableRows(i, a)) {
        if (this.branches.isLocalOnlyRow(e, s)) continue;
        let l = { ...s, user: r };
        (e.branchId !== E && (l.branchId = e.branchId), o.push(l));
      }
    return o;
  }
  getLastEntry() {
    let e = this.remoteTrees[this.remoteTrees.length - 1];
    return (y(e, "Timeline has no entries"), e);
  }
  getEntry(e) {
    return this.remoteTrees[e];
  }
  hasLocalEdits() {
    for (let e of this.branchStates.values()) if (e.localRanges.length > 0) return !0;
    return !1;
  }
  getEditReasons() {
    let e = this.recentEditReasons.join(" ");
    return ((this.recentEditReasons = []), e);
  }
  getTreeForVersion(e) {
    return this.remoteTrees.findLast((t) => t.remoteVersion === e && !t.containsLocalEdits);
  }
  hasPendingHierarchyChanges() {
    for (let [e, t] of this.branchStates) {
      if (t.localRanges.length === 0) continue;
      let r = this.branches.getStore(e);
      for (let [o, i] of t.localRanges)
        for (let a of r.getRows(o, i)) if (a.key === "parentid") return !0;
    }
    return !1;
  }
  hasHierarchyChangesAfterVersion(e) {
    let t = !1;
    for (let r of this.remoteTrees)
      if ((r.remoteVersion === e && (t = !0), !(r.remoteVersion <= e) && r.hasHierarchyChanges))
        return !0;
    return t ? this.hasPendingHierarchyChanges() : !0;
  }
  recordHistoricTree(e, t) {
    ((this.remoteTreeVersion = e),
      this.remoteTrees.push(
        new Zi(
          this.tree,
          e,
          t.containsLocalEdits || this.hasActiveLocalOnlyOverlays(),
          t.hasHierarchyChanges
        )
      ));
    let r = 120;
    if (this.remoteTrees.length > r) {
      let o = this.remoteTrees.length - r,
        i = this.remoteTrees.splice(0, o);
      for (let a of i) a.tree.releaseMemory();
    }
  }
  replayBranchRowsToTarget(e, t) {
    let r = this.branches.getStore(e),
      o = this.branches.getStore(t),
      i = o.length,
      a = r.getRowsSorted(),
      s = a.filter((d) => (Xf.has(d.key) ? !1 : d.id !== e || d.key !== eo)),
      l = s.length > 0 ? o.replayRowList(s) : { supersededRows: [], addedNodeIds: [] };
    return (
      a.length > 0 && (np(o, a), this.afterStoreWrite(o, i)),
      { replayResult: l, targetStore: o }
    );
  }
  handleRemotePatches(e, t, r) {
    let o = this.applyPatchesToCurrentTree(e);
    return (this.recordHistoricTree(t, r), o);
  }
  applyPatchesToCurrentTree(e) {
    if (e.size === 0) return [];
    y(this.tree.isLatest(), "Tree must be latest");
    let t = this.tree,
      r = t.isViewOnly;
    ((t.editClosed = !1), (t.isViewOnly = !1), t.beginAllowPartialScopeAccess());
    let o = t.lineage.withoutEditHooks(() => Rc(t, this.componentLoader, e));
    o.beginAllowPartialScopeAccess();
    let i = t.lineage.withoutEditHooks(() => Mc(t, o, e));
    for (let a of i) this.trackChange(a.id, a);
    for (let a of t.getNodesChangedByCommit()) this.trackChange(a.id);
    return (
      this.cacheTreeChanges(i),
      (this.tree = o),
      this.resetDataTree(),
      t.endAllowPartialScopeAccess(),
      o.endAllowPartialScopeAccess(),
      y(!o.lineage.allowPartialNodeAccess, "Partial scope access should be disabled after commit"),
      (o.isViewOnly = r),
      t !== o && t.releaseMemory(),
      i
    );
  }
  handleRemoteConfirm(e, t) {
    (this.recordHistoricTree(e, t), (this.oldestLocalCommitTime = 0));
  }
  loadedAllScopes(e) {
    (y(this.isPartialLoading, "Must be in loading mode"),
      (this.isPartialLoading = !1),
      this.recordHistoricTree(this.remoteTreeVersion, e));
  }
  loadOneScope(e, t) {
    return (
      bn.debug("loadOneScope:", e.id),
      y(this.isPartialLoading, "Must be loading"),
      y(!e.cache.isShallowLoad, "Scope must not be shallow"),
      y(this.tree.isLatest(), "Tree must be latest"),
      this.tree.lineage.withoutEditHooks(() => {
        let r = this.tree.isViewOnly;
        ((this.tree.editClosed = !1),
          (this.tree.isViewOnly = !1),
          (this.tree.inEditor = !1),
          this.tree.makeLatest());
        let o = this.tree.root.children.findIndex((s) => s.id === e.id);
        if (o === -1) y(!this.tree.get(e.id), "Scope must have been deleted by remote diffs");
        else if (e.__class === "WebPageNode" || e.__class === "SmartComponentNode") {
          pc(e);
          let s = this.tree.commitWithLoadedScope(this.componentLoader, e);
          ((this.tree.inEditor = !0), (this.tree = s));
          for (let l of e.walk()) this.trackChange(l.id);
          this.tree.loadReplicasAndCodeComponents(e);
        } else
          (this.tree.remove(e.id),
            this.tree.insertNode(e, this.tree.root.id, o),
            this.tree.loadReplicasAndCodeComponents(e));
        let i = this.tree.commit(this.componentLoader, (s, l) => {
          let d = s?.id ?? l?.id;
          d && this.trackChange(d);
        });
        ((this.tree.inEditor = !0),
          (this.tree = i),
          (this.tree.inEditor = !0),
          (this.tree.isViewOnly = r));
        let a = i.get(e.id);
        return (
          a && a.cache.updateSerializedCache(a),
          this.legacyMode && this.invalidateAllCursors(),
          i
        );
      })
    );
  }
  debugOverwriteCurrentTree(e) {
    (bn.debug("debugOverwriteCurrentTree:", e.root.id),
      (this.tree = e),
      this.remoteTrees.length > 0 && (this.getLastEntry().tree = e),
      this.invalidateAllCursors());
  }
  saveTimelineDataForRecovery() {}
  setFlagsForNextCommit(e) {
    this.flagsForNextCommit = e;
  }
  setExtraChangesForNextCommit(e) {
    this.extraChangesForNextCommit = e;
  }
  recordEditReasons(e) {
    e && this.recentEditReasons.at(-1) !== e && this.recentEditReasons.push(e);
  }
  getLatestChangesForUndo() {
    return this.latestReversibleNodeChanges;
  }
  openNodeChangesReader() {
    return new Sn(this.nodeChangesBuffers);
  }
  clearNodeChangesReader() {
    for (let e of this.nodeChangesBuffers) e.clear();
  }
  applyFlagsToChange(e) {
    if (!e) return;
    let t = this.flagsForNextCommit;
    t &&
      (t.ignoreInUndo && (e.ignoreInUndo = !0),
      t.ignoreInCodeGeneration && (e.ignoreInCodeGeneration = !0));
  }
  hasAutoBranchingChanges(e) {
    return e.some((t) =>
      this.isBranchMetadataChange(t) || t.ignoreInUndo
        ? !1
        : !!(
            t.added ||
            t.removed ||
            t.position !== void 0 ||
            t.fromPosition !== void 0 ||
            this.hasAutoBranchingPropertyChanges(t.to)
          )
    );
  }
  hasAutoBranchingPropertyChanges(e) {
    if (!e) return !1;
    for (let t of Object.keys(e)) {
      if (t !== "_deleted") {
        if (!kc.has(t)) return !0;
        continue;
      }
      let r = e[t];
      if (!r || r.some((o) => !kc.has(o))) return !0;
    }
    return !1;
  }
  isBranchMetadataChange(e) {
    let t = this.branches.getStore(E).getObjectKey(e.id, "__class");
    return t === "BranchesNode" || t === "BranchNode";
  }
  shouldAutoCreateBranchForUserEdit(e) {
    let t = this.branches.activeBranchId;
    if (t === E) return !0;
    if (!e) return !1;
    let r = this.tree.get(t);
    return r ? !rd(r, e) : !1;
  }
  updateStoreFromNodeChanges(e, t) {
    for (let r of e)
      if (!(this.store.branchId !== E && this.isBranchMetadataChange(r))) {
        if ((r.removed && (this.store.removeNode(r.id), t.add(r.id)), r.added)) {
          let o = io(r.to);
          ((o.id = r.id), (o.__class = r.added));
          let i = oo(o.__class);
          (i && gl(i) && (o.children = []), this.store.insertNode(o, o.parentid, r.position));
          continue;
        }
        if (r.to.parentid || r.position !== r.fromPosition) {
          let o = r.to.parentid ?? this.store.getParentId(r.id);
          o && o !== Y && this.store.moveNode(r.id, o, r.position);
        }
      }
    for (let r of e)
      if (
        !(this.store.branchId !== E && this.isBranchMetadataChange(r)) &&
        !(r.added || r.removed || t.has(r.id))
      )
        if (fd(r.id)) {
          let [o, i] = pd(r.id);
          y(o && i);
          let a = io(ud(r.to, Y));
          this.store.updateNestedObject([o, "replicaInfo", "overrides", i], a);
        } else {
          let { _deleted: o, ...i } = r.to;
          for (let a of o ?? []) i[a] = Y;
          (delete i.parentid, Object.keys(i).length && this.store.updateNode(r.id, io(i)));
        }
  }
  syncChanges(e) {
    let t = this.store.length,
      r = new Set();
    for (let o = 0; o < e.length; o += Oc) {
      let i = e.slice(o, o + Oc);
      this.store.batch(() => this.updateStoreFromNodeChanges(i, r));
    }
    (this.afterStoreWrite(this.store, t, e.length > 0),
      this.touchActiveBranchLastEdited(this.store, t));
  }
  touchActiveBranchLastEdited(e, t) {
    if (t === e.length) return;
    let r = e.branchId;
    if (r === E || r !== this.branches.activeBranchId) return;
    let o = Date.now(),
      i = this.tree.get(r);
    if (i) {
      let l = this.tree.hasUncommittedChanges();
      (i.set({ lastEdited: o }), l || (this.tree = this.tree.commit(this.componentLoader)));
    }
    let a = this.branches.getStore(E),
      s = a.length;
    (a.updateNode(r, { [to]: o }), this.afterStoreWrite(a, s));
  }
  afterStoreWrite(e, t, r = !1) {
    (t === e.length && !r) ||
      (this.getOrCreateBranchState(e.branchId).localRanges.push([t, e.length]),
      this.oldestLocalCommitTime === 0 &&
        t !== e.length &&
        ((this.oldestLocalCommitTime = performance.now()),
        (this.eventLoopRanSinceWrite = !1),
        setTimeout(() => {
          this.eventLoopRanSinceWrite = !0;
        }, 0)),
      this.afterLocalWrite?.(e.branchId, e.seq),
      this.recorder && this.recorder({ source: "local", rows: e.getSerializableRows(t) }));
  }
  trackChange(e, t = null) {
    if (e) for (let r of this.nodeChangesBuffers) r.trackChange(e, t);
  }
  validateUpdatesAreSentToServer() {
    if (!this.validationEnabled || !this.online) return;
    if (this.tree.isViewOnly) {
      this.wasViewOnly = !0;
      return;
    }
    if (this.wasViewOnly) {
      ((this.oldestLocalCommitTime = performance.now()), (this.wasViewOnly = !1));
      return;
    }
    let e = performance.now();
    e - this.onlineStartTime < zc ||
      this.oldestLocalCommitTime === 0 ||
      !this.eventLoopRanSinceWrite ||
      e - this.oldestLocalCommitTime < Fc ||
      (this.onLocalChangesProcessingTimeout
        ? setTimeout(this.validateUpdatesAreSentToServerAgain, Yf)
        : ((this.onlineStartTime = e),
          y(
            !1,
            () => `Local changes not been processed in a while: ${this.oldestLocalCommitTime} ${e}`
          )));
  }
  commitLocalTree() {
    this.validateUpdatesAreSentToServer();
    let e = this.tree.lineage.getActiveChangeset(),
      t = {};
    if (Ue.isOn("dataOnlyTree") && this.dataTree?.hasUncommittedChanges())
      for (let u of this.dataTree.preCommit(this.componentLoader)) t[u.id] = u;
    let r = this.tree.commit(this.componentLoader, (u, m) => {
        if (!u && !m) return;
        let h = mt(u, m, !0),
          g = u?.id ?? m?.id;
        (y(g, "Expected current or draft node id"), this.trackChange(g, null), h && (t[h.id] = h));
        let v = [];
        pt(v, u, m);
        for (let S of v) t[S.id] = S;
      }),
      o = this.onCommitVerifyError && !this.verifyCrashed,
      i = o ? { ...t } : void 0;
    this.tree.hierarchyChanges = this.tree.hierarchyChanges.filter(
      (u) => u.type !== "remove" || u.skip || t[u.node] !== void 0
    );
    let a = new Do(),
      s = [],
      l = new Set();
    for (let u of this.tree.hierarchyChanges) u.type === "move" && !u.skip && l.add(u.node);
    for (let u of this.tree.hierarchyChanges) {
      if (u.skip) {
        if (u.type === "insert") {
          if (l.has(u.node)) continue;
          a.recordSkippedInsert(u.toParent, u.toPosition, u.node);
        } else if (u.type === "remove") {
          if (l.has(u.node)) continue;
          a.recordSkippedRemove(u.fromParent, u.fromPosition, u.node);
        } else
          u.type === "move" &&
            a.recordSkippedMove(u.fromParent, u.fromPosition, u.toParent, u.toPosition, u.node);
        continue;
      }
      let m;
      if (u.type === "insert") {
        let h = t[u.node];
        y(h, "Expected insert hierarchy change to have a node change");
        let g = a.commitInsert(u.toParent, u.toPosition);
        ((m = {
          id: u.node,
          from: h.from,
          to: { ...h.to, parentid: u.toParent },
          position: u.toPosition + g,
          added: h.added,
          ignoreInUndo: h.ignoreInUndo,
        }),
          delete t[u.node]);
      } else if (u.type === "move") {
        let { fromOffset: h, toOffset: g } = a.commitMove(
          u.fromParent,
          u.fromPosition,
          u.toParent,
          u.toPosition
        );
        if (
          ((m = {
            id: u.node,
            from: { parentid: u.fromParent },
            to: { parentid: u.toParent },
            fromPosition: u.fromPosition + h,
            position: u.toPosition + g,
            ignoreInUndo: t[u.node]?.ignoreInUndo,
          }),
          u.fromParent !== u.toParent)
        ) {
          let v = this.tree.getNodeAtStart(u.node);
          if (v) {
            let S = this.tree.getScopeNodeAtStartFor(v),
              b = r.getScopeNodeFor(r.get(u.node));
            S && S.id !== b?.id && (m.previousScope = S.id);
          }
        }
      } else if (u.type === "remove") {
        let h = t[u.node];
        y(h, "Expected remove hierarchy change to have a node change");
        let g = a.commitRemove(u.fromParent, u.fromPosition);
        ((m = {
          id: u.node,
          from: { ...h.from, parentid: u.fromParent },
          to: h.to,
          fromPosition: u.fromPosition + g,
          removed: h.removed,
          ignoreInUndo: h.ignoreInUndo,
        }),
          delete t[u.node]);
      } else ee(u);
      s.push(m);
    }
    for (let u of Object.values(t)) {
      if (!u.removed || u.from.parentid !== void 0) continue;
      let m = this.store.getParentId(u.id);
      if (!(m === void 0 || m === Y) && ((u.from.parentid = m), u.fromPosition === void 0)) {
        let h = this.store.getChildrenIds(m).indexOf(u.id);
        h >= 0 && (u.fromPosition = h);
      }
    }
    for (let u of Object.values(t)) s.push(u);
    if (this.extraChangesForNextCommit) for (let u of this.extraChangesForNextCommit) s.push(u);
    for (let u of s)
      (this.applyFlagsToChange(u),
        this.trackChange(u.id, u),
        e && this.tree.lineage.applyChangesetFlagsToChange(u, e.id));
    (this.cacheTreeChanges(s), (this.latestReversibleNodeChanges = s));
    let d = this.tree;
    ((this.tree = r), d !== this.tree && d.releaseMemory());
    let f = this.flagsForNextCommit;
    ((this.flagsForNextCommit = void 0), (this.extraChangesForNextCommit = void 0));
    let c;
    if (
      (!this.autoBranchingSuppressed &&
        Td.isOn("canUseBranching") &&
        ft.isAutoBranchingEnabled(this.tree) &&
        !Ve.isE2E &&
        !Ve.isIntegrationTest &&
        f?.isUserEdit &&
        this.shouldAutoCreateBranchForUserEdit(f.userId) &&
        (f.isUserModuleEdit || this.hasAutoBranchingChanges(s)) &&
        ((c = Ts()), this.createBranch(c, f.userId), this.switchActiveBranch(c)),
      bn.debug("commit local tree:", s.length, this.recentEditReasons),
      this.syncChanges(s),
      this.pendingReplayedChangesetChanges)
    ) {
      y(
        this.latestReversibleNodeChanges.length === 0,
        "We can't replay a changeset while processing other changes"
      );
      for (let u of this.pendingReplayedChangesetChanges) this.applyFlagsToChange(u);
      for (let u of this.pendingReplayedChangesetChanges) this.latestReversibleNodeChanges.push(u);
      this.pendingReplayedChangesetChanges = void 0;
    }
    if (o && this.tree.size() < Sd) {
      let u = this.verifyTree();
      if (u) {
        this.verifyCrashed = !0;
        let m = this.store.getHierarchy(),
          h = Sc({
            error: u,
            preReconcileDataLedger: i ?? {},
            hierarchyChanges: d.hierarchyChanges,
            reconciledChanges: s,
            editReasons: [...this.recentEditReasons],
            probeStore: (g) => {
              let v = m.getParentId(g);
              return { exists: m.has(g), parentid: typeof v == "string" ? v : void 0 };
            },
          });
        (y(this.onCommitVerifyError, "onCommitVerifyError must be set to capture"),
          this.onCommitVerifyError(u, h));
      }
    }
    return { tree: this.tree, autoCreatedBranchId: c };
  }
  invalidateAllCursors() {
    ((this.epoch += 1), (this.cachedTreeChanges = []), (this.cachedTreeChangesIndex = 0));
  }
  getChangeTrackingCursor() {
    return {
      remoteTree: 0,
      localTree: this.cachedTreeChangesIndex,
      timeline: this,
      tree: this.tree,
      epoch: this.epoch,
    };
  }
  cacheTreeChanges(e) {
    e.length !== 0 &&
      ((this.cachedTreeChangesIndex += 1),
      this.cachedTreeChanges.push({ index: this.cachedTreeChangesIndex, changes: e }),
      this.cachedTreeChanges.length > Zf && this.cachedTreeChanges.shift());
  }
  mergeCachedTreeChanges(e, t) {
    if (e === t) return [];
    let r = this.cachedTreeChanges[0];
    if (!r || e >= t || e + 1 < r.index) return;
    let o = {};
    for (let i of this.cachedTreeChanges)
      if (!(i.index <= e)) {
        if (i.index > t) break;
        for (let a of i.changes) {
          let s = o[a.id];
          (s || (s = o[a.id] = { id: a.id, to: {} }), Jn(s, a));
        }
      }
    return Object.values(o);
  }
  fetchForwardChanges(e) {
    if (!e || e.epoch !== this.epoch) return;
    if (e.tree === this.tree) return [];
    if (
      e.tree.lineage !== this.tree.lineage ||
      e.tree.root.id !== this.tree.root.id ||
      e.timeline !== this
    )
      return;
    let t = this.mergeCachedTreeChanges(e.localTree, this.cachedTreeChangesIndex);
    if (t) return ((e.localTree = this.cachedTreeChangesIndex), (e.tree = this.tree), t);
  }
  invalidatedByLoadCompletedDocument(e) {
    return !1;
  }
  verifyTree() {
    return ep(this.effectiveViewStore.getHierarchy(), this.tree);
  }
  applyChanges(e) {
    let t = this.store.length;
    (gd(this.tree, e, this.store, this.dataTree),
      this.afterStoreWrite(this.store, t),
      this.touchActiveBranchLastEdited(this.store, t));
  }
  applyReverseChanges(e, t) {
    let r = this.store.length;
    (yd(this.tree, e, this.store, this.dataTree, t),
      this.afterStoreWrite(this.store, r),
      this.touchActiveBranchLastEdited(this.store, r));
  }
  discardChangeset(e) {
    let t = this.tree.lineage.getChangesetRecord(e.id);
    (y(t, "Unknown changeset"),
      t.status !== "discarded" &&
        (t.changes.length > 0 && this.applyReverseChanges(t.changes),
        this.tree.lineage.setChangesetStatus(e.id, "discarded")));
  }
  replayChangeset(e) {
    let t = this.tree.lineage.changesets[e.id];
    if ((y(t, () => `Changeset record does not exist: ${e.id}`), t.status === "discarded")) {
      (t.changes.length > 0 && this.applyChanges(t.changes),
        this.tree.lineage.setChangesetStatus(e.id, "open"));
      return;
    }
    ((this.pendingReplayedChangesetChanges = t.changes), this.tree.root.asDraft());
  }
};
function Bc(n, e) {
  return n
    .getChildrenIds(ut)
    .every((t) => (t === e || n.getObjectKey(t, ze) ? !0 : n.getObjectKey(t, Li) !== e));
}
function ep(n, e) {
  e.beginAllowPartialScopeAccess();
  try {
    let t = (...f) => new Error(f.map((c) => String(c)).join(" ")),
      r = (f, ...c) => {
        if (!f) return t(...c);
      },
      o = n.getRootId();
    if (!o) return t("hierarchy root does not exist");
    if (o !== e.root.id) return t("hierarchy root does not match tree root", o, e.root.id);
    let i = new Set(),
      a = [],
      s = (f) => (a.length > 0 ? `${a.join(" -> ")} -> ${f}` : f),
      l = (f) => {
        let c = e.get(f);
        if (!c) return t("Node missing in CanvasTree", f, "path:", s(f));
        let u = c.children;
        if (!c.cache.isShallowLoad) return u ? u.filter((m) => !Dr(m)).map((m) => m.id) : [];
      },
      d = (f) => {
        let c = r(!i.has(f), "cycle", f, "path:", s(f));
        if (c) return c;
        i.add(f);
        let u = r(n.getNodeData(f), "node data not found in hierarchy", f, "path:", s(f));
        if (u) return u;
        let m = r(n.has(f), "node not in hierarchy", f, "path:", s(f));
        if (m) return m;
        let h = r(e.has(f), "node not in canvas tree", f, "path:", s(f));
        if (h) return h;
        let g = n.getChildrenIds(f),
          v = l(f);
        if (v instanceof Error) return v;
        if (v) {
          if (g.length !== v.length)
            return t(
              "Child count mismatch",
              "node:",
              f,
              "path:",
              s(f),
              "hierarchyChildren:",
              JSON.stringify(g),
              "canvasTreeChildren:",
              JSON.stringify(v)
            );
          for (let S = 0; S < g.length; S++) {
            let b = g[S],
              w = v[S];
            if (!b)
              return t("Missing child id in hierarchy", "node:", f, "path:", s(f), "index:", S);
            if (!w)
              return t("Missing child id in canvas tree", "node:", f, "path:", s(f), "index:", S);
            if (b !== w)
              return t(
                "Child order mismatch",
                "node:",
                f,
                "path:",
                s(f),
                "index:",
                S,
                "hierarchyChild:",
                b,
                "canvasTreeChild:",
                w
              );
          }
          a.push(f);
          for (let S of g) {
            let b = d(S);
            if (b) return b;
          }
          a.pop();
        }
      };
    return d(o);
  } finally {
    e.endAllowPartialScopeAccess();
  }
}
function tp(n) {
  return {
    updateNestedObject(...e) {
      return n.updateNestedObject(...e);
    },
    updateNode(...e) {
      return n.updateNode(...e);
    },
  };
}
function np(n, e) {
  let t = new Set();
  for (let r of e) {
    if (r.key === fn) {
      t.add(r.id);
      continue;
    }
    let o = /^(?<type>[^/]+)\/(?<name>.+)\.save(?:$|\.)/u.exec(r.id),
      i = o?.groups?.type,
      a = o?.groups?.name;
    if (i && a && Aa(i)) {
      let s = tc({ type: i, name: a });
      n.has(s) && t.add(s);
    }
  }
  t.size !== 0 &&
    n.batch(() => {
      for (let r of t) {
        if (n.getObjectKey(r, "id") === void 0) continue;
        let o = n.getObjectKey(r, fn);
        n.updateNode(r, { [fn]: (o ?? 0) + 1 });
      }
    });
}
function _c(n) {
  return new Promise((e) => {
    setTimeout(e, n);
  });
}
var nr = new Set();
function rp(n) {
  for (let e of nr)
    e.check() &&
      (e.effect?.(),
      n.preventDefault(),
      (n.returnValue = "You have unsaved changes. They might be lost if you leave the page."));
}
window.addEventListener("beforeunload", rp);
function $c(n, e) {
  (nr.add(n), e?.addEventListener("abort", () => nr.delete(n)));
}
function op() {
  for (let n of nr) n.check() && n.effect?.();
}
function ip() {
  for (let n of nr) if (n.check()) return !0;
  return !1;
}
async function ov({ timeout: n = 3e4, polling: e = 50 } = {}) {
  op();
  let t = Date.now() + n;
  for (; Date.now() < t; ) {
    if (!ip()) return !0;
    await _c(e);
  }
  return !1;
}
var ap = me("CodeGenerationStore"),
  sp = "transform",
  Tn,
  jt,
  Eo = class {
    constructor(e) {
      C(this, "sourceNode", e);
      de(this, Tn, []);
      de(this, jt);
    }
    start(e) {
      let t = performance.now();
      return {
        end: (r) => {
          (I(this, Tn).push([e, performance.now() - t]),
            _e(this, jt, I(this, jt) ?? r),
            e === sp && this.completeSession());
        },
      };
    }
    restartSession() {
      (_e(this, Tn, []), _e(this, jt, void 0));
    }
    completeSession() {
      (I(this, jt)?.forEach(({ type: e, source: t, artifacts: r }) => {
        let o = lp(e, this.sourceNode);
        if (!o) return;
        let i = {
          codeType: o,
          durationMs: 0,
          updateCount: 0,
          updateDurationMs: 0,
          serializationDurationMs: 0,
          compilationDurationMs: 0,
          synchronizationDurationMs: 0,
          size: t.length,
          id: this.sourceNode.id,
          nodes: r?.metrics?.nodes ?? 1,
          variants: r?.metrics?.variants ?? 0,
          svgBytes: r?.metrics?.svgBytes,
          textBytes: r?.metrics?.textBytes,
        };
        for (let [s, l] of I(this, Tn))
          switch (((i.durationMs += l), s)) {
            case "update":
              (i.updateCount++, (i.updateDurationMs += l));
              break;
            case "serialize":
              i.serializationDurationMs += l;
              break;
            case "transform":
              i.compilationDurationMs += l;
              break;
            case "synchronize":
              i.synchronizationDurationMs += l;
              break;
            default:
              ee(s);
          }
        let a;
        for (a in i) {
          let s = i[a];
          U(s) && (i[a] = Math.round(s));
        }
        (ap.trace("Code Generation Metrics", i), ct("code_generation", i));
      }),
        this.restartSession());
    }
  };
((Tn = new WeakMap()), (jt = new WeakMap()));
function lp(n, e) {
  switch (n) {
    case "canvasComponent":
      return "smart_component";
    case "css":
      return "styles_preset";
    case "collection":
      return "collection";
    case "draftCollection":
      return "draft_collection";
    case "componentPresets":
      return "component_presets";
    case "screen":
      return D(e) ? "web_page" : "prototype_screen";
    case "prototype":
      return "prototype";
    case "vector":
      return "vector";
    case "vectorSet":
      return "vector_set";
    case "snippets":
      return "snippets";
    case "codeFile":
    case "config":
    case "siteMetadata":
    case "webPageMetadata":
    case "layoutTemplate":
    case "localization":
    case "design":
    case "kit":
    case "shader":
      return;
    default:
      ee(n);
  }
}
function dp(n, e) {
  let t = {};
  if (!e || dn(n)) return t;
  for (let r of n.cases)
    for (let o of e) {
      let i = r.nameLocalized;
      if (!i) continue;
      let a = i[o.id]?.value;
      if (!L(a)) continue;
      let s = t[r.id] ?? {};
      ((t[r.id] = s), (s[o.id] = a));
    }
  return t;
}
function Uc(n, e, t) {
  if (n.cases.length === 0) return p`(value) => value`;
  let r = dp(n, e),
    o = p`value`,
    i = p`locale`,
    a = p`fallbackLocale`;
  return new N(
    A.lines(
      p`(${o}, ${i}) => {`,
      p`const ${a} = ${i}?.fallback;`,
      p`switch (${o}) {`,
      ...n.cases.map(({ id: s, name: l }) => {
        let d = r[s];
        return d
          ? new N(
              A.lines(
                p`case ${s}:`,
                p`switch (${i}?.id) {`,
                ...Object.entries(d).map(([f, c]) => p`case ${f}: return ${c};`),
                p`default:`,
                p`if (${a}) return ${N.fn(t, o, a)};`,
                p`return ${l};`,
                p`}`
              )
            )
          : p`case ${s}: return ${l};`;
      }),
      p`default: return "";`,
      p`}`,
      p`}`
    )
  );
}
var ta = "framercms";
function Wc(n, e, t) {
  return `${n}-${e}.${t}`;
}
var Mo = class {
  constructor(e) {
    C(this, "componentSourceNodeId", e);
    C(this, "assets", {});
  }
  create(e, t, r) {
    let o = Wc(this.componentSourceNodeId, e, t);
    return ((this.assets[o] = r), p`new URL(${`./${o}`}, import.meta.url).href`);
  }
};
function jc(n, e) {
  let t =
      n.itemVariables.length === 1 && n.itemVariables[0]?.type === "image"
        ? n.itemVariables[0]
        : void 0,
    r = p`item`,
    o = { id: p`typeof ${r}?.[${"id"}] === "string" ? ${r}[${"id"}] : undefined` };
  for (let a of n.itemVariables) {
    let s = _r(a.name);
    s ? (o[a.id] = p`${r}?.[${s}] ?? ${r}?.[${a.id}]`) : (o[a.id] = p`${r}?.[${a.id}]`);
  }
  let i = e.bindings.create("arrayFromHumanReadable" + n.id);
  return (
    e.declarations.file.create(
      p.joinLines(
        p`const ${i} = (value) => {${p.joinLines(p`if (!Array.isArray(value)) return`, p`return value.map(${r} => {${p.joinLines(t ? p`if (typeof ${r} === "string") return { [${t.id}]: ${r} }` : void 0, p`return ${o}`)}})`)}}`
      )
    ),
    i
  );
}
function up(n) {
  return [n[1], n[0]];
}
var fp = new Map(Object.entries(Ee).map(up));
function Ye(n, e) {
  let t = e.addImport("framer", { exportSpecifier: "ControlType" }),
    r = fp.get(n),
    o = new N(r);
  return p`${t}.${o}`;
}
var pp = {
  boolean: "boolean",
  number: "number",
  string: "string",
  richtext: "string",
  fusednumber: "any",
  enum: "string",
  segmentedenum: "string",
  color: "string",
  image: "string",
  responsiveimage: "{src: string; srcSet?: string; alt?: string}",
  file: "string",
  componentinstance: "any",
  slot: "any",
  array: "any[]",
  eventhandler: "any",
  changehandler: "(value: any) => void",
  transition: "Record<string, any>",
  link: "string",
  linkrelvalues: "string[] | undefined",
  pagescope: "string",
  date: "string",
  object: "Record<string, any>",
  font: "Record<string, any> | undefined",
  scrollsectionref: "React.MutableRefObject<HTMLElement>",
  customcursor: "string",
  border: "Record<string, any>",
  cursor: "string",
  boxshadow: "string",
  gap: "string",
  padding: "string",
  borderradius: "string",
  collectionreference: "string | undefined",
  multicollectionreference: "string[] | undefined",
  trackingid: "string | undefined",
  vectorsetitem: "React.ComponentType<any> | undefined",
  location: "Record<string, any>",
};
function mp(n) {
  return n ? new N(pp[n]) : p`any`;
}
var Jc = "variant";
function hp(n) {
  return !q(n?.defaultValue);
}
function gp(n, e, t) {
  if (!he(n)) return {};
  y(n.isLoaded(), "Node must be loaded to generate controls");
  let r = n.getTopLevelVariants();
  if (r?.length < 2) return {};
  let o = [],
    i = [];
  for (let a of r) {
    let s = a.resolveValue("name") ?? dt(e, a);
    (o.push(a.id), i.push(s));
  }
  return { [Jc]: { type: Ye("enum", t), title: "Variant", options: o, optionTitles: i } };
}
function yp(n, e, t, r) {
  if (n.type === "responsiveimage" && bi(n) && Si(n.__vekterDefault)) {
    let { assetReference: o, alt: i } = n.__vekterDefault,
      a = t.assets?.resolveResponsiveImage(o, { focalPoint: n.__vekterDefault });
    return (a && L(i) && (a.alt = i), a);
  } else if (vi(n))
    switch (n.type) {
      case "responsiveimage": {
        let o = t.assets?.resolveResponsiveImage(n.__defaultAssetReference);
        return o || void 0;
      }
      case "file": {
        let o = t.assets?.resolve(n.__defaultAssetReference);
        return o ? o.src : void 0;
      }
    }
  else {
    if (e?.type === "array")
      return e.initialValue
        ? e.initialValue.map((o) => {
            let i = {};
            if (!o || o.type !== "object" || !$e(o.value)) return i;
            for (let a of e.itemVariables) {
              if (!je(a)) continue;
              let s = o.value[a.id],
                l = Gc(s, t, r);
              (q(l) && !Cp(s) && (l = Gc(Gs(e, a, r), t, r)), q(l) || (i[a.id] = l));
            }
            return i;
          })
        : [];
    if (hp(n)) {
      if (e && ln(e, r)) return;
      let o = n.defaultValue;
      if (n.type === "vectorsetitem")
        return (
          y($s(o), "Vector set item must be a module export identifier"),
          t.imports.addModuleImport(o.identifier)
        );
      if (n.type === "richtext" && L(o))
        return _i(o, { collectors: t, useFramerMotion: r === "SmartComponent" });
      if (n.type === "boxshadow" && kl(o))
        return Ol(o, "emptyStringWhenEmpty", (i) => (kr(i) ? zr(i) : ri.toFormatString(i)));
      if (Ni(o)) return kr(o) ? zr(o) : o;
    }
  }
}
function Gc(n, e, t) {
  if (n && Us(n.type))
    switch (n.type) {
      case "image": {
        if (!L(n.value)) return;
        let r = e.assets?.resolveResponsiveImage(n.value, { focalPoint: n });
        return r ? ((r.alt = n.alt ?? ""), r) : void 0;
      }
      case "boolean":
        return Ha(n.value) ? n.value : void 0;
      case "number":
        return U(n.value) ? n.value : void 0;
      case "string":
        return L(n.value) ? n.value : void 0;
      case "richtext":
        return !It(n.value) || Xn(n.value)
          ? void 0
          : _i(n.value, { collectors: e, useFramerMotion: t === "SmartComponent" });
      case "color":
        return L(n.value) ? (kr(n.value) ? zr(n.value) : ri.toFormatString(n.value)) : void 0;
      case "vectorsetitem":
        return !L(n.value) || !ot(n.value) ? void 0 : e.imports.addModuleImport(n.value);
      default:
        ee(n.type);
    }
}
function Cp(n) {
  return n?.type !== "richtext" ? !1 : It(n.value) && Xn(n.value);
}
function Fo(n, e) {
  return p`${n}?: ${e};`;
}
var qc = ["id", "width", "height"],
  vp = Object.fromEntries(
    qc.map((n) => {
      let e = new N(n);
      return [e, e];
    })
  );
function Sp(n, e) {
  let t = new B(n);
  return (B.addSpreadAtEnd(t, e), t);
}
var Kc = [
  { name: T.style, type: p`Record<string, unknown>` },
  { name: T.className, type: p`string` },
  { name: T.layoutId, type: p`string | number` },
];
function bp(n, e) {
  if (!he(n) && !D(n) && !te(n) && !Ce(n)) return [];
  let t = Kc.map(({ name: o, type: i }) => ({ key: _(o), type: Fo(o, i) })),
    r = _(T.variant);
  return e && Ge(n)
    ? t.concat([
        {
          key: r,
          type: Fo(T.variant, p`keyof typeof ${e}`),
          value: p`${e}[props.${T.variant}] ?? props.${T.variant} ?? ${n.baseVariantId}`,
        },
      ])
    : t.concat([{ key: r }]);
}
function ra(n, e, t) {
  let r = p`props.${new N(n)}`;
  return (e && (r = p`${e} ?? ${r}`), t && (r = p`${r} ?? ${t}`), r);
}
function Qc(n, e, t) {
  if (!D(n) || !At(t, n)) return;
  let o = n.getProvidedVariableMap(t, void 0, e, e.activeBundleHash);
  if (!o) return;
  let i = new Map();
  for (let [a, s] of o) (D(n) && n.hasVariable(a)) || i.set(a, s);
  if (i.size !== 0) return i;
}
var Tp = [...Pl, Jc, ...qc, ...Kc.map(({ name: n }) => n.toString()), "name"];
function Ip(n, e, t, r, o) {
  let i = new Set(Tp),
    a = Object.entries(n).map(([b, { control: w, variable: P, binding: V }]) => {
      if (!w) return { key: b, binding: V };
      let x = Vl(_r(w.title) ?? b, i),
        R = new N(x),
        F = yp(w, P, t, r),
        J = Fo(R, mp(w.type)),
        O;
      if (w.type === "enum" && w.options && w.optionTitles && R) {
        let $ = new B();
        w.options.forEach((G, H) => {
          if (!L(G) || !w.optionTitles) return;
          let De = w.optionTitles[H];
          L(De) && ($[De] = G);
        });
        let j = t.declarations.file.dedupe("humanReadableEnumMap", $);
        ((J = Fo(R, p`keyof typeof ${j}`)), (O = ra(b, R, F)), (O = p`${j}[${R}] ?? ${O}`));
      } else if (P?.type === "array") {
        let $ = jc(P, t),
          j = N.fn($, R);
        O = ra(b, j, F);
      } else O = ra(b, R, F);
      return { key: b, binding: V, name: x, type: J, value: O };
    }),
    s = "",
    l = [],
    d = [],
    f = new B(),
    c = Qc(e, t.componentLoader, t.tree),
    u = {},
    m = { ...vp },
    h = bp(e, o);
  for (let { key: b, binding: w, type: P, value: V, name: x } of [...h, ...a]) {
    let R = c?.get(b),
      F = w ?? new N(b);
    if (R) {
      let J = p`${F} = ${T.getFromCurrentRouteData}(${R.id})`;
      if (Is(R.type)) {
        let O = $r(R.type);
        Ni(O) && (J = p`${J} ?? ${O}`);
      }
      l.push(J);
    } else {
      let J = t.variables.getSetterBinding(b);
      if (J) {
        let O = Xc(b),
          $ = _(F),
          j = t.bindings.create(`external${$}`),
          G = t.bindings.create(`setExternal${$}`);
        (l.push(p`${b}: ${j}`), l.push(p`${O}: ${G}`));
        let H = t.declarations.file.dedupe(
          "useStateVariable",
          p.joinLines(
            p`(externalState, setExternalState) => {`,
            p`    const [internalState, setInternalState] = React.useState(externalState)`,
            p`    const [previousExternalState, setPreviousExternalState] = React.useState(externalState)`,
            p`    if (setExternalState) {`,
            p`        return [externalState, setExternalState]`,
            p`    }`,
            p`    if (externalState !== previousExternalState) {`,
            p`        setInternalState(externalState)`,
            p`        setPreviousExternalState(externalState)`,
            p`    }`,
            p`    return [internalState, setInternalState]`,
            p`}`
          )
        );
        d.push(p`const [${F}, ${J}] = ${N.fn(H, j, G)}`);
      } else _(F) !== b ? l.push(p`${b}: ${F}`) : l.push(F);
    }
    (P && (s += P), V && (f[b] = V), x && ((u[b] = x), (m[x] = new N(x))));
  }
  (r === "LayoutTemplate" && l.push(T.children),
    r === "DesignPage" && l.push(T.style),
    B.addSpreadAtStart(f, T.props));
  let g = t.declarations.file.dedupe("getProps", p`(${Sp(m, T.props)}) => { return ${f} }`),
    v = l.join(", "),
    S = v.length > 0 ? new N(`{ ${v}, ...${T.restProps} }`) : T.restProps;
  return {
    variableMap: vt(u) ? void 0 : u,
    types: new N(s),
    props: p.joinSections(p`const ${S} = ${N.fn(g, T.props)}`, ...d),
  };
}
function Np(n, e) {
  let t = [],
    r = Ce(n),
    o = Yc(n),
    i = e.variables.getBindings(n.id);
  for (let s of o) {
    if (!je(s)) continue;
    let l = i.get(s.id),
      d = e.variables.getSetterBinding(s.id);
    (!r && !l && !d) || t.push({ id: s.id, binding: l, definition: s });
  }
  let a = Qc(n, e.componentLoader, e.tree);
  if (a)
    for (let [s] of a) {
      let l = i.get(s);
      l && t.push({ id: s, binding: l });
    }
  return t;
}
function Rp(n, e) {
  let { hidden: t, ...r } = n;
  if (!e) return r;
  let { description: o, ...i } = r;
  return i;
}
function wp(n, e, t, r, o, i, a, s, l) {
  let { id: d, type: f } = e;
  switch (f) {
    case "controlReference": {
      let c = se(Hs(r, e.entityIdentifier));
      if (!c || !e.entityIdentifier || !ot(c)) return;
      let u = o.addModuleImport(c, {
        composeWithImports: [o.addImport("framer", { exportSpecifier: "getPropertyControls" })],
      });
      if (!u) return;
      let m = new B({ hidden: p`undefined`, description: p`undefined` });
      return (
        B.addSpreadAtStart(m, p`${u}[${e.controlKey}]`),
        L(t.title) && (m.title = t.title),
        L(t.description) && (m.description = t.description),
        js(e, l) && (m.optional = ln(e, l) ? !0 : p`undefined`),
        t.type === "responsiveimage" &&
          bi(t) &&
          Si(t.__vekterDefault) &&
          (m[ml] = N.fromValue(t.__vekterDefault)),
        ln(e, l)
          ? (m.defaultValue = p`undefined`)
          : vi(t)
            ? (m[pl] = N.fromValue(t.__defaultAssetReference))
            : wr(e.initialValue) || (m.defaultValue = N.fromValue(e.initialValue)),
        p`${u}?.[${e.controlKey}] && ${m}`
      );
    }
    case "nodePropertyControlReference":
    case "array":
    case "vectorsetitem":
    case "boolean":
    case "color":
    case "date":
    case "enum":
    case "eventhandler":
    case "file":
    case "image":
    case "link":
    case "linkrelvalues":
    case "number":
    case "richtext":
    case "scrollsectionref":
    case "customcursor":
    case "cursor":
    case "transition":
    case "boxshadow":
    case "string":
    case "border":
    case "borderradius":
    case "gap":
    case "padding":
    case "collectionreference":
    case "multicollectionreference":
    case "trackingid":
    case "location":
    case "slug": {
      let c = { ...Rp(t, a), type: Ye(t.type, o) };
      return (s === "vector" && i && (c.hidden = !i.getBinding(n.id, d)), c);
    }
    default:
      ee(f);
  }
}
function Yc(n) {
  return Ur(n) ? n.variables : [];
}
function oa(n, e, t, r, o, i, a, s = Yc(n)) {
  let l = gp(n, e, r),
    d = i === "canvasComponent",
    f = Rs(n),
    c = [...s];
  if (Q(n)) {
    let u = el();
    c.push(u);
    let m = tl();
    c.push(m);
    let h = Xs(n);
    c.push(h);
    let g = Zs(n);
    c.push(g);
  }
  for (let u of c) {
    if (!je(u)) continue;
    let m = fi(t, e, u, a);
    if (it(m)) continue;
    y(m.type !== "image", "Image variables should become responsive images");
    let h = wp(n, u, m, t, r, o, f, i, a);
    if (q(h) || ((l[u.id] = h), !d) || !Fd(u)) continue;
    let g = { type: "changehandler", changes: u.id };
    l[Xc(u.id)] = { ...g, type: Ye("changehandler", r) };
  }
  if (!vt(l)) return new B(l, 1);
}
function zo(n, e, t, r, o) {
  let i = {},
    a = [];
  for (let { id: s, binding: l, definition: d } of Np(n, e)) {
    if (d && D(n) && n.hasVariable(d.id)) continue;
    let c = d ? fi(e.tree, e.componentLoader, d, t) : void 0;
    ((i[s] = { control: c ?? void 0, variable: d, binding: l }), d && a.push(d));
  }
  return {
    propertyControls: oa(n, e.componentLoader, e.tree, e.imports, e.variables, r, t, a),
    ...Ip(i, n, e, t, o),
  };
}
function Xc(n) {
  return `on${n}Change`;
}
var Zc = Zt(ja(), 1);
function X(n, ...e) {
  if (!n) throw Error("Assertion Error" + (e.length > 0 ? ": " + e.join(" ") : ""));
}
function rr(n) {
  throw new Error(`Unexpected value: ${n}`);
}
var xp = Zt(Rr(), 1);
var eu = `// src/code-generation/components/cms/bundled/getRichTextJsonResolver.tsx?bundle
import { AutoBreakpointVariant, ComponentPresetsConsumer, Link, motion } from "framer";

// ../../library/src/router/lazy.tsx
import { forwardRef, isValidElement, useEffect } from "react";

// ../../library/src/utils/utils.ts
var isWindow = typeof window !== "undefined";
var supportsRequestIdleCallback = isWindow && typeof window.requestIdleCallback === "function";

// ../../library/src/router/lazy.tsx
var preloadKey = "preload";
function isLazyComponentType(componentType) {
  return typeof componentType === "object" && componentType !== null && !isValidElement(componentType) && preloadKey in componentType;
}

// src/code-generation/components/cms/bundled/getRichTextJsonResolver.tsx?bundle
import { Fragment, createElement } from "react";

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}

// src/code-generation/components/cms/bundled/getRichTextJsonResolver.tsx?bundle
var RichTextJsonType = /* @__PURE__ */ ((RichTextJsonType2) => {
  RichTextJsonType2[RichTextJsonType2["Fragment"] = 1] = "Fragment";
  RichTextJsonType2[RichTextJsonType2["Link"] = 2] = "Link";
  RichTextJsonType2[RichTextJsonType2["Module"] = 3] = "Module";
  RichTextJsonType2[RichTextJsonType2["Tag"] = 4] = "Tag";
  RichTextJsonType2[RichTextJsonType2["Text"] = 5] = "Text";
  return RichTextJsonType2;
})(RichTextJsonType || {});
function getRichTextJsonResolver(components) {
  const cache = /* @__PURE__ */ new Map();
  function deserializeChildren(children) {
    return children.map(deserializeNode);
  }
  function deserializeNode(node) {
    switch (node[0]) {
      case 1 /* Fragment */: {
        const [, ...children] = node;
        const childNodes = deserializeChildren(children);
        return createElement(Fragment, void 0, ...childNodes);
      }
      case 2 /* Link */: {
        const [, props, ...children] = node;
        const childNodes = deserializeChildren(children);
        return createElement(Link, props, ...childNodes);
      }
      case 3 /* Module */: {
        const [, identifier, props, richTextKeys, vectorSetItemKeys] = node;
        for (const key of richTextKeys) {
          const value = props[key];
          if (!value) continue;
          props[key] = deserializeNode(value);
        }
        for (const key of vectorSetItemKeys) {
          const value = props[key];
          if (typeof value !== "string") continue;
          const VectorSetItem = components[value];
          if (!VectorSetItem) continue;
          if (isLazyComponentType(VectorSetItem)) {
            void VectorSetItem.preload();
          }
          props[key] = VectorSetItem;
        }
        const Component = components[identifier];
        assert(Component, "Module not found");
        if (isLazyComponentType(Component)) {
          void Component.preload();
        }
        return <ComponentPresetsConsumer componentIdentifier={identifier}>
						{(presetProps) => <AutoBreakpointVariant component={Component} props={{ ...presetProps, ...props }} />}
					</ComponentPresetsConsumer>;
      }
      case 4 /* Tag */: {
        const [, tag, props, ...children] = node;
        const childNodes = deserializeChildren(children);
        if (tag === "a") {
          return createElement(motion.a, props, ...childNodes);
        }
        return createElement(tag, props, ...childNodes);
      }
      case 5 /* Text */: {
        const [, text] = node;
        return text;
      }
    }
  }
  return (pointer) => {
    const cached = cache.get(pointer);
    if (cached) return cached;
    const json = JSON.parse(pointer);
    const result = deserializeNode(json);
    cache.set(pointer, result);
    return result;
  };
}
export {
  RichTextJsonType,
  getRichTextJsonResolver
};
`;
var ko = class {
  constructor(e, t, r, o, i, a, s, l, d) {
    C(this, "collectionNode", e);
    C(this, "componentLoader", t);
    C(this, "tree", r);
    C(this, "imports", o);
    C(this, "submodules", i);
    C(this, "bindings", a);
    C(this, "declarations", s);
    C(this, "assets", l);
    C(this, "links", d);
    C(this, "scopeNode");
    C(this, "cache", new Map());
    C(this, "components", {});
    C(this, "resolvers");
    C(this, "serializers", {
      fragment: (e) => [1, ...e],
      link: (e, t) => [2, e, ...t],
      module: (e, t) => {
        if (!this.addModuleImport(e)) return;
        let o = this.componentLoader.reactComponentForIdentifier(e);
        y(o, "Component must exist for module");
        let i = [],
          a = [];
        for (let s in o.properties) {
          let l = o.properties[s];
          if (l) {
            if (l.type === "richtext") i.push(s);
            else if (l.type === "vectorsetitem") {
              let d = t[s];
              if (!L(d) || !ot(d) || !this.addModuleImport(d)) continue;
              a.push(s);
            }
          }
        }
        return [3, e, t, i, a];
      },
      tag: (e, t, r) => [4, e, t, ...r],
      text: (e) => [5, e],
    });
    C(this, "resolveRichTextBinding");
    ((this.resolveRichTextBinding = this.bindings.create("resolveRichText")),
      (this.scopeNode = this.tree.getScopeNodeFor(this.collectionNode)),
      (this.resolvers = Od(this.assets, this.links, void 0, void 0)));
  }
  addModuleImport(e) {
    if (e in this.components) return !0;
    let t = se(e),
      r = this.imports.addModuleImport(t, { lazy: !0 });
    return r ? ((this.components[e] = r), !0) : !1;
  }
  add(e) {
    let t = Lp(e),
      r = this.cache.get(t);
    if (r) return r;
    let o = kd(e, this.resolvers, this.serializers, {
        componentLoader: this.componentLoader,
        resolveVectorSetItem: (a) => a,
        tree: this.tree,
        nodeId: this.collectionNode.id,
        scopeId: this.scopeNode?.id,
      }),
      i = JSON.stringify(o);
    return (this.cache.set(t, i), i);
  }
  serialize() {
    let e = this.imports.addImport(this.submodules.create(eu).submoduleImport, {
        exportSpecifier: "getRichTextJsonResolver",
      }),
      t = this.declarations.dedupe("richTextComponents", this.components);
    this.declarations.create(p`const ${this.resolveRichTextBinding} = ${N.fn(e, t)}`);
  }
};
function Lp(n) {
  return typeof n == "string" ? `html:${n}` : `document:${JSON.stringify(n)}`;
}
var Oo = class {
  constructor() {
    C(this, "values", new Map());
  }
  add(e) {
    let t = this.values.get(e);
    if (U(t)) return t;
    let r = this.values.size;
    return (this.values.set(e, r), r);
  }
  serialize(e, t, r) {
    let o = [];
    for (let [a, s] of this.values) {
      let l = se(a),
        { importSpecifier: d } = Ml(l, "collection", e, t);
      (y(d, "Import specifier must be defined"), (o[s] = p`() => import(${d})`));
    }
    let i = r.dedupe("vectors", o);
    return r.dedupe(
      "resolveVectorSetItem",
      p`(pointer: number) => {${p.joinLines(p`const vector = ${i}[pointer]`, p`if (vector) return vector().then(v => v.default)`)}}`
    );
  }
};
var oe = {
    Uint8: 1,
    Uint16: 2,
    Uint32: 4,
    BigUint64: 8,
    Int8: 1,
    Int16: 2,
    Int32: 4,
    BigInt64: 8,
    Float32: 4,
    Float64: 8,
  },
  Bo = class {
    constructor(e) {
      C(this, "bytes", e);
      C(this, "offset", 0);
      C(this, "view");
      C(this, "decoder", new TextDecoder());
      this.view = In(this.bytes);
    }
    getOffset() {
      return this.offset;
    }
    ensureLength(e) {
      let t = this.bytes.length;
      if (!(this.offset + e <= t)) throw new Error("Reading out of bounds");
    }
    readUint8() {
      let e = oe.Uint8;
      this.ensureLength(e);
      let t = this.view.getUint8(this.offset);
      return ((this.offset += e), t);
    }
    readUint16() {
      let e = oe.Uint16;
      this.ensureLength(e);
      let t = this.view.getUint16(this.offset);
      return ((this.offset += e), t);
    }
    readUint32() {
      let e = oe.Uint32;
      this.ensureLength(e);
      let t = this.view.getUint32(this.offset);
      return ((this.offset += e), t);
    }
    readUint64() {
      let e = this.readBigUint64();
      return Number(e);
    }
    readBigUint64() {
      let e = oe.BigUint64;
      this.ensureLength(e);
      let t = this.view.getBigUint64(this.offset);
      return ((this.offset += e), t);
    }
    readInt8() {
      let e = oe.Int8;
      this.ensureLength(e);
      let t = this.view.getInt8(this.offset);
      return ((this.offset += e), t);
    }
    readInt16() {
      let e = oe.Int16;
      this.ensureLength(e);
      let t = this.view.getInt16(this.offset);
      return ((this.offset += e), t);
    }
    readInt32() {
      let e = oe.Int32;
      this.ensureLength(e);
      let t = this.view.getInt32(this.offset);
      return ((this.offset += e), t);
    }
    readInt64() {
      let e = this.readBigInt64();
      return Number(e);
    }
    readBigInt64() {
      let e = oe.BigInt64;
      this.ensureLength(e);
      let t = this.view.getBigInt64(this.offset);
      return ((this.offset += e), t);
    }
    readFloat32() {
      let e = oe.Float32;
      this.ensureLength(e);
      let t = this.view.getFloat32(this.offset);
      return ((this.offset += e), t);
    }
    readFloat64() {
      let e = oe.Float64;
      this.ensureLength(e);
      let t = this.view.getFloat64(this.offset);
      return ((this.offset += e), t);
    }
    readBytes(e) {
      let t = this.offset,
        r = t + e,
        o = this.bytes.subarray(t, r);
      return ((this.offset = r), o);
    }
    readString() {
      let e = this.readUint32(),
        t = this.readBytes(e);
      return this.decoder.decode(t);
    }
    readJson() {
      let e = this.readString();
      return JSON.parse(e);
    }
  };
function In(n) {
  return new DataView(n.buffer, n.byteOffset, n.byteLength);
}
var Dp = 1024,
  Pp = 1.5,
  ia = (n) => 2 ** n - 1,
  aa = (n) => -(2 ** (n - 1)),
  sa = (n) => 2 ** (n - 1) - 1,
  Xe = {
    Uint8: 0,
    Uint16: 0,
    Uint32: 0,
    Uint64: 0,
    BigUint64: 0,
    Int8: aa(8),
    Int16: aa(16),
    Int32: aa(32),
    Int64: Number.MIN_SAFE_INTEGER,
    BigInt64: -(BigInt(2) ** BigInt(63)),
  },
  Ze = {
    Uint8: ia(8),
    Uint16: ia(16),
    Uint32: ia(32),
    Uint64: Number.MAX_SAFE_INTEGER,
    BigUint64: BigInt(2) ** BigInt(64) - BigInt(1),
    Int8: sa(8),
    Int16: sa(16),
    Int32: sa(32),
    Int64: Number.MAX_SAFE_INTEGER,
    BigInt64: BigInt(2) ** BigInt(63) - BigInt(1),
  };
function et(n, e, t, r) {
  (X(n >= e, n, "outside lower bound for", r), X(n <= t, n, "outside upper bound for", r));
}
var Ht = class {
  constructor() {
    C(this, "offset", 0);
    C(this, "bytes", new Uint8Array(Dp));
    C(this, "view", In(this.bytes));
    C(this, "encoder", new TextEncoder());
    C(this, "encodedStrings", new Map());
  }
  getOffset() {
    return this.offset;
  }
  slice(e = 0, t = this.offset) {
    return this.bytes.slice(e, t);
  }
  subarray(e = 0, t = this.offset) {
    return this.bytes.subarray(e, t);
  }
  ensureLength(e) {
    let t = this.bytes.length;
    if (this.offset + e <= t) return;
    let r = Math.ceil(t * Pp) + e,
      o = new Uint8Array(r);
    (o.set(this.bytes), (this.bytes = o), (this.view = In(o)));
  }
  writeUint8(e) {
    et(e, Xe.Uint8, Ze.Uint8, "Uint8");
    let t = oe.Uint8;
    (this.ensureLength(t), this.view.setUint8(this.offset, e), (this.offset += t));
  }
  writeUint16(e) {
    et(e, Xe.Uint16, Ze.Uint16, "Uint16");
    let t = oe.Uint16;
    (this.ensureLength(t), this.view.setUint16(this.offset, e), (this.offset += t));
  }
  writeUint32(e) {
    et(e, Xe.Uint32, Ze.Uint32, "Uint32");
    let t = oe.Uint32;
    (this.ensureLength(t), this.view.setUint32(this.offset, e), (this.offset += t));
  }
  writeUint64(e) {
    et(e, Xe.Uint64, Ze.Uint64, "Uint64");
    let t = BigInt(e);
    this.writeBigUint64(t);
  }
  writeBigUint64(e) {
    et(e, Xe.BigUint64, Ze.BigUint64, "BigUint64");
    let t = oe.BigUint64;
    (this.ensureLength(t), this.view.setBigUint64(this.offset, e), (this.offset += t));
  }
  writeInt8(e) {
    et(e, Xe.Int8, Ze.Int8, "Int8");
    let t = oe.Int8;
    (this.ensureLength(t), this.view.setInt8(this.offset, e), (this.offset += t));
  }
  writeInt16(e) {
    et(e, Xe.Int16, Ze.Int16, "Int16");
    let t = oe.Int16;
    (this.ensureLength(t), this.view.setInt16(this.offset, e), (this.offset += t));
  }
  writeInt32(e) {
    et(e, Xe.Int32, Ze.Int32, "Int32");
    let t = oe.Int32;
    (this.ensureLength(t), this.view.setInt32(this.offset, e), (this.offset += t));
  }
  writeInt64(e) {
    et(e, Xe.Int64, Ze.Int64, "Int64");
    let t = BigInt(e);
    this.writeBigInt64(t);
  }
  writeBigInt64(e) {
    et(e, Xe.BigInt64, Ze.BigInt64, "BigInt64");
    let t = oe.BigInt64;
    (this.ensureLength(t), this.view.setBigInt64(this.offset, e), (this.offset += t));
  }
  writeFloat32(e) {
    let t = oe.Float32;
    (this.ensureLength(t), this.view.setFloat32(this.offset, e), (this.offset += t));
  }
  writeFloat64(e) {
    let t = oe.Float64;
    (this.ensureLength(t), this.view.setFloat64(this.offset, e), (this.offset += t));
  }
  writeBytes(e) {
    let t = e.length;
    (this.ensureLength(t), this.bytes.set(e, this.offset), (this.offset += t));
  }
  encodeString(e) {
    let t = this.encodedStrings.get(e);
    if (t) return t;
    let r = this.encoder.encode(e);
    return (this.encodedStrings.set(e, r), r);
  }
  writeString(e) {
    let t = this.encodeString(e),
      r = t.length;
    (this.writeUint32(r), this.writeBytes(t));
  }
  writeJson(e) {
    let t = JSON.stringify(e);
    this.writeString(t);
  }
};
var tu = `var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

// ../../../node_modules/dataloader/index.js
var require_dataloader = __commonJS({
  "../../../node_modules/dataloader/index.js"(exports, module) {
    "use strict";
    var DataLoader2 = /* @__PURE__ */ (function() {
      function DataLoader3(batchLoadFn, options) {
        if (typeof batchLoadFn !== "function") {
          throw new TypeError("DataLoader must be constructed with a function which accepts " + ("Array<key> and returns Promise<Array<value>>, but got: " + batchLoadFn + "."));
        }
        this._batchLoadFn = batchLoadFn;
        this._maxBatchSize = getValidMaxBatchSize(options);
        this._batchScheduleFn = getValidBatchScheduleFn(options);
        this._cacheKeyFn = getValidCacheKeyFn(options);
        this._cacheMap = getValidCacheMap(options);
        this._batch = null;
        this.name = getValidName(options);
      }
      var _proto = DataLoader3.prototype;
      _proto.load = function load(key) {
        if (key === null || key === void 0) {
          throw new TypeError("The loader.load() function must be called with a value, " + ("but got: " + String(key) + "."));
        }
        var batch = getCurrentBatch(this);
        var cacheMap = this._cacheMap;
        var cacheKey = this._cacheKeyFn(key);
        if (cacheMap) {
          var cachedPromise = cacheMap.get(cacheKey);
          if (cachedPromise) {
            var cacheHits = batch.cacheHits || (batch.cacheHits = []);
            return new Promise(function(resolve) {
              cacheHits.push(function() {
                resolve(cachedPromise);
              });
            });
          }
        }
        batch.keys.push(key);
        var promise = new Promise(function(resolve, reject) {
          batch.callbacks.push({
            resolve,
            reject
          });
        });
        if (cacheMap) {
          cacheMap.set(cacheKey, promise);
        }
        return promise;
      };
      _proto.loadMany = function loadMany(keys) {
        if (!isArrayLike(keys)) {
          throw new TypeError("The loader.loadMany() function must be called with Array<key> " + ("but got: " + keys + "."));
        }
        var loadPromises = [];
        for (var i = 0; i < keys.length; i++) {
          loadPromises.push(this.load(keys[i])["catch"](function(error) {
            return error;
          }));
        }
        return Promise.all(loadPromises);
      };
      _proto.clear = function clear(key) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          cacheMap["delete"](cacheKey);
        }
        return this;
      };
      _proto.clearAll = function clearAll() {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          cacheMap.clear();
        }
        return this;
      };
      _proto.prime = function prime(key, value) {
        var cacheMap = this._cacheMap;
        if (cacheMap) {
          var cacheKey = this._cacheKeyFn(key);
          if (cacheMap.get(cacheKey) === void 0) {
            var promise;
            if (value instanceof Error) {
              promise = Promise.reject(value);
              promise["catch"](function() {
              });
            } else {
              promise = Promise.resolve(value);
            }
            cacheMap.set(cacheKey, promise);
          }
        }
        return this;
      };
      return DataLoader3;
    })();
    var enqueuePostPromiseJob = typeof process === "object" && typeof process.nextTick === "function" ? function(fn) {
      if (!resolvedPromise) {
        resolvedPromise = Promise.resolve();
      }
      resolvedPromise.then(function() {
        process.nextTick(fn);
      });
    } : typeof setImmediate === "function" ? function(fn) {
      setImmediate(fn);
    } : function(fn) {
      setTimeout(fn);
    };
    var resolvedPromise;
    function getCurrentBatch(loader) {
      var existingBatch = loader._batch;
      if (existingBatch !== null && !existingBatch.hasDispatched && existingBatch.keys.length < loader._maxBatchSize) {
        return existingBatch;
      }
      var newBatch = {
        hasDispatched: false,
        keys: [],
        callbacks: []
      };
      loader._batch = newBatch;
      loader._batchScheduleFn(function() {
        dispatchBatch(loader, newBatch);
      });
      return newBatch;
    }
    function dispatchBatch(loader, batch) {
      batch.hasDispatched = true;
      if (batch.keys.length === 0) {
        resolveCacheHits(batch);
        return;
      }
      var batchPromise;
      try {
        batchPromise = loader._batchLoadFn(batch.keys);
      } catch (e) {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function " + ("errored synchronously: " + String(e) + ".")));
      }
      if (!batchPromise || typeof batchPromise.then !== "function") {
        return failedDispatch(loader, batch, new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise: " + String(batchPromise) + ".")));
      }
      batchPromise.then(function(values) {
        if (!isArrayLike(values)) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did " + ("not return a Promise of an Array: " + String(values) + "."));
        }
        if (values.length !== batch.keys.length) {
          throw new TypeError("DataLoader must be constructed with a function which accepts Array<key> and returns Promise<Array<value>>, but the function did not return a Promise of an Array of the same length as the Array of keys." + ("\\n\\nKeys:\\n" + String(batch.keys)) + ("\\n\\nValues:\\n" + String(values)));
        }
        resolveCacheHits(batch);
        for (var i = 0; i < batch.callbacks.length; i++) {
          var value = values[i];
          if (value instanceof Error) {
            batch.callbacks[i].reject(value);
          } else {
            batch.callbacks[i].resolve(value);
          }
        }
      })["catch"](function(error) {
        failedDispatch(loader, batch, error);
      });
    }
    function failedDispatch(loader, batch, error) {
      resolveCacheHits(batch);
      for (var i = 0; i < batch.keys.length; i++) {
        loader.clear(batch.keys[i]);
        batch.callbacks[i].reject(error);
      }
    }
    function resolveCacheHits(batch) {
      if (batch.cacheHits) {
        for (var i = 0; i < batch.cacheHits.length; i++) {
          batch.cacheHits[i]();
        }
      }
    }
    function getValidMaxBatchSize(options) {
      var shouldBatch = !options || options.batch !== false;
      if (!shouldBatch) {
        return 1;
      }
      var maxBatchSize = options && options.maxBatchSize;
      if (maxBatchSize === void 0) {
        return Infinity;
      }
      if (typeof maxBatchSize !== "number" || maxBatchSize < 1) {
        throw new TypeError("maxBatchSize must be a positive number: " + maxBatchSize);
      }
      return maxBatchSize;
    }
    function getValidBatchScheduleFn(options) {
      var batchScheduleFn = options && options.batchScheduleFn;
      if (batchScheduleFn === void 0) {
        return enqueuePostPromiseJob;
      }
      if (typeof batchScheduleFn !== "function") {
        throw new TypeError("batchScheduleFn must be a function: " + batchScheduleFn);
      }
      return batchScheduleFn;
    }
    function getValidCacheKeyFn(options) {
      var cacheKeyFn = options && options.cacheKeyFn;
      if (cacheKeyFn === void 0) {
        return function(key) {
          return key;
        };
      }
      if (typeof cacheKeyFn !== "function") {
        throw new TypeError("cacheKeyFn must be a function: " + cacheKeyFn);
      }
      return cacheKeyFn;
    }
    function getValidCacheMap(options) {
      var shouldCache = !options || options.cache !== false;
      if (!shouldCache) {
        return null;
      }
      var cacheMap = options && options.cacheMap;
      if (cacheMap === void 0) {
        return /* @__PURE__ */ new Map();
      }
      if (cacheMap !== null) {
        var cacheFunctions = ["get", "set", "delete", "clear"];
        var missingFunctions = cacheFunctions.filter(function(fnName) {
          return cacheMap && typeof cacheMap[fnName] !== "function";
        });
        if (missingFunctions.length !== 0) {
          throw new TypeError("Custom cacheMap missing methods: " + missingFunctions.join(", "));
        }
      }
      return cacheMap;
    }
    function getValidName(options) {
      if (options && options.name) {
        return options.name;
      }
      return null;
    }
    function isArrayLike(x) {
      return typeof x === "object" && x !== null && typeof x.length === "number" && (x.length === 0 || x.length > 0 && Object.prototype.hasOwnProperty.call(x, x.length - 1));
    }
    module.exports = DataLoader2;
  }
});

// src/code-generation/components/cms/bundled/DatabaseCollection.ts?bundle
var import_dataloader = __toESM(require_dataloader(), 1);

// src/code-generation/components/cms/bundled/BufferReader.ts
var BYTE_LENGTH = {
  Uint8: 1,
  Uint16: 2,
  Uint32: 4,
  BigUint64: 8,
  Int8: 1,
  Int16: 2,
  Int32: 4,
  BigInt64: 8,
  Float32: 4,
  Float64: 8
};
var BufferReader = class {
  constructor(bytes) {
    __publicField(this, "bytes", bytes);
    __publicField(this, "offset", 0);
    __publicField(this, "view");
    __publicField(this, "decoder", new TextDecoder());
    this.view = getDataView(this.bytes);
  }
  getOffset() {
    return this.offset;
  }
  ensureLength(requiredBytes) {
    const oldLength = this.bytes.length;
    if (this.offset + requiredBytes <= oldLength) return;
    throw new Error("Reading out of bounds");
  }
  readUint8() {
    const size = BYTE_LENGTH.Uint8;
    this.ensureLength(size);
    const value = this.view.getUint8(this.offset);
    this.offset += size;
    return value;
  }
  readUint16() {
    const size = BYTE_LENGTH.Uint16;
    this.ensureLength(size);
    const value = this.view.getUint16(this.offset);
    this.offset += size;
    return value;
  }
  readUint32() {
    const size = BYTE_LENGTH.Uint32;
    this.ensureLength(size);
    const value = this.view.getUint32(this.offset);
    this.offset += size;
    return value;
  }
  readUint64() {
    const bigint = this.readBigUint64();
    return Number(bigint);
  }
  readBigUint64() {
    const size = BYTE_LENGTH.BigUint64;
    this.ensureLength(size);
    const value = this.view.getBigUint64(this.offset);
    this.offset += size;
    return value;
  }
  readInt8() {
    const size = BYTE_LENGTH.Int8;
    this.ensureLength(size);
    const value = this.view.getInt8(this.offset);
    this.offset += size;
    return value;
  }
  readInt16() {
    const size = BYTE_LENGTH.Int16;
    this.ensureLength(size);
    const value = this.view.getInt16(this.offset);
    this.offset += size;
    return value;
  }
  readInt32() {
    const size = BYTE_LENGTH.Int32;
    this.ensureLength(size);
    const value = this.view.getInt32(this.offset);
    this.offset += size;
    return value;
  }
  readInt64() {
    const bigint = this.readBigInt64();
    return Number(bigint);
  }
  readBigInt64() {
    const size = BYTE_LENGTH.BigInt64;
    this.ensureLength(size);
    const value = this.view.getBigInt64(this.offset);
    this.offset += size;
    return value;
  }
  readFloat32() {
    const size = BYTE_LENGTH.Float32;
    this.ensureLength(size);
    const value = this.view.getFloat32(this.offset);
    this.offset += size;
    return value;
  }
  readFloat64() {
    const size = BYTE_LENGTH.Float64;
    this.ensureLength(size);
    const value = this.view.getFloat64(this.offset);
    this.offset += size;
    return value;
  }
  readBytes(length) {
    const start = this.offset;
    const end = start + length;
    const value = this.bytes.subarray(start, end);
    this.offset = end;
    return value;
  }
  readString() {
    const length = this.readUint32();
    const bytes = this.readBytes(length);
    return this.decoder.decode(bytes);
  }
  readJson() {
    const string = this.readString();
    return JSON.parse(string);
  }
};
function getDataView(bytes) {
  return new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
}

// src/code-generation/components/cms/bundled/DatabaseDictionaryIndex.ts
import { ControlType as ControlType2 } from "framer";

// ../../library/src/utils/utils.ts
var isWindow = typeof window !== "undefined";
var supportsRequestIdleCallback = isWindow && typeof window.requestIdleCallback === "function";

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}
function assertNever(condition) {
  throw new Error(\`Unexpected value: \${condition}\`);
}

// src/code-generation/components/cms/bundled/BufferWriter.ts
var INITIAL_BUFFER_SIZE = 1024;
var GROWTH_FACTOR = 1.5;
var maxUnsignedNumber = (bits) => 2 ** bits - 1;
var minSignedNumber = (bits) => -(2 ** (bits - 1));
var maxSignedNumber = (bits) => 2 ** (bits - 1) - 1;
var MIN_VALUE = {
  Uint8: 0,
  Uint16: 0,
  Uint32: 0,
  Uint64: 0,
  BigUint64: 0,
  Int8: minSignedNumber(8),
  Int16: minSignedNumber(16),
  Int32: minSignedNumber(32),
  Int64: Number.MIN_SAFE_INTEGER,
  BigInt64: -(BigInt(2) ** BigInt(63))
};
var MAX_VALUE = {
  Uint8: maxUnsignedNumber(8),
  Uint16: maxUnsignedNumber(16),
  Uint32: maxUnsignedNumber(32),
  Uint64: Number.MAX_SAFE_INTEGER,
  BigUint64: BigInt(2) ** BigInt(64) - BigInt(1),
  Int8: maxSignedNumber(8),
  Int16: maxSignedNumber(16),
  Int32: maxSignedNumber(32),
  Int64: Number.MAX_SAFE_INTEGER,
  BigInt64: BigInt(2) ** BigInt(63) - BigInt(1)
};
function assertRange(value, min, max, dataType) {
  assert(value >= min, value, "outside lower bound for", dataType);
  assert(value <= max, value, "outside upper bound for", dataType);
}
var BufferWriter = class {
  constructor() {
    __publicField(this, "offset", 0);
    __publicField(this, "bytes", new Uint8Array(INITIAL_BUFFER_SIZE));
    __publicField(this, "view", getDataView(this.bytes));
    __publicField(this, "encoder", new TextEncoder());
    __publicField(this, "encodedStrings", /* @__PURE__ */ new Map());
  }
  getOffset() {
    return this.offset;
  }
  slice(start = 0, end = this.offset) {
    return this.bytes.slice(start, end);
  }
  subarray(start = 0, end = this.offset) {
    return this.bytes.subarray(start, end);
  }
  ensureLength(requiredBytes) {
    const oldLength = this.bytes.length;
    if (this.offset + requiredBytes <= oldLength) return;
    const newLength = Math.ceil(oldLength * GROWTH_FACTOR) + requiredBytes;
    const newBytes = new Uint8Array(newLength);
    newBytes.set(this.bytes);
    this.bytes = newBytes;
    this.view = getDataView(newBytes);
  }
  writeUint8(value) {
    assertRange(value, MIN_VALUE.Uint8, MAX_VALUE.Uint8, "Uint8");
    const size = BYTE_LENGTH.Uint8;
    this.ensureLength(size);
    this.view.setUint8(this.offset, value);
    this.offset += size;
  }
  writeUint16(value) {
    assertRange(value, MIN_VALUE.Uint16, MAX_VALUE.Uint16, "Uint16");
    const size = BYTE_LENGTH.Uint16;
    this.ensureLength(size);
    this.view.setUint16(this.offset, value);
    this.offset += size;
  }
  writeUint32(value) {
    assertRange(value, MIN_VALUE.Uint32, MAX_VALUE.Uint32, "Uint32");
    const size = BYTE_LENGTH.Uint32;
    this.ensureLength(size);
    this.view.setUint32(this.offset, value);
    this.offset += size;
  }
  writeUint64(value) {
    assertRange(value, MIN_VALUE.Uint64, MAX_VALUE.Uint64, "Uint64");
    const bigint = BigInt(value);
    this.writeBigUint64(bigint);
  }
  writeBigUint64(value) {
    assertRange(value, MIN_VALUE.BigUint64, MAX_VALUE.BigUint64, "BigUint64");
    const size = BYTE_LENGTH.BigUint64;
    this.ensureLength(size);
    this.view.setBigUint64(this.offset, value);
    this.offset += size;
  }
  writeInt8(value) {
    assertRange(value, MIN_VALUE.Int8, MAX_VALUE.Int8, "Int8");
    const size = BYTE_LENGTH.Int8;
    this.ensureLength(size);
    this.view.setInt8(this.offset, value);
    this.offset += size;
  }
  writeInt16(value) {
    assertRange(value, MIN_VALUE.Int16, MAX_VALUE.Int16, "Int16");
    const size = BYTE_LENGTH.Int16;
    this.ensureLength(size);
    this.view.setInt16(this.offset, value);
    this.offset += size;
  }
  writeInt32(value) {
    assertRange(value, MIN_VALUE.Int32, MAX_VALUE.Int32, "Int32");
    const size = BYTE_LENGTH.Int32;
    this.ensureLength(size);
    this.view.setInt32(this.offset, value);
    this.offset += size;
  }
  writeInt64(value) {
    assertRange(value, MIN_VALUE.Int64, MAX_VALUE.Int64, "Int64");
    const bigint = BigInt(value);
    this.writeBigInt64(bigint);
  }
  writeBigInt64(value) {
    assertRange(value, MIN_VALUE.BigInt64, MAX_VALUE.BigInt64, "BigInt64");
    const size = BYTE_LENGTH.BigInt64;
    this.ensureLength(size);
    this.view.setBigInt64(this.offset, value);
    this.offset += size;
  }
  writeFloat32(value) {
    const size = BYTE_LENGTH.Float32;
    this.ensureLength(size);
    this.view.setFloat32(this.offset, value);
    this.offset += size;
  }
  writeFloat64(value) {
    const size = BYTE_LENGTH.Float64;
    this.ensureLength(size);
    this.view.setFloat64(this.offset, value);
    this.offset += size;
  }
  writeBytes(value) {
    const size = value.length;
    this.ensureLength(size);
    this.bytes.set(value, this.offset);
    this.offset += size;
  }
  encodeString(value) {
    const cached = this.encodedStrings.get(value);
    if (cached) return cached;
    const bytes = this.encoder.encode(value);
    this.encodedStrings.set(value, bytes);
    return bytes;
  }
  writeString(value) {
    const bytes = this.encodeString(value);
    const size = bytes.length;
    this.writeUint32(size);
    this.writeBytes(bytes);
  }
  writeJson(value) {
    const json = JSON.stringify(value);
    this.writeString(json);
  }
};

// src/utils/typeChecks.ts
function isString(value) {
  return typeof value === "string";
}
function isNumber(value) {
  return Number.isFinite(value);
}
function isNull(value) {
  return value === null;
}

// src/code-generation/components/cms/bundled/models/DatabaseItemPointerModel.ts
var DatabaseItemPointerModel = class _DatabaseItemPointerModel {
  constructor(chunkId, offset, length) {
    __publicField(this, "chunkId", chunkId);
    __publicField(this, "offset", offset);
    __publicField(this, "length", length);
  }
  static fromString(value) {
    const [chunkId, offset, length] = value.split("/").map(Number);
    assert(isNumber(chunkId), "Invalid chunkId");
    assert(isNumber(offset), "Invalid offset");
    assert(isNumber(length), "Invalid length");
    return new _DatabaseItemPointerModel(chunkId, offset, length);
  }
  toString() {
    return \`\${this.chunkId}/\${this.offset}/\${this.length}\`;
  }
  static read(reader) {
    const chunkId = reader.readUint16();
    const offset = reader.readUint32();
    const length = reader.readUint32();
    return new _DatabaseItemPointerModel(chunkId, offset, length);
  }
  write(writer) {
    writer.writeUint16(this.chunkId);
    writer.writeUint32(this.offset);
    writer.writeUint32(this.length);
  }
  compare(other) {
    if (this.chunkId < other.chunkId) return -1;
    if (this.chunkId > other.chunkId) return 1;
    if (this.offset < other.offset) return -1;
    if (this.offset > other.offset) return 1;
    assert(this.length === other.length);
    return 0;
  }
};

// src/code-generation/components/cms/bundled/models/DatabaseValueModel.ts
import { ControlType } from "framer";
function getValueType(value) {
  if (isNull(value)) {
    return 0 /* Null */;
  }
  switch (value.type) {
    case ControlType.Array:
      return 1 /* Array */;
    case ControlType.Boolean:
      return 2 /* Boolean */;
    case ControlType.Color:
      return 3 /* Color */;
    case ControlType.Date:
      return 4 /* Date */;
    case ControlType.Enum:
      return 5 /* Enum */;
    case ControlType.File:
      return 6 /* File */;
    case ControlType.ResponsiveImage:
      return 10 /* ResponsiveImage */;
    case ControlType.Link:
      return 7 /* Link */;
    case ControlType.Number:
      return 8 /* Number */;
    case ControlType.Object:
      return 9 /* Object */;
    case ControlType.RichText:
      return 11 /* RichText */;
    case ControlType.String:
      return 12 /* String */;
    case ControlType.VectorSetItem:
      return 13 /* VectorSetItem */;
    default:
      assertNever(value);
  }
}
var DatabaseValueModel;
((DatabaseValueModel2) => {
  function read(reader) {
    const type = reader.readUint8();
    switch (type) {
      case 0 /* Null */:
        return null;
      case 1 /* Array */:
        return readArray(reader);
      case 2 /* Boolean */:
        return readBoolean(reader);
      case 3 /* Color */:
        return readColor(reader);
      case 4 /* Date */:
        return readDate(reader);
      case 5 /* Enum */:
        return readEnum(reader);
      case 6 /* File */:
        return readFile(reader);
      case 7 /* Link */:
        return readLink(reader);
      case 8 /* Number */:
        return readNumber(reader);
      case 9 /* Object */:
        return readObject(reader);
      case 10 /* ResponsiveImage */:
        return readResponsiveImage(reader);
      case 11 /* RichText */:
        return readRichText(reader);
      case 12 /* String */:
        return readString(reader);
      case 13 /* VectorSetItem */:
        return readVectorSetItem(reader);
      default:
        assertNever(type);
    }
  }
  DatabaseValueModel2.read = read;
  function write(writer, value) {
    const type = getValueType(value);
    writer.writeUint8(type);
    if (isNull(value)) return;
    switch (value.type) {
      case ControlType.Array:
        return writeArray(writer, value);
      case ControlType.Boolean:
        return writeBoolean(writer, value);
      case ControlType.Color:
        return writeColor(writer, value);
      case ControlType.Date:
        return writeDate(writer, value);
      case ControlType.Enum:
        return writeEnum(writer, value);
      case ControlType.File:
        return writeFile(writer, value);
      case ControlType.Link:
        return writeLink(writer, value);
      case ControlType.Number:
        return writeNumber(writer, value);
      case ControlType.Object:
        return writeObject(writer, value);
      case ControlType.ResponsiveImage:
        return writeResponsiveImage(writer, value);
      case ControlType.RichText:
        return writeRichText(writer, value);
      case ControlType.VectorSetItem:
        return writeVectorSetItem(writer, value);
      case ControlType.String:
        return writeString(writer, value);
      default:
        assertNever(value);
    }
  }
  DatabaseValueModel2.write = write;
  function compare(left, right, collation) {
    const leftType = getValueType(left);
    const rightType = getValueType(right);
    if (leftType < rightType) return -1;
    if (leftType > rightType) return 1;
    if (isNull(left) || isNull(right)) return 0;
    switch (left.type) {
      case ControlType.Array:
        assert(right.type === ControlType.Array);
        return compareArray(left, right, collation);
      case ControlType.Boolean:
        assert(right.type === ControlType.Boolean);
        return compareBoolean(left, right);
      case ControlType.Color:
        assert(right.type === ControlType.Color);
        return compareColor(left, right);
      case ControlType.Date:
        assert(right.type === ControlType.Date);
        return compareDate(left, right);
      case ControlType.Enum:
        assert(right.type === ControlType.Enum);
        return compareEnum(left, right);
      case ControlType.File:
        assert(right.type === ControlType.File);
        return compareFile(left, right);
      case ControlType.Link:
        assert(right.type === ControlType.Link);
        return compareLink(left, right);
      case ControlType.Number:
        assert(right.type === ControlType.Number);
        return compareNumber(left, right);
      case ControlType.Object:
        assert(right.type === ControlType.Object);
        return compareObject(left, right, collation);
      case ControlType.ResponsiveImage:
        assert(right.type === ControlType.ResponsiveImage);
        return compareResponsiveImage(left, right);
      case ControlType.RichText:
        assert(right.type === ControlType.RichText);
        return compareRichText(left, right);
      case ControlType.VectorSetItem:
        assert(right.type === ControlType.VectorSetItem);
        return compareVectorSetItem(left, right);
      case ControlType.String:
        assert(right.type === ControlType.String);
        return compareString(left, right, collation);
      default:
        assertNever(left);
    }
  }
  DatabaseValueModel2.compare = compare;
})(DatabaseValueModel || (DatabaseValueModel = {}));
function readArray(reader) {
  const length = reader.readUint16();
  const value = [];
  for (let i = 0; i < length; i++) {
    const item = DatabaseValueModel.read(reader);
    value.push(item);
  }
  return {
    type: ControlType.Array,
    value
  };
}
function writeArray(writer, value) {
  writer.writeUint16(value.value.length);
  for (const item of value.value) {
    DatabaseValueModel.write(writer, item);
  }
}
function compareArray(left, right, collation) {
  const leftLength = left.value.length;
  const rightLength = right.value.length;
  if (leftLength < rightLength) return -1;
  if (leftLength > rightLength) return 1;
  for (let i = 0; i < leftLength; i++) {
    const leftItem = left.value[i];
    const rightItem = right.value[i];
    const result = DatabaseValueModel.compare(leftItem, rightItem, collation);
    if (result !== 0) return result;
  }
  return 0;
}
function readBoolean(reader) {
  return {
    type: ControlType.Boolean,
    value: reader.readUint8() !== 0
  };
}
function writeBoolean(writer, value) {
  writer.writeUint8(value.value ? 1 : 0);
}
function compareBoolean(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readColor(reader) {
  return {
    type: ControlType.Color,
    value: reader.readString()
  };
}
function writeColor(writer, value) {
  writer.writeString(value.value);
}
function compareColor(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readDate(reader) {
  const timestamp = reader.readInt64();
  const date = new Date(timestamp);
  return {
    type: ControlType.Date,
    value: date.toISOString()
  };
}
function writeDate(writer, value) {
  const date = new Date(value.value);
  const timestamp = date.getTime();
  writer.writeInt64(timestamp);
}
function compareDate(left, right) {
  const leftDate = new Date(left.value);
  const rightDate = new Date(right.value);
  if (leftDate < rightDate) return -1;
  if (leftDate > rightDate) return 1;
  return 0;
}
function readEnum(reader) {
  return {
    type: ControlType.Enum,
    value: reader.readString()
  };
}
function writeEnum(writer, value) {
  writer.writeString(value.value);
}
function compareEnum(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readFile(reader) {
  return {
    type: ControlType.File,
    value: reader.readString()
  };
}
function writeFile(writer, value) {
  writer.writeString(value.value);
}
function compareFile(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readLink(reader) {
  return {
    type: ControlType.Link,
    value: reader.readJson()
  };
}
function writeLink(writer, value) {
  writer.writeJson(value.value);
}
function compareLink(left, right) {
  const leftEncoded = JSON.stringify(left.value);
  const rightEncoded = JSON.stringify(right.value);
  if (leftEncoded < rightEncoded) return -1;
  if (leftEncoded > rightEncoded) return 1;
  return 0;
}
function readNumber(reader) {
  return {
    type: ControlType.Number,
    value: reader.readFloat64()
  };
}
function writeNumber(writer, value) {
  writer.writeFloat64(value.value);
}
function compareNumber(left, right) {
  if (left.value < right.value) return -1;
  if (left.value > right.value) return 1;
  return 0;
}
function readObject(reader) {
  const length = reader.readUint16();
  const result = {};
  for (let i = 0; i < length; i++) {
    const key = reader.readString();
    result[key] = DatabaseValueModel.read(reader);
  }
  return {
    type: ControlType.Object,
    value: result
  };
}
function writeObject(writer, value) {
  const entries = Object.entries(value.value);
  writer.writeUint16(entries.length);
  for (const [key, item] of entries) {
    writer.writeString(key);
    DatabaseValueModel.write(writer, item);
  }
}
function compareObject(left, right, collation) {
  const leftKeys = Object.keys(left.value).sort();
  const rightKeys = Object.keys(right.value).sort();
  if (leftKeys.length < rightKeys.length) return -1;
  if (leftKeys.length > rightKeys.length) return 1;
  for (let i = 0; i < leftKeys.length; i++) {
    const leftKey = leftKeys[i];
    const rightKey = rightKeys[i];
    if (leftKey < rightKey) return -1;
    if (leftKey > rightKey) return 1;
    const leftValue = left.value[leftKey] ?? null;
    const rightValue = right.value[rightKey] ?? null;
    const result = DatabaseValueModel.compare(leftValue, rightValue, collation);
    if (result !== 0) return result;
  }
  return 0;
}
function readResponsiveImage(reader) {
  return {
    type: ControlType.ResponsiveImage,
    value: reader.readJson()
  };
}
function writeResponsiveImage(writer, value) {
  writer.writeJson(value.value);
}
function compareResponsiveImage(left, right) {
  const leftEncoded = JSON.stringify(left.value);
  const rightEncoded = JSON.stringify(right.value);
  if (leftEncoded < rightEncoded) return -1;
  if (leftEncoded > rightEncoded) return 1;
  return 0;
}
function readRichText(reader) {
  const type = reader.readInt8();
  if (type === 0) {
    return {
      type: ControlType.RichText,
      value: reader.readUint32()
    };
  }
  if (type === 1) {
    return {
      type: ControlType.RichText,
      value: reader.readString()
    };
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function writeRichText(writer, value) {
  if (isNumber(value.value)) {
    writer.writeInt8(0);
    writer.writeUint32(value.value);
    return;
  }
  if (isString(value.value)) {
    writer.writeInt8(1);
    writer.writeString(value.value);
    return;
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function compareRichText(left, right) {
  const leftValue = left.value;
  const rightValue = right.value;
  if (isNumber(leftValue) && isNumber(rightValue)) {
    if (leftValue < rightValue) return -1;
    if (leftValue > rightValue) return 1;
    return 0;
  }
  if (isString(leftValue) && isString(rightValue)) {
    if (leftValue < rightValue) return -1;
    if (leftValue > rightValue) return 1;
    return 0;
  }
  throw new Error(\`Invalid rich text pointer\`);
}
function readString(reader) {
  return {
    type: ControlType.String,
    value: reader.readString()
  };
}
function writeString(writer, value) {
  writer.writeString(value.value);
}
function compareString(left, right, collation) {
  let leftValue = left.value;
  let rightValue = right.value;
  if (collation.type === 0 /* CaseInsensitive */) {
    leftValue = left.value.toLowerCase();
    rightValue = right.value.toLowerCase();
  }
  if (leftValue < rightValue) return -1;
  if (leftValue > rightValue) return 1;
  return 0;
}
function readVectorSetItem(reader) {
  return {
    type: ControlType.VectorSetItem,
    value: reader.readUint32()
  };
}
function writeVectorSetItem(writer, value) {
  writer.writeUint32(value.value);
}
function compareVectorSetItem(left, right) {
  const leftValue = left.value;
  const rightValue = right.value;
  if (leftValue < rightValue) return -1;
  if (leftValue > rightValue) return 1;
  return 0;
}

// src/code-generation/components/cms/bundled/models/DatabaseDictionaryIndexModel.ts
var DatabaseDictionaryIndexModel = class _DatabaseDictionaryIndexModel {
  constructor(fieldNames, options) {
    __publicField(this, "fieldNames", fieldNames);
    __publicField(this, "options", options);
    __publicField(this, "entries", []);
  }
  sortEntries() {
    this.entries.sort((left, right) => {
      for (let i = 0; i < this.fieldNames.length; i++) {
        const leftValue = left.values[i];
        const rightValue = right.values[i];
        const result = DatabaseValueModel.compare(leftValue, rightValue, this.options.collation);
        if (result !== 0) return result;
      }
      return left.pointer.compare(right.pointer);
    });
  }
  static deserialize(bytes) {
    const reader = new BufferReader(bytes);
    const collation = reader.readJson();
    const fieldCount = reader.readUint8();
    const fieldNames = [];
    for (let i = 0; i < fieldCount; i++) {
      const fieldName = reader.readString();
      fieldNames.push(fieldName);
    }
    const index = new _DatabaseDictionaryIndexModel(fieldNames, {
      collation
    });
    const entryCount = reader.readUint32();
    for (let i = 0; i < entryCount; i++) {
      const values = [];
      for (let j = 0; j < fieldCount; j++) {
        const fieldValue = DatabaseValueModel.read(reader);
        values.push(fieldValue);
      }
      const pointer = DatabaseItemPointerModel.read(reader);
      index.entries.push({ values, pointer });
    }
    return index;
  }
  serialize() {
    const writer = new BufferWriter();
    writer.writeJson(this.options.collation);
    writer.writeUint8(this.fieldNames.length);
    for (const fieldName of this.fieldNames) {
      writer.writeString(fieldName);
    }
    this.sortEntries();
    writer.writeUint32(this.entries.length);
    for (const entry of this.entries) {
      const { values, pointer } = entry;
      for (const value of values) {
        DatabaseValueModel.write(writer, value);
      }
      pointer.write(writer);
    }
    return writer.subarray();
  }
  addItem(item, pointer) {
    const values = this.fieldNames.map((fieldName) => {
      return item.getField(fieldName) ?? null;
    });
    this.entries.push({ values, pointer });
  }
};

// src/code-generation/components/cms/bundled/fetchWithRetries.ts
var maxRetries = 3;
var baseDelay = 250;
var retriableHTTPCodes = [
  408,
  // Request Timeout
  429,
  // Too Many Requests
  500,
  // Internal Server Error
  502,
  // Bad Gateway
  503,
  // Service Unavailable
  504
  // Gateway Timeout
];
var fetchWithRetries = async (input, init) => {
  let retryCount = 0;
  while (true) {
    try {
      const res = await fetch(input, init);
      if (!retriableHTTPCodes.includes(res.status) || ++retryCount > maxRetries) {
        return res;
      }
    } catch (error) {
      if (init?.signal?.aborted || ++retryCount > maxRetries) {
        throw error;
      }
    }
    await retryDelay(retryCount);
  }
};
async function retryDelay(retryCount) {
  const backoffExponent = retryCount - 1;
  const jitter = Math.random() + 1;
  const timeout = Math.floor(baseDelay * jitter * 2 ** backoffExponent);
  await new Promise((resolve) => {
    setTimeout(resolve, timeout);
  });
}

// src/code-generation/components/cms/bundled/rangeRequest.ts
async function rangeRequest(url, ranges) {
  const collapsedRanges = collapseRanges(ranges);
  const rangeStrings = [];
  let expectedLength = 0;
  for (const range of collapsedRanges) {
    rangeStrings.push(\`\${range.from}-\${range.to - 1}\`);
    expectedLength += range.to - range.from;
  }
  const rangeUrl = new URL(url);
  const rangeQuery = rangeStrings.join(",");
  rangeUrl.searchParams.set("range", rangeQuery);
  const response = await fetchWithRetries(rangeUrl);
  if (response.status !== 200) {
    throw new Error(\`Request failed: \${response.status} \${response.statusText}\`);
  }
  const buffer = await response.arrayBuffer();
  const bytes = new Uint8Array(buffer);
  if (bytes.length !== expectedLength) {
    throw new Error("Request failed: Unexpected response length");
  }
  const file = new PartialFile();
  let start = 0;
  for (const range of collapsedRanges) {
    const length = range.to - range.from;
    const end = start + length;
    const part = bytes.subarray(start, end);
    file.write(range.from, part);
    start = end;
  }
  return ranges.map((range) => {
    return file.read(range.from, range.to - range.from);
  });
}
var PartialFile = class {
  constructor() {
    __publicField(this, "chunks", []);
  }
  read(position, length) {
    for (const chunk of this.chunks) {
      if (position < chunk.start) {
        throw new Error("Missing data");
      }
      if (position > chunk.end) {
        continue;
      }
      if (position + length > chunk.end) {
        throw new Error("Missing data");
      }
      const from = position - chunk.start;
      const to = from + length;
      return chunk.data.slice(from, to);
    }
    throw new Error("Missing data");
  }
  write(position, data) {
    let start = position;
    let end = start + data.length;
    let fromIndex = 0;
    let toIndex = this.chunks.length;
    for (; fromIndex < toIndex; fromIndex++) {
      const chunk2 = this.chunks[fromIndex];
      assert(chunk2, "Missing chunk");
      if (start > chunk2.end) {
        continue;
      }
      if (start > chunk2.start) {
        const offset = start - chunk2.start;
        const prefix = chunk2.data.subarray(0, offset);
        data = join(prefix, data);
        start = chunk2.start;
      }
      break;
    }
    for (; toIndex > fromIndex; toIndex--) {
      const chunk2 = this.chunks[toIndex - 1];
      assert(chunk2, "Missing chunk");
      if (end < chunk2.start) {
        continue;
      }
      if (end < chunk2.end) {
        const offset = end - chunk2.start;
        const suffix = chunk2.data.subarray(offset);
        data = join(data, suffix);
        end = chunk2.end;
      }
      break;
    }
    const chunk = {
      start,
      end,
      data
    };
    const deleteCount = toIndex - fromIndex;
    this.chunks.splice(fromIndex, deleteCount, chunk);
  }
};
function join(left, right) {
  const length = left.length + right.length;
  const result = new Uint8Array(length);
  result.set(left, 0);
  result.set(right, left.length);
  return result;
}
function collapseRanges(ranges) {
  assert(ranges.length > 0, "Must have at least one range");
  const sorted = [...ranges].sort((a, b) => a.from - b.from);
  const collapsed = [];
  for (const range of sorted) {
    const index = collapsed.length - 1;
    const previous = collapsed[index];
    if (previous && range.from <= previous.to) {
      collapsed[index] = {
        from: previous.from,
        to: Math.max(previous.to, range.to)
      };
    } else {
      collapsed.push(range);
    }
  }
  return collapsed;
}

// src/code-generation/components/cms/bundled/DatabaseDictionaryIndex.ts
var DatabaseDictionaryIndex = class {
  constructor(options) {
    __publicField(this, "options", options);
    __publicField(this, "schema");
    __publicField(this, "fields");
    __publicField(this, "supportedLookupTypes", [
      "All" /* All */,
      "Equals" /* Equals */,
      "NotEquals" /* NotEquals */,
      "LessThan" /* LessThan */,
      "GreaterThan" /* GreaterThan */,
      "Contains" /* Contains */,
      "StartsWith" /* StartsWith */,
      "EndsWith" /* EndsWith */
    ]);
    __publicField(this, "modelPromise");
    __publicField(this, "model");
    __publicField(this, "collation");
    const schema = {};
    const fields = [];
    for (const fieldName of this.options.fieldNames) {
      const definition = this.options.collectionSchema[fieldName];
      assert(definition, "Missing definition for field", fieldName);
      schema[fieldName] = definition;
      fields.push({ type: "Identifier", name: fieldName });
    }
    this.schema = schema;
    this.fields = fields;
    this.collation = this.options.collation;
  }
  async loadModel() {
    const [bytes] = await rangeRequest(this.options.url, [this.options.range]);
    assert(bytes, "Failed to load model");
    return DatabaseDictionaryIndexModel.deserialize(bytes);
  }
  async getModel() {
    this.modelPromise ??= this.loadModel();
    this.model ??= await this.modelPromise;
    return this.model;
  }
  async lookupItems(queries) {
    assert(queries.length === this.fields.length, "Invalid query length");
    const model = await this.getModel();
    const chunks = queries.reduce(
      (memo, query, position) => memo.flatMap((entries) => {
        switch (query.type) {
          case "All" /* All */:
            return [entries];
          case "Equals" /* Equals */:
            return this.queryEquals(entries, query, position);
          case "NotEquals" /* NotEquals */:
            return this.queryNotEquals(entries, query, position);
          case "LessThan" /* LessThan */:
            return this.queryLessThan(entries, query, position);
          case "GreaterThan" /* GreaterThan */:
            return this.queryGreaterThan(entries, query, position);
          case "Contains" /* Contains */:
            return this.queryContains(entries, query, position);
          case "StartsWith" /* StartsWith */:
            return this.queryStartsWith(entries, query, position);
          case "EndsWith" /* EndsWith */:
            return this.queryEndsWith(entries, query, position);
          default:
            assertNever(query);
        }
      }),
      [model.entries]
    );
    const result = [];
    for (const entries of chunks) {
      for (const entry of entries) {
        const data = {};
        for (let i = 0; i < this.options.fieldNames.length; i++) {
          const fieldName = this.options.fieldNames[i];
          const fieldValue = entry.values[i];
          data[fieldName] = fieldValue;
        }
        result.push({ pointer: entry.pointer.toString(), data });
      }
    }
    return result;
  }
  queryEquals(entries, query, position) {
    const leftMost = this.getLeftMost(entries, position, query.value);
    const rightMost = this.getRightMost(entries, position, query.value);
    const slice = entries.slice(leftMost, rightMost + 1);
    return slice.length > 0 ? [slice] : [];
  }
  queryNotEquals(entries, query, position) {
    const leftMost = this.getLeftMost(entries, position, query.value);
    const rightMost = this.getRightMost(entries, position, query.value);
    const result = [];
    const before = entries.slice(0, leftMost);
    if (before.length > 0) result.push(before);
    const after = entries.slice(rightMost + 1);
    if (after.length > 0) result.push(after);
    return result;
  }
  queryLessThan(entries, query, position) {
    const nullBound = this.getRightMost(entries, position, null);
    entries = entries.slice(nullBound + 1);
    if (query.inclusive) {
      const rightMost = this.getRightMost(entries, position, query.value);
      const slice2 = entries.slice(0, rightMost + 1);
      return slice2.length > 0 ? [slice2] : [];
    }
    const leftMost = this.getLeftMost(entries, position, query.value);
    const slice = entries.slice(0, leftMost);
    return slice.length > 0 ? [slice] : [];
  }
  queryGreaterThan(entries, query, position) {
    const nullBound = this.getRightMost(entries, position, null);
    entries = entries.slice(nullBound + 1);
    if (query.inclusive) {
      const leftMost = this.getLeftMost(entries, position, query.value);
      const slice2 = entries.slice(leftMost);
      return slice2.length > 0 ? [slice2] : [];
    }
    const rightMost = this.getRightMost(entries, position, query.value);
    const slice = entries.slice(rightMost + 1);
    return slice.length > 0 ? [slice] : [];
  }
  queryContains(entries, query, position) {
    return this.findItems(entries, position, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.includes(target);
    });
  }
  queryStartsWith(entries, query, position) {
    return this.findItems(entries, position, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.startsWith(target);
    });
  }
  queryEndsWith(entries, query, position) {
    return this.findItems(entries, position, (current) => {
      if (current?.type !== ControlType2.String) return false;
      if (query.value?.type !== ControlType2.String) return false;
      let source = current.value;
      let target = query.value.value;
      if (this.collation.type === 0 /* CaseInsensitive */) {
        source = source.toLowerCase();
        target = target.toLowerCase();
      }
      return source.endsWith(target);
    });
  }
  /**
   * Returns the index of the left most entry that is equal to the target.
   *
   * \`\`\`text
   *   Left most
   *       \u2193
   * \u250C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2510
   * \u2502 1 \u2502 2 \u2502 2 \u2502 2 \u2502 2 \u2502 3 \u2502
   * \u2514\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2518
   * \`\`\`
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param target The target value to search for.
   * @returns The index of the left most entry that is equal to the target.
   */
  getLeftMost(entries, position, target) {
    let left = 0;
    let right = entries.length;
    while (left < right) {
      const middle = left + right >> 1;
      const entry = entries[middle];
      const value = entry.values[position];
      if (DatabaseValueModel.compare(value, target, this.collation) < 0) {
        left = middle + 1;
      } else {
        right = middle;
      }
    }
    return left;
  }
  /**
   * Returns the index of the right most entry that is equal to the target.
   *
   * \`\`\`text
   *              Right most
   *                   \u2193
   * \u250C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u252C\u2500\u2500\u2500\u2510
   * \u2502 1 \u2502 2 \u2502 2 \u2502 2 \u2502 2 \u2502 3 \u2502
   * \u2514\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2518
   * \`\`\`
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param target The target value to search for.
   * @returns The index of the right most entry that is equal to the target.
   */
  getRightMost(entries, position, target) {
    let left = 0;
    let right = entries.length;
    while (left < right) {
      const middle = left + right >> 1;
      const entry = entries[middle];
      const value = entry.values[position];
      if (DatabaseValueModel.compare(value, target, this.collation) > 0) {
        right = middle;
      } else {
        left = middle + 1;
      }
    }
    return right - 1;
  }
  /**
   * Finds all items that are matching the predicate and groups adjacent items together.
   *
   * @param entries The entries array to search in.
   * @param position The position of the value in the entry.
   * @param predicate The predicate to match the values against.
   * @returns An array of chunks that match the predicate.
   */
  findItems(entries, position, predicate) {
    const result = [];
    let start = 0;
    for (let i = 0; i < entries.length; i++) {
      const entry = entries[i];
      const value = entry.values[position];
      const isMatching = predicate(value);
      if (isMatching) continue;
      if (start < i) {
        const slice = entries.slice(start, i);
        result.push(slice);
      }
      start = i + 1;
    }
    if (start < entries.length) {
      const slice = entries.slice(start);
      result.push(slice);
    }
    return result;
  }
};

// src/code-generation/components/cms/bundled/models/DatabaseItemModel.ts
var DatabaseItemModel = class _DatabaseItemModel {
  constructor() {
    __publicField(this, "fields", /* @__PURE__ */ new Map());
  }
  static read(reader) {
    const item = new _DatabaseItemModel();
    const fieldCount = reader.readUint16();
    for (let i = 0; i < fieldCount; i++) {
      const key = reader.readString();
      const value = DatabaseValueModel.read(reader);
      item.setField(key, value);
    }
    return item;
  }
  write(writer) {
    writer.writeUint16(this.fields.size);
    for (const [key, value] of this.fields) {
      writer.writeString(key);
      DatabaseValueModel.write(writer, value);
    }
  }
  getData() {
    const data = {};
    for (const [key, value] of this.fields) {
      data[key] = value;
    }
    return data;
  }
  setField(key, value) {
    this.fields.set(key, value);
  }
  getField(key) {
    return this.fields.get(key);
  }
};

// src/code-generation/components/cms/bundled/DatabaseCollection.ts?bundle
var DatabaseChunk = class {
  constructor(id, url) {
    __publicField(this, "id", id);
    __publicField(this, "url", url);
    __publicField(this, "itemsPromise");
    __publicField(this, "itemLoader", new import_dataloader.default(
      async (pointers) => {
        const ranges = pointers.map((pointer) => {
          const pointerModel = DatabaseItemPointerModel.fromString(pointer);
          return { from: pointerModel.offset, to: pointerModel.offset + pointerModel.length };
        });
        const rangeBytes = await rangeRequest(this.url, ranges);
        return rangeBytes.map((bytes, index) => {
          const reader = new BufferReader(bytes);
          const itemModel = DatabaseItemModel.read(reader);
          const pointer = pointers[index];
          assert(pointer, "Missing pointer");
          return {
            pointer,
            data: itemModel.getData()
          };
        });
      },
      { maxBatchSize: 250 }
    ));
  }
  scanItems() {
    this.itemsPromise ??= fetchWithRetries(this.url).then(async (response) => {
      if (!response.ok) {
        throw new Error(\`Request failed: \${response.status} \${response.statusText}\`);
      }
      const buffer = await response.arrayBuffer();
      const bytes = new Uint8Array(buffer);
      const reader = new BufferReader(bytes);
      const items = [];
      const itemCount = reader.readUint32();
      for (let i = 0; i < itemCount; i++) {
        const offset = reader.getOffset();
        const itemModel = DatabaseItemModel.read(reader);
        const length = reader.getOffset() - offset;
        const pointerModel = new DatabaseItemPointerModel(this.id, offset, length);
        const pointer = pointerModel.toString();
        const item = {
          pointer,
          data: itemModel.getData()
        };
        this.itemLoader.prime(pointer, item);
        items.push(item);
      }
      return items;
    });
    return this.itemsPromise;
  }
  resolveItem(pointer) {
    return this.itemLoader.load(pointer);
  }
};
var DatabaseCollection = class {
  constructor(options) {
    __publicField(this, "options", options);
    __publicField(this, "id");
    __publicField(this, "schema");
    __publicField(this, "indexes");
    __publicField(this, "resolveRichText");
    __publicField(this, "resolveVectorSetItem");
    __publicField(this, "chunks");
    this.chunks = this.options.chunks.map((url, id) => {
      return new DatabaseChunk(id, url);
    });
    this.schema = options.schema;
    this.indexes = options.indexes;
    this.resolveRichText = options.resolveRichText;
    this.resolveVectorSetItem = options.resolveVectorSetItem;
    this.id = options.id;
  }
  async scanItems() {
    const chunkedItems = await Promise.all(
      this.chunks.map(async (chunk) => {
        return chunk.scanItems();
      })
    );
    return chunkedItems.flat();
  }
  resolveItems(pointers) {
    return Promise.all(
      pointers.map((pointer) => {
        const pointerModel = DatabaseItemPointerModel.fromString(pointer);
        const chunk = this.chunks[pointerModel.chunkId];
        assert(chunk, "Missing chunk");
        return chunk.resolveItem(pointer);
      })
    );
  }
  compareItems(left, right) {
    const leftModel = DatabaseItemPointerModel.fromString(left.pointer);
    const rightModel = DatabaseItemPointerModel.fromString(right.pointer);
    return leftModel.compare(rightModel);
  }
  compareValues(left, right, collation) {
    return DatabaseValueModel.compare(left, right, collation);
  }
};
export {
  DatabaseCollection,
  DatabaseDictionaryIndex
};
`;
var nu = `var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

// src/code-generation/components/cms/bundled/DraftDatabaseCollection.ts?bundle
import { ControlType } from "framer";

// src/utils/typeChecks.ts
function isString(value) {
  return typeof value === "string";
}
function isNumber(value) {
  return Number.isFinite(value);
}

// src/code-generation/components/cms/bundled/assert.ts
function assert(condition, ...msg) {
  if (condition) return;
  throw Error("Assertion Error" + (msg.length > 0 ? ": " + msg.join(" ") : ""));
}

// src/code-generation/components/cms/bundled/DraftDatabaseCollection.ts?bundle
var DraftDatabaseCollection = class {
  constructor(draftCollection, originalCollection, previousItemId, nextItemId) {
    __publicField(this, "draftCollection", draftCollection);
    __publicField(this, "originalCollection", originalCollection);
    __publicField(this, "previousItemId", previousItemId);
    __publicField(this, "nextItemId", nextItemId);
    __publicField(this, "schema");
    // We cannot use the original indexes because the draft items
    // are not in the original collection.
    // Having no indexes means that we perform a full collection scan for every query.
    __publicField(this, "indexes", []);
    __publicField(this, "itemsPromise");
    this.schema = originalCollection.schema;
    this.itemsPromise = this.getItems();
  }
  async getItems() {
    const [[...draftItems], [...originalItems]] = await Promise.all([
      this.draftCollection.scanItems(),
      this.originalCollection.scanItems()
    ]);
    const lastDraftItem = draftItems.pop();
    const firstOriginalItem = originalItems.shift();
    if (lastDraftItem) {
      draftItems.push({
        ...lastDraftItem,
        data: {
          ...lastDraftItem.data,
          [this.nextItemId]: firstOriginalItem?.data.id ?? null
        }
      });
    }
    if (firstOriginalItem) {
      originalItems.unshift({
        ...firstOriginalItem,
        data: {
          ...firstOriginalItem.data,
          [this.previousItemId]: lastDraftItem?.data.id ?? null
        }
      });
    }
    return draftItems.map(draftItem).concat(...originalItems);
  }
  /**
   * Get all items, updating pointers for draft items.
   */
  scanItems() {
    return this.itemsPromise;
  }
  /**
   * Depending on the pointer, resolve draft rich text from the draft
   * collection, and original rich text from the original collection.
   */
  resolveRichText(pointer) {
    if (isDraftRichTextPointer(pointer)) {
      const originalPointer = undraftRichTextPointer(pointer);
      return this.draftCollection.resolveRichText(originalPointer);
    }
    return this.originalCollection.resolveRichText(pointer);
  }
  resolveVectorSetItem(pointer) {
    if (isDraftVectorSetItemPointer(pointer)) {
      const originalPointer = undraftVectorSetItemPointer(pointer);
      return this.draftCollection.resolveVectorSetItem?.(originalPointer);
    }
    return this.originalCollection.resolveVectorSetItem?.(pointer);
  }
  async resolveItems(pointers) {
    const items = await this.itemsPromise;
    return pointers.map((pointer) => {
      const item = items.find((current) => current.pointer === pointer);
      assert(item, "Item must exist");
      return item;
    });
  }
  /**
   * Compare two items, the draft items should come first.
   */
  compareItems(left, right) {
    const leftIsDraft = isDraftPointer(left.pointer);
    const rightIsDraft = isDraftPointer(right.pointer);
    if (leftIsDraft && rightIsDraft) {
      const originalLeft = undraftItem(left);
      const originalRight = undraftItem(right);
      return this.draftCollection.compareItems(originalLeft, originalRight);
    }
    if (leftIsDraft) return -1;
    if (rightIsDraft) return 1;
    return this.originalCollection.compareItems(left, right);
  }
};
var draftPrefix = "draft:";
function isDraftPointer(pointer) {
  return pointer.startsWith(draftPrefix);
}
function draftPointer(pointer) {
  assert(!isDraftPointer(pointer), "Should be a non-draft pointer");
  return draftPrefix + pointer;
}
function undraftPointer(pointer) {
  assert(isDraftPointer(pointer), "Should be a draft pointer");
  return pointer.slice(draftPrefix.length);
}
function isDraftRichTextPointer(pointer) {
  if (isNumber(pointer)) {
    return pointer < 0;
  }
  if (isString(pointer)) {
    return pointer.startsWith(draftPrefix);
  }
  throw new Error("Invalid rich text pointer");
}
function draftRichTextPointer(pointer) {
  assert(!isDraftRichTextPointer(pointer), "Should be a non-draft rich text pointer");
  if (isNumber(pointer)) {
    return -pointer - 1;
  }
  if (isString(pointer)) {
    return draftPrefix + pointer;
  }
  throw new Error("Invalid rich text pointer");
}
function undraftRichTextPointer(pointer) {
  assert(isDraftRichTextPointer(pointer), "Should be a draft rich text pointer");
  if (isNumber(pointer)) {
    return -pointer - 1;
  }
  if (isString(pointer)) {
    return pointer.slice(draftPrefix.length);
  }
  throw new Error("Invalid rich text pointer");
}
function isDraftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  return pointer < 0;
}
function draftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  assert(!isDraftVectorSetItemPointer(pointer), "Should be a non-draft vector set item pointer");
  return -pointer - 1;
}
function undraftVectorSetItemPointer(pointer) {
  assert(isNumber(pointer), "Invalid vector set item pointer");
  assert(isDraftVectorSetItemPointer(pointer), "Should be a draft vector set item pointer");
  return -pointer - 1;
}
function draftItem(item) {
  const data = {};
  for (const [key, value] of Object.entries(item.data)) {
    if (value?.type === ControlType.RichText) {
      data[key] = {
        type: ControlType.RichText,
        value: draftRichTextPointer(value.value)
      };
    } else if (value?.type === ControlType.VectorSetItem) {
      data[key] = {
        type: ControlType.VectorSetItem,
        value: draftVectorSetItemPointer(value.value)
      };
    } else {
      data[key] = value;
    }
  }
  return {
    pointer: draftPointer(item.pointer),
    data
  };
}
function undraftItem(item) {
  const data = {};
  for (const [key, value] of Object.entries(item.data)) {
    if (value?.type === ControlType.RichText) {
      data[key] = {
        type: ControlType.RichText,
        value: undraftRichTextPointer(value.value)
      };
    } else if (value?.type === ControlType.VectorSetItem) {
      data[key] = {
        type: ControlType.VectorSetItem,
        value: undraftVectorSetItemPointer(value.value)
      };
    } else {
      data[key] = value;
    }
  }
  return {
    pointer: undraftPointer(item.pointer),
    data
  };
}
export {
  DraftDatabaseCollection
};
`;
var Nn = class n {
  constructor(e, t, r) {
    C(this, "chunkId", e);
    C(this, "offset", t);
    C(this, "length", r);
  }
  static fromString(e) {
    let [t, r, o] = e.split("/").map(Number);
    return (
      X(U(t), "Invalid chunkId"),
      X(U(r), "Invalid offset"),
      X(U(o), "Invalid length"),
      new n(t, r, o)
    );
  }
  toString() {
    return `${this.chunkId}/${this.offset}/${this.length}`;
  }
  static read(e) {
    let t = e.readUint16(),
      r = e.readUint32(),
      o = e.readUint32();
    return new n(t, r, o);
  }
  write(e) {
    (e.writeUint16(this.chunkId), e.writeUint32(this.offset), e.writeUint32(this.length));
  }
  compare(e) {
    return this.chunkId < e.chunkId
      ? -1
      : this.chunkId > e.chunkId
        ? 1
        : this.offset < e.offset
          ? -1
          : this.offset > e.offset
            ? 1
            : (X(this.length === e.length), 0);
  }
};
function la(n) {
  if (it(n)) return 0;
  switch (n.type) {
    case "array":
      return 1;
    case "boolean":
      return 2;
    case "color":
      return 3;
    case "date":
      return 4;
    case "enum":
      return 5;
    case "file":
      return 6;
    case "responsiveimage":
      return 10;
    case "link":
      return 7;
    case "number":
      return 8;
    case "object":
      return 9;
    case "richtext":
      return 11;
    case "string":
      return 12;
    case "vectorsetitem":
      return 13;
    default:
      rr(n);
  }
}
var xe;
((r) => {
  function n(o) {
    let i = o.readUint8();
    switch (i) {
      case 0:
        return null;
      case 1:
        return Ep(o);
      case 2:
        return zp(o);
      case 3:
        return Bp(o);
      case 4:
        return $p(o);
      case 5:
        return jp(o);
      case 6:
        return Jp(o);
      case 7:
        return Qp(o);
      case 8:
        return Zp(o);
      case 9:
        return nm(o);
      case 10:
        return im(o);
      case 11:
        return lm(o);
      case 12:
        return um(o);
      case 13:
        return mm(o);
      default:
        rr(i);
    }
  }
  r.read = n;
  function e(o, i) {
    let a = la(i);
    if ((o.writeUint8(a), !it(i)))
      switch (i.type) {
        case "array":
          return Mp(o, i);
        case "boolean":
          return kp(o, i);
        case "color":
          return Ap(o, i);
        case "date":
          return Up(o, i);
        case "enum":
          return Gp(o, i);
        case "file":
          return qp(o, i);
        case "link":
          return Yp(o, i);
        case "number":
          return em(o, i);
        case "object":
          return rm(o, i);
        case "responsiveimage":
          return am(o, i);
        case "richtext":
          return dm(o, i);
        case "vectorsetitem":
          return hm(o, i);
        case "string":
          return fm(o, i);
        default:
          rr(i);
      }
  }
  r.write = e;
  function t(o, i, a) {
    let s = la(o),
      l = la(i);
    if (s < l) return -1;
    if (s > l) return 1;
    if (it(o) || it(i)) return 0;
    switch (o.type) {
      case "array":
        return (X(i.type === "array"), Fp(o, i, a));
      case "boolean":
        return (X(i.type === "boolean"), Op(o, i));
      case "color":
        return (X(i.type === "color"), _p(o, i));
      case "date":
        return (X(i.type === "date"), Wp(o, i));
      case "enum":
        return (X(i.type === "enum"), Hp(o, i));
      case "file":
        return (X(i.type === "file"), Kp(o, i));
      case "link":
        return (X(i.type === "link"), Xp(o, i));
      case "number":
        return (X(i.type === "number"), tm(o, i));
      case "object":
        return (X(i.type === "object"), om(o, i, a));
      case "responsiveimage":
        return (X(i.type === "responsiveimage"), sm(o, i));
      case "richtext":
        return (X(i.type === "richtext"), cm(o, i));
      case "vectorsetitem":
        return (X(i.type === "vectorsetitem"), gm(o, i));
      case "string":
        return (X(i.type === "string"), pm(o, i, a));
      default:
        rr(o);
    }
  }
  r.compare = t;
})((xe ||= {}));
function Ep(n) {
  let e = n.readUint16(),
    t = [];
  for (let r = 0; r < e; r++) {
    let o = xe.read(n);
    t.push(o);
  }
  return { type: "array", value: t };
}
function Mp(n, e) {
  n.writeUint16(e.value.length);
  for (let t of e.value) xe.write(n, t);
}
function Fp(n, e, t) {
  let r = n.value.length,
    o = e.value.length;
  if (r < o) return -1;
  if (r > o) return 1;
  for (let i = 0; i < r; i++) {
    let a = n.value[i],
      s = e.value[i],
      l = xe.compare(a, s, t);
    if (l !== 0) return l;
  }
  return 0;
}
function zp(n) {
  return { type: "boolean", value: n.readUint8() !== 0 };
}
function kp(n, e) {
  n.writeUint8(e.value ? 1 : 0);
}
function Op(n, e) {
  return n.value < e.value ? -1 : n.value > e.value ? 1 : 0;
}
function Bp(n) {
  return { type: "color", value: n.readString() };
}
function Ap(n, e) {
  n.writeString(e.value);
}
function _p(n, e) {
  return n.value < e.value ? -1 : n.value > e.value ? 1 : 0;
}
function $p(n) {
  let e = n.readInt64(),
    t = new Date(e);
  return { type: "date", value: t.toISOString() };
}
function Up(n, e) {
  let r = new Date(e.value).getTime();
  n.writeInt64(r);
}
function Wp(n, e) {
  let t = new Date(n.value),
    r = new Date(e.value);
  return t < r ? -1 : t > r ? 1 : 0;
}
function jp(n) {
  return { type: "enum", value: n.readString() };
}
function Gp(n, e) {
  n.writeString(e.value);
}
function Hp(n, e) {
  return n.value < e.value ? -1 : n.value > e.value ? 1 : 0;
}
function Jp(n) {
  return { type: "file", value: n.readString() };
}
function qp(n, e) {
  n.writeString(e.value);
}
function Kp(n, e) {
  return n.value < e.value ? -1 : n.value > e.value ? 1 : 0;
}
function Qp(n) {
  return { type: "link", value: n.readJson() };
}
function Yp(n, e) {
  n.writeJson(e.value);
}
function Xp(n, e) {
  let t = JSON.stringify(n.value),
    r = JSON.stringify(e.value);
  return t < r ? -1 : t > r ? 1 : 0;
}
function Zp(n) {
  return { type: "number", value: n.readFloat64() };
}
function em(n, e) {
  n.writeFloat64(e.value);
}
function tm(n, e) {
  return n.value < e.value ? -1 : n.value > e.value ? 1 : 0;
}
function nm(n) {
  let e = n.readUint16(),
    t = {};
  for (let r = 0; r < e; r++) {
    let o = n.readString();
    t[o] = xe.read(n);
  }
  return { type: "object", value: t };
}
function rm(n, e) {
  let t = Object.entries(e.value);
  n.writeUint16(t.length);
  for (let [r, o] of t) (n.writeString(r), xe.write(n, o));
}
function om(n, e, t) {
  let r = Object.keys(n.value).sort(),
    o = Object.keys(e.value).sort();
  if (r.length < o.length) return -1;
  if (r.length > o.length) return 1;
  for (let i = 0; i < r.length; i++) {
    let a = r[i],
      s = o[i];
    if (a < s) return -1;
    if (a > s) return 1;
    let l = n.value[a] ?? null,
      d = e.value[s] ?? null,
      f = xe.compare(l, d, t);
    if (f !== 0) return f;
  }
  return 0;
}
function im(n) {
  return { type: "responsiveimage", value: n.readJson() };
}
function am(n, e) {
  n.writeJson(e.value);
}
function sm(n, e) {
  let t = JSON.stringify(n.value),
    r = JSON.stringify(e.value);
  return t < r ? -1 : t > r ? 1 : 0;
}
function lm(n) {
  let e = n.readInt8();
  if (e === 0) return { type: "richtext", value: n.readUint32() };
  if (e === 1) return { type: "richtext", value: n.readString() };
  throw new Error("Invalid rich text pointer");
}
function dm(n, e) {
  if (U(e.value)) {
    (n.writeInt8(0), n.writeUint32(e.value));
    return;
  }
  if (L(e.value)) {
    (n.writeInt8(1), n.writeString(e.value));
    return;
  }
  throw new Error("Invalid rich text pointer");
}
function cm(n, e) {
  let t = n.value,
    r = e.value;
  if ((U(t) && U(r)) || (L(t) && L(r))) return t < r ? -1 : t > r ? 1 : 0;
  throw new Error("Invalid rich text pointer");
}
function um(n) {
  return { type: "string", value: n.readString() };
}
function fm(n, e) {
  n.writeString(e.value);
}
function pm(n, e, t) {
  let r = n.value,
    o = e.value;
  return (
    t.type === 0 && ((r = n.value.toLowerCase()), (o = e.value.toLowerCase())),
    r < o ? -1 : r > o ? 1 : 0
  );
}
function mm(n) {
  return { type: "vectorsetitem", value: n.readUint32() };
}
function hm(n, e) {
  n.writeUint32(e.value);
}
function gm(n, e) {
  let t = n.value,
    r = e.value;
  return t < r ? -1 : t > r ? 1 : 0;
}
var tt = class n {
  constructor(e, t) {
    C(this, "fieldNames", e);
    C(this, "options", t);
    C(this, "entries", []);
  }
  sortEntries() {
    this.entries.sort((e, t) => {
      for (let r = 0; r < this.fieldNames.length; r++) {
        let o = e.values[r],
          i = t.values[r],
          a = xe.compare(o, i, this.options.collation);
        if (a !== 0) return a;
      }
      return e.pointer.compare(t.pointer);
    });
  }
  static deserialize(e) {
    let t = new Bo(e),
      r = t.readJson(),
      o = t.readUint8(),
      i = [];
    for (let l = 0; l < o; l++) {
      let d = t.readString();
      i.push(d);
    }
    let a = new n(i, { collation: r }),
      s = t.readUint32();
    for (let l = 0; l < s; l++) {
      let d = [];
      for (let c = 0; c < o; c++) {
        let u = xe.read(t);
        d.push(u);
      }
      let f = Nn.read(t);
      a.entries.push({ values: d, pointer: f });
    }
    return a;
  }
  serialize() {
    let e = new Ht();
    (e.writeJson(this.options.collation), e.writeUint8(this.fieldNames.length));
    for (let t of this.fieldNames) e.writeString(t);
    (this.sortEntries(), e.writeUint32(this.entries.length));
    for (let t of this.entries) {
      let { values: r, pointer: o } = t;
      for (let i of r) xe.write(e, i);
      o.write(e);
    }
    return e.subarray();
  }
  addItem(e, t) {
    let r = this.fieldNames.map((o) => e.getField(o) ?? null);
    this.entries.push({ values: r, pointer: t });
  }
};
var Ao = class n {
  constructor() {
    C(this, "fields", new Map());
  }
  static read(e) {
    let t = new n(),
      r = e.readUint16();
    for (let o = 0; o < r; o++) {
      let i = e.readString(),
        a = xe.read(e);
      t.setField(i, a);
    }
    return t;
  }
  write(e) {
    e.writeUint16(this.fields.size);
    for (let [t, r] of this.fields) (e.writeString(t), xe.write(e, r));
  }
  getData() {
    let e = {};
    for (let [t, r] of this.fields) e[t] = r;
    return e;
  }
  setField(e, t) {
    this.fields.set(e, t);
  }
  getField(e) {
    return this.fields.get(e);
  }
};
function da(n) {
  return p`${n}.replace("/modules/", "/cms/")`;
}
var ym = me("serializeDatabase"),
  ca = 4 * 1024 * 1024,
  Cm = 5e3;
async function ru(n, e, t, r, o, i, a, s, l, d, f, c, u) {
  let m = new ko(n, e, t, a, o, d, f, s, l),
    h = n.getPropertyControls(t, e),
    g = Object.entries(h),
    v = $n(t, "includeDrafts"),
    S = {
      id: { type: Ye("string", a), isNullable: !1 },
      [pi]: { type: Ye("date", a), isNullable: !0 },
      [mi]: { type: Ye("date", a), isNullable: !0 },
      [Un]: { type: Ye("string", a), isNullable: !0 },
      [Wn]: { type: Ye("string", a), isNullable: !0 },
    };
  function b(re) {
    return Ye(re, a);
  }
  for (let [re, Te] of g) {
    let Be = dc(Te, b);
    Be && (S[re] = Be);
  }
  let w = f.dedupe("schema", new B(S, 0)),
    P = ac(n.variables),
    V = n.getSlugVariable(),
    x = o.create(tu),
    R,
    F,
    J = u ? !0 : void 0,
    O = Ue.isOn("cmsDatabase")
      ? []
      : n.getSortedChildren("public-only", "group-drafts").filter((re) => re.isDraft === J),
    $ = crypto.randomUUID();
  if (u) {
    let re = ye("collection", n.id);
    if (
      ((R = p`${a.addImport(`../${re}`, { exportSpecifier: "default", importBinding: "originalCollection" })}.collectionByLocaleId`),
      O.length === 0)
    ) {
      let Be = { id: $, displayName: n.resolveValue("name") ?? "Unknown", collectionByLocaleId: R };
      return p`${Be}`;
    }
    let Te = o.create(nu);
    F = a.addImport(Te.submoduleImport, { exportSpecifier: "DraftDatabaseCollection" });
  }
  let j = a.addImport(x.submoduleImport, { exportSpecifier: "DatabaseCollection" }),
    G = {};
  for (let re of v) {
    let Qt = function () {
        let z = new Ht();
        return (z.writeUint32(0), z);
      },
      Mn = function (z) {
        let k = Ae.subarray(0, z);
        (In(k).setUint32(0, En), Yt.push(k));
      };
    var De = Qt,
      be = Mn;
    let Te = [
      new tt(["id"], { collation: { type: 1 } }),
      new tt([Un], { collation: { type: 1 } }),
      new tt([Wn], { collation: { type: 1 } }),
    ];
    V &&
      Te.push(
        new tt(["id", V.id], { collation: { type: 1 } }),
        new tt([V.id, "id"], { collation: { type: 1 } })
      );
    for (let [z] of g) Te.push(new tt([z], { collation: { type: 0 } }));
    let Be = new Oo(),
      Kt = O.filter((z) => {
        let k = vn(t, z);
        return k ? k.includes(re.id) : !0;
      }),
      mr = [];
    for (let z = 0; z < Kt.length; z++) {
      let k = Kt[z];
      y(k, "Node must exist");
      let ae = new Ao();
      ae.setField("id", { type: "string", value: k.id });
      let rt = k.createdAt;
      rt && ae.setField(pi, { type: "date", value: new Date(rt).toISOString() });
      let Xt = k.updatedAt;
      Xt && ae.setField(mi, { type: "date", value: new Date(Xt).toISOString() });
      let Cr = Kt[z - 1];
      Cr && ae.setField(Un, { type: "string", value: Cr.id });
      let vr = Kt[z + 1];
      vr && ae.setField(Wn, { type: "string", value: vr.id });
      let Vt = {
        resolveColor(pe) {
          return c.addColor(pe);
        },
        resolveFile(pe) {
          return s.resolve(pe)?.src;
        },
        resolveImage(pe, Pe) {
          return s.resolveResponsiveImage(pe, { focalPoint: Pe });
        },
        resolveLink(pe) {
          let Pe = l.create(pe);
          if (!q(Pe) && !Cs(Pe)) return Pe;
        },
        resolveRichTextPointer(pe) {
          return m.add(pe);
        },
        resolveVectorSetItemPointer(pe) {
          return Be.add(pe);
        },
      };
      for (let [pe, Pe] of g) {
        let Sr = sc(pe, Pe, P, h, k);
        if (q(Sr)) continue;
        let za = lc({ control: Pe, controlProp: Sr, resolvers: Vt, locale: re });
        q(za) || ae.setField(pe, za);
      }
      mr.push(ae);
    }
    let Yt = [],
      En = 0,
      Ae = Qt();
    for (let z of mr) {
      let k = Ae.getOffset();
      (z.write(Ae),
        Ae.getOffset() > ca && (Mn(k), (Ae = Qt()), (k = Ae.getOffset()), (En = 0), z.write(Ae)),
        En++);
      let ae = Ae.getOffset() - k,
        rt = new Nn(Yt.length, k, ae);
      for (let Xt of Te) Xt.addItem(z, rt);
    }
    Ae.getOffset() > 0 && Mn();
    let hr = a.addImport(x.submoduleImport, { exportSpecifier: "DatabaseDictionaryIndex" }),
      ei = Yt.map((z, k) => da(i.create(`chunk-${re.id}-${k}`, ta, z)));
    Yt.reduce((z, k) => z + k.length, 0) < Cm && (Te.length = 0);
    let Pt = [],
      Fn = 1 / 0;
    for (let z of Te) {
      let k = z.serialize();
      if (k.length >= ca) {
        ym.warn("Skipping index:", k.length);
        continue;
      }
      Fn + k.length > ca && (Pt.push([]), (Fn = 0));
      let ae = Pt.at(-1);
      (y(ae, "Chunk must exist"), ae.push({ model: z, bytes: k }), (Fn += k.length));
    }
    let gr = [];
    for (let z = 0; z < Pt.length; z++) {
      let k = Pt[z];
      y(k, "Chunk must exist");
      let ae = new Ht(),
        rt = [];
      for (let { bytes: Vt } of k) {
        let pe = ae.getOffset();
        ae.writeBytes(Vt);
        let Pe = ae.getOffset();
        rt.push({ from: pe, to: Pe });
      }
      let Xt = ae.subarray(),
        Cr = da(i.create(`indexes-${re.id}-${z}`, ta, Xt)),
        vr = k.map(({ model: Vt }, pe) => {
          if (Vt instanceof tt) {
            let Pe = rt[pe];
            y(Pe, "Can't find range for index");
            let Sr = {
              url: Cr,
              range: Pe,
              fieldNames: f.dedupe("fieldNames", Vt.fieldNames),
              collation: f.dedupe("collation", Vt.options.collation),
              collectionSchema: w,
            };
            return p`new ${hr}(${Sr})`;
          }
          throw new Error("Unsupported index type");
        });
      gr.push(...vr);
    }
    let ti = Be.serialize(e, r, f),
      yr = {
        id: $ + re.id,
        schema: w,
        chunks: ei,
        indexes: gr,
        resolveRichText: m.resolveRichTextBinding,
        resolveVectorSetItem: ti,
      };
    if (R && F) {
      let z = p`new ${j}(${yr})`,
        k = p`${R}[${re.id}]`;
      G[re.id] = p`new ${F}(${z}, ${k}, ${Un}, ${Wn})`;
    } else G[re.id] = p`new ${j}(${yr})`;
  }
  m.serialize();
  let H = { id: $, displayName: n.resolveValue("name") ?? "Unknown", collectionByLocaleId: G };
  return p`${H}`;
}
var iu = ve(async (n, e, t) => Promise.all([ou(n.id, e, !1), ou(n.id, e, !0)]));
async function ou(n, e, t) {
  let { componentLoader: r, modulesStore: o, tree: i, assetMap: a } = e,
    s = t ? "draftCollection" : "collection",
    l = new mo(a),
    d = new Mo(n),
    f = new jd(s, r, o, n),
    c = i.getNodeWithTrait(n, Q);
  y(c, "Must have a source node to serialize");
  let u = c.moduleSourceRevision,
    m = Pd(c);
  await lo(m, o, r);
  let { module: h } = Ld(r, o, c);
  ct("collection_update", { moduleId: h?.id, recordCount: c.getItemCount(), source: "unknown" });
  let g = c.getSlugVariable()?.id,
    v = new Bt(),
    S = new pn(s, r, o, v),
    b = new Gr(v),
    w = new Yr(c, r, S, v, void 0, i, o),
    P = new jr(),
    V = await ru(c, r, i, o, f, d, S, l, w, v, b, P, t),
    x = Nt("", {
      framerRecordIdKey: "id",
      framerSlug: g,
      framerEnumToDisplayNameUtils: "2",
      framerCollectionUtils: "1",
      framerCollectionId: c.id,
      framerColorSyntax: P.hasColorsOfType("wideGamut"),
      framerAutoSizeImages: !0,
      framerData: !0,
    }),
    R = v.create("data"),
    F = Im(c, R, i, r, S, s),
    J = Sm(S, b, R, "id", g),
    O = p.joinLines(
      ...S.statements,
      ...b.list(),
      x,
      p`const ${R} = ${V}`,
      p`export default ${R}`,
      F,
      ...vm(c.variables, i.root.locales),
      J
    );
  return {
    type: s,
    revision: u,
    source: O,
    artifacts: {
      assets: new Set(l.keys),
      binaryAssets: d.assets,
      submodules: f.serialize(),
      metrics: { nodes: c.getItemCount() },
    },
  };
}
function vm(n, e) {
  let t = [],
    r = new B();
  for (let o of n) {
    if (o.type !== "enum") continue;
    let i = new N(ad(o.id));
    (t.push(p`export const ${i} = ${Uc(o, e, i)};`), (r[o.id] = i));
  }
  return (t.push(p`export const ${new N(ro)} = ${r};`), t);
}
function Sm(n, e, t, r, o) {
  return p.joinLines(
    p`export const utils = {`,
    p`async getSlugByRecordId(id, locale) {`,
    o ? bm(n, e, t, r, o) : void 0,
    p`},`,
    p`async getRecordIdBySlug(slug, locale) {`,
    o ? Tm(n, e, t, r, o) : void 0,
    p`},`,
    p`}`
  );
}
function au(n, e) {
  let t = n.addImport("framer", { exportSpecifier: "QueryEngine" });
  return e.dedupe("queryEngine", p`new ${t}()`);
}
function bm(n, e, t, r, o) {
  let i = au(n, e),
    a = {
      from: { type: "Collection", data: t },
      select: [{ type: "Identifier", name: o }],
      where: {
        type: "BinaryOperation",
        operator: "==",
        left: { type: "Identifier", name: r },
        right: { type: "LiteralValue", value: p`id` },
      },
      limit: { type: "LiteralValue", value: 1 },
    };
  return p.joinLines(p`const [item] = await ${i}.query(${a}, locale)`, p`return item?.[${o}]`);
}
function Tm(n, e, t, r, o) {
  let i = au(n, e),
    a = {
      from: { type: "Collection", data: t },
      select: [{ type: "Identifier", name: r }],
      where: {
        type: "BinaryOperation",
        operator: "==",
        left: { type: "Identifier", name: o },
        right: { type: "LiteralValue", value: p`slug` },
      },
      limit: { type: "LiteralValue", value: 1 },
    };
  return p.joinLines(p`const [item] = await ${i}.query(${a}, locale)`, p`return item?.[${r}]`);
}
function Im(n, e, t, r, o, i) {
  let a = oa(n, r, t, o, void 0, i, "ContentManagement");
  if (q(a)) return;
  let s = o.addImport("framer", { exportSpecifier: "addPropertyControls" });
  return N.fn(s, e, a);
}
function _o({
  componentName: n,
  fontObjects: e,
  fontReferences: t,
  imports: r,
  declarations: o,
  sharedFontArrays: i = [],
}) {
  return p.joinLines(
    ...o.list(),
    N.fn(
      r.addImport("framer", { exportSpecifier: "addFonts" }),
      n,
      or({ fontObjects: e, fontReferences: t, sharedFontArrays: i }),
      { supportsExplicitInterCodegen: !0 }
    )
  );
}
function or({ fontObjects: n, fontReferences: e, sharedFontArrays: t = [] }) {
  let r = [],
    o = { explicitInter: !0, fonts: [] };
  for (let i of n) {
    try {
      new URL(i.url);
    } catch (a) {
      throw new Error(
        `Expected URL for font "${i.uiFamilyName}" to be a valid, absolute URL, but got: ${i.url}`,
        { cause: a }
      );
    }
    o.fonts.push(i);
  }
  r.push(o);
  for (let i of e.values()) r.push(p`...${i}`);
  for (let i of t) r.push(p`...${i}`);
  return r;
}
var su = ve(async (n, { componentLoader: e, assetMap: t, tree: r, modulesStore: o }) => {
  let i = new Bt(),
    a = new mo(t),
    s = new pn("componentPresets", e, o, i),
    l = new Yr(n, e, s, i, void 0, r, o),
    d = new Gr(i),
    f = new Fl(n, i),
    c = new _d(n, r, f, d, i, s, a),
    u = new Wd(i, f, c, a, l),
    m = n.moduleSourceRevision,
    h = {},
    g = {},
    v = n.getComponentPresets();
  await lo(
    v.map((w) => w.componentIdentifier),
    o,
    e
  );
  let S = new Bt(),
    b = new Hr(S);
  for (let w of v) {
    let P = e.reactComponentForIdentifier(w.componentIdentifier);
    y(P, "Can't find react component for preset");
    let V = w.getFontsForCodeGeneration(e);
    await Ga.loadFonts(V);
    let x = new go(b),
      R = jn(Zr(P, "onlyPresets"), w.getRawControlProps()),
      F = Bd(w, P.properties, R, {
        context: { assets: a, imports: s, links: l, fonts: x, fetches: u },
      });
    ((h[w.id] = new B(F)),
      (x.fontObjects.size > 0 || x.fontReferences.size > 0) &&
        (g[w.id] = or({ fontObjects: x.fontObjects, fontReferences: x.fontReferences })));
  }
  return [
    {
      revision: m,
      type: "componentPresets",
      source: p.joinLines(
        ...s.statements,
        p`export const props = ${h}`,
        ...b.list(),
        p`export const fonts = ${g}`
      ),
      artifacts: { assets: a.keys, metrics: { nodes: Object.keys(h).length } },
    },
  ];
});
var Nm = me("CodeGenerationStore");
function lu(n, e, t) {
  return (
    t.add(n),
    e[n]?.forEach((r) => {
      t.has(r) || lu(r, e, t);
    }),
    t
  );
}
function Rm(n, e) {
  let t = {};
  for (let { fromNode: r, toNode: o } of e) {
    let i = t[r] ?? new Set();
    (i.add(o), (t[r] = i));
  }
  return lu(n, t, new Set([n]));
}
var du = ve(async (n, { componentLoader: e, tree: t, treeStore: r, modulesStore: o }) => {
  let i = r.treeIndex,
    a = _(`Prototype/${n.name || "Generated Prototype"}`),
    s = t.get(n.homeNodeId);
  y(s, () => `Can't serialize ${a}: home node must exist.`);
  let l = t.getRect(s),
    d = new pn("prototype", e, o);
  (d.addImport("react", { exportSpecifier: "*", importBinding: "React" }),
    d.addImport("framer-motion", { exportSpecifier: "LayoutGroup" }),
    d.addImport("framer", { exportSpecifier: "PageRoot" }),
    d.addImport("framer", { exportSpecifier: "useInitialRouteComponent" }));
  let f = {};
  for (let g of Rm(s.id, i.getPrototypeScreenLinks(t, n.id))) {
    let S = `../${ye("screen", g)}`;
    try {
      await Nd(e, [Je("screen", g, "default").value], o);
      let b = d.addImport(S, {
        exportSpecifier: "default",
        importBinding: "Screen",
        lazy: g !== n.homeNodeId,
      });
      f[g] = { page: b, path: g };
    } catch {
      Nm.error("Missing module for prototype screen", { screenId: g });
    }
  }
  let c = _(n.homeNodeId),
    u = _(f),
    m = p`${d.addImport("react", { exportSpecifier: "*", importBinding: "React" })}.useId`,
    h = new N(
      A.sections(
        A.linesFrom(d.statements),
        `const routes = ${u}`,
        A.lines(
          "function Prototype({ layoutId }: { layoutId?: string }): JSX.Element {",
          `  const InitialRouteComponent = useInitialRouteComponent(routes, "${n.homeNodeId}")`,
          `  const defaultLayoutId = ${N.fn(m)};`,
          "  if (!InitialRouteComponent) return null",
          "  return (",
          "    <LayoutGroup id={layoutId ?? defaultLayoutId} inherit={false}>",
          `      <PageRoot RootComponent={InitialRouteComponent} isWebsite={false} routeId={${c}} routes={routes} />`,
          "    </LayoutGroup>",
          "  )",
          "}"
        ),
        A.lines(
          Nt("This is a generated Framer component.", {
            framerIntrinsicHeight: l?.height,
            framerIntrinsicWidth: l?.width,
            framerSupportedLayoutWidth: "fixed",
            framerSupportedLayoutHeight: "fixed",
            framerPrototype: !0,
          }),
          "const Component: React.ComponentType<{}> = React.memo(Prototype)",
          "export default Component;"
        ),
        `Component.displayName = ${a};`
      )
    );
  return [{ type: "prototype", source: h, revision: void 0 }];
});
async function $o(n, e, t, r, o, i) {
  return xs(e) &&
    e.usesDOMRectCached() &&
    !e.getDOMRect() &&
    (await wm(e, r, t, o), !e.cache.getRawDOMRect(e.id) && i)
    ? Nr.atOrigin(i)
    : n.getRect(e);
}
async function wm(n, e, t, r) {
  if (e) {
    let a = (await e([n.id]))?.[0];
    if (a) {
      let { layoutMetrics: s } = a,
        l = Nr.from(s);
      n.cache.setRawDOMRect(n.id, l);
      return;
    }
  }
  if (!t) return;
  let o = xm(t, r);
  if (o) {
    let i = Nr.atOrigin(o);
    n.cache.setRawDOMRect(n.id, i);
  }
}
function xm(n, e) {
  if (!zn(n)) return;
  let t = e.getPersistedModuleByLocalIdentifier(n);
  if (!t) return;
  let { intrinsicWidth: r, intrinsicHeight: o } = t.metadata;
  if (U(r) && U(o)) return { width: r, height: o };
}
var Oe = {
    Screen: p`Screen`,
    activeVariantCallback: T.activeVariantCallback,
    css: T.css,
    delay: T.delay,
    ref: T.ref,
    serializationHash: T.serializationHash,
    sharedStyleClassNames: T.sharedStyleClassNames,
    restProps: T.restProps,
    refBinding: T.refBinding,
  },
  Lm = new Set(Object.values(Oe).map((n) => _(n))),
  cu = ve(async (n, e, { serializationId: t, initialIntrinsicSize: r }) => {
    let { modulesStore: o, tree: i } = e,
      a = i.getScopeNodeFor(n);
    if (!Fe(a)) return [];
    let s = i.get(n.id);
    (y(s, "Must have a source node to serialize. This should be a primary variant."),
      y(Fe(a), "Must have a scope node to serialize. This should be a CanvasPage Node."));
    let l = `FramerScreen${bt(n.id)}`,
      d = Je("screen", n.id, "default").value,
      { jsx: f, ctx: c } = await yo(s, a, e, new Set(Lm).add(l), t);
    y(f, "There must be JSX to serialize.");
    let u = c.imports,
      m = await $o(i, s, d, e.measureNodes, o, r),
      h = c.events.navigationTargets.size > 0,
      g = c.events.handlers.size > 0,
      v = c.events.variantOnAppearId.get(n.id),
      S = c.events.keyBindHooks.size > 0,
      b = [];
    for (let H of c.events.navigationTargets) H !== $d && b.push(H);
    let w = c.fonts.fontObjects,
      P = new Map(c.fonts.fontReferences),
      V = [],
      x = new Set(c.css.sharedStyleIds),
      R = [];
    for (let H of x) {
      let De = ye("css", H),
        be = u.addImport(`../${De}`, { exportSpecifier: "*", importBinding: "sharedStyle" });
      (R.push(be),
        V.push(
          N.fn(
            u.addImport("framer", { exportSpecifier: "getFontsFromSharedStyle" }),
            p`${be}.fonts`
          )
        ));
    }
    let F;
    for (let H of c.componentPresetIds) {
      F ??= u.addImport(`../${po}`, { exportSpecifier: "*", importBinding: "componentPresets" });
      let De = p`${F}.fonts?.[${H}]`;
      V.push(
        p`(${De} ? ${N.fn(c.imports.addImport("framer", { exportSpecifier: "getFontsFromComponentPreset" }), De)} : [])`
      );
    }
    let J = new Map();
    mn(c.css.get(), J);
    let O = new N(l),
      $ = u.addImport("react", { exportSpecifier: "useRef" }),
      j = a.resolveValue("name"),
      G = A.lines(
        h && `const preload = ${_(b)};`,
        ...c.variables.fallbackDeclarations(),
        ...c.declarations.file.list(),
        p`const ${Oe.serializationHash} = ${t}`,
        p`const ${Oe.Screen} = ${Dm({ props: p`style, className` }, p`const fallbackRef = ${N.fn(p`${$}<HTMLElement>`, p`null`)}`, p`const ${Oe.refBinding} = ${Oe.ref} ?? fallbackRef`, h ? p`const navigate = ${N.fn(u.addImport("framer", { exportSpecifier: "usePrototypeNavigate" }), { preload: p`preload` })}` : void 0, g && p`const { ${Oe.activeVariantCallback}, ${Oe.delay} } = ${N.fn(u.addImport("framer", { exportSpecifier: "useActiveVariantCallback" }))}`, ...c.events.handlers.values(), p`const ${Oe.sharedStyleClassNames} = ${R.map((H) => p`${H}.className`)}`, ...c.declarations.component.list(), S && A.linesFrom(c.events.keyBindHooks), v && N.fn(u.addImport("framer", { exportSpecifier: "useOnAppear" }), v), p`return ${f}`)}`,
        p`const ${Oe.css} = ${[...c.css.conditionalRules(t), Kr, ...hn(J), ...R.map((H) => p`...${H}.css`)]}`,
        Qr(
          O,
          t,
          Oe.Screen,
          Oe.css,
          {
            framerIntrinsicHeight: m.height,
            framerIntrinsicWidth: m.width,
            framerSupportedLayoutWidth: "fixed",
            framerSupportedLayoutHeight: "fixed",
            framerScreen: void 0,
            framerDisplayContentsDiv: !1,
          },
          u
        ),
        p`${O}.displayName = ${`Screens/${j ? j.trim() : "Generated Component"}`};`,
        p`${O}.defaultProps = ${{ width: m.width, height: m.height }};`,
        _o({
          componentName: O,
          fontObjects: w,
          fontReferences: P,
          imports: u,
          declarations: c.declarations.font,
          sharedFontArrays: V,
        })
      );
    return [
      {
        type: "screen",
        revision: void 0,
        source: new N(A.sections(A.linesFrom(u.statements), G)),
        artifacts: {
          assets: c.assets.keys,
          packageDependencies: c.packages.dependencies,
          metrics: c.metrics.get(),
        },
      },
    ];
  });
function Dm({ props: n }, ...e) {
  return new N(
    A.lines(p`React.forwardRef(function({ ${n}, ...${Oe.restProps} }, ${Oe.ref}) {`, ...e, p`});`)
  );
}
var fu = "{serializationId}";
function uu(n) {
  return `.${fu} ${n}`;
}
var pu = ve(async (n, { componentLoader: e, tree: t }, { serializationId: r }) => {
  let o = new Bt(),
    i = new Hr(o),
    a = new go(i),
    s = n.getFontsForCodeGeneration(),
    l = n.moduleSourceRevision,
    d = ys(n);
  for (let b of s) a.addFontObjectBySelector(b, d);
  let f = [],
    c = new jr(),
    u = { componentLoader: e, colors: c };
  for (let { selectors: b, declarations: w } of n.getCSS(u)) {
    let P = b.map(uu);
    f.push(`${P.join(", ")} { ${w} }`);
  }
  let m = t
    .getNodesWithTrait(n.cache.replicaInstances ?? [], Wl)
    .filter(Sl)
    .sort((b, w) => (w.breakpointWidth ?? 0) - (b.breakpointWidth ?? 0));
  for (let b = 0; b < m.length; b++) {
    let w = m[b];
    if (q(w)) continue;
    let P = m[b + 1],
      V = w.breakpointWidth - 1,
      x = P?.breakpointWidth ?? 0;
    for (let { selectors: R, declarations: F } of w.getCSS(u)) {
      let O = `${R.map(uu).join(", ")} { ${F} }`;
      f.push(`@media (max-width: ${V}px) and (min-width: ${x}px) { ${O} }`);
    }
  }
  let h = c.getWideGamutColorRules(`.${r}`);
  h.length && f.push(`@supports (color: color(display-p3 1 1 1)) { ${h.join(" ")} }`);
  let g = f.map((b) => b.replaceAll(fu, r)),
    v = or({ fontObjects: a.fontObjects, fontReferences: a.fontReferences }),
    S = p.joinLines(
      p`import { fontStore } from "framer";`,
      p`fontStore.loadFonts(${s});`,
      ...i.list(),
      p`export const fonts = ${v};`,
      p`export const css = ${g};`,
      p`export const className = ${r};`
    );
  return [{ revision: l, type: "css", source: S, artifacts: { metrics: { nodes: f.length } } }];
});
function ua(n) {
  if (us(n))
    return {
      widthType: n.widthType,
      heightType: n.heightType,
      ...(cl(n)
        ? {
            minHeight: n.minHeight,
            maxHeight: n.maxHeight,
            minWidth: n.minWidth,
            maxWidth: n.maxWidth,
          }
        : dl),
    };
}
function fa(n, e) {
  if (bs(e)) return { min: n.getRect(e).width };
}
var Pm = new Set(Object.values(T).map((n) => _(n)));
function Vm(n, e) {
  if (rn(n)) {
    let t = e.get(n.parentid);
    return !We(t) || !t.isLoaded() ? null : t;
  }
  return e.current(n);
}
var pa = ve(async (n, e, { serializationId: t, initialIntrinsicSize: r }) => {
  let { modulesStore: o, tree: i, measureNodes: a } = e,
    s = Re(n, i) ? n.moduleSourceRevision : void 0,
    l = Vm(n, i);
  y(l?.isLoaded(), "The scope node must be valid.");
  let d = Hd(l),
    f = Ge(l) ? l.baseVariantId : n.id,
    c = i.get(f);
  y(c, "Must have a source node to serialize. This should be a primary variant.");
  let u = `Framer${n.id}`,
    m = new N(u),
    { jsx: h, ctx: g } = await yo(c, l, e, new Set(Pm).add(u), t);
  y(h, "There must be JSX to serialize.");
  let v = g.imports,
    S = We(l),
    b = S ? void 0 : l.instanceIdentifier,
    w = await $o(i, c, b, a, o, r),
    P = Ge(l) ? l.getReplicaVariants() : [],
    { variants: V, hook: x } = g.preload.getHook(),
    { gestures: R, breakpoints: F, classNames: J, sizes: O, appearEventMap: $ } = qm(c, P, g, V),
    j = he(l) ? [l.baseVariantId, ...l.getTopLevelReplicaVariants().map(bo)] : [],
    G = j.length > 1,
    H = !vt(R),
    De = !vt($),
    be = !vt(F),
    re = p`${v.addImport("react", { exportSpecifier: "*", importBinding: "React" })}.useId`,
    { mediaQueries: Te, sharedStyleImports: Be, sharedFontArrays: Kt, css: mr } = jm(t, g, P, F, l),
    Qt = Jm(l, g.componentLoader, g.declarations),
    Yt = Ke.applyTransforms(h, Km(l, g, Wm(l, c, P, Te, i, g), be ? T.baseVariant : void 0, Qt)),
    En = zm(l, g),
    Ae = Om(l, g),
    Mn = Ae ? g.cmsCodegen.createDetailPageDataCode(Ae) : void 0,
    hr = g.preload.getRouteLoaderCode(),
    { props: ei, types: Fa, propertyControls: Pt, variableMap: Fn } = zo(l, g, ho(l), d, Qt),
    gr =
      !be &&
      p`const ${T.layoutDependency} = ${N.fn(g.declarations.file.dedupe("createLayoutDependency", p`(props, variants) => {${p.joinLines(p`if (props.layoutDependency) return variants.join('-') + props.layoutDependency`, p`return variants.join('-')`)}}`), T.props, T.variants)}`,
    { cursors: ti } = g.customCursors,
    yr = v.addImport("react", { exportSpecifier: "useRef" }),
    z = Um(l),
    k = A.sections(
      H && p`const ${T.enabledGestures} = ${R};`,
      G && `const ${T.cycleOrder} = ${_(j)};`,
      be && `const ${T.breakpoints} = ${_(Te)}`,
      be && `const ${T.isBrowser} = () => typeof document !== "undefined";`,
      S && p`const ${T.variant} = undefined;`,
      S && p`const ${T.breakpoints} = undefined;`,
      S && p`const ${T.layoutId} = undefined;`,
      S && p`const ${T.className} = undefined;`,
      S && p`const ${T.style} = undefined;`,
      D(l) && p`export const ${T.queryParamNames} = ${z}`,
      p`const ${T.serializationHash} = ${t}`,
      `const ${T.variantClassNames} = ${_(J)};`,
      !be && Bm,
      p.linesFrom(g.variables.fallbackDeclarations()),
      ...g.declarations.file.list(),
      `export interface Props extends React.HTMLAttributes<HTMLDivElement> { ${Fa} }`,
      `const ${T.Component} = ${Mm(p`const fallbackRef = ${N.fn(p`${yr}<HTMLElement>`, p`null`)}`, p`const ${T.refBinding} = ${T.ref} ?? fallbackRef`, p`const ${T.defaultLayoutId} = ${N.fn(re)}`, `const { ${T.activeLocale}, ${T.setLocale} } = ${N.fn(v.addImport("framer", { exportSpecifier: "useLocaleInfo" }))}`, g.variables.shouldProvideLayoutDirection ? p`const ${T.layoutDirection} = ${N.fn(v.addImport("framer", { exportSpecifier: "useLayoutDirection" }))}` : void 0, he(l) || D(l) ? p`const ${T.componentViewport} = ${N.fn(g.imports.addImport("framer", { exportSpecifier: "useComponentViewport" }))}` : void 0, Mn, D(l) && $m(i, e.componentLoader, l, v, g.variables), ei, D(l) && Am({ webPageNodeId: n.id, metadataDependencies: En, imports: v, bindings: g.bindings }), be ? p.joinLines(p`const [${T.baseVariant}, ${T.hydratedBaseVariant}] = ${N.fn(v.addImport("framer", { exportSpecifier: "useHydratedBreakpointVariants" }), T.variant, T.breakpoints, p`false`)};`, p`const ${T.gestureVariant} = undefined;`) : Em(c.id, H, G, v), gr, Hm(g), ...g.events.routeHandlers.values(), g.events.handlers.size > 0 && p`const { ${T.activeVariantCallback}, ${T.delay} } = ${N.fn(v.addImport("framer", { exportSpecifier: "useActiveVariantCallback" }), be ? p`undefined` : T.baseVariant)}`, ...g.events.handlers.values(), De && N.fn(v.addImport("framer", { exportSpecifier: "useOnVariantChange" }), be ? T.hydratedBaseVariant : T.baseVariant, $), p`const ${T.sharedStyleClassNames} = ${Be.map((rt) => p`${rt}.className`)}`, ...g.declarations.component.list(), ti, x?.(H ? T.enabledGestures : p`undefined`), p`return (${Yt})`)}`,
      mr,
      Qr(m, t, T.Component, T.css, Fm(l, g, w, O, Fn), v),
      `${u}.displayName = ${_(l.resolveValue("name")?.trim() || "Generated Component")};`,
      `${u}.defaultProps = ${_({ width: w.width, height: w.height })};`,
      Pt &&
        N.fn(
          v.addImport("framer", { exportSpecifier: "addPropertyControls" }),
          m,
          new N(`${_(Pt)} as any`)
        ),
      _o({
        componentName: m,
        fontObjects: g.fonts.fontObjects,
        fontReferences: g.fonts.fontReferences,
        imports: v,
        declarations: g.declarations.font,
        sharedFontArrays: Kt,
      }),
      hr && p`${m}.loader = ${hr}`
    ),
    [ae] = D(l) ? await Yd(l, e, { serializationId: t }) : [];
  return Co([
    {
      type: d,
      revision: s,
      source: new N(A.sections(A.linesFrom(v.statements), k)),
      artifacts: {
        packageDependencies: g.packages.dependencies,
        errors: g.errors.errors,
        assets: g.assets.keys,
        metrics: g.metrics.get(),
      },
    },
    ae,
    g.localizedValues.module(s),
  ]);
});
function Em(n, e, t, r) {
  let o = B.fromEntries([
      [T.defaultVariant, n],
      [T.variant, T.variant],
      [T.variantClassNames, T.variantClassNames],
      [T.enabledGestures, e ? T.enabledGestures : void 0],
      [T.cycleOrder, t ? T.cycleOrder : void 0],
      [T.ref, T.refBinding],
    ]),
    i = N.fn(r.addImport("framer", { exportSpecifier: "useVariantState" }), o),
    a = T.variants,
    s = T.baseVariant,
    l = p`setVariant`,
    d = T.gestureVariant,
    f = T.classNames,
    c = T.setGestureState,
    u = T.gestureHandlers,
    m = T.clearLoadingGesture,
    h = T.isLoading;
  return A.lines(
    p`const ${{ variants: a, baseVariant: s, setVariant: l, gestureVariant: d, classNames: f, setGestureState: c, gestureHandlers: u, clearLoadingGesture: m, isLoading: h }} = ${i}`
  );
}
function Mm(...n) {
  return A.sections(
    p`React.forwardRef<HTMLDivElement, Props>(function(${T.props}, ${T.ref}) {`,
    ...n,
    p`});`
  );
}
function Fm(n, e, t, r, o) {
  let { height: i, width: a } = t,
    s = {
      propertyName: "variant",
      data: Object.fromEntries(Object.entries(r).map(([d, f]) => [d, Nl(f)])),
    },
    l = {
      framerIntrinsicHeight: i,
      framerIntrinsicWidth: a,
      framerRootFontSize: ws(n) ? n.getPrimaryVariant()?.rootFontSize : void 0,
      framerCanvasComponentVariantDetails: JSON.stringify(s),
      framerVariables: o && JSON.stringify(o),
      framerImmutableVariables: !0,
      framerDisplayContentsDiv: !1,
      framerAutoSizeImages: !0,
      framerComponentViewportWidth: !0,
      framerColorSyntax: !0,
      framerVectorSets: e.vectorSets.annotation(),
    };
  if (te(n)) {
    let d = {},
      f = n.getBreakpointValues();
    for (let c in f) {
      let u = e.tree.get(c);
      d[c] = {
        ...f[c],
        canvasClassName: yu(c),
        minHeight: u?.height,
        viewportHeight: u?.viewportHeight,
        name: u?.resolveValue("name") ?? void 0,
        width: u?.widthType === 0 ? u?.width : void 0,
        rootFontSize: u?.rootFontSize ?? ms,
      };
    }
    ((l.framerHitTargets = e.hitTargets.annotation()),
      (l.framerBreakpoints = JSON.stringify(d)),
      (l.framerDefaultVariant = n.baseVariantId),
      (l.framerTrackingIds = e.trackingIds.annotation()),
      (l.framerLayoutTemplate = !0));
  } else
    D(n)
      ? ((l.framerAcceptsLayoutTemplate = e.acceptsLayoutTemplate),
        (l.framerTrackingIds = e.trackingIds.annotation()),
        (l.framerScrollSections = e.elementIds.annotation() ?? !1),
        (l.framerResponsiveScreen = !0),
        (l.framerLayoutTemplateFlowEffect = !0))
      : he(n)
        ? (l.framerTrackingIds = e.trackingIds.annotation())
        : We(n)
          ? (l.framerDesign = !0)
          : he(n) || (l.framerScreen = !0);
  return l;
}
function zm(n, e) {
  if (!D(n)) return {};
  let t = km(n, e),
    r = Xd(n, t),
    o = {};
  for (let i of r) {
    let a = Xa(i, n.id);
    o[i] = e.variables.create(a);
  }
  return o;
}
function km(n, e) {
  if (On(n) && At(e.tree, n))
    return (
      n.getProvidedControlMap(
        e.tree,
        void 0,
        e.componentLoader,
        e.componentLoader.activeBundleHash
      ) ?? Ci
    );
}
function Om(n, e) {
  if (!D(n) || !On(n) || !At(e.tree, n)) return;
  let r =
      n.getProvidedControlMap(
        e.tree,
        void 0,
        e.componentLoader,
        e.componentLoader.activeBundleHash
      ) ?? Ci,
    o = e.variables.getBindings(n.id),
    i = new Set();
  for (let [a] of r) o.has(a) && i.add(a);
  return { scopeNode: n, controlMap: r, usedVariableIds: i };
}
var Bm = A.lines(
  "function addPropertyOverrides(overrides, ...variants) {",
  "const nextOverrides = {}",
  "variants?.forEach(variant => variant && Object.assign(nextOverrides, overrides[variant]))",
  "return nextOverrides",
  "}"
);
function Am({ webPageNodeId: n, metadataDependencies: e, imports: t, bindings: r }) {
  let o = t.addImport(`../${Kd(n).localId}`, {
      exportSpecifier: "default",
      importBinding: "metadataProvider",
    }),
    i = r.create("metadata"),
    a = t.addImport("framer", { exportSpecifier: "useMetadata" }),
    s = N.fn(o, e, T.activeLocale),
    l = Object.values(e).concat(T.activeLocale);
  return A.lines(p`const ${i} = React.useMemo(() => ${s}, ${l})`, N.fn(a, i));
}
function _m(n) {
  if (Ss(n)) return { type: n.gesture, topLevel: n.replicaInfo.inheritsFrom };
}
function $m(n, e, t, r, o) {
  let i = t.variables.filter(je);
  if (i.length === 0) return;
  let a = o.getBindings(t.id),
    s = [];
  for (let l of i) {
    if (!ui(l)) continue;
    let d = $i(l),
      f;
    switch (l.type) {
      case "boolean": {
        let h = r.addImport("framer", { exportSpecifier: "useBooleanQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: d,
            optional: l.optional ? !0 : void 0,
          });
        f = N.fn(h, g);
        break;
      }
      case "string": {
        let h = r.addImport("framer", { exportSpecifier: "useStringQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: d,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = N.fn(h, g);
        break;
      }
      case "number": {
        let h = r.addImport("framer", { exportSpecifier: "useNumberQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: d,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = N.fn(h, g);
        break;
      }
      case "date": {
        let h = r.addImport("framer", { exportSpecifier: "useDateQueryParam" }),
          g = new B({
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: d,
            displayTime: l.options?.displayTime === !0 ? !0 : void 0,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = N.fn(h, g);
        break;
      }
      case "collectionreference": {
        let h = r.addImport("framer", { exportSpecifier: "useCollectionReferenceQueryParam" }),
          g = new B({
            collectionId: ci(l.dataIdentifier),
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: d,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = N.fn(h, g);
        break;
      }
      case "multicollectionreference": {
        let h = r.addImport("framer", { exportSpecifier: "useMultiCollectionReferenceQueryParam" }),
          g = new B({
            collectionId: ci(l.dataIdentifier),
            initialValue: l.optional ? void 0 : l.initialValue,
            parameterName: d,
            optional: l.optional === !0 ? !0 : void 0,
          });
        f = N.fn(h, g);
        break;
      }
      case "controlReference": {
        if (l.expectedType !== "enum") continue;
        let h = Js(n, e, l);
        if (!h || h.type !== "enum") continue;
        let g = r.addImport("framer", { exportSpecifier: "useEnumQueryParam" }),
          v = l.entityIdentifier,
          S = se(v);
        if (!ot(S)) continue;
        let b = l.controlKey,
          P = e.dataForIdentifier(v)?.annotations?.framerEnumToDisplayNameUtils;
        y(L(P) && Number.parseInt(P, 10) === 2, "Unexpected FramerEnumToDisplayNameUtils version");
        let V = r.addModuleImport(S, { exportSpecifier: ro });
        if (!V) continue;
        let x = r.addModuleImport(S, { importBinding: `${_s(b)}CollectionData` });
        if (!x) continue;
        let R = L(l.initialValue) ? l.initialValue : $r("enum"),
          F;
        (l.optional === !0 || !R
          ? (F = new B({
              getOptionTitle: p`${V}?.[${b}]`,
              initialValue: void 0,
              optional: !0,
              options: p`${x}.propertyControls?.[${b}]?.options`,
              parameterName: d,
            }))
          : (F = new B({
              getOptionTitle: p`${V}?.[${b}]`,
              initialValue: R,
              options: p`${x}.propertyControls?.[${b}]?.options`,
              parameterName: d,
            })),
          (f = N.fn(g, F)));
        break;
      }
      default:
        ee(l);
    }
    let c = a.get(l.id);
    if (!c) continue;
    let u = o.getSetterBinding(l.id),
      m = [c];
    (u && m.push(u), s.push(p`const ${m} = ${f};`));
  }
  return p.joinLines(...s);
}
function Um(n) {
  return D(n) ? n.variables.flatMap((e) => (je(e) ? (ui(e) ? [$i(e)] : []) : [])) : [];
}
function yu(n) {
  return `framer-${bt(n)}-override`;
}
function mu(n, e) {
  if (!(!Ks(n) || (Qs(n) && !n.fillEnabled)))
    return n.fillType === "color" && at(n.fillColor) ? e(n.fillColor, n, "fill") : bl(n);
}
function hu(n) {
  if (!hs(n)) return;
  let e = (n.rootFontSize / 16) * 100;
  return gs(e, "%");
}
function gu(n, e) {
  return L(n) ? n : N.fn(e.imports.addImport("framer", { exportSpecifier: "safeCSSValue" }), n);
}
function Wm(n, e, t, r, o, i) {
  if (!te(n) && !D(n)) return;
  let a = Gd(o, i.declarations, i.variables, i.valueTransforms),
    s = te(n) ? ":root" : "html",
    l = `${s} body`,
    d = hu(e),
    f = mu(e, a) ?? "none",
    c = [qe`${l} { background: ${gu(f, i)}; }`];
  L(d) && c.push(`${s} { font-size: ${d}; }`);
  for (let u of t) {
    let m = r[u.id];
    if (m) {
      let h = mu(u, a) ?? "none",
        g = hu(u),
        v = [];
      if (
        (Ai(f, h) || v.push(qe`${l} { background: ${gu(h, i)}; }`),
        Ai(d, g) || v.push(`${s} { font-size: ${g}; }`),
        v.length === 0)
      )
        continue;
      c.push(qe`@media ${m} { ${qe.join(v, " ")} }`);
    }
  }
  return qe.join(c, " ");
}
function jm(n, e, t, r, o) {
  let i = Jl(Hl(r)),
    a = [],
    s = new Map();
  if (te(o)) {
    let g = o.getPrimaryVariant(),
      v = { marginBottom: xi(g, e) };
    e.css.createRule(`[${Bi}="true"] > #overlay`, Ti(v));
    for (let S of t) {
      let b = { marginBottom: xi(S, e) },
        w = Md(b, v);
      !w || vt(w) || e.css.createRule(`[${Bi}="true"] > #overlay`, Ti(w), S.id);
    }
  }
  mn(e.css.get(), s);
  let l = {},
    d = {};
  for (let g of t) {
    let v = i[g.id];
    if (v) {
      let S = new Map();
      if ((mn(e.css.get(g.id), S), !S.size)) continue;
      te(o)
        ? ((l[g.id] = hn(S)), (d[g.id] = `.${yu(g.id)}`))
        : a.push(qe`@media ${v} { ${qe.join(hn(S), " ")}}`);
    } else mn(e.css.get(g.id), s);
  }
  let f = [],
    c = [];
  for (let g of e.css.sharedStyleIds) {
    let v = ye("css", g),
      S = e.imports.addImport(`../${v}`, { exportSpecifier: "*", importBinding: "sharedStyle" });
    (c.push(S),
      f.push(
        N.fn(
          e.imports.addImport("framer", { exportSpecifier: "getFontsFromSharedStyle" }),
          p`${S}.fonts`
        )
      ));
  }
  let u;
  for (let g of e.componentPresetIds) {
    u ??= e.imports.addImport(`../${po}`, {
      exportSpecifier: "*",
      importBinding: "componentPresets",
    });
    let v = p`${u}.fonts?.[${g}]`;
    f.push(
      p`(${v} ? ${N.fn(e.imports.addImport("framer", { exportSpecifier: "getFontsFromComponentPreset" }), v)} : [])`
    );
  }
  let m = [Kr, ...hn(s), ...c.map((g) => p`...${g}.css`), ...e.css.conditionalRules(n)],
    h = Gm(o, e, m, a, l, d, i);
  return {
    sharedStyleImports: c,
    sharedFontArrays: f,
    mediaQueries: i,
    css: p`const ${T.css} = ${h}`,
  };
}
function Gm(n, e, t, r, o, i, a) {
  if (he(n) || We(n)) return t;
  if (D(n)) return [...t, ...r];
  if (te(n)) {
    let s = e.declarations.file.dedupe("breakpointRules", o),
      l = e.declarations.file.dedupe("breakpointKeys", p`Object.keys(${s})`),
      d = e.declarations.file.dedupe("selectors", i),
      f = e.declarations.file.dedupe("rules", t),
      c = e.declarations.file.dedupe("mediaQueries", a),
      u = e.bindings.create("target"),
      m = e.bindings.create("query"),
      h = e.bindings.create("selector"),
      g = e.bindings.create("rule"),
      v = e.imports.addImport("framer", { exportSpecifier: "RenderTarget" });
    return p`(target) => {${p.joinLines(p`const isStaticRenderer = ${u} === ${v}.canvas || ${u} === ${v}.export`, p`if (!isStaticRenderer) {${p.joinLines(p`return [...${f}, ...${l}.map(key => {${p.joinLines(p`const ${m} = ${c}[key]`, p`return ${qe`@media ${m} { ${p`${s}[key].join(" ")`} }`}`)}})]`)}}`, p`return [...${f}, ...${l}.flatMap(key => {${p.joinLines(p`const ${h} = ${d}[key]`, p`return ${s}[key].map(${g} => ${qe`${h} {${g}}`})`)}})]`)}}`;
  }
  ee(n);
}
function Hm(n) {
  if (n.events.needsPathVariables)
    return `const ${Ud} = React.useContext(${n.imports.addImport("framer", { exportSpecifier: "PathVariablesContext" })});`;
}
function Jm(n, e, t) {
  let r = Ge(n) ? n.getTopLevelVariants() : [];
  if (r.length < 2) return;
  let o = new B();
  for (let i of r) {
    let a = i.resolveValue("name") ?? dt(e, i);
    o[a] = i.id;
  }
  return t.file.dedupe("humanReadableVariantMap", o);
}
function qm(n, e, t, r) {
  let o = ua(n);
  y(o, "The primary variant should have a default size as it must be a FrameNode.");
  let i = { default: o },
    a = { [n.id]: `framer-v-${bt(n.id)}` },
    s = {},
    l = {},
    d = {},
    f = fa(t.tree, n);
  f && (d[n.id] = f);
  let c = t.events.variantOnAppearId.get(n.id),
    u = {};
  !q(c) && !N.isUndefined(c) && (u.default = c);
  for (let m of e) {
    t.metrics.count("variants");
    let h = _m(m);
    if (h) {
      let w = s[h.topLevel] ?? {};
      ((w[h.type] = h.type === "loading" ? r?.has(h.topLevel) : !0),
        (s[h.topLevel] = w),
        (l[h.topLevel] = [...(l[h.topLevel] ?? []), m.id]));
    } else a[m.id] = `framer-v-${bt(m.id)}`;
    let g = ua(m);
    g && (i[m.id] = g);
    let v = fa(t.tree, m);
    v && (d[m.id] = v);
    let S = t.events.variantOnAppearId.get(m.id),
      b = _(u[m.replicaInfo.inheritsFrom ?? ""] ?? c);
    !q(S) && _(S) !== b && (u[m.id] = S);
  }
  return { gestures: s, breakpoints: d, classNames: a, sizes: i, appearEventMap: u };
}
function Km(n, e, t, r, o) {
  let i = [];
  if ((te(n) || D(n)) && t) {
    let s = e.declarations.file.dedupe(
      "HTMLStyle",
      p`({value}) => {${p.joinLines(p`const onCanvas = ${N.fn(e.imports.addImport("framer", { exportSpecifier: "useIsOnFramerCanvas" }))}`, p`if (onCanvas) return null`, p`return ${Ke.tag("style", new B({ "data-framer-html-style": "", dangerouslySetInnerHTML: { __html: p`value` } }))}`)}}`
    );
    i.push((l) => Ke`${p.joinLines(Ke.tag(s, new B({ value: t })), l)}`);
  }
  if (he(n)) {
    let s = e.imports.addImport("react", { exportSpecifier: "*", importBinding: "React" }),
      l = e.imports.addImport("framer-motion", { exportSpecifier: "motion.create" }),
      d = e.declarations.file.dedupe("Variants", N.fn(l, p`${s}.Fragment`));
    i.push((f) => Ke.tag(d, new B({ initial: !1, animate: T.variants }), f));
  }
  if (D(n) || te(n) || We(n)) {
    let s = new B({ id: te(n) ? ps : fs });
    i.push((l) => Ke`${p.joinLines(l, Ke.tag("div", s))}`);
  }
  let a = e.imports.addImport("framer-motion", { exportSpecifier: "LayoutGroup" });
  if (
    (i.push((s) => Ke.tag(a, new B({ id: p`layoutId ?? ${T.defaultLayoutId}` }), s)), D(n) || te(n))
  ) {
    let s = e.imports.addImport("framer", { exportSpecifier: "GeneratedComponentContext" });
    i.push((l) =>
      Ke.tag(
        p`${s}.Provider`,
        new B({
          value: {
            primaryVariantId: n.baseVariantId,
            variantClassNames: T.variantClassNames,
            activeVariantId: r,
            humanReadableVariantMap: o,
            isLayoutTemplate: te(n) ? !0 : void 0,
          },
        }),
        l
      )
    );
  }
  return i;
}
function Cu() {
  let n = new N("3f060d6");
  return p`// Generated by Framer (${n})`;
}
var ma = {
    ref: T.ref,
    restProps: T.restProps,
    style: T.style,
    props: T.props,
    className: T.className,
    layoutId: T.layoutId,
  },
  ha = p`css`,
  ir = p`Icon`,
  Uo = p`Component`,
  ar = p`Props`,
  Qm = [...Object.values(ma), ha, ir, Uo, ar].map((n) => _(n)),
  vu = ve(async (n, e, t) => {
    let { serializationId: r } = t,
      { modulesStore: o, tree: i, vectorsStore: a } = e,
      s = i.getScopeNodeFor(n);
    y(Ce(s), "Vector set item must be in a VectorSetNode.");
    let l = i.get(n.id);
    y(l, "Must have a source node to serialize");
    let { ctx: d, jsx: f, svg: c, type: u } = zl(l, s, e, r, Qm),
      m = Je("vectorSet", s.id, "default").localId,
      h = o.getPersistedModuleByLocalId(m);
    y(h, "VectorSet must be persisted.");
    let g = i.getRect(n),
      {
        propertyControls: v,
        variableMap: S,
        types: b,
        props: w,
      } = zo(s, { ...e, ...d, cssVariableDefinitions: void 0 }, "VectorSet", "vector", void 0),
      P = Ym(d),
      V = {
        name: l.resolveValue("name") ?? "Vector",
        empty: Xm(l) ? !0 : void 0,
        color: P,
        set: { localId: m, id: s.id, moduleId: h.id },
      },
      x = d.imports.addImport("react", { exportSpecifier: "*", importBinding: "React" }),
      R = d.imports.addImport("framer", { exportSpecifier: "withCSS" }),
      F = Zm[u],
      J = A.lines(
        ...d.variables.fallbackDeclarations(),
        ...d.declarations.file.list(),
        p`interface ${ar} extends ${x}.HTMLAttributes<${x}.${F}> { ${b ?? p``} }`,
        p`const ${Uo}: ${x}.ComponentType<${ar}> = ${x}.forwardRef<${x}.${F}, ${ar}>(function(${ma.props}, ${ma.ref}) {${p.joinLines(w, ...d.declarations.component.list(), p`return (${f})`)}})`,
        p`const ${ha} = ${d.css.rules}`,
        Nt("This is a generated Framer component.", {
          framerIntrinsicWidth: g.width,
          framerIntrinsicHeight: g.height,
          framerSupportedLayoutWidth: "any-prefer-fixed",
          framerSupportedLayoutHeight: "any-prefer-fixed",
          framerVariables: S && JSON.stringify(S),
          framerImmutableVariables: !0,
          framerVector: JSON.stringify(V),
          framerDisableUnlink: Vd.has(V.set.moduleId) ? !0 : void 0,
        }),
        p`const ${ir}: ${x}.ComponentType<${ar}> = ${N.fn(R, Uo, ha, r)} as typeof ${Uo};`,
        p`${ir}.displayName = ${l.resolveValue("name") ?? "Vector"};`,
        p`export default ${ir}`,
        v &&
          N.fn(
            d.imports.addImport("framer", { exportSpecifier: "addPropertyControls" }),
            ir,
            new N(`${_(v)} as any`)
          )
      );
    return (
      a.cache(n.id, { svg: _(c), color: P }),
      Co([
        {
          revision: l.moduleSourceRevision,
          type: "vector",
          source: new N(A.sections(A.linesFrom(d.imports.statements), J)),
          artifacts: { files: [{ name: "icon.svg", type: "svgIcon", content: _(c) }] },
        },
      ])
    );
  });
function Ym(n) {
  if (n.colors.uniqueColorCount !== 1) return;
  let e = n.colors.singleColor;
  if (e) return { type: "static", value: e };
  let t = n.colors.singleVariableColor;
  if (t) return { type: "variable", value: bt(t) };
}
function Xm(n) {
  return !n.children || n.children.length === 0 ? !0 : n.children.every((e) => e.visible === !1);
}
var Zm = {
  0: p`HTMLImageElement`,
  1: p`HTMLDivElement`,
  3: p`HTMLSVGElement`,
  2: p`HTMLSVGElement`,
};
var Su = ve(async (n, { modulesStore: e }, t) => {
  let r = [];
  for (let a of n.getVectorSetItems()) {
    let s = Je("vector", a.id, "default").localId,
      l = e.getPersistedModuleByLocalId(s);
    l &&
      r.push({
        id: a.id,
        name: a.resolveValue("name") ?? "Vector",
        moduleId: l.id,
        saveId: t.includeVectorSetSaveIds ? l.saveId : void 0,
      });
  }
  let o = { framerVectorSet: JSON.stringify({ name: n.resolveValue("name") ?? "Set", items: r }) },
    i = new N(
      A.lines(
        Nt("This is a generated Framer component.", o),
        p`const VectorSet: React.ComponentType<unknown> = function VectorSet() { return null; };`,
        p`export default VectorSet;`
      )
    );
  return [{ revision: n.moduleSourceRevision, type: "vectorSet", source: i }];
});
function Iu(n) {
  return Math.ceil(n / Math.ceil(n / 100));
}
var bu = class {
    constructor(e, t) {
      C(this, "status", null);
      C(this, "results");
      C(this, "reportProgress");
      C(this, "onDone");
      ((this.reportProgress = e), (this.onDone = t));
    }
    async import(e, t, r) {
      ((this.status = "importing"), (this.results = { scopeId: t, vectors: [], targetLayout: r }));
      let o = r ? { left: r.x + r.width * 2, top: r.y } : { left: 0, top: 0 };
      for (let i = 0; i < e.length; i++) {
        let a = e[i];
        if (!a) continue;
        let s = await th(a, t, o);
        if (this.status !== "importing") return;
        (s && this.results.vectors.push(s),
          this.reportProgress?.({ status: this.status, finishedCount: i + 1 }));
      }
      await this.done();
    }
    cancel() {
      this.status = "canceled";
    }
    async done() {
      (this.results && (await this.onDone?.(this.results)), (this.status = "done"));
    }
  },
  Tu = "run-svgo",
  eh = 1e6;
async function th(n, e, t) {
  if (n.size > eh) {
    Kn({
      type: "add",
      variant: "error",
      key: Tu,
      primaryText: "SVG is too large.",
      secondaryText: "You may need a PNG.",
      icon: "error",
      duration: 5e3,
    });
    return;
  }
  try {
    let r = await n.text(),
      o = rc(r),
      i = rh(n.name),
      a = Id(r, so.createEmpty, o);
    return a
      ? ((a.parentid = e), a.set({ name: i, isVectorSetItem: !0, ...t }), { node: a, size: o })
      : void 0;
  } catch (r) {
    (console.error("Failed to insert SVG:", r),
      Kn({
        type: "add",
        variant: "error",
        key: Tu,
        primaryText: "Failed to insert",
        secondaryText: "your SVG.",
        icon: "error",
      }));
    return;
  }
}
var nh = /[_-]/gu;
function rh(n) {
  let [e] = Ds(n);
  return Ya(e.replace(nh, " "));
}
var oh = {
  cache: !0,
  mutable: !0,
  update: !0,
  parentid: !0,
  originalid: !0,
  duplicatedFrom: !0,
  moduleSourceRevision: !0,
  moduleSourceRevisionHint: !0,
  moduleSourceRevisionCommittedHint: !0,
  kitSectionsStructure: !0,
  kitSectionHash: !0,
  kitSectionSource: !0,
  kitPageHTMLHash: !0,
  customizations: !0,
  customizationsDescription: !0,
  customizationsSectionId: !0,
  customizationsCategory: !0,
  customizationsRules: !0,
  isApplyingCustomizations: !0,
  layoutTemplateIdentifierOverride: !0,
  abTestingParentId: !0,
  abTestingDistributionWeightPpm: !0,
};
function ht(n, e) {
  if (!q(e)) {
    if (n === "children" && e instanceof Fr) return e.toArray();
    if (!oh[n] && !n.startsWith("export") && !il(n)) return e;
  }
}
function ih(n, e) {
  if (n !== "children") return ht(n, e);
}
function ah(n, e, t, r, o) {
  if (!Ns(n) || !kt(e)) return e;
  if (e.type === "slot")
    return !oi(e.value) || (Ne(t) && t.slotsAreChildNodes)
      ? e
      : e.value.map((i) => {
          let a = o.get(i.reference);
          return ao(r, a) ? a : void 0;
        });
  if (e.type === "componentinstance") {
    if (!L(e.value)) return e;
    let i = o.get(e.value);
    return ao(r, i) ? i : void 0;
  } else if (e.type === "array") {
    if (!oi(e.value)) return e;
    let i = [],
      a = !1;
    for (let s of e.value) {
      if (!kt(s) || s.type !== "componentinstance" || ((a = !0), !L(s.value))) continue;
      let l = o.get(s.value);
      ao(r, l) && i.push(l);
    }
    return a ? i : e;
  }
  return e;
}
function sh(n, e) {
  return (t, r, o) => ht(t, ah(t, r, o, e, n));
}
var sr = me("ModuleSourceRevision");
function VR(n) {
  return M(ce({ path: n.path, targetUrl: n.targetUrl }));
}
function lh(n) {
  return M(ce(n, ht));
}
function ER(n, e) {
  return lh(n.cloneWithIds({ id: e }));
}
function dh(n, e) {
  switch (n.__class) {
    case "CollectionItemNode":
    case "ImageStylePresetNode":
    case "InlineCodeStylePresetNode":
    case "LayoutTemplateNode":
    case "LinkStylePresetNode":
    case "SmartComponentNode":
    case "TableStylePresetNode":
    case "WebPageNode":
      return M(ce(n, ht));
    case "FrameNode": {
      let t = e.get(n.parentid);
      return !t || !We(t) ? void 0 : M(ce(n, sh(e, t.id)));
    }
    case "CollectionNode": {
      y(Q(n), "Expected collection node");
      let t = ce(e.root.locales),
        r = ce(e.root.webMetadata?.translatePagePaths),
        o = ce(n, ih),
        i = ce(n.getUnsortedChildren()?.map((a) => a.moduleSourceRevision));
      return M(t + r + o + i);
    }
    case "PresetsListNode": {
      let t = n.children?.filter((r) => !Cl(r, e)) ?? [];
      return M(ce(t, ht));
    }
    case "TextStylePresetNode":
    case "BlockquoteStylePresetNode": {
      if (St(n)) return n.moduleSourceRevision;
      let t = ce(n, ht);
      return (
        n.cache.replicaInstances?.forEach((r) => {
          let o = e.getNodeWithTrait(r, St);
          o && (t += ce(o, ht));
        }),
        M(t)
      );
    }
    case "VectorSetNode": {
      y(Ce(n), "Expected VectorSetNode");
      let t = n.getVectorSetItems().map((r) => {
        let o = Je("vector", r.id, "default").localId,
          i = e.getNodeWithTrait(o, Al);
        return i ? i.save.moduleId : "";
      });
      return M(ce(t) + n.name);
    }
    case "ShapeContainerNode": {
      if (!Qe(n)) return;
      let t = e.getScopeNodeFor(n);
      if (!Ce(t)) return;
      let r = ce(n, ht);
      return M(r + ce(t.variables));
    }
    case "CustomCodeScopeNode": {
      y(zt(n), "Expected CustomCodeScopeNode");
      let t = n.loaded;
      y(t, "Expected CustomCodeScopeNode to be loaded");
      let r = {};
      for (let i of t.children)
        if (!(!cs(i) || !i.pageIds))
          for (let a of i.pageIds) {
            let s = e.getNodeWithTrait(a, On);
            s && (r[a] = s.dataIdentifier);
          }
      let o = ce(n, ht);
      return M(o + ce(r));
    }
    case "CanvasPageNode":
    case "RootNode":
      return;
    case "ComponentPresetNode":
      return;
    case "AbTestsEntityTypeRootNode":
    case "AgentPanelTool":
    case "AgentDragSelectTool":
    case "AgentSelectTool":
    case "AnalyticsScopeNode":
    case "BlockquoteEntityTypeRootNode":
    case "BooleanShapeNode":
    case "BoxShadow":
    case "BranchNode":
    case "BranchesNode":
    case "CanvasNode":
    case "CanvasScopeNode":
    case "CustomCodeNode":
    case "DesignPageNode":
    case "DesignPageEntityTypeRootNode":
    case "ChatTool":
    case "CMSEntityTypeRootNode":
    case "CodeComponentNode":
    case "CodeFileEntityTypeRootNode":
    case "CollectionReferenceVariableEditTool":
    case "Color":
    case "ColorEntityTypeRootNode":
    case "ColorStyleTokenListNode":
    case "ColorStyleTokenNode":
    case "ComponentEntityTypeRootNode":
    case "ConicGradient":
    case "ContentManagementNode":
    case "CreateCodeComponentBaseTool":
    case "CreateFrameBaseTool":
    case "CreateShapeBaseTool":
    case "CreateTextTool":
    case "EntityFolderNode":
    case "EntityReferenceNode":
    case "EntityRootNode":
    case "ContentManagementEntityTypeRootNode":
    case "ErrorListNode":
    case "ErrorNode":
    case "ExportOptions":
    case "ExternalModuleNode":
    case "ExternalModulesListNode":
    case "FeedbackTool":
    case "FormBooleanInputNode":
    case "FormPlainTextInputNode":
    case "FormSelectNode":
    case "FunnelNode":
    case "FunnelsEntityTypeRootNode":
    case "FunnelStepActionNode":
    case "FunnelStepNode":
    case "GapTool":
    case "GradientColorStop":
    case "GradientToolManagerTool":
    case "GuideMoveTool":
    case "HeaderRouteNode":
    case "HighlightStackItemTool":
    case "HighlightTool":
    case "InitialTool":
    case "InlineCodeEntityTypeRootNode":
    case "LayoutTemplateEntityTypeRootNode":
    case "LineAnchorMoveTool":
    case "LinearGradient":
    case "LinkEntityTypeRootNode":
    case "LinkTool":
    case "LoadingShieldTool":
    case "LocalizationGlossaryItemNode":
    case "LocalizationGlossaryNode":
    case "LocalModuleNode":
    case "LocalModulesListNode":
    case "MoveTool":
    case "MutableNode":
    case "OvalShapeNode":
    case "OverlayNode":
    case "PaddingTool":
    case "PageLinkTool":
    case "PanTool":
    case "PathCurveBendTool":
    case "PathDefaultTool":
    case "PathNode":
    case "PathSegment":
    case "PathSegmentAdditionTool":
    case "PathSegmentHandleMoveTool":
    case "PathSegmentMoveTool":
    case "PathSegmentSelectTool":
    case "PolygonShapeNode":
    case "PreviewSettings":
    case "ProxyRouteNode":
    case "RadialGradient":
    case "RadiusTool":
    case "RecoveryTool":
    case "RectangleShapeNode":
    case "RedirectRouteNode":
    case "ResizeTool":
    case "RewriteRouteNode":
    case "RichTextEditTool":
    case "RichTextNode":
    case "RichTextVariableEditTool":
    case "RotateTool":
    case "RouteNode":
    case "RouteLocaleNode":
    case "RouteSegmentNode":
    case "RouteSegmentRootNode":
    case "RoutesNode":
    case "SampleColorTool":
    case "ScaleTool":
    case "ScopeNode":
    case "SelectTool":
    case "ShaderNode":
    case "Shadow":
    case "ShapeGroupNode":
    case "SlotConnectTool":
    case "SlotNode":
    case "SlotPropertyNode":
    case "StarShapeNode":
    case "StringVariableEditTool":
    case "DateVariableEditTool":
    case "EnumVariableEditTool":
    case "StyledTextDraft":
    case "SVGNode":
    case "TestTool":
    case "TextEditTool":
    case "TextEntityTypeRootNode":
    case "TextNode":
    case "VectorSetEntityTypeRootNode":
    case "VekterGradientTool":
    case "VekterPathEngine":
    case "VekterTool":
    case "ViewportResizeTool":
    case "ZoomSelectTool":
    case "ZoomTool":
      return;
    default:
      ee(n.__class, "getModuleRevision: node.__class must be a valid value");
  }
}
function ya(n, e) {
  let t = n.get(e);
  return !t || !Re(t, n)
    ? !1
    : q(t.moduleSourceRevisionHint) ||
        t.moduleSourceRevisionCommittedHint === t.moduleSourceRevisionHint
      ? (sr.debug(`Hint hasn't changed for ${t.id}.`), !1)
      : (sr.debug(`Initial Revision: ${t.moduleSourceRevision}`), !0);
}
function ga(n, e) {
  let t = n.get(e);
  if (!t || !Re(t, n)) return;
  let r = performance.now(),
    o = dh(t, n);
  sr.debug(`Spent ${performance.now() - r}ms computing module revision for ${t.id}`);
  let i = U(o) && o !== t.moduleSourceRevision;
  sr.debug(`Revisions: ${t.moduleSourceRevision}, ${o}`);
  let a = t.moduleSourceRevisionHint,
    s = i
      ? { moduleSourceRevision: o, moduleSourceRevisionCommittedHint: a }
      : { moduleSourceRevisionCommittedHint: a };
  return (
    sr.debug(
      i
        ? `Updating moduleSourceRevision for ${t.id}.`
        : `Updating moduleSourceRevisionCommittedHint for ${t.id}.`
    ),
    t.setIgnoringReplica(s, n),
    i ? o : void 0
  );
}
function Nu(n, e) {
  return e((t) => {
    if (t.isViewOnly) return;
    let r = t.get(n);
    return (
      Q(r) &&
        r.getUnsortedChildren()?.forEach((o) => {
          ya(t, o.id) && ga(t, o.id);
        }),
      ga(t, n)
    );
  });
}
function Ru(n, e) {
  return e((t) => {
    if (!t.isViewOnly) return n.reduce((r, o) => ((r[o.id] = ga(t, o.id) ?? 1), r), {});
  });
}
var ch = 2;
function Ca(n) {
  if (D(n)) return ch;
}
function _R(n, e, t) {
  if (e.type === "siteMetadata" || e.type === "webPageMetadata") return !0;
  let r = Yn(n, t).includes(e.type);
  if ((y(n.id === e.name && r, "sourceNode and module must match"), To(e))) return !1;
  let o = Ca(n);
  if (q(o)) return !0;
  let i = e.metadata.codeGenVersion;
  return U(i) && i >= o;
}
var va = 1;
function uh(n) {
  let e = n.metadata.compatibleTextStyleVersion;
  return U(e) ? e : 0;
}
function jR(n) {
  return uh(n) < va;
}
var Lt = Zt(Rr(), 1);
function xu(n, e) {
  if (he(n)) return "Smart Component";
  if (D(n)) return "web page";
  if (Fe(e.get(n.parentid))) return "prototype screen";
  if (Fe(n)) return "prototype";
  if (Q(n)) return "CMS collection";
}
function Lu(n, e, t) {
  return D(t) ? Qn(n, e, t) : (t.resolveValue("name") ?? void 0);
}
function Du(n, e = "module", t) {
  let r = "is too large and will not update";
  Kn({
    type: "add",
    variant: "warning",
    text: t
      ? (0, Lt.jsxs)(Lt.Fragment, {
          children: [
            (0, Lt.jsxs)(xr, { children: ["The ", e] }),
            " \u201C",
            t,
            "\u201D ",
            (0, Lt.jsx)(xr, { children: r }),
          ],
        })
      : (0, Lt.jsxs)(xr, { children: ["A ", e, " ", r] }),
    key: `module-too-large-${n}`,
    duration: 1 / 0,
    extraAction: {
      title: "Learn more",
      onClick() {
        (ct("ui_interaction", { page: "module-too-large-toast", id: "learn_more" }),
          Qa("https://www.framer.com/help/articles/how-to-fix-module-too-large-warning/"));
      },
    },
  });
  let i = `module-too-large-toast-${e.split(" ").join("-")}`;
  ct("ui_impression", { page: i });
}
var hh = 1e4,
  gh = 1e3,
  lr = me("CodeGenerationStore");
function yh(n) {
  return !(Q(n) || Fe(n) || Me(n) || rn(n) || on(n) || Ce(n));
}
function Ch(n, e) {
  if (!Qe(e)) return;
  let t = n.getScopeNodeFor(e);
  return (y(Ce(t), "Vector set item must be in a VectorSetNode."), t.variables);
}
function vh(n, e) {
  let t = e.resolveValue("name")?.trim();
  return t || (Tl(e) ? "Text" : dt(n, e, e.__class));
}
function Pu(n, e, t, r, o, i) {
  let a = {},
    s = vh(e, t);
  a.metadata || (a.metadata = {});
  let l = [...(i?.files ?? [])];
  yh(t) &&
    l.push({
      name: "tree.json",
      type: "nodeTree",
      content: JSON.stringify(
        Object.assign(t.toJS(), {
          __treeSerializationVersion: n.serializationVersion,
          __vectorSetVariables: Ch(n, t),
        })
      ),
    });
  let d = Ca(t);
  d && (a.metadata.codeGenVersion = d);
  let f = i?.packageDependencies;
  return (
    f && (a.metadata.packageDependencies = Array.from(f)),
    ic(r) && (a.metadata.compatibleCmsVersion = oc),
    ds(t) && (a.metadata.compatibleTextStyleVersion = va),
    (a.assets = i?.assets ? Array.from(i.assets) : []),
    { ...a, title: s, files: l, errors: i?.errors ?? [], type: r, name: o }
  );
}
async function Sh(n, e, t, r, o) {
  if (n && e instanceof Wa && e.code === 1001) {
    let i = jl(n, (s) => r.treeIndex.getScopeIdFor(s)) ?? void 0,
      a = new Gl({
        type: "CodeGeneration",
        nodeId: Ge(n) ? n.baseVariantId : n.id,
        reason: "FileTooLarge",
        sourceNodeId: n.id,
        scopeId: i,
      });
    (Du(n.id, xu(n, r.tree), Lu(r.tree, t, n)),
      await o((s) => {
        if (s.isViewOnly) return;
        s.lineage.setEditReason("codegeneration");
        let l = Ul.ensure(s);
        (lr.debug("Writing ClientFileTooLarge error to tree for", n.id), s.insertNode(a, l.id));
      }));
  } else lr.reportError(e);
}
var Le,
  wn,
  Wo = class {
    constructor(e, t, r, o, i, a) {
      C(this, "componentLoader", e);
      C(this, "treeStore", t);
      C(this, "modulesStore", r);
      C(this, "runWhenIdle", o);
      C(this, "scheduleDocumentUpdateIgnoringUndo", i);
      de(this, Le, new cc());
      de(this, wn);
      C(this, "process", I(this, Le).synchronize);
      C(this, "processWithProgress", I(this, Le).synchronizeWithProgress);
      C(this, "changeScope", () => I(this, wn).debounce(gh));
      C(this, "isIdle", () => I(this, Le).size === 0);
      C(this, "isBlocking", () =>
        I(this, Le)
          .snapshot()
          .some((e) => e.promise.state === "pending")
      );
      _e(
        this,
        wn,
        new Tr({
          delay: hh,
          task: () =>
            this.runWhenIdle(() => {
              I(this, Le).synchronize();
            }),
          abortSignal: a,
        })
      );
    }
    enqueue(e, t, r) {
      I(this, Le).snapshot((s) =>
        s.promise.isResolved() ? !1 : s.id === t ? s.promise.state !== "initial" : !0
      );
      let o = e.get(t);
      if (!o || !nc(o, e)) return;
      let i = Rt(o),
        a = {};
      for (let { type: s, artifacts: l } of r) {
        let d = Pu(e, this.componentLoader, o, s, i, l);
        a[ye(s, i)] = d;
      }
      I(this, Le).request(
        async () => {
          try {
            (lr.debug("Persisting transient modules for", t),
              await this.modulesStore.upsertBatch(a));
          } catch (s) {
            await Sh(
              o,
              s,
              this.componentLoader,
              this.treeStore,
              this.scheduleDocumentUpdateIgnoringUndo
            );
          }
        },
        { id: t, synchronize: !1 }
      );
    }
    async persist(e, t, r) {
      let o = this.treeStore.getDataTreeOrPartialTree(),
        i = o.get(e);
      if (i)
        return (
          this.enqueue(o, e, t),
          r
            ? I(this, wn).debounce()
            : (await I(this, Le).synchronize(),
              vo(i, o).map((a) => this.modulesStore.getPersistedModuleByLocalId(a)))
        );
    }
    async persistBatch(e) {
      let t = this.treeStore.getDataTreeOrPartialTree(),
        r = {},
        o = "batch";
      for (let i in e) {
        let a = t.get(i),
          s = e[i];
        if (!a || !s) continue;
        let l = Rt(a),
          d = new Set();
        for (let { type: f, artifacts: c } of s) {
          let u = ye(f, l);
          (y(!d.has(u), () => `Duplicate local module id: ${u}`),
            d.add(u),
            (r[u] = Pu(t, this.componentLoader, a, f, l, c)),
            (o += `-${u}`));
        }
      }
      (I(this, Le).snapshot((i) =>
        i.promise.isResolved() ? !1 : i.id === o ? i.promise.state !== "initial" : !0
      ),
        await I(this, Le).request(
          async () => {
            lr.debug("Persisting transient modules for batch: ", o);
            try {
              await this.modulesStore.upsertBatch(r);
            } catch (i) {
              lr.reportError(i);
            }
          },
          { id: o, synchronize: !0 }
        ));
    }
    get size() {
      return I(this, Le).size;
    }
  };
((Le = new WeakMap()), (wn = new WeakMap()));
var bh = () => ({
    stylePresets: [],
    componentPresets: [],
    collections: [],
    smartComponents: [],
    screens: [],
    prototypes: [],
    webpages: [],
    metadata: [],
    layoutTemplates: [],
    snippets: [],
  }),
  K = me("CodeGenerationStore");
var Th = 500,
  Eu = 1500,
  Ih = 5e3,
  Nh = 6e4;
function Rh(n) {
  for (let e in n.to) if (e in Kl) return !0;
  return !1;
}
function wh(n) {
  return Ce(n) ? Th : Mt(n) || Q(n) ? Ih : Eu;
}
var gt,
  ne,
  yt,
  xn,
  dr,
  Ln,
  Dn,
  Pn,
  Vn,
  fe,
  jo,
  le,
  ge,
  He,
  Mu,
  Vu = class {
    constructor(e, t, r, o, i, a, s, l, d, f, c, u, m = new Map()) {
      C(this, "scheduler", e);
      C(this, "componentLoader", t);
      C(this, "treeStore", r);
      C(this, "modulesStore", o);
      C(this, "vectorsStore", i);
      C(this, "assetMap", a);
      C(this, "timeline", s);
      C(this, "engineChanges", l);
      C(this, "scheduleDocumentUpdateIgnoringUndo", d);
      C(this, "updateTreeCache", f);
      C(this, "measureNodes", c);
      de(this, fe);
      de(this, gt, new Set());
      de(this, ne);
      de(this, yt, new Map());
      de(this, xn);
      de(this, dr, 0);
      de(this, Ln, 0);
      de(this, Dn);
      de(this, Pn, new Map());
      de(this, Vn);
      C(this, "testing", {
        setComponentGenerationQueue: (e) => {
          _e(this, ne, e);
        },
        flushQueuedCallbacks: () => {
          I(this, yt).forEach((e) => e.forEach((t) => t()));
        },
        generateAndUpdateQueuedComponents: (e) => this.generateAndUpdateQueuedComponents(e),
        setSerializationId: (e) => {
          _e(this, Vn, e);
        },
        setModulesStore: (e) => {
          (Object.assign(this, { modulesStore: e }),
            Object.assign(this.persistence, { modulesStore: e }));
        },
        waitForProcessing: async (e) => {
          await this.serialization.promise(e);
        },
        compiledBarrier: (e) => I(this, ge).get(e),
        markSerializeStarted: (e) => {
          (I(this, le).get(e)?.resolve(), I(this, le).delete(e));
        },
      });
      C(this, "persistence");
      C(this, "moduleTypeOrder", {
        localization: 0,
        webPageMetadata: 1,
        siteMetadata: 2,
        collection: 3,
        draftCollection: 4,
        css: 5,
        componentPresets: 6,
        vector: 7,
        vectorSet: 8,
        canvasComponent: 9,
        screen: 10,
        ["prototype"]: 11,
        codeFile: 12,
        config: 13,
        layoutTemplate: 14,
        design: 15,
        kit: 16,
        snippets: 17,
        shader: 18,
      });
      de(this, le, new Map());
      de(this, ge, new Map());
      C(this, "serialization", {
        idle: async () => {
          let e = [];
          for (let t of I(this, le).keys()) {
            let r = this.serialization.promise(t);
            if (!r) continue;
            let o = I(this, ge)
              .get(t)
              ?.catch(() => {});
            e.push(r.then(() => o));
          }
          await Promise.all(e);
        },
        isIdle: () => I(this, le).size === 0,
        prepare: (e) => {
          I(this, ne).delete(e);
          let t = I(this, le).get(e);
          I(this, le).set(e, t ?? new ni());
          let r = (t && I(this, ge).get(e)) || new ni();
          (r.catch(() => {}), I(this, ge).set(e, r));
        },
        cancel: (e) => {
          (I(this, ne).delete(e),
            I(this, le).get(e)?.resolve(),
            I(this, le).delete(e),
            Ie(this, fe, jo).call(this, e),
            Ie(this, fe, He).call(this, e, I(this, ge).get(e)));
        },
        preparing: (e) => I(this, le).has(e),
        promise: (e) => I(this, le).get(e),
        commit: async (e, t, r, o, i) => {
          let a = () => {
            (I(this, le).get(e.id)?.reject(), I(this, le).delete(e.id));
          };
          try {
            let s = await Ie(this, fe, Mu).call(this, e, t, r, i);
            return (
              s || (a(), Ie(this, fe, jo).call(this, e.id), Ie(this, fe, He).call(this, e.id, o)),
              s
            );
          } catch (s) {
            throw (K.reportError(s), a(), Ie(this, fe, He).call(this, e.id, o, s), s);
          }
        },
      });
      (_e(this, ne, m),
        _e(this, xn, this.timeline.openNodeChangesReader()),
        _e(
          this,
          Dn,
          new Tr({
            delay: Eu,
            maxDelay: Nh,
            task: () =>
              this.scheduler.runWhenIdle(() => {
                this.generateAndUpdateQueuedComponents();
              }),
            abortSignal: u,
          })
        ),
        (this.persistence = new Wo(
          this.componentLoader,
          this.treeStore,
          this.modulesStore,
          (h) => this.scheduler.runWhenIdle(h),
          (h) => this.scheduleDocumentUpdateIgnoringUndo(h),
          u
        )),
        $c(
          {
            check: () => {
              if (Ve.isTest) return !1;
              let h = this.treeStore.getDataTreeOrPartialTree();
              return h.isViewOnly ? !1 : this.hasBlockingUpdates(h);
            },
            effect: () => {
              K.debug(
                "Showing Unsaved Changes dialog (generation queue #: ",
                I(this, ne).size,
                ", component ids to generate: ",
                [...I(this, ne).keys()],
                ")"
              );
              let h = this.getMetrics();
              (h.paused > 1
                ? K.reportError("Tab closed with paused code generations", h)
                : (h.persisting > 5 || h.serializing > 5 || h.queue > 5) &&
                  K.reportError("Tab closed with multiple code generations in progress", h),
                I(this, gt).clear(),
                this.generateAndUpdateQueuedComponents()
                  .then(() => {
                    this.persistence.process();
                  })
                  .catch((g) => K.reportError(g)));
            },
          },
          u
        ),
        window.addEventListener(
          "blur",
          async () => {
            try {
              await this.flushQueuedCodeGenerationOnWindowBlur();
            } catch (h) {
              K.reportError(h);
            }
          },
          { signal: u }
        ));
    }
    isIdle() {
      return I(this, ne).size === 0 && this.serialization.isIdle() && this.persistence.isIdle();
    }
    getMetrics() {
      return {
        queue: I(this, ne).size,
        paused: I(this, gt).size,
        serializing: I(this, le).size,
        persisting: this.persistence.size,
        updating: I(this, Ln),
        completed: I(this, dr),
      };
    }
    documentDidLoad() {
      (I(this, ne).clear(), I(this, yt).clear(), I(this, ge).clear(), I(this, Pn).clear());
    }
    telemetrySession(e, t) {
      let r = I(this, Pn).get(t);
      if (r) return r;
      let o = e.get(t);
      y(o, "TelemetrySession: source node must exist");
      let i = new Eo(o);
      return (I(this, Pn).set(t, i), i);
    }
    getSerializationId() {
      return I(this, Vn) ? `framer-${I(this, Vn)}` : `framer-${Ed(5)}`;
    }
    async flushQueuedCodeGenerationOnWindowBlur() {
      document.visibilityState === "hidden" && (await this.generateAndPersistAllQueuedComponents());
    }
    async generateAndPersistAllQueuedComponents() {
      (await this.ensureSiteMetadataModule(),
        await this.ensureSnippetsModule(),
        await this.generateAndUpdateQueuedComponents(),
        await this.serialization.idle(),
        await this.persistence.process());
    }
    debugGetComponentSourceNodes() {
      let e = bh();
      y(!1, "This method should only be used in tests.");
      let t = this.treeStore.getDataTreeOrPartialTree();
      for (let { node: r, skipChildren: o } of t.root.walkWithSkipChildren())
        D(r)
          ? (e.webpages.push(r), e.metadata.push(r), o())
          : he(r)
            ? (e.smartComponents.push(r), o())
            : Q(r)
              ? (e.collections.push(r), o())
              : Ri(r)
                ? (e.stylePresets.push(r), o())
                : on(r)
                  ? e.componentPresets.push(r)
                  : ec(r, t)
                    ? (e.screens.push(r), o())
                    : Fe(r)
                      ? e.prototypes.push(r)
                      : Me(r)
                        ? e.metadata.push(r)
                        : te(r)
                          ? e.layoutTemplates.push(r)
                          : zt(r) && e.snippets.push(r);
      return e;
    }
    postProcess(e, t, r) {
      if (!e.isViewOnly) {
        if (r) {
          I(this, xn).clear();
          return;
        }
        (this.ingestChanges(e), I(this, Dn).debounce(wh(t)));
      }
    }
    ingestChanges(e) {
      for (let [t, r] of I(this, xn).read()) {
        if (r.length === 0 || r.every((i) => i.ignoreInCodeGeneration)) continue;
        let o = e.get(t);
        if (o) {
          if (D(o) && ns(r)) {
            let i = e.get(od);
            i && this.addChangeToQueue(i);
          }
          if (Re(o, e)) {
            vl(o, r) && this.addChangeToQueue(o);
            continue;
          }
          if (Me(o)) {
            if (!r.some(Rh)) continue;
            this.addChangeToQueue(o);
          } else if (Fe(o)) this.addChangeToQueue(o);
          else {
            let i = this.treeStore.treeIndex.getGroundNodeIdFor(t),
              a = e.get(i);
            if (!a || Ne(a)) continue;
            let s = e.getScopeNodeFor(a);
            if (!Fe(s)) continue;
            this.queueCanvasPageNodesFollowingLinks(e, a);
          }
        }
      }
    }
    queueCanvasPageNodesFollowingLinks(e, t) {
      if ((this.addChangeToQueue(t), !(!t.cache.links || t.cache.links.size === 0)))
        for (let r of t.cache.links) {
          let o = this.treeStore.treeIndex.getGroundNodeIdFor(r),
            i = e.get(o);
          !i || Ne(i) || this.queueCanvasPageNodesFollowingLinks(e, i);
        }
    }
    changeScope() {
      this.treeStore.tree.isViewOnly ||
        Ve.isTest ||
        this.isIdle() ||
        this.serialization
          .idle()
          .then(() => this.generateAndUpdateQueuedComponents())
          .then(() => {
            if (!Ue.isOn("prioritizedModuleEvaluation")) {
              this.persistence.process();
              return;
            }
            this.persistence.isIdle() || this.persistence.changeScope();
          })
          .catch((e) => K.reportError(e));
    }
    async createVectorSet(e, t) {
      let r = e[0];
      if (!r) return;
      let i = this.treeStore.getDataTreeOrPartialTree().getScopeNodeFor(r);
      y(Ce(i), "VectorSet: VectorSetNode must exist.");
      let a = performance.now(),
        s = await this.batchSerializeVectors(e, t);
      if (!s) return;
      let l = {},
        d = 0,
        f = Object.keys(s).length,
        c = Iu(f),
        u = [];
      for (let m in s) {
        let h = s[m];
        h && ((l[m] = h), d++, !(d % c !== 0 && d !== f) && (u.push(l), (l = {})));
      }
      for (let m = 0; m < u.length; m++) {
        let h = u[m];
        h && (await this.persistence.persistBatch(h), t?.("persisting", m));
      }
      (await this.forceComponentUpdate(i.id),
        K.debug("createVectorSet: Spent ", performance.now() - a, "ms creating a new vector set."));
    }
    async createModuleFromComponentSource(e, t) {
      let r = this.treeStore.getDataTreeOrPartialTree();
      if (
        (y(
          !r.isViewOnly,
          "CodeGenerationStore: viewers cannot create smart components or web pages."
        ),
        y($t(e, r), "CodeGenerationStore: componentSourceNode must be a valid component source."),
        Cn(e, r, this.modulesStore))
      )
        return;
      (K.debug("createModuleFromComponentSource", e.id), this.serialization.prepare(e.id));
      let i = I(this, ge).get(e.id);
      return r.withPersistedCaches(async () => {
        let a = await this.generateComponent(e.id, 1, void 0, { intrinsicSize: t });
        if (!a) throw new Error("Code not generated");
        return (
          await this.compileOutput(e, a, i),
          (r = this.treeStore.getDataTreeOrPartialTree()),
          vo(e, r).map((s) => this.modulesStore.getPersistedModuleByLocalId(s))
        );
      });
    }
    revisionNodes(e) {
      let t = [];
      for (let r of e.root.children)
        (Re(r, e) && t.push(r),
          (on(r) || Mt(r)) &&
            r.children.forEach((o) => {
              (Re(o, e) && t.push(o),
                Q(o) &&
                  o.getUnsortedChildren().forEach((i) => {
                    Re(i, e) && t.push(i);
                  }));
            }));
      return t;
    }
    nodesWithStaleModules(e, t) {
      let r = [],
        o = t?.ignoreHint ?? !1,
        i = this.treeStore.getDataTreeOrPartialTree();
      for (let a of this.revisionNodes(i))
        Ft(a) ||
          (e && !e(a)) ||
          (((!o && a.moduleSourceRevisionHint !== a.moduleSourceRevisionCommittedHint) ||
            !this.allModulesAreAtRevision(i, a.id)) &&
            r.push(a));
      return r;
    }
    debugNodeIdsByCodeGenPhase() {
      return {
        stale: new Set(this.nodesWithStaleModules().map((e) => e.id)),
        queued: new Set(I(this, ne).keys()),
        generating: new Set(I(this, le).keys()),
        paused: new Set(I(this, gt)),
      };
    }
    canSerialize(e) {
      let t = this.treeStore.getDataTreeOrPartialTree();
      if (!t.isViewOnly) return !0;
      let r = t.get(e);
      if (!r || !t.isGroundNode(r)) return !1;
      let o = t.getScopeNodeFor(r);
      return We(o);
    }
    async updateComponent(e, t = 0) {
      y(
        this.canSerialize(e),
        "CodeGenerationStore: viewers cannot generate smart component or web page updates."
      );
      let r = this.treeStore.getDataTreeOrPartialTree(),
        o = r.get(e);
      if (!o || !$t(o, r)) return !1;
      if (this.serialization.preparing(e)) {
        let s = I(this, ge).get(e);
        return (await this.serialization.promise(e), await s, !0);
      }
      K.debug("Update component: ", e, ", Condition: ", t);
      let i = I(this, ne).get(e);
      this.serialization.prepare(e);
      let a = I(this, ge).get(e);
      br(this, Ln)._++;
      try {
        return await r.withPersistedCaches(async () => {
          let s = await this.generateComponent(e, t, i);
          return s ? this.compileOutput(o, s, a) : !1;
        });
      } finally {
        br(this, Ln)._--;
      }
    }
    async forceComponentUpdate(e) {
      return (
        y(
          this.canSerialize(e),
          "CodeGenerationStore: viewers cannot generate smart component or web page updates."
        ),
        (await this.updateComponent(e, 2)) ? (await this.persistence.process(), !0) : !1
      );
    }
    queueCallbackAfterComponentCompilation(e, t) {
      let r = I(this, yt).get(e);
      r ? r.push(t) : I(this, yt).set(e, [t]);
    }
    untilCompiled(e) {
      return new Promise((t) => {
        this.queueCallbackAfterComponentCompilation(e, t);
      });
    }
    calculateNewModuleRevision(e) {
      let t = this.treeStore.getDataTreeOrPartialTree(),
        r = t.get(e);
      if (!r || !Re(r, t)) return 1;
      if (!((!zt(r) && !ya(t, e)) || !t.has(e)))
        return Nu(e, this.scheduleDocumentUpdateIgnoringUndo);
    }
    allModulesAreAtRevision(e, t, r) {
      let o = e.get(t);
      if (!o || !Re(o, e)) return !1;
      let i = r ?? o.moduleSourceRevision;
      return Yn(o, e).every(
        (a) => this.modulesStore.forType(a).getByStableName(Rt(o)).sourceRevision() === i
      );
    }
    async ensureDependencyModulesExist() {
      let e = this.treeStore.getDataTreeOrPartialTree();
      for (let t of this.revisionNodes(e))
        if ($t(t, e) && !Cn(t, e, this.modulesStore))
          try {
            await this.updateComponent(t.id);
          } catch (r) {
            K.error("Error updating dependency module", t.id, r);
          }
    }
    async ensureSourceNode(e) {
      let t = this.treeStore.getDataTreeOrPartialTree().get(e),
        r = ue(t) && !t.isLoaded() ? await t.load() : t;
      if (r) {
        if (
          (D(r) &&
            (await this.ensureSiteMetadataModule(),
            y(r.loaded, "WebPageNode must be loaded"),
            await this.ensureReferencedCollectionsExportUpToDateEnumUtils(r.loaded)),
          Q(r) && (await this.ensureDependentModulesAreCompatibleWithCollection(r)),
          Qe(r))
        ) {
          let o = this.treeStore.getDataTreeOrPartialTree().getScopeNodeFor(r);
          if (!Ce(o)) return;
          let i = ye("vectorSet", o.id);
          if (this.modulesStore.getModuleEntryByLocalId(i)) return;
          await this.createModuleFromComponentSource(o);
        }
        await this.ensureAllNodesWithLinksHaveLinkedScopesLoaded(r);
      }
    }
    async ensureSiteMetadataModule() {
      let e = this.treeStore.getDataTreeOrPartialTree();
      if (e.isViewOnly) return;
      let t = e.root;
      if (!Cn(t, e, this.modulesStore)) return this.updateComponent(t.id);
    }
    async ensureSnippetsModule() {
      let e = this.treeStore.getDataTreeOrPartialTree();
      if (e.isViewOnly) return;
      let t = id.get(e);
      if (!t) return;
      let r = await t.load();
      if (!(!r || Cn(r, e, this.modulesStore))) return this.updateComponent(r.id);
    }
    hasBlockingUpdates(e) {
      if (I(this, ne).size > 0) {
        for (let [t, r] of I(this, ne)) if (e.has(t) && r) return !0;
      }
      return !this.persistence.isIdle() || !this.serialization.isIdle();
    }
    async updateModules(e, t) {
      let r = this.treeStore.getDataTreeOrPartialTree(),
        o = r.get(e),
        i = this.timeline.remoteTreeVersion;
      if (!o || !$t(o, r)) return !1;
      let a = [];
      for (let { revision: l, source: d, type: f, artifacts: c } of t) {
        let u = {
            assets: c?.assets,
            submodules: c?.submodules,
            binaryAssets: c?.binaryAssets,
            preventCircularImports: f === "canvasComponent",
            telemetrySession: this.telemetrySession(r, e),
            treeVersion: i,
            sourceRevision: l,
            svgIcon: c?.files?.find((v) => v.type === "svgIcon")?.content,
            stableName: !0,
          },
          m = this.modulesStore.forType(f).getByStableName(Rt(o)),
          h = m.sourceRevision() !== l,
          g = A.sections(Cu(), d);
        (!h && m.currentSourceEquals(g, u)) ||
          a.push({ localId: m.localId, source: g, options: u });
      }
      if (a.length === 0) return !1;
      let s = await this.modulesStore.updateSources(a);
      return (Qe(o) && this.vectorsStore.delete(e), s);
    }
    allModulesExistForSources(e, t) {
      let r = Rt(e);
      for (let o of t) if (!this.modulesStore.getPersistedModuleByLocalId(ye(o.type, r))) return !1;
      return !0;
    }
    async compileOutput(e, t, r) {
      if (!this.canSerialize(e.id)) return (Ie(this, fe, He).call(this, e.id, r), !1);
      try {
        let o = t
          .slice()
          .sort((a, s) => this.moduleTypeOrder[a.type] - this.moduleTypeOrder[s.type]);
        if (this.allModulesExistForSources(e, o)) await this.updateModules(e.id, o);
        else
          for (let a of o) {
            let s = await this.updateModules(e.id, [a]),
              l = Rt(e);
            !this.modulesStore.getPersistedModuleByLocalId(ye(a.type, l)) &&
              s &&
              (await this.persistence.persist(e.id, [a]));
          }
        Q(e) && (await this.ensureDependentModulesCanRenderCollectionColors(e));
      } catch (o) {
        throw (Ie(this, fe, He).call(this, e.id, r, o), o);
      }
      return (
        br(this, dr)._++,
        Ie(this, fe, jo).call(this, e.id),
        Ie(this, fe, He).call(this, e.id, r),
        this.persistence.persist(e.id, t, !0),
        !0
      );
    }
    pauseCodeGeneration(e) {
      return (
        K.debug("Pausing code generation for", e),
        I(this, gt).add(e),
        () => {
          (K.debug("Resuming code generation for", e),
            I(this, gt).delete(e),
            I(this, Dn).debounce());
        }
      );
    }
    async generateAndUpdateQueuedComponents(e) {
      if (this.treeStore.getDataTreeOrPartialTree().isViewOnly) return [];
      await this.ensureDependencyModulesExist();
      let t = [],
        r = new Map();
      for (let [a] of I(this, ne)) {
        if (I(this, gt).has(a) || this.serialization.preparing(a)) continue;
        let s = this.treeStore.getDataTreeOrPartialTree(),
          l = s.get(a);
        if (!l || !$t(l, s) || (e && !Yn(l, s).includes(e))) {
          I(this, ne).delete(a);
          continue;
        }
        if ((this.serialization.prepare(a), Qe(l))) {
          let d = s.getScopeNodeFor(l);
          if (!d) continue;
          let f = r.get(d.id) ?? [];
          (f.push(l), r.set(d.id, f));
        } else t.push(l);
      }
      let o = new Set();
      K.debug("Batch generating queued vector nodes");
      for (let a of r.values())
        try {
          let s = await this.batchSerializeVectors(a, void 0, o);
          for (let l in s) this.persistence.persist(l, s[l], !0);
        } catch {}
      return (
        K.debug("Generating queued components", { ids: t.map(bo) }),
        await this.treeStore.getDataTreeOrPartialTree().withPersistedCaches(async () => {
          for (let a of t)
            try {
              K.debug("Flushing queue of ", a.id);
              let s = I(this, ge).get(a.id),
                l = await this.generateComponent(a.id, 1);
              l && (await this.compileOutput(a, l, s), o.add(a.id));
            } catch {}
        }),
        Array.from(o)
      );
    }
    async batchSerializeVectors(e, t, r) {
      let o = e[0];
      if (!o) return;
      (await this.ensureSourceNode(o.id),
        await this.ensureSourceCache(o.id),
        e.forEach((f) => this.serialization.prepare(f.id)));
      let i = await Ru(e, this.scheduleDocumentUpdateIgnoringUndo);
      y(i, "VectorSet: newModuleRevisions must exist");
      let a = {
          componentLoader: this.componentLoader,
          assetMap: this.assetMap,
          modulesStore: this.modulesStore,
          treeStore: this.treeStore,
          tree: this.treeStore.getDataTreeOrPartialTree(),
          vectorsStore: this.vectorsStore,
          engineChanges: this.engineChanges,
          measureNodes: this.measureNodes,
        },
        s = await Promise.all(
          e.map(async (f) => {
            let c = I(this, ge).get(f.id);
            try {
              if (!i[f.id]) {
                Ie(this, fe, He).call(this, f.id, c);
                return;
              }
              let g = (
                await this.serializerForSourceNode(this.treeStore.getDataTreeOrPartialTree(), f)(
                  f,
                  a,
                  { serializationId: this.getSerializationId() }
                )
              )?.[0];
              return (
                y(g, () => `VectorSet: source must exist. ${f.id}`),
                this.serialization.promise(f.id)?.resolve(),
                I(this, le).delete(f.id),
                [f.id, g, c]
              );
            } catch (u) {
              throw (Ie(this, fe, He).call(this, f.id, c, u), u);
            }
          })
        ),
        l = 0,
        d = {};
      for (let f of s) {
        if (!f) continue;
        let [c, u, m] = f,
          h;
        try {
          h = await this.updateModules(c, [u]);
        } catch (g) {
          throw (Ie(this, fe, He).call(this, c, m, g), g);
        }
        (Ie(this, fe, He).call(this, c, m),
          h && ((d[c] = [u]), t?.("serializing", l), l++, r?.add(c)));
      }
      return d;
    }
    async createVersionedVectorSet(e) {
      return this.treeStore.getDataTreeOrPartialTree().withPersistedCaches(async () => {
        await this.generateAndPersistAllQueuedComponents();
        let r = I(this, ge).get(e.id),
          o = await this.generateComponent(e.id, 2, void 0, { includeVectorSetSaveIds: !0 });
        return !o || !(await this.compileOutput(e, o, r))
          ? !1
          : (await this.persistence.process(), !0);
      });
    }
    isSkippedEmptyScopeNode(e) {
      return zt(e) ? !1 : !!(ue(e) && (!e.isLoaded() || e.loaded.children?.length === 0));
    }
    async generateComponent(e, t, r, o) {
      let i = I(this, ge).get(e);
      try {
        (K.debug("Ensure source node", e),
          await this.ensureSourceNode(e),
          K.debug("Ensure source cache", e),
          await this.ensureSourceCache(e));
      } catch (d) {
        throw (this.serialization.cancel(e), K.error("Failed to ensure source cache", e), d);
      }
      let a = await this.getComponentSourceNodeAndRoot(e, t, r);
      if (!a) return;
      K.debug("Serialize", e);
      let { componentSourceNode: s, root: l } = a;
      return this.serialization.commit(s, l, t, i, o);
    }
    async getComponentSourceNodeAndRoot(e, t, r) {
      try {
        let o = await this.calculateNewModuleRevision(e),
          i = this.treeStore.getDataTreeOrPartialTree(),
          a = i.get(e);
        if (!a || !$t(a, i) || !this.shouldGenerateUnderCondition(i, t, a, o, r)) {
          (K.debug("Skipping component source node", e), this.serialization.cancel(e));
          return;
        }
        let s = Ge(a) ? a.getPrimaryVariant() : a;
        return { componentSourceNode: a, root: s };
      } catch (o) {
        throw (this.serialization.cancel(e), o);
      }
    }
    shouldGenerateUnderCondition(e, t, r, o, i) {
      let a = i ?? I(this, ne).get(r.id);
      if ((I(this, ne).delete(r.id), !Cn(r, e, this.modulesStore))) return !0;
      switch (t) {
        case 2:
          return !0;
        case 1:
          return !q(o) || !this.allModulesAreAtRevision(e, r.id, o);
        case 0:
          return !q(o) && !!a && !this.allModulesAreAtRevision(e, r.id, o);
        default:
          ee(t);
      }
    }
    addChangeToQueue(e) {
      if (!this.shouldSkipChange(e)) {
        if ((Qe(e) && this.vectorsStore.queue(e.id), Ft(e))) {
          let t = e.parentid;
          (y(t, "Collection node has no parent"), I(this, ne).set(t, !0));
          return;
        }
        I(this, ne).set(e.id, !0);
      }
    }
    serializerForSourceNode(e, t) {
      return Q(t)
        ? iu
        : Ge(t)
          ? pa
          : Ri(t)
            ? pu
            : Fe(t)
              ? du
              : Me(t)
                ? Qd
                : on(t)
                  ? su
                  : Ce(t)
                    ? Su
                    : Qe(t)
                      ? vu
                      : zt(t)
                        ? Zd
                        : We(e.get(t.parentid))
                          ? pa
                          : cu;
    }
    shouldSkipChange(e) {
      if (Ne(e)) {
        let t = se(e.codeComponentIdentifier);
        if (zn(t) && t.type === "screen") return !0;
      }
      return !1;
    }
    async ensureSourceCache(e) {
      let t = this.treeStore.getDataTreeOrPartialTree().get(e);
      !t ||
        t.update === t.cache.lastUpdate ||
        (await new Promise((r) => {
          this.scheduler.runBeforeNextFrame(() => {
            (ue(t) && t.update !== t.cache.lastUpdate && this.updateTreeCache(t.id), r());
          });
        }));
    }
    async ensureDependentModulesAreCompatibleWithCollection(e) {
      let t = ye("collection", e.id),
        r = this.modulesStore.getDependentsOfModule(t);
      !r ||
        r.size === 0 ||
        (await Promise.all(
          Array.from(r).map(async (o) => {
            let i = o,
              a = this.modulesStore.getPersistedModuleByLocalId(i);
            if (!(!a || !To(a))) return this.forceComponentUpdate(a.name);
          })
        ));
    }
    async ensureDependentModulesCanRenderCollectionColors(e) {
      let t = ye("collection", e.id),
        r = this.modulesStore.getDependentsOfModule(t);
      if (!r || r.size === 0) return;
      let o = this.modulesStore.forType("collection").getByStableName(e.id);
      o &&
        o.annotations(null, "default")?.framerColorSyntax &&
        (await Promise.all(
          Array.from(r).map(async (i) => {
            let [a, s] = El(i);
            a === "draftCollection" ||
              this.modulesStore.forType(a).getByStableName(s)?.annotations(null, "default")
                ?.framerColorSyntax ||
              (await this.forceComponentUpdate(s));
          })
        ));
    }
    async ensureReferencedCollectionsExportUpToDateEnumUtils(e) {
      let t = new Set();
      for (let r of e.variables) {
        if (r.type !== "controlReference" || r.expectedType !== "enum") continue;
        let o = se(r.entityIdentifier);
        if (!zn(o) || o.type !== "collection") continue;
        let s = this.modulesStore
          .forType("collection")
          .getByStableName(o.localIdName)
          ?.annotations(null, "default")?.framerEnumToDisplayNameUtils;
        (U(s) && s >= 2) || t.add(o.localIdName);
      }
      for (let r of t)
        (ct("ui_interaction", { page: "canvas", id: "regenerate-collection-for-enum-utils" }),
          await this.forceComponentUpdate(r));
    }
    async ensureAllNodesWithLinksHaveLinkedScopesLoaded(e) {
      let t = new Set(),
        r = this.treeStore.getDataTreeOrPartialTree();
      r.beginAllowPartialScopeAccess();
      for (let o of e.walkWithSkipChildren()) {
        if (!Dl(o.node)) continue;
        let i = o.node.getLink();
        if (!Vs(i)) continue;
        let a = r.get(i.webPageId);
        if (!a || a.loaded) continue;
        let s = this.modulesStore
          .forType("screen")
          .getByStableName(i.webPageId)
          ?.annotations(null, "default");
        q(s?.framerScrollSections) && (t.add(a), o.skipChildren());
      }
      (r.endAllowPartialScopeAccess(), await r.loadScopes(Array.from(t)));
    }
  };
((gt = new WeakMap()),
  (ne = new WeakMap()),
  (yt = new WeakMap()),
  (xn = new WeakMap()),
  (dr = new WeakMap()),
  (Ln = new WeakMap()),
  (Dn = new WeakMap()),
  (Pn = new WeakMap()),
  (Vn = new WeakMap()),
  (fe = new WeakSet()),
  (jo = function (e) {
    (I(this, yt)
      .get(e)
      ?.forEach((t) => t()),
      I(this, yt).delete(e));
  }),
  (le = new WeakMap()),
  (ge = new WeakMap()),
  (He = function (e, t, r) {
    if (t) {
      if ((I(this, ge).get(e) === t && I(this, ge).delete(e), r !== void 0)) {
        t.reject(r);
        return;
      }
      t.resolve();
    }
  }),
  (Mu = async function (e, t, r, o) {
    let i = this.treeStore.getDataTreeOrPartialTree();
    if (!Me(e) && !i.has(t.id)) return;
    let a = this.telemetrySession(i, e.id),
      s = a.start("synchronize"),
      l = performance.now();
    (K.debug("Waiting for ModulesStore to be idle", { id: e.id }),
      await this.modulesStore.whenIdle());
    let d = performance.now() - l;
    if (
      (K.debug("Waited", Math.round((d / 1e3) * 100) / 100, "seconds for ModulesStore to be idle", {
        id: e.id,
      }),
      s.end(),
      (i = this.treeStore.getDataTreeOrPartialTree()),
      !i.has(t.id))
    )
      return;
    let f = e.draftOrCurrent();
    if (r !== 2 && this.isSkippedEmptyScopeNode(f)) return;
    let c = a.start("serialize");
    Q(e) && xh(e, this.componentLoader, i, this);
    let u = this.serializerForSourceNode(i, e);
    (I(this, le).get(e.id)?.resolve(), I(this, le).delete(e.id));
    let m = await u(
      e,
      {
        componentLoader: this.componentLoader,
        assetMap: this.assetMap,
        modulesStore: this.modulesStore,
        treeStore: this.treeStore,
        tree: this.treeStore.getDataTreeOrPartialTree(),
        vectorsStore: this.vectorsStore,
        engineChanges: this.engineChanges,
        measureNodes: this.measureNodes,
      },
      {
        serializationId: this.getSerializationId(),
        initialIntrinsicSize: o?.intrinsicSize,
        includeVectorSetSaveIds: o?.includeVectorSetSaveIds,
      }
    );
    return (
      m.forEach(({ source: h, artifacts: g }) => {
        if (
          (K.trace(
            "serializeComponent",
            `
>>>> >>>>
`,
            _(h),
            `
<<<< <<<<`
          ),
          g?.submodules)
        )
          for (let [v, S] of g.submodules)
            K.trace(
              "serializeComponent",
              `
>>>>`,
              v,
              `>>>>
`,
              S,
              `
<<<<`,
              v,
              "<<<<"
            );
      }),
      c.end(m),
      m
    );
  }));
function xh(n, e, t, r) {
  let o = n.instanceIdentifier,
    i = e.dataForIdentifier(o);
  if (!i || !Object.values(i.properties).some((l) => l?.type === "image")) return;
  let s = new Set();
  for (let l of t.root.children) {
    if (!ue(l)) continue;
    if (D(l) && l.dataIdentifier === o) {
      s.add(l);
      continue;
    }
    l.loaded?.some((f) => rs(f) && f.dataIdentifier === o) && s.add(l);
  }
  s.forEach((l) => {
    r.forceComponentUpdate(l.id).catch(Oa);
  });
}
function nt(n, e) {
  return `${n ?? ""}.${e}`;
}
function Jt(n, e) {
  return `${n ?? ""}[${e}]`;
}
function Sa(n) {
  let e = [],
    t = n.split(".");
  for (let r of t) {
    let [o, i] = r.split("[");
    if ((o.length !== 0 && e.push({ type: "object", key: o }), i)) {
      let a = i.slice(0, -1);
      (y(a.length > 0), e.push({ type: "array", id: a }));
    }
  }
  return e;
}
function ie(n, e = !1) {
  let t = (e ? _n(n) : n).trim();
  return t === "" ? 0 : t.split(/\W+\b/).length;
}
function cr(n) {
  return he(n) || D(n) || te(n);
}
function vx(n) {
  return cr(n) || Q(n) || Ft(n) || Me(n) || st(n);
}
function Fu(n) {
  return `${n ?? "Field"} is required`;
}
function zu(n, e) {
  return `Exceeded max length by ${Jr(n - e, "character", "characters")}`;
}
function Nx(n, e) {
  let t = Wi(n);
  return `Must have at least ${ku(e, t)}`;
}
function Rx(n, e) {
  let t = Wi(n);
  return `Must have at most ${ku(e, t)}`;
}
var wx = "Slug value is already taken and must be unique";
function ku(n, e) {
  return e ? Jr(n, "image", "images") : Jr(n, "item", "items");
}
function ba(n, e) {
  let t = ks(e).getRouteSegmentIdToRawPathMap(e).get(n.id);
  return t ? Ot(t) : "Unknown";
}
function Lh(n, e, t) {
  if (n.id === e.root.homePageNodeId) return "Home";
  let o = At(e, n);
  if (!o) return "Unknown";
  let i = n.dataIdentifier;
  if (!i) return Ot(o);
  let a = t.dataForIdentifier(i);
  return a ? Ot(Bl(a, o)) : Ot(o);
}
function Dh(n, e) {
  let t = e.getParent(n.id);
  return Q(t) ? (co(t, n) ?? "Unknown") : "Unknown";
}
function kx(n, e, t) {
  if (Me(n)) return "Site";
  if (st(n)) return ba(n, e);
  if (Q(n)) return n.resolveValue("name") || "Unknown";
  if (Ft(n)) return Dh(n, e) || "Unknown";
  if (D(n)) return Lh(n, e, t);
  if (he(n)) return n.resolveValue("name") || "Unknown";
  if (te(n)) return n.resolveValue("name") || "Unknown";
  ee(n, "Invalid localization scope");
}
function Ou(n) {
  let e = sn(n);
  for (let t of e.querySelectorAll("*"))
    t.tagName !== "BR" &&
      (t.removeAttribute("class"),
      t.removeAttribute("style"),
      t.removeAttribute("data-styles-preset"));
  return e.innerHTML;
}
var Na = "framer-module[data-module-identifier]",
  Au = "framer-module-translation-text",
  Ho = "framer-module-translation-richtext",
  _u = "data-module-control-key",
  $u = "data-module-control-ancestor-path",
  Uu = "data-module-control-storage-type",
  Ph = nl(rl),
  qt = class extends Error {
    constructor() {
      super(...arguments);
      C(this, "name", "RichTextModuleTranslationError");
    }
  };
function tL(n) {
  return n instanceof qt;
}
function Wu(n, e) {
  let r = sn(n).querySelectorAll(Na);
  if (r.length === 0) return "";
  let o = [];
  for (let i of r) {
    let a = i.getAttribute("data-module-identifier");
    if (!L(a)) continue;
    let s = i.getAttribute("data-module-props");
    if (!L(s)) continue;
    let l;
    try {
      l = JSON.parse(s);
    } catch {
      continue;
    }
    if (!l) continue;
    let d = Ra(a, e);
    if (!d) continue;
    let f = jn(d, l),
      c = [];
    (ju(f, d, c, { suppressGeneratedIds: !0 }), c.length !== 0 && (c.sort(Vh), o.push(c)));
  }
  return o.length === 0 ? "" : ce(o);
}
function Vh(n, e) {
  return (
    Go(n.controlPropKey, e.controlPropKey) ||
    Go(n.ancestorControlKeyPath, e.ancestorControlKeyPath) ||
    Go(n.tagName, e.tagName) ||
    Go(n.storageType, e.storageType)
  );
}
function Go(n, e) {
  let t = n ?? "",
    r = e ?? "";
  return t < r ? -1 : t > r ? 1 : 0;
}
function nL(n, e) {
  let t = sn(n),
    r = t.querySelectorAll(Na);
  for (let o of r) {
    let i = o.getAttribute("data-module-identifier");
    if (!L(i)) continue;
    let a = o.getAttribute("data-module-props");
    if (!L(a)) continue;
    let s;
    try {
      s = JSON.parse(a);
    } catch {
      continue;
    }
    if (!s) continue;
    let l = Ra(i, e);
    if (!l) continue;
    let d = jn(l, s),
      f = [];
    ju(d, l, f);
    for (let c of f) {
      let u = t.ownerDocument.createElement(c.tagName);
      (u.setAttribute(_u, c.controlPropKey),
        c.ancestorControlKeyPath && u.setAttribute($u, c.ancestorControlKeyPath),
        c.storageType && u.setAttribute(Uu, c.storageType),
        c.tagName === Ho ? (u.innerHTML = c.value) : (u.textContent = c.value),
        o.append(u));
    }
  }
  return t.innerHTML;
}
function rL(n, e, { strict: t = !0 } = {}) {
  let r = sn(n),
    o = r.querySelectorAll(Na);
  for (let i of o) {
    let a = i.getAttribute("data-module-identifier");
    if (!L(a)) continue;
    let s = i.getAttribute("data-module-props");
    if (!L(s)) continue;
    let l = Array.from(i.querySelectorAll(`${Au}, ${Ho}`));
    if (l.length === 0) continue;
    let d;
    try {
      d = JSON.parse(s);
    } catch {
      if (!t) continue;
      throw new qt("Translated module props are no longer valid JSON.");
    }
    if (!d || !Ra(a, e)) continue;
    let c = d;
    for (let u of l) {
      if (u.parentElement !== i) {
        if (!t) continue;
        throw new qt(
          "Translated module controls must remain direct children of the module element."
        );
      }
      let m = u.getAttribute(_u);
      if (!L(m) || m.length === 0) {
        if (!t) continue;
        throw new qt("Translated module control key is missing or invalid.");
      }
      let h = u.getAttribute($u) ?? void 0;
      if (u.tagName.toLowerCase() === Ho) {
        let S = Mh(u.getAttribute(Uu));
        if (!S) {
          if (!t) continue;
          throw new qt("Translated rich text module control storage type is missing or invalid.");
        }
        c = Bu(c, m, h, Fh(u.innerHTML, S), "richtext");
      } else c = Bu(c, m, h, u.textContent, "string");
      u.remove();
    }
    c !== d && i.setAttribute("data-module-props", JSON.stringify(c));
  }
  return r.innerHTML;
}
function ju(n, e, t, { suppressGeneratedIds: r = !1 } = {}) {
  for (let [o, i] of Object.entries(n)) {
    if (!i) continue;
    let a = e[o];
    a && Ta(o, i, a, void 0, t, r);
  }
}
function Ta(n, e, t, r, o, i) {
  switch (t.type) {
    case "array": {
      if (!Bs(e)) return;
      for (let a of e.value) Ar(a) && Ta(n, a, t.control, Eh(r, a.id, i), o, i);
      return;
    }
    case "object": {
      if (e.type !== "object" || !$e(e.value)) return;
      for (let [a, s] of Object.entries(e.value)) {
        let l = t.controls[a];
        l && Ta(n, s, l, nt(r, a), o, i);
      }
      return;
    }
    case "string": {
      if (t.preventLocalization || e.type !== "string" || !L(e.value) || e.value.length === 0)
        return;
      o.push({
        controlPropKey: n,
        ancestorControlKeyPath: r,
        tagName: Au,
        storageType: void 0,
        value: e.value,
      });
      return;
    }
    case "richtext": {
      if (t.preventLocalization || e.type !== "richtext" || !It(e.value)) return;
      let a = typeof e.value == "string" ? "html" : "json",
        s = typeof e.value == "string" ? e.value : Rd(e.value, Ph);
      if (s.length === 0) return;
      o.push({
        controlPropKey: n,
        ancestorControlKeyPath: r,
        tagName: Ho,
        storageType: a,
        value: s,
      });
      return;
    }
    default:
      return;
  }
}
function Eh(n, e, t) {
  return t ? `${n ?? ""}[]` : Jt(n, e);
}
function Mh(n) {
  return n === "html" || n === "json" ? n : null;
}
function Ra(n, e) {
  let t = e.reactComponentForIdentifier(n);
  return t ? zd(Zr(t, "onlyNonPresets")) : null;
}
function Fh(n, e) {
  return e === "json" ? wd(n, "collectionControlPropLocalized") : n;
}
function Bu(n, e, t, r, o) {
  let i = n[e];
  if (!i) {
    if (!t) return { ...n, [e]: { type: o, value: r } };
    let s = Sa(t);
    return { ...n, [e]: Jo(s, o, r) };
  }
  let a = zh(i, t, r, o);
  return a === i ? n : { ...n, [e]: a };
}
function Jo(n, e, t) {
  if (n.length === 0) return { type: e, value: t };
  let [r, ...o] = n;
  y(r);
  let i = Jo(o, e, t);
  return r.type === "array"
    ? { type: "array", value: [{ ...i, id: r.id }] }
    : { type: "object", value: { [r.key]: i } };
}
function zh(n, e, t, r) {
  return e ? Ia(n, Sa(e), t, r) : Gu(n, t, r);
}
function Ia(n, e, t, r) {
  let [o, ...i] = e;
  if (!o) return Gu(n, t, r);
  if (o.type === "array") {
    if ((n.type !== "array" && n.type !== null) || !Array.isArray(n.value)) return n;
    let l = n.value,
      d = !1,
      f = !1,
      c = Dd(l, (u) => {
        if (!kt(u) || !Ar(u) || u.id !== o.id) return u;
        d = !0;
        let m = Ia(u, i, t, r);
        return ((f ||= m !== u), m);
      });
    if (!d && l.some((u) => kt(u) && !Ar(u))) return n;
    if (!d) {
      let u = Jo(i, r, t);
      return { ...n, value: [...l, { ...u, id: o.id }] };
    }
    return f ? { ...n, value: c } : n;
  }
  if ((n.type !== "object" && n.type !== null) || !$e(n.value)) return n;
  let a = n.value[o.key];
  if (!kt(a)) {
    let l = Jo(i, r, t);
    return { ...n, value: { ...n.value, [o.key]: l } };
  }
  let s = Ia(a, i, t, r);
  return s === a ? n : { ...n, value: { ...n.value, [o.key]: s } };
}
function Gu(n, e, t) {
  return n.type !== t && n.type !== null
    ? n
    : t === "string"
      ? typeof e == "string" && n.value !== e
        ? { ...n, type: t, value: e }
        : n
      : n.value !== e
        ? { ...n, type: t, value: e }
        : n;
}
function dD(n) {
  return n === "hidden" ? null : n;
}
var cD = ["new", "needsReview", "done", "hidden"],
  kh = "controlPropKey";
function uD(n) {
  return kh in n;
}
function fD(n) {
  return "groups" in n;
}
var Oh = "collectionNodeId";
function pD(n) {
  return Oh in n;
}
var Bh = { string: !0, richtext: !0, image: !0, enum: !0, link: !0, slug: !0 };
function Ah(n) {
  return n in Bh;
}
function qu(n) {
  return je(n) && Ah(n.type);
}
var _h = { string: !0, richtext: !0, image: !0, link: !0 };
function Ku(n) {
  return n in _h;
}
function $h(n) {
  return n
    ? n.type === "richtext"
      ? !0
      : n.type === "RichTextNode"
        ? n.textType === "rich-text"
        : !1
    : !1;
}
function mD(n) {
  return n?.type === "richtext";
}
function Uh(n) {
  return n.type === "richtext" ? typeof n.richTextValue == "string" : n.type === "RichTextNode";
}
function Wh(n) {
  return $h(n) && Uh(n);
}
function hD(n) {
  return n ? n.type === "image" || n.type === "FrameNode" : !1;
}
function gD(n) {
  return n ? n.type === "nodeLink" || n.type === "link" : !1;
}
function yD(n) {
  return n ? n.type === "RootNode" || n.type === "WebPageNode" : !1;
}
function CD(n) {
  return n?.type === "slug" || jh(n);
}
function jh(n) {
  return n?.type === "RouteSegmentNode";
}
function Gh(n) {
  return wr(n) || n === "" || Xn(n);
}
function vD(n, e) {
  if (!n || Wh(n)) return !1;
  if (
    n.value.includes(`
`)
  )
    return !0;
  let t = n.localizedValues[e.id];
  return Br(t)
    ? !!di(t, { preserveNewLines: !0 }).includes(`
`)
    : !1;
}
function Qu(n, e, t) {
  return e ? (e.hash !== t || e.needsReview ? "needsReview" : "done") : Gh(n) ? "done" : "new";
}
function Z(n, e, t, r, o, i) {
  return Object.fromEntries(
    e.map((a) => {
      let s = t[a.id];
      if (o?.[a.id]) return [a.id, "warning"];
      if (i) {
        let l = i(a);
        if (l) return [a.id, l];
      }
      return [a.id, Qu(n, s, r)];
    })
  );
}
function La(n) {
  return (e) => (n[e.id] ? null : "done");
}
function Da(n, e, t) {
  return (r) => {
    if (L(n) && e) {
      let o = e[r.id];
      if (o) return Qu(n, o, t);
    }
    return null;
  };
}
var W = {};
Object.freeze(W);
function Hh(n, e, t, r, o, i, a) {
  let s = (l, d, f) => {
    if (tn(d) || !L(d) || (d.length === 0 && !f)) return;
    let c = M(d),
      u = f ?? W,
      m = Z(d, i, u, c);
    a.push({
      type: n.name,
      transformId: n.id,
      keyPath: nt(e, l),
      nodeId: o.id,
      scopeId: r.id,
      property: l,
      value: d,
      controlPropKey: t,
      controlPropName: void 0,
      ancestorControlKeyPath: void 0,
      valueWordCount: ie(d),
      hash: c,
      localizedValues: u,
      localizedValueStatuses: m,
      managedByPluginButUserEditable: !1,
    });
  };
  (s("truthy", n.truthy, n.truthyLocalized),
    s("falsy", n.falsy, n.falsyLocalized),
    n.fallback && s("fallback", n.fallback.value, n.fallback.valueLocalized));
}
function Jh(n, e, t, r, o, i, a, s) {
  if (e.outputType !== "string") return;
  for (let c of e.cases) {
    if (ts(n) && n.startValue.id === fl) break;
    if (tn(c.to) || !L(c.to) || (c.to.length === 0 && !c.toLocalized)) continue;
    let u = M(c.to),
      m = c.toLocalized ?? W,
      h = Z(c.to, a, m, u),
      g = nt(t, "cases");
    s.push({
      type: "convertFromEnum",
      transformId: e.id,
      keyPath: Jt(g, c.id),
      nodeId: i.id,
      scopeId: o.id,
      caseId: c.id,
      isDefaultCase: !1,
      value: c.to,
      ancestorControlKeyPath: void 0,
      controlPropKey: r,
      controlPropName: void 0,
      valueWordCount: ie(c.to),
      hash: u,
      localizedValues: m,
      localizedValueStatuses: h,
      managedByPluginButUserEditable: !1,
    });
  }
  if (!e.default || tn(e.default) || !L(e.default)) return;
  let l = M(e.default),
    d = e.defaultLocalized ?? W,
    f = Z(e.default, a, d, l);
  s.push({
    type: "convertFromEnum",
    transformId: e.id,
    keyPath: nt(t, "default"),
    nodeId: i.id,
    scopeId: o.id,
    caseId: null,
    isDefaultCase: !0,
    ancestorControlKeyPath: void 0,
    value: e.default,
    controlPropKey: r,
    controlPropName: void 0,
    valueWordCount: ie(e.default),
    hash: l,
    localizedValues: d,
    localizedValueStatuses: f,
    managedByPluginButUserEditable: !1,
  });
}
function Ko(n, e, t, r, o, i, a) {
  if (!(!es(t.outputType) || t.outputType !== "string")) {
    for (let s of t.transforms)
      if (Za(s))
        switch (s.name) {
          case "convertFromEnum": {
            Jh(t, s, r, o, n, e, i, a);
            break;
          }
          case "convertFromBoolean": {
            Hh(s, r, o, n, e, i, a);
            break;
          }
          case "prefix":
          case "suffix": {
            if (tn(s.value) || (s.value.length === 0 && !s.valueLocalized)) return;
            let l = M(s.value),
              d = s.valueLocalized ?? W,
              f = Z(s.value, i, d, l);
            a.push({
              type: s.name,
              transformId: s.id,
              keyPath: Jt(r, s.id),
              controlPropKey: o,
              controlPropName: void 0,
              ancestorControlKeyPath: void 0,
              nodeId: e.id,
              scopeId: n.id,
              value: s.value,
              valueWordCount: ie(s.value),
              hash: l,
              localizedValues: d,
              localizedValueStatuses: f,
              managedByPluginButUserEditable: !1,
            });
            continue;
          }
          default:
        }
  }
}
function qh(n, e, t, r, o, i) {
  if (Lr(t.textContent)) {
    if (!we(n, t, "textContent")) return;
    Ko(e, t, t.textContent, t.id, t.id, r, o);
    return;
  }
  if (at(t.htmlContent) || at(t.textContent)) return;
  let a = we(n, t, "html"),
    s = we(n, t, "htmlLocalized");
  if (!a && !s) return;
  let l = t.html,
    d = _n(l),
    f = Ad(l),
    c = i === "canvasHtmlAsTextContentIfPossible" ? f : "rich-text",
    u = c === "text" ? d : Ou(l);
  if (c === "text" && !s && Dr(t)) {
    let S = n.getNode(t.originalid);
    if (S && _n(S.html) === d) return;
  }
  let h = M(f === c ? u : d),
    g = t.htmlLocalized ?? W,
    v = Z(u, r, g, h);
  o.push({
    type: "RichTextNode",
    keyPath: Kh(t.id),
    nodeId: t.id,
    scopeId: e.id,
    value: u,
    valueWordCount: ie(u, !0),
    textType: c,
    hash: h,
    localizedValues: g,
    localizedValueStatuses: v,
    managedByPluginButUserEditable: !1,
    inherited: !s,
  });
}
function Kh(n) {
  return `${n}_html`;
}
function Hu(n) {
  if (Me(n)) return "RootNode";
  if (D(n)) return "WebPageNode";
  ee(n);
}
function Ju(n, e, t, r) {
  let o = {};
  for (let i of e) {
    let a = t[i.id];
    if (!a || !L(a.value) || !a.value) continue;
    let s = ql({ collectionData: r, componentLoader: n, type: "id", value: a.value });
    s && (o[i.id] = s);
  }
  return o;
}
function Yu(n, e, t, r, o, i) {
  let a = null;
  if (
    (D(r) && (a = e.dataForIdentifier(r.dataIdentifier)), r.webMetadata?.title || r.titleLocalized)
  ) {
    let s = r.webMetadata?.title ?? n.root.webMetadata?.title ?? "",
      l = M(s),
      d = r.titleLocalized ?? W,
      f = Ju(e, o, d, a),
      c = Z(s, o, d, l, f);
    i.push({
      type: Hu(r),
      keyPath: `${r.id}_title`,
      nodeId: r.id,
      scopeId: t.id,
      value: s,
      valueWordCount: ie(s),
      hash: l,
      localizedValues: d,
      localizedValueStatuses: c,
      managedByPluginButUserEditable: !1,
      key: "title",
      dataIdentifier: ii(r) ? r.dataIdentifier : void 0,
      warnings: f,
    });
  }
  if (r.webMetadata?.description || r.descriptionLocalized) {
    let s = r.webMetadata?.description ?? n.root.webMetadata?.description ?? "",
      l = M(s),
      d = r.descriptionLocalized ?? W,
      f = Ju(e, o, d, a),
      c = Z(s, o, d, l, f);
    i.push({
      type: Hu(r),
      keyPath: `${r.id}_description`,
      nodeId: r.id,
      scopeId: t.id,
      value: s,
      valueWordCount: ie(s),
      hash: l,
      localizedValues: d,
      localizedValueStatuses: c,
      managedByPluginButUserEditable: !1,
      key: "description",
      dataIdentifier: ii(r) ? r.dataIdentifier : void 0,
      warnings: f,
    });
  }
}
function Qh(n) {
  return `${n}_fillImageLocalized`;
}
function Yh(n, e, t, r, o) {
  if (!Ys(t) || !L(t.fillImage) || t.fillImage.length === 0) return;
  let i = we(n, t, "fillImage"),
    a = we(n, t, "fillImageLocalized"),
    s = we(n, t, "altAttribute"),
    l = we(n, t, "altAttributeLocalized");
  if (!i && !a && !s && !l) return;
  let d = t.altAttribute ?? Xr,
    f = M(d),
    c = t.altAttributeLocalized ?? W,
    u = t.fillImage,
    m = M(u),
    h = t.fillImageLocalized ?? W,
    g = Z(d, r, c, f, void 0, Da(u, h, m));
  o.push({
    type: "FrameNode",
    keyPath: Qh(t.id),
    nodeId: t.id,
    scopeId: e.id,
    value: d,
    valueWordCount: ie(d),
    hash: f,
    localizedValues: c,
    localizedValueStatuses: g,
    managedByPluginButUserEditable: !1,
    inherited: !l,
    supportsAlt: !0,
    image: u,
    imageHash: m,
    imageLocalizedValues: h,
    imageInherited: !a,
    positionX: si(t.fillImagePositionX) ? t.fillImagePositionX : void 0,
    positionY: si(t.fillImagePositionY) ? t.fillImagePositionY : void 0,
    supportsFocalPoint: !0,
  });
}
function Xu(n, e, t, r) {
  if (!Ue.isOn("localizedLinks")) return;
  let o = e.getLink();
  if (!Or(o)) return;
  let i = o.url,
    a = M(i),
    s = e.linkLocalized ?? W,
    l = Z(i, t, s, a, void 0, La(s));
  r.push({
    type: "nodeLink",
    keyPath: `${e.id}_linkLocalized`,
    nodeId: e.id,
    scopeId: n.id,
    value: i,
    valueWordCount: i ? 1 : 0,
    hash: a,
    localizedValues: s,
    localizedValueStatuses: l,
    managedByPluginButUserEditable: !1,
    aiTranslationDisabled: !0,
  });
}
function Xh(n, e, t, r, o) {
  (Yh(n, e, t, r, o), Xu(e, t, r, o));
}
function Qo(n) {
  return !!(Ku(n) || n === "array" || n === "object");
}
function qo(n, e, t) {
  return `${n}.${e}.${t ?? ""}`;
}
function Zh(n) {
  if (D(n) && Ls(n)) return n.layoutTemplateIdentifier;
  if (Ne(n)) return n.codeComponentIdentifier;
}
function Zu(n, e) {
  let t = xd(n);
  if (typeof n != "string" || !n.includes("data-module-identifier")) return t;
  let r = Wu(n, e);
  return r.length === 0 ? t : M(`${t}:${r}`);
}
function eg(n, e, t, r, o, i) {
  let a = Zh(r);
  if (!a) return;
  let s = e.componentForIdentifier(a);
  if (!s) return;
  let l = r.getControlProps(s.properties);
  function d(f, c, u, m) {
    let h = c.type;
    if (h === "array" && Ja(c.value)) {
      if (u.type !== "array") return;
      let g = u.control;
      for (let v of c.value) {
        if (!Qo(v.type)) continue;
        let S = Jt(m, v.id);
        d(f, v, g, S);
      }
    }
    if (h === "object" && $e(c.value)) {
      if (u.type !== "object") return;
      for (let g in c.value) {
        let v = c.value[g];
        if (!v || !Qo(v.type)) continue;
        let S = nt(m, g),
          b = u.controls[g];
        b && d(f, v, b, S);
      }
    }
    if (Ku(h))
      switch (h) {
        case "string": {
          if (
            !we(n, r, Bn(f)) ||
            (u.type === "string" && (u.preventLocalization || (u.optional && it(c.value)))) ||
            (Lr(c.value) && Ko(t, r, c.value, nt(r.id, f), f, o, i), !L(c.value))
          )
            break;
          let g = c.value,
            v = M(g),
            S = c.valueLocalized ?? W,
            b = Z(g, o, S, v);
          i.push({
            type: "string",
            keyPath: qo(r.id, f, m),
            controlPropKey: f,
            controlPropName: u.title || kn(f),
            ancestorControlKeyPath: m,
            nodeId: r.id,
            scopeId: t.id,
            value: g,
            valueWordCount: ie(g),
            hash: v,
            localizedValues: S,
            localizedValueStatuses: b,
            managedByPluginButUserEditable: !1,
          });
          break;
        }
        case "link": {
          if (
            !Ue.isOn("localizedLinks") ||
            !we(n, r, Bn(f)) ||
            (Lr(c.value) && Ko(t, r, c.value, nt(r.id, f), f, o, i), !Or(c.value))
          )
            break;
          let g = c.value.url,
            v = M(g),
            S = c.valueLocalized ?? W,
            b = Z(g, o, S, v, void 0, La(S));
          i.push({
            type: "link",
            keyPath: qo(r.id, f, m),
            controlPropKey: f,
            controlPropName: u.title || kn(f),
            ancestorControlKeyPath: m,
            nodeId: r.id,
            scopeId: t.id,
            value: g,
            valueWordCount: g ? 1 : 0,
            hash: v,
            localizedValues: S,
            localizedValueStatuses: b,
            managedByPluginButUserEditable: !1,
            aiTranslationDisabled: !0,
          });
          break;
        }
        case "richtext": {
          if (
            h !== "richtext" ||
            !It(c.value) ||
            (u.type === "richtext" && u.preventLocalization) ||
            !we(n, r, Bn(f))
          )
            break;
          let g = c.value,
            v = typeof g == "string" ? g : Oi(g),
            S = Zu(g, e),
            b = c.valueLocalized ?? W,
            w = Z(v, o, b, S);
          i.push({
            type: "richtext",
            keyPath: qo(r.id, f, m),
            controlPropKey: f,
            controlPropName: u.title || kn(f),
            ancestorControlKeyPath: m,
            nodeId: r.id,
            scopeId: t.id,
            richTextValue: g,
            value: v,
            valueWordCount: ie(v, typeof g == "string"),
            hash: S,
            localizedValues: b,
            localizedValueStatuses: w,
            managedByPluginButUserEditable: !1,
            contentType: void 0,
          });
          break;
        }
        case "image": {
          if (
            !c ||
            c.type !== "image" ||
            !L(c.value) ||
            (u.type !== "responsiveimage" && u.type !== "image") ||
            !we(n, r, Bn(f))
          )
            break;
          let g = u.type === "responsiveimage",
            v = c.alt ?? Xr,
            S = M(v),
            b = g ? (c.altLocalized ?? W) : W,
            w = c.value,
            P = M(w),
            V = c.valueLocalized ?? W,
            x = Z(v, o, b, S, void 0, Da(w, V, P)),
            R = u.type === "responsiveimage";
          i.push({
            type: "image",
            keyPath: qo(r.id, f, m),
            controlPropKey: f,
            controlPropName: u.title || kn(f),
            ancestorControlKeyPath: m,
            nodeId: r.id,
            scopeId: t.id,
            value: v,
            valueWordCount: ie(v),
            hash: S,
            localizedValues: b,
            localizedValueStatuses: x,
            managedByPluginButUserEditable: !1,
            supportsAlt: g,
            image: w,
            imageHash: P,
            imageLocalizedValues: V,
            supportsFocalPoint: R,
            positionX: R ? c.positionX : void 0,
            positionY: R ? c.positionY : void 0,
          });
          break;
        }
        default:
      }
  }
  for (let f in l) {
    let c = l[f];
    if (!c) continue;
    let u = s.properties[f];
    u && d(f, c, u);
  }
}
function tg(n, e, t, r) {
  switch (e.bindingType) {
    case "form": {
      let o = e.formSelectOptions;
      if (!o || o.length === 0) return;
      for (let i of o) {
        let a = i.title;
        if (!a) continue;
        let s = i.titleLocalized ?? W,
          l = M(a),
          d = Z(a, t, s, l);
        r.push({
          type: "FormSelectNode",
          keyPath: `${e.id}_${i.id}`,
          nodeId: e.id,
          optionId: i.id,
          scopeId: n.id,
          value: a,
          valueWordCount: ie(a),
          hash: l,
          localizedValues: s,
          localizedValueStatuses: d,
          managedByPluginButUserEditable: !1,
        });
      }
      break;
    }
    case "variable": {
      if (!e.variableBinding || !ue(n) || !Ur(n)) return;
      let o = n.getVariable(e.variableBinding.id);
      if (!o) return;
      (ln(o, ho(n)) &&
        r.push(wa(e, n, t, "allItemsLabel", e.allItemsLabelWithDefault, e.allItemsLabelLocalized)),
        o.type === "boolean" &&
          (r.push(
            wa(
              e,
              n,
              t,
              "booleanTrueLabel",
              e.booleanTrueLabelWithDefault,
              e.booleanTrueLabelLocalized
            )
          ),
          r.push(
            wa(
              e,
              n,
              t,
              "booleanFalseLabel",
              e.booleanFalseLabelWithDefault,
              e.booleanFalseLabelLocalized
            )
          )));
      break;
    }
    default:
      ee(e.bindingType);
  }
}
function wa(n, e, t, r, o, i) {
  let a = i ?? W,
    s = M(o),
    l = Z(o, t, a, s);
  return {
    type: "formSelectGenericOption",
    keyPath: `${n.id}_${r}`,
    nodeId: n.id,
    key: r,
    scopeId: e.id,
    value: o,
    valueWordCount: ie(o),
    hash: s,
    localizedValues: a,
    localizedValueStatuses: l,
    managedByPluginButUserEditable: !1,
  };
}
function ng(n, e, t, r, o) {
  let i = `${t.id}_placeholder`;
  if (at(t.formInputPlaceholder)) {
    if (tn(t.formInputPlaceholder) || !we(n, t, "formInputPlaceholder")) return;
    Ko(e, t, t.formInputPlaceholder, i, t.id, r, o);
    return;
  }
  if (!t.formInputPlaceholder || !we(n, t, "formInputPlaceholder")) return;
  let a = M(t.formInputPlaceholder),
    s = t.formInputPlaceholderLocalized ?? W;
  o.push({
    keyPath: i,
    type: "FormPlainTextInputNode",
    nodeId: t.id,
    scopeId: e.id,
    value: t.formInputPlaceholder,
    valueWordCount: ie(t.formInputPlaceholder),
    hash: M(t.formInputPlaceholder),
    localizedValues: s,
    localizedValueStatuses: Z(t.formInputPlaceholder, r, s, a),
    managedByPluginButUserEditable: !1,
  });
}
function ef(n, e, t, r, o, i, a, s) {
  (s?.(),
    D(r) && Yu(n, e, t, r, o, i),
    os(r) && qh(n, t, r, o, i, a),
    (Ne(r) || D(r)) && eg(n, e, t, r, o, i),
    rn(r) && Xh(n, t, r, o, i),
    (is(r) || as(r)) && Xu(t, r, o, i),
    ss(r) && ng(n, t, r, o, i),
    ls(r) && tg(t, r, o, i));
}
var tf = (n, e) => (e ? `${n.update}-${e.update}` : n.update.toString());
function nf(n) {
  return n !== "canvasHtmlAsHtml";
}
function Yo(n, e) {
  return Array.from(e, (t) => [t, n.getDefinitionHash(t)]);
}
var rg = [];
function fr(n, e, t, r, o, i, a, s, l) {
  if (!nf(l) || e.mutable || t?.mutable) return;
  let d = e.cache.localizationSourceGroup;
  if (
    d &&
    !(
      d.update !== tf(e, t) ||
      d.locales !== r ||
      d.parentVariables !== i ||
      !Tt(d.subGroups, a) ||
      !Tt(d.settings, s)
    )
  ) {
    for (let [f, c] of d.loaderDependencies) if (o.getDefinitionHash(f) !== c) return;
    if (!(Gn(n, e) && d.includedLocales !== e.includedLocales)) return d;
  }
}
function pr(n, e, t, r, o, i, a, s, l, d, f) {
  nf(d) &&
    (e.mutable ||
      t?.mutable ||
      (e.cache.localizationSourceGroup = {
        update: tf(e, t),
        locales: o,
        loaderDependencies: i,
        parentVariables: a,
        group: r,
        includedLocales: Gn(n, e) ? e.includedLocales : void 0,
        subGroups: s,
        settings: l,
        includesWebPage: f,
      }));
}
function rf(n, e, t, r, o, i, a) {
  a?.();
  let s = D(t) && t.abTestingParentId !== void 0;
  if (Xo(n, t) && !s) return;
  let l = fr(n, t, void 0, r, e, lt, void 0, void 0, i);
  if (l) {
    l.group && o.push(l.group);
    return;
  }
  let d = [],
    f,
    c = new Set();
  (e.trackEntityReads(c, () => {
    for (let h of t.walk()) ef(n, e, t, h, r, d, i, a);
    if (d.length === 0) return;
    let u = D(t) ? Qn(n, e, t) : (t.resolveValue("name") ?? "Unknown");
    s && (u = `${u} \xB7 ${t.resolveValue("name") || dt(e, t)}`);
    let m = !s && Gn(n, t);
    ((f = {
      type: "sourceGroup",
      name: u,
      groupType: D(t) ? "page" : te(t) ? "template" : "component",
      nodeId: t.id,
      supportsLocaleVisibility: m,
      includedLocaleIds: m ? vn(n, t) : void 0,
      items: d,
    }),
      o.push(f));
  }),
    pr(n, t, void 0, f, r, Yo(e, c), lt, void 0, void 0, i));
}
function ur(n, e, t) {
  return t ? `${n}.${e}.${t}` : `${n}.${e}`;
}
function og(n) {
  return di(n).length;
}
function xa(n, e, t) {
  let r = t.required,
    o = t.type === "link" ? void 0 : t.options?.maxLength;
  if (!r && !U(o)) return;
  let i = {};
  for (let a of n) {
    let s = e[a.id];
    if (!Br(s)) continue;
    let l = og(s);
    r && l === 0 ? (i[a.id] = Fu(t.name)) : U(o) && l > o && (i[a.id] = zu(l, o));
  }
  return i;
}
function of(n, e, t, r) {
  let o = {};
  for (let i of n) {
    let a = e[i.id];
    if (!Br(a) || !L(a.value)) continue;
    let l = t[i.id]?.get(a.value);
    l && l > 1 && (o[i.id] = r);
  }
  return o;
}
function ig(n, e, t, r, o, i, a, s, l, d) {
  let f = a.type;
  switch (f) {
    case "string": {
      if (dn(a)) return;
      let c;
      if (l) {
        if (l.type !== f || !L(l.value)) return;
        c = l.value;
      } else {
        if (qs(a)) return;
        c = a.initialValue;
      }
      let u = M(c),
        m = l?.valueLocalized ?? W,
        h = xa(o, m, a),
        g = Z(c, o, m, u, h);
      return {
        type: "string",
        keyPath: ur(r.id, s, i),
        nodeId: r.id,
        scopeId: t,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        value: c,
        valueWordCount: ie(c),
        hash: u,
        localizedValues: m,
        localizedValueStatuses: g,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: h,
      };
    }
    case "link": {
      if (!l || l.type !== f || !Or(l.value) || !Ue.isOn("localizedLinks")) return;
      let c = l.value.url,
        u = M(c),
        m = l?.valueLocalized ?? W,
        h = xa(o, m, a),
        g = Z(c, o, m, u, h);
      return {
        type: "link",
        keyPath: ur(r.id, s, i),
        nodeId: r.id,
        scopeId: t,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        value: c,
        valueWordCount: c ? 1 : 0,
        hash: u,
        localizedValues: m,
        localizedValueStatuses: g,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: h,
        aiTranslationDisabled: !0,
      };
    }
    case "slug": {
      if (n.root.translatePagePaths === "no" || !l) return;
      (y(l.type === "string", "expected slug controlType to be a string"),
        y(L(l.value), "expected slug value to be a string"));
      let c = l.value,
        u = M(c),
        m = l?.valueLocalized ?? W,
        h = of(o, m, d, "Each item in the Collection must have a unique slug"),
        g = Z(c, o, m, u, h, n.root.translatePagePaths === "legacy" ? La(m) : void 0);
      return {
        type: "slug",
        keyPath: ur(r.id, s, i),
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        nodeId: r.id,
        scopeId: t,
        value: c,
        valueWordCount: ie(c),
        hash: u,
        localizedValues: m,
        localizedValueStatuses: g,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: h,
        aiTranslationDisabled: !n.root.translatePagePathsWithAI,
        collectionItemIsDraft: r.isDraft ?? !1,
      };
    }
    case "richtext": {
      let c;
      if (dn(a)) return;
      if (l) {
        if (l.type !== f || !It(l.value)) return;
        c = l.value;
      } else c = a.initialValue;
      let u = typeof c == "string" ? c : Oi(c),
        m = Zu(c, e),
        h = l?.valueLocalized ?? W,
        g = xa(o, h, a),
        v = Z(u, o, h, m, g);
      return {
        type: "richtext",
        keyPath: ur(r.id, s, i),
        nodeId: r.id,
        scopeId: t,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        richTextValue: c,
        value: u,
        valueWordCount: ie(u, typeof c == "string"),
        hash: m,
        localizedValues: h,
        localizedValueStatuses: v,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        warnings: g,
        contentType: a.type === "richtext" ? a.contentType : void 0,
      };
    }
    case "image": {
      let c;
      if (l) {
        if (l.type !== f || !L(l.value)) return;
        c = l.value;
      } else {
        if (!L(a.initialValue)) return;
        c = a.initialValue;
      }
      let u = M(c),
        m = l?.valueLocalized ?? W,
        h = l?.alt ?? Xr,
        g = M(h),
        v = l?.altLocalized ?? W,
        S = Z(h, o, v, g, void 0, Da(c, m, u));
      return {
        type: "image",
        keyPath: ur(r.id, s, i),
        nodeId: r.id,
        scopeId: t,
        controlPropKey: s,
        controlPropName: a.name,
        ancestorControlKeyPath: i,
        value: h,
        valueWordCount: ie(h),
        hash: g,
        localizedValues: v,
        localizedValueStatuses: S,
        supportsAlt: !0,
        image: c,
        imageHash: u,
        imageLocalizedValues: m,
        managedByPluginButUserEditable: a.userEditable ?? !1,
        supportsFocalPoint: !0,
        positionX: l?.positionX,
        positionY: l?.positionY,
      };
    }
    case "enum":
      return;
    default:
  }
}
function ag(n, e, t, r, o, i, a) {
  let s = r.id;
  function l(d, f, c, u) {
    if (f && f.type === "array") {
      if (c.type !== "array" || at(f.value)) return;
      for (let h of f.value) {
        if (!Qo(h.type) || !As(h) || !h.value) continue;
        let g = Jt(u, h.id);
        for (let v of c.itemVariables) {
          if (!je(v)) continue;
          let S = h.value[v.id];
          if (!S || !Qo(S.type)) continue;
          let b = nt(g, v.id);
          l(d, S, v, b);
        }
      }
    }
    if (!qu(c)) return;
    let m = ig(n, e, s, r, o, u, c, d, f, a);
    m && i.push(m);
  }
  for (let d of t.variables) {
    if (!je(d)) continue;
    let f = r.getControlProp(d.id);
    l(d.id, f, d);
  }
}
var af = zs((n, e) => ({ translatePagePaths: n, translatePagePathsWithAI: e }));
function sf(n, e, t, r, o, i, a, s) {
  s?.();
  let l = af(n.root.translatePagePaths, n.root.translatePagePathsWithAI),
    d = fr(n, r, void 0, o, e, t.variables, void 0, l, void 0);
  if (d) {
    d.group && a.push(d.group);
    return;
  }
  let f = [],
    c = vn(n, r),
    u = new Set();
  e.trackEntityReads(u, () => {
    ag(n, e, t, r, o, f, i);
  });
  let m;
  if (f.length > 0) {
    for (let h of f) h.insetLevel = 1;
    ((m = {
      type: "sourceGroup",
      groupType: "collectionItem",
      collectionName: t.resolveValue("name") ?? "Unknown",
      collectionNodeId: t.id,
      supportsLocaleVisibility: !0,
      includedLocaleIds: c,
      name: co(t, r) ?? "Unknown",
      nodeId: r.id,
      items: f,
    }),
      a.push(m));
  }
  pr(n, r, void 0, m, o, Yo(e, u), t.variables, void 0, l, void 0);
}
function sg(n, e, t, r) {
  let o = t.name,
    i = M(o),
    a = t.nameLocalized ?? W,
    s = Z(o, r, a, i);
  return {
    type: "enum",
    keyPath: `${n.id}_${e.id}_${t.id}`,
    nodeId: n.id,
    scopeId: n.id,
    variableId: e.id,
    enumCaseId: t.id,
    value: o,
    valueWordCount: ie(o),
    hash: i,
    localizedValues: a,
    localizedValueStatuses: s,
    managedByPluginButUserEditable: !1,
  };
}
function lf(n, e, t, r, o, i) {
  i?.();
  let a = [],
    s = df(r, t);
  for (let u of t.getUnsortedChildren()) sf(n, e, t, u, r, s, a, i);
  let l = fr(n, t, void 0, r, e, lt, a, void 0, void 0);
  if (l) {
    l.group && o.push(l.group);
    return;
  }
  let d = t.variables.filter(qu),
    f = [];
  for (let u of d) {
    let m = u.type;
    switch (m) {
      case "enum": {
        if (dn(u)) break;
        for (let h of u.cases) f.push(sg(t, u, h, r));
        break;
      }
      case "string":
      case "richtext":
      case "image":
      case "link":
      case "slug":
        break;
      default:
    }
  }
  let c =
    f.length > 0 || a.length > 0
      ? {
          type: "sourceGroup",
          groupType: "collection",
          name: t.resolveValue("name") ?? "Unknown",
          nodeId: t.id,
          supportsLocaleVisibility: !1,
          includedLocaleIds: void 0,
          items: f,
          groups: a,
        }
      : void 0;
  (pr(n, t, void 0, c, r, rg, lt, a, void 0, void 0), c && o.push(c));
}
function df(n, e) {
  let t = {};
  for (let o of n) t[o.id] = new Map();
  let r = e.getSlugVariable();
  if (!r) return t;
  for (let o of e.getUnsortedChildren()) {
    let i = o.getControlProp(r.id);
    if (
      i &&
      (y(i.type === "string", "expect slug control type to be be of type string"),
      y(L(i.value), "expected slug control prop value to be string"),
      !!i.valueLocalized)
    )
      for (let a in i.valueLocalized) {
        let s = i.valueLocalized[a],
          l = t[a];
        if (!l || !L(s?.value) || !s.value) continue;
        let d = (l.get(s.value) ?? 0) + 1;
        l.set(s.value, d);
      }
  }
  return t;
}
function lg(n, e, t) {
  let r = n.getParent(t.id)?.children ?? [],
    o = {};
  for (let i of e) i.id !== li && (o[i.id] = new Map());
  for (let i of r) {
    if (!st(i)) continue;
    let { segment: a, segmentLocalized: s } = i;
    for (let l of e) {
      if (l.id === li) continue;
      let d = Fs(l, s)?.value,
        c = (L(d) ? d : void 0) || a,
        u = o[l.id];
      if (!u) continue;
      let m = (u.get(c) ?? 0) + 1;
      u.set(c, m);
    }
  }
  return o;
}
function cf(n, e, t, r) {
  let o = fr(n, n.root, void 0, t, e, lt, void 0, void 0, void 0);
  if (o) {
    o.group && r.push(o.group);
    return;
  }
  let i = [],
    a = new Set();
  e.trackEntityReads(a, () => {
    Yu(n, e, n.root, n.root, t, i);
  });
  let s =
    i.length > 0
      ? {
          type: "sourceGroup",
          groupType: "settings",
          name: "Site Settings",
          nodeId: n.root.id,
          supportsLocaleVisibility: !1,
          includedLocaleIds: void 0,
          items: i,
        }
      : void 0;
  (pr(n, n.root, void 0, s, t, Yo(e, a), lt, void 0, void 0, void 0), s && r.push(s));
}
function dg(n, e, t, r, o, i) {
  for (let a of t.children) lf(n, e, a, r, o, i);
}
function cg(n, e, t, r, o, i) {
  if (!Os(t)) return;
  let a = Ot(t.segment),
    s = M(t.segment),
    l = t.segmentLocalized ?? W,
    d = of(r, l, lg(n, o, t), "Sibling pages must have unique slugs");
  i.push({
    keyPath: t.id,
    type: "RouteSegmentNode",
    nodeId: t.id,
    scopeId: e.id,
    value: a,
    valueWordCount: ie(a),
    hash: M(t.segment),
    localizedValues: l,
    localizedValueStatuses: Z(t.segment, r, l, s, d, void 0),
    managedByPluginButUserEditable: !1,
    warnings: d,
    aiTranslationDisabled: !n.root.translatePagePathsWithAI,
  });
}
function ug(n, e, t, r, o, i, a) {
  let s = $n(n, "includeDrafts"),
    l = new Set();
  for (let d of t.walk()) st(d) && uf(n, e, d, r, s, o, i, l, a);
}
function uf(n, e, t, r, o, i, a, s, l) {
  l?.();
  let d = t.webPageId ? n.get(t.webPageId) : void 0,
    f = D(d) && d.isLoaded() ? d : null,
    c = af(n.root.translatePagePaths, n.root.translatePagePathsWithAI),
    u = d && s.has(d.id),
    m = f || t,
    h = m === f ? t : void 0,
    g = fr(n, m, h, r, e, lt, void 0, c, a),
    v = !!(d && !u);
  if (g && g.includesWebPage === v) {
    (g.group && i.push(g.group), v && d && s.add(d.id));
    return;
  }
  let S = [],
    b = new Set(),
    w = e.trackEntityReads(b, () => {
      if ((n.root.translatePagePaths === "yes" && cg(n, m, t, r, o, S), d && !u)) {
        for (let R of d.walk()) ef(n, e, m, R, r, S, a, l);
        s.add(d.id);
      }
      let x = D(d) ? Qn(n, e, d) : _l(ba(t, n));
      return (
        D(m) && m.abTestingParentId && (x = `${x} \xB7 ${m.resolveValue("name") || dt(e, m)}`),
        t.segment === "" && (x = `${x} /`),
        x
      );
    }),
    P = Gn(n, m),
    V;
  (S.length > 0 &&
    ((V = {
      type: "sourceGroup",
      name: w,
      groupType: "page",
      nodeId: m.id,
      supportsLocaleVisibility: P,
      includedLocaleIds: P ? vn(n, m) : void 0,
      items: S,
    }),
    i.push(V)),
    pr(n, m, h, V, r, Yo(e, b), lt, void 0, c, a, v));
}
function SD(n, e, t, r, o) {
  let i = [];
  if (!t || t.length === 0) return i;
  cf(n, e, t, i);
  for (let a of n.root.children) {
    let s = D(a) && a.abTestingParentId !== void 0;
    (Xo(n, a) && !s) ||
      (cr(a) && (y(a.isLoaded(), "Scope node must be fully loaded"), rf(n, e, a, t, i, r, o)),
      Mt(a) && (y(a.isLoaded(), "Scope node must be fully loaded"), dg(n, e, a, t, i, o)),
      Er(a) && ug(n, e, a, t, i, r, o));
  }
  return i;
}
function ff(n) {
  return n.children;
}
function fg(n) {
  let e = 1;
  for (let t of n.root.children) {
    let r = D(t) && t.abTestingParentId !== void 0;
    if (!(Xo(n, t) && !r)) {
      if ((cr(t) && e++, Mt(t))) {
        y(t.isLoaded(), "Scope node must be fully loaded");
        for (let o of ff(t)) e += o.getUnsortedChildren().length + 1;
      }
      if (Er(t)) for (let o of t.walk()) st(o) && e++;
    }
  }
  return e;
}
async function bD(n, e, t, r, o) {
  if (!t || t.length === 0) return !0;
  let i = fg(n),
    a = 0,
    s = async () => {
      (a++,
        o.onProgress({ completedScopeCount: a, totalScopeCount: i }),
        await o.yieldAfterScope());
    };
  (cf(n, e, t, []), await s());
  for (let d of n.root.children) {
    if (!o.shouldContinue()) return !1;
    let f = D(d) && d.abTestingParentId !== void 0;
    if (!(Xo(n, d) && !f)) {
      if (
        (cr(d) &&
          (y(d.isLoaded(), "Scope node must be fully loaded"), rf(n, e, d, t, [], r), await s()),
        Mt(d))
      ) {
        y(d.isLoaded(), "Scope node must be fully loaded");
        for (let c of ff(d)) {
          let u = df(t, c);
          for (let m of c.getUnsortedChildren()) {
            if (!o.shouldContinue()) return !1;
            (sf(n, e, c, m, t, u, []), await s());
          }
          (lf(n, e, c, t, []), await s());
        }
      }
      if (Er(d)) {
        let c = $n(n, "includeDrafts"),
          u = new Set();
        for (let m of d.walk())
          if (st(m)) {
            if (!o.shouldContinue()) return !1;
            (uf(n, e, m, t, c, [], r, u), await s());
          }
      }
    }
  }
  return !0;
}
function Xo(n, e) {
  return D(e) && e.id !== n.root.homePageNodeId;
}
function TD(n, e) {
  return Et(n, e);
}
function ID(n, e) {
  return Tt(n, e);
}
function Pa(n) {
  if (n < 1) return Pa(n + 1) * Math.pow(Math.random(), 1 / n);
  let e = n - 1 / 3,
    t = 1 / Math.sqrt(9 * e);
  for (;;) {
    let r = 0,
      o = 0;
    do {
      let a = Math.random(),
        s = Math.random();
      ((r = Math.sqrt(-2 * Math.log(a)) * Math.cos(2 * Math.PI * s)), (o = 1 + t * r));
    } while (o <= 0);
    o = o * o * o;
    let i = Math.random();
    if (i < 1 - 0.0331 * (r * r) * (r * r) || Math.log(i) < 0.5 * r * r + e * (1 - o + Math.log(o)))
      return e * o;
  }
}
function pf(n, e, t = 0.95, r = 5e3) {
  let o = e + 1,
    i = n - e + 1,
    a = [];
  for (let c = 0; c < r; c++) {
    let u = Pa(o),
      m = Pa(i);
    a.push(u / (u + m));
  }
  a.sort((c, u) => c - u);
  let s = (1 - t) / 2,
    l = 1 - s,
    d = a[Math.floor(a.length * s)] ?? 0,
    f = a[Math.floor(a.length * l)] ?? 0;
  return [d, f];
}
var pg = 864e5,
  mg = 36e5;
function hg(n, e) {
  return Ka({ start: n, end: e }).map((t) => t.toISOString());
}
function gg(n, e) {
  let t = [],
    r = n.getTime(),
    o = e.getTime();
  for (; r <= o; ) (t.push(new Date(r).toISOString()), (r += mg));
  return t;
}
function yg(n, e) {
  return qa({ start: n, end: e }).map((t) => t.toISOString());
}
function Cg(n, e) {
  let t = [],
    r = n.getTime(),
    o = e.getTime();
  for (; r <= o; ) (t.push(new Date(r).toISOString()), (r += pg));
  return t;
}
function mf(n, e, t, r, o = "local") {
  let i = n.map(({ timestamp: u }) => new Date(u)).sort((u, m) => u.getTime() - m.getTime()),
    a = o === "utc" ? new Date(e) : ji(e),
    s = o === "utc" ? new Date(t) : ji(t),
    l = i[0],
    d = i[i.length - 1],
    f = l && l > a ? l : a,
    c = d && d > s ? d : s;
  return r === "hour" ? (o === "utc" ? gg(f, c) : hg(f, c)) : o === "utc" ? Cg(f, c) : yg(f, c);
}
function vg(n, e, t) {
  let o = t.length,
    i = new Map();
  for (let { step: f, count: c } of e) {
    let u = i.get(f) ?? 0;
    i.set(f, u + c);
  }
  let a = i.get(1) ?? 0,
    s = i.get(o) ?? 0,
    l = a > 0 ? s / a : 0,
    d = [];
  for (let f = 1; f <= o; f++) {
    let c = i.get(f) ?? 0;
    if (f > 1 && c > 0 && (i.get(f - 1) ?? 0) < c) {
      let h = d[d.length - 1];
      h && ((h.count = c), (h.cumulativeDropOff = a > 0 ? (a - c) / a : 0));
    }
    let u = a > 0 ? (a - c) / a : 0;
    d.push({ step: f, count: c, cumulativeDropOff: u, stepwiseDropOff: 0 });
  }
  return { timestamp: n, startCount: a, finalCount: s, conversionRate: l, funnelSteps: d };
}
function Sg(n, e, t, r) {
  let i = t.length,
    a = Map.groupBy(e, (l) => l.abVariantId),
    s = [];
  for (let [l, d] of a) {
    let f = r.get(l);
    f || ((f = new Map()), r.set(l, f));
    for (let { step: g, count: v } of d) {
      let S = f.get(g) ?? 0;
      f.set(g, S + v);
    }
    let c = f.get(1) ?? 0,
      u = f.get(i) ?? 0;
    if (c === 0 || u > c) continue;
    let m = c > 0 ? u / c : 0,
      h = pf(c, u);
    s.push({
      abVariantId: l,
      startCount: c,
      finalCount: u,
      conversionRate: m,
      conversionRateCredibleInterval: h,
    });
  }
  return { timestamp: n, variants: s };
}
function bg(n) {
  return n[0]?.abVariantId !== void 0;
}
function Va(n, e, t, { fromDay: r, toDay: o }, i) {
  if (n.length === 0) return [];
  let a = bg(n),
    s = new Map(),
    l = mf(n, r, o, e, i),
    d = Map.groupBy(n, (c) => c.timestamp),
    f = [];
  for (let c of l) {
    let u = d.get(c) ?? [];
    (u.length === 0 && f.length === 0) || (a ? f.push(Sg(c, u, t, s)) : f.push(vg(c, u, t)));
  }
  return f;
}
function hf(n, e) {
  return 1 - n / e;
}
function Ea(n, e) {
  let t = n[0],
    r = 0;
  if (!t) return [];
  let o = "abVariantId" in t ? t.abVariantId : void 0;
  return n.reduce(
    (i, { step: a, count: s, abVariantId: l }, d) => {
      let f = 0,
        c = 0;
      return (
        d > 0 && ((f = s === 0 ? 1 : hf(s, t.count)), (c = r === 0 ? 1 : hf(s, r))),
        (i[d] = {
          step: a,
          count: s,
          cumulativeDropOff: f,
          stepwiseDropOff: c,
          ...(l && { abVariantId: l }),
        }),
        (r = s),
        i
      );
    },
    e.map((i, a) => ({
      step: a + 1,
      count: 0,
      cumulativeDropOff: 1,
      stepwiseDropOff: 1,
      abVariantId: o,
    }))
  );
}
var Tg = "other";
function Zo(n, e) {
  return e ? Map.groupBy(n, (t) => t[e] ?? Tg) : new Map([["all", n]]);
}
var yf = 20;
function BD(n, e, t) {
  let r = e.fromDay === e.toDay ? "hour" : "day";
  return Ct("stats", n, e, t, { granularity: r });
}
function AD(n, e, t) {
  return Ct("session-stats", n, e, t);
}
function _D(n, e, t) {
  return Ct("live-sessions", n, e, t);
}
function $D(n, e, t, r, o = yf) {
  return Ct("top-field", n, t, r, { name: e, limit: String(o) });
}
function UD(n, e, t, r, o = yf) {
  return Ct("top-fields", n, t, r, { names: e, limit: String(o) });
}
async function WD(n, e, t) {
  if (e.steps.length === 0) return;
  let { dropOff: r } = await Ct("funnel-drop-off", n, e, t),
    o = Zo(r, e.groupBy),
    i = new Map();
  return (
    o.forEach((a, s) => {
      i.set(s, Ea(a, e.steps));
    }),
    i
  );
}
async function jD(n, e, t) {
  if (e.steps.length === 0) return;
  let { counts: r, granularity: o } = await Ct("funnel-counts", n, e, t),
    i = Zo(r, e.groupBy),
    a = new Map();
  return (
    i.forEach((s, l) => {
      a.set(l, Va(s, o, e.steps, e));
    }),
    a
  );
}
async function GD(n, e, t) {
  let { dropOff: r, metrics: o } = await Ct("funnel-drop-off", n, e, t),
    i = Zo(r, "abVariantId"),
    a = new Map();
  return (
    i.forEach((s, l) => {
      a.set(l, Ea(s, e.steps));
    }),
    { dropOff: a, metrics: o }
  );
}
async function HD(n, e, t) {
  let { counts: r, granularity: o } = await Ct("funnel-counts", n, e, t);
  return Va(r, o, e.steps, e, "utc");
}
var gf = Intl.DateTimeFormat().resolvedOptions().timeZone;
async function Ct(
  n,
  e,
  {
    fromDay: t,
    toDay: r,
    filters: o,
    steps: i,
    funnelFilters: a,
    groupBy: s,
    abTestId: l,
    controlVariantId: d,
  },
  f,
  c
) {
  let u = new URLSearchParams();
  if ((t && u.set("startDate", t), r && u.set("endDate", r), gf && !l && u.set("timezone", gf), o))
    for (let h in o) {
      let g = o[h];
      g && u.set(h, g.value);
    }
  if (c)
    for (let [h, g] of Object.entries(c))
      if (Array.isArray(g)) for (let v of g) u.append(h, v);
      else u.set(h, g);
  if (
    (l && u.set("abTestId", l),
    d && u.set("controlVariantId", d),
    i && i.length > 0 && u.set("steps", JSON.stringify(i)),
    a)
  )
    for (let h in a) {
      let g = a[h];
      if (g) for (let v of g) u.append(h, v);
    }
  return (
    s && (u.set("groupBy", s), u.set("groupByLimit", "5")),
    await So.get(`/web/v3/projects/${e}/site-stats/${n}?${u.toString()}`, void 0, f)
  );
}
function KD(n) {
  return new URL(`/multiplayer${n}`, en().api).href;
}
function QD(n) {
  try {
    return new URL(n).pathname.startsWith("/multiplayer");
  } catch {
    return !1;
  }
}
async function eP(n, e, t) {
  let r = e && e !== E ? { branchId: e } : void 0;
  return So.get(`/web/v1/sites/hostnames/${n}`, r, t);
}
var Cf = class {
  constructor(e) {
    C(this, "scheduler", e);
  }
};
var Dt = Zt(ja(), 1);
var vf = Symbol("uninitialized");
function Ig(n) {
  return n + 1;
}
function aP(n, e, t, r) {
  let o = Ps(),
    [, i] = (0, Dt.useReducer)(Ig, 0),
    a = (0, Dt.useRef)(vf);
  a.current === vf && (a.current = e());
  let s = (0, Dt.useRef)(e);
  s.current = e;
  let l = (0, Dt.useRef)(t);
  if (!Tt(t, l.current)) {
    l.current = t;
    let f = a.current,
      c = s.current();
    (r ? !Et(f, c) : !Tt(f, c)) && (a.current = c);
  }
  let d = Array.isArray(n) ? n : [n];
  return (
    (0, Dt.useEffect)(() => {
      let f = () => {
          let m = a.current,
            h = s.current();
          (r ? Et(m, h) : Tt(m, h)) || ((a.current = h), i());
        },
        c = [...d, f],
        u = o.scheduler.changes.observe(...c);
      return () => o.scheduler.changes.removeObserver(u);
    }, d),
    a.current
  );
}
function Ng(n) {
  return Ne(n) || Vr(n);
}
function Ma(n) {
  if (Ne(n)) return n.codeComponentIdentifier;
  if (Vr(n)) return n.shaderModuleIdentifier;
}
function Rg(n, e) {
  (Ne(n) && n.set({ codeComponentIdentifier: e }), Vr(n) && n.set({ shaderModuleIdentifier: e }));
}
function wg(n, e) {
  return e.moduleId === n.moduleId && e.saveId !== n.saveId;
}
function Sf(n, e) {
  switch (e?.type) {
    case "canvasComponent":
    case "codeFile":
    case "shader": {
      let t = se(e.codeComponentIdentifier);
      return wg(n, t);
    }
    default:
      return !1;
  }
}
function xg(n, e) {
  let t = se(n),
    r = se(e);
  return (
    y(t.moduleId === r.moduleId, "Module id should be the same"),
    y(t.saveId !== r.saveId, "Module identifier should have a different save id than the instance"),
    Ua(t.moduleId, r.saveId, t.file, t.exportSpecifier).value
  );
}
function uP(n, e) {
  let t = n.tree.getNodeWithTrait(e, Ng);
  if (!t) return;
  y(!Pr(t), "Module instance node should not be a replica or replica child");
  let r = Ma(t),
    o = se(r);
  y(o?.kind === "externalModuleExport", "Module identifier should be external");
  let i = n.tree.getNodeWithTrait(o.moduleId, Mr);
  y(i, "Module node should exist");
  let a = xg(o.value, i.codeComponentIdentifier);
  Rg(t, a);
}
function bf(n, e, t, r, o) {
  let i = se(n);
  if (!$a(i)) return;
  let a = _a(i);
  if (a && !i.moduleId) return;
  let s = a ? t.getNodeWithTrait(i.moduleId, Mr) : null,
    l = a ? (s?.type ?? "codeFile") : i.type,
    d = l === "shader" ? !!e.shaderForIdentifier(i.value) : !!e.componentForIdentifier(i.value);
  return a
    ? {
        type: l,
        hasDefinition: d,
        hasModuleEntry: r.hasModuleForIdentifier(i),
        outdatedInstance: Sf(i, s),
      }
    : {
        type: l,
        hasDefinition: d,
        hasModuleEntry: !!o.getModuleEntryByLocalId(i.localId),
        outdatedInstance: !1,
      };
}
function Lg(n, e, t, r, o) {
  let i = Ma(n);
  if (!i) return;
  let a = bf(i, e, t, r, o);
  if (!a) return;
  let { hasDefinition: s, hasModuleEntry: l, type: d, outdatedInstance: f } = a;
  if (!s || !l)
    switch (d) {
      case "codeFile":
        if (l) {
          if (s) break;
          return { kind: "code", codeError: 2 };
        } else return { kind: "code", codeError: 5 };
      case "shader":
        if (l) {
          if (s) break;
          return { kind: "code", codeError: 4 };
        } else return { kind: "code", codeError: 6 };
      case "canvasComponent":
        return { kind: "code", codeError: 3 };
      default:
        return { kind: "code", codeError: 1 };
    }
  if (f) return { kind: "outdatedInstance", originalNodeId: n.getPrimaryId() };
}
function Dg(n, e, t, r, o) {
  if (!ol(n)) return;
  let i = bf(n.codeOverrideIdentifier, e, t, r, o);
  if (!i) return;
  let { hasDefinition: a, hasModuleEntry: s } = i;
  if (!a) return s ? { kind: "code", codeError: 7 } : { kind: "code", codeError: 8 };
}
function Pg(n, e) {
  let t = e[n.id];
  if (t)
    switch (t.type) {
      case "Form":
        return { kind: "form", reason: t.reason };
      case "CodeGeneration":
        return t.reason === "FileTooLarge" ? { kind: "code", codeError: 0 } : void 0;
      default:
        ee(t.type);
    }
}
function vP(n, e, t, r, o, i) {
  return Lg(n, e, t, r, o) || Dg(n, e, t, r, o) || (i && Pg(n, i));
}
function Vg(n) {
  switch (n.type) {
    case "Form":
      return !0;
    case "CodeGeneration":
      return n.reason === "FileTooLarge";
    default:
      return !1;
  }
}
function SP(n) {
  let e = {},
    t = n.get($l);
  return (
    t &&
      t.children?.forEach((r) => {
        r.nodeId &&
          (!r.type ||
            !Vg(r) ||
            !r.reason ||
            r.nodeId in e ||
            (e[r.nodeId] = { type: r.type, reason: r.reason }));
      }),
    e
  );
}
function NP(n) {
  if (!L(n)) return { destinationType: void 0, reason: "" };
  let e = n.indexOf(":");
  if (e === -1) return { destinationType: void 0, reason: n.trim().toLowerCase() };
  let t = n.slice(0, e).trim(),
    r = n
      .slice(e + 1)
      .split("|")[0]
      .trim()
      .toLowerCase();
  return { destinationType: t, reason: r };
}
function Eg(n) {
  return Es(n.timeline);
}
function LP(n, e, t) {
  return Eg(n) === e
    ? !1
    : (y(
        n.tree === t.initialTree && !Ms(n.timeline) && !n.tree.hasUncommittedChanges(),
        "Cannot switch tree mode after tree data has been loaded because it would reset timeline data"
      ),
      an(n.tree, e),
      (n.timeline = t.createTimeline(e)),
      t.resetTimeline && n.timeline.reset(n.tree),
      !0);
}
var Mg = me("exportToHTML");
function Tf(n, e, t, r = new Set([t]), o = new Set()) {
  for (let { node: i, skipChildren: a } of t.walkWithSkipChildren()) {
    if (o.has(i)) {
      a();
      continue;
    }
    o.add(i);
    for (let s of Ll(i, e)) {
      let l = xl(s);
      if (!l) continue;
      let d = n.getNode(l);
      if (d) {
        if (!n.isGroundNode(d)) {
          Mg.reportError("Navigation target node must be ground node", {
            sourceId: i.id,
            targetId: d.id,
          });
          continue;
        }
        (r.add(d), Tf(n, e, d, r, o));
      }
    }
    Ne(i) &&
      Object.values(bd(n, i, e))
        .flat()
        .forEach((s) => Tf(n, e, s.node, r, o));
  }
  return r;
}
function kP(n, e) {
  let t = e.getPrimaryVariant();
  wi(t) && (n.routes[e.id] = t.pageEffects);
}
function OP(n, e, t) {
  let r = e.getPrimaryVariant();
  if (wi(r)) {
    let o = {};
    for (let i in r.pageEffects) o[i] = t.dedupe("effect", r.pageEffects[i]);
    n.routes[e.id] = o;
  }
}
var If = "utils";
function Nf(n, e) {
  let t = se(e);
  if (!ot(t) || t.kind !== "localModuleExport") return;
  let r = n.getPersistedModuleByLocalId(t.localId);
  if (r && r.exports.includes(If))
    return { exportIdentifier: If, moduleURL: r.moduleURL, collectionId: t.localIdName };
}
function UP(n, e) {
  if (D(e) && e.dataIdentifier) return Nf(n, e.dataIdentifier);
}
function WP(n, e, t) {
  for (let r of e.variables) {
    if (!Ws(r)) continue;
    let o = Nf(n, r.dataIdentifier);
    o && t(o);
  }
}
var KP = Ba({
  canvas: () => {
    let n = Fg();
    return (fo(n), uo(n));
  },
  onPageCanvas: () => {
    let n = zg();
    fo(n);
    let { url: e, isCrossOriginProtected: t } = uo(n),
      r = Bg(e);
    return { url: r, origin: new URL(r).origin, isCrossOriginProtected: t };
  },
  patch: () => {
    let n = kg();
    return (fo(n), uo(n));
  },
});
function Fg() {
  let n = Ve.isDebugBuild ? "canvas-sandbox.debug.html" : "canvas-sandbox.html",
    e = qr(`./${n}`),
    t = Ve.debugPerformance ? `?${Ir}` : "",
    r = JSON.stringify(en());
  return e + t + `#services=${encodeURIComponent(r)}`;
}
function zg() {
  let n = Ve.isDebugBuild ? "canvas-sandbox-on-page.debug.html" : "canvas-sandbox-on-page.html",
    e = qr(`./${n}`),
    t = Ve.debugPerformance ? `?${Ir}` : "",
    r = JSON.stringify(en());
  return e + t + `#services=${encodeURIComponent(r)}`;
}
function kg() {
  let n = Ve.isDebugBuild ? "patch-sandbox.debug.html" : "patch-sandbox.html",
    e = qr(`./${n}`),
    t = Ve.debugPerformance ? `?${Ir}` : "",
    r = JSON.stringify(en());
  return e + t + `#services=${encodeURIComponent(r)}`;
}
var Og = 63;
function Bg(n) {
  let e = new URL(n);
  if (
    e.hostname === "localhost" ||
    e.hostname === "127.0.0.1" ||
    e.hostname.split(".").length - 2 >= 2
  )
    return n;
  let r = new URLSearchParams(window.location.search).get("framerSiteId");
  y(r, "onPageCanvas must be loaded with a framerSiteId");
  let o = `site-${r}`.slice(0, Og);
  return ((e.hostname = `${o}.${e.hostname}`), e.toString());
}
var Ag = ka() && navigator.userAgent.includes("Version/16"),
  XP = window.CompressionStream && !Ag;
export {
  uc as a,
  fc as b,
  pc as c,
  py as d,
  xo as e,
  Rc as f,
  yC as g,
  Vc as h,
  Qf as i,
  Mc as j,
  ZC as k,
  ea as l,
  cr as m,
  vx as n,
  Fu as o,
  zu as p,
  Nx as q,
  Rx as r,
  wx as s,
  nt as t,
  Jt as u,
  Sa as v,
  kx as w,
  Ou as x,
  tL as y,
  nL as z,
  rL as A,
  dD as B,
  cD as C,
  uD as D,
  fD as E,
  pD as F,
  $h as G,
  mD as H,
  Uh as I,
  Wh as J,
  hD as K,
  gD as L,
  yD as M,
  CD as N,
  jh as O,
  Gh as P,
  vD as Q,
  Kh as R,
  Yu as S,
  Qh as T,
  qo as U,
  ef as V,
  ur as W,
  ig as X,
  ag as Y,
  sg as Z,
  df as _,
  dg as $,
  cg as aa,
  SD as ba,
  bD as ca,
  TD as da,
  ID as ea,
  BD as fa,
  AD as ga,
  _D as ha,
  $D as ia,
  UD as ja,
  WD as ka,
  jD as la,
  GD as ma,
  HD as na,
  Eg as oa,
  LP as pa,
  KD as qa,
  QD as ra,
  _c as sa,
  $c as ta,
  ov as ua,
  Tp as va,
  rr as wa,
  Iu as xa,
  bu as ya,
  ht as za,
  ah as Aa,
  VR as Ba,
  lh as Ca,
  ER as Da,
  dh as Ea,
  _R as Fa,
  jR as Ga,
  Vu as Ha,
  Tf as Ia,
  kP as Ja,
  OP as Ka,
  Nf as La,
  UP as Ma,
  WP as Na,
  KP as Oa,
  Og as Pa,
  XP as Qa,
  eP as Ra,
  Cf as Sa,
  aP as Ta,
  uP as Ua,
  vP as Va,
  SP as Wa,
  NP as Xa,
};
//# sourceMappingURL=chunk-PPMMTNWW.mjs.map
